# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0950
name: SMSG_PATCH_SPELL_DURATION
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of SpellDuration.dbc (bound_by: L1 ref, patch-table-names.md): the handler's single 16-byte bulk read exactly equals the table's 4-column/16-byte record (client DBC header), so this is a full, position-by-position decomposition. All 4 columns are named by AzerothCore's own SpellDurationEntry struct (L1, another project's reading of the same file).

layout:
  - name: id
    type: u32
    notes: 'DBC column 0 (index field, fmt ''n''); core: SpellDurationEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar5,16]], one 16-byte bulk read == the full 4-column/16-byte SpellDuration.dbc record); name L1 (core DBCStructure.h, another project''s reading of the same file).'
    evidence: L1
  - name: duration_0
    type: i32
    notes: 'DBC column 1; core: SpellDurationEntry::Duration[0] (int32). Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
  - name: duration_1
    type: i32
    notes: 'DBC column 2; core: SpellDurationEntry::Duration[1] (int32). Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
  - name: duration_2
    type: i32
    notes: 'DBC column 3; core: SpellDurationEntry::Duration[2] (int32). Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h SpellDurationEntry + DBCfmt.h SpellDurationfmt (L1: another project's reading of the same client DBC file, not ours).

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00231b90"
    registration_site_rva: "0x00228f67"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h SpellDurationEntry + DBCfmt.h SpellDurationfmt (L1: another project's reading of the same client DBC file, not ours)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "client/patch-table-names.md (the naming-sources sweep): 0x0950 bound_by 'L1 ref' to SpellDuration.dbc, 4 fields / 16 bytes, matching the client DBC header"
    claim: "this opcode's table is SpellDuration.dbc, at L1 (only the third-party reference names it; patch-tables.yaml does not bind this opcode)"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0950 is named SMSG_PATCH_SPELL_DURATION"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
