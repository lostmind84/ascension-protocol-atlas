# Generated by tools/codegen/stub_fiches.py, then hand-filled (patch-unnamed-1 sweep, 2026-09-20).
# This is a CoA-custom DBC table row with NO naming source in anything this repo holds
# (see client/patch-table-names.md). field_N names are placeholders; do not rename them
# without a cited source (lua-keys.yaml, a UI getter, or handler-strings.yaml).

id: 0x06a9
name: SMSG_PATCH_CHARACTER_ADVANCEMENT_CATEGORIES
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  SMSG_PATCH_CHARACTER_ADVANCEMENT_CATEGORIES: one packet that writes a row into TWO tables, CharacterAdvancementCategories.dbc and CharacterAdvancementClassTypes.dbc. Both rows are reduced: 7 of 39 fields for the first table, 7 of 23 for the second. The second record is read behind the same apparent stack-cookie branch (`swi(0x29)`) as the shared 0x0567/0x0568 handler -- see that fiche's note; read here as real, always-executed code for the same reasons, not confirmed at the assembly level. No column name is established for either table.

layout:
  - name: field_0
    type: u32
    notes: "record 1 (CharacterAdvancementCategories.dbc side), offset 0; bounds-checked against that table object's minId/maxId"
    evidence: L3
  - name: field_1
    type: u32
    notes: "offset 4, first half of an 8-byte load"
    evidence: L3
  - name: field_2
    type: u32
    notes: "offset 8, second half; read as one 8-byte load in the handler; split into two 4-byte fields here to line up with the table's own field count -- could be a single 64-bit value instead, not resolved further"
    evidence: L3
  - name: field_3
    type: u32
    notes: "offset 0xc"
    evidence: L3
  - name: field_4
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (1st of three consecutive gaps)"
    evidence: L3
  - name: field_5
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (2nd of three)"
    evidence: L3
  - name: field_6
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (3rd of three)"
    evidence: L3
  - name: field_7
    type: cstring
    notes: "1st of 3, NUL-terminated, scanned byte by byte; not FUN_100d3680"
    evidence: L3
  - name: field_8
    type: cstring
    notes: "2nd of 3"
    evidence: L3
  - name: field_9
    type: cstring
    notes: "3rd of 3; end of record 1 (7 fields, matches the struct FUN_10ae5d20(0x1c) allocates)"
    evidence: L3
  - name: field_10
    type: u32
    notes: "record 2 (CharacterAdvancementClassTypes.dbc side) starts here, offset 0 of record 2; bounds-checked against that table object's minId/maxId at 0x10bdfa98"
    evidence: L3
  - name: field_11
    type: cstring
    notes: "immediately after the id, NUL-terminated, scanned byte by byte"
    evidence: L3
  - name: field_12
    type: u32
    notes: "first u32 after the string"
    evidence: L3
  - name: field_13
    type: u32
    notes: "second u32 after the string"
    evidence: L3
  - name: field_14
    type: u32
    notes: "third u32 after the string"
    evidence: L3
  - name: field_15
    type: u32
    notes: "fourth u32 after the string"
    evidence: L3
  - name: field_16
    type: cstring
    notes: "2nd string of record 2, NUL-terminated, scanned byte by byte; end of record 2 (7 fields)"
    evidence: L3

client:
  handler:
    rva: "0x001dd0b0"
    registration_site_rva: "0x001e69da"
    evidence: L3

patches_table:   # the client DBC table(s) this packet writes into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3 unless noted; record_size/fields/rows from client/patch-table-names.md's own header read against the ascension-live-2026-09-17 snapshot, L2)
  - dbc: CharacterAdvancementCategories.dbc
    instance: "0x10be03bc"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 156
    fields: 39
    rows: 51
  - dbc: CharacterAdvancementClassTypes.dbc
    instance: "0x10bdfa98"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 92
    fields: 23
    rows: 46

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x06a9.c (already-decompiled handler, read by hand this pass)"
    claim: "the widths, order and destination struct of the wire fields"
  - level: L3
    source: "hand-tracing both records past the swi(0x29) branch this pass, same shape as 0x0567/0x0568"
    claim: "record 2 is real, always-executed code, not a dead overflow handler"
    note: "not confirmed at the assembly/disassembly level; an open point, same caveat as 0x0567/0x0568"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (handler 0x06a9)"
    claim: "no string literal is referenced near this handler's store -- checked, empty"
    note: "rules out an event name or format string that might have hinted at a field's purpose"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06a9 is named SMSG_PATCH_CHARACTER_ADVANCEMENT_CATEGORIES"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L1
    source: "client/patch-table-names.md (the naming-source sweep over all 228 SMSG_PATCH_* opcodes)"
    claim: "no lua-keys builder, UI getter, or DBCfmt entry names this table's columns; the reference community source offers no names for it either (bucket 'none found')"

replay: null
