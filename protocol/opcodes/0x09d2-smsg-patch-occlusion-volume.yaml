# Generated by tools/codegen/stub_fiches.py, then completed by hand (naming-source closure pass).
id: 0x09d2
name: SMSG_PATCH_OCCLUSION_VOLUME
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of OcclusionVolume.dbc, written into the client's own copy of that table
  (patch-tables.yaml's CustomDBCMgr scan binds this opcode to it, and the decompile confirms
  the idiom: the row goes into the table object's rows array at (id - minId), bounds-checked
  against minId/maxId, exactly as client/patch-tables.md describes). The wire's 16-byte fixed
  part is not a plain 4-dword block copy: the client reads only 3 of its 4 dwords into named
  values (offsets 0x00, 0x08, 0x0c; the dword at 0x04 is never read into a value at all) and
  then, at store time, places the trailing cstring's pointer into that 4th row slot instead --
  exactly the "string stitched over the record's string-offset column" pattern
  client/patch-tables.md describes, just with the stitched column in the middle of the row
  rather than at its end. No source in this repository names any of the table's 4 columns:
  checked handler-strings.yaml, lua-keys.yaml and the UI corpus (see naming_source below).

layout:
  - name: row
    type: bytes_16
    notes: >
      the wire's fixed 16-byte part (cursor advances by exactly 0x10 before the trailing
      string). Of its four 4-byte columns, the client reads three into named values -- offset
      0x00 (the id, used for the minId/maxId bounds check and stored at row slot 0), offset
      0x08 (stored at row slot 2) and offset 0x0c (stored at row slot 3) -- and never reads
      offset 0x04 into a value at all (build/ghidra/handlers-c/0x09d2.c, re-read this pass:
      `uVar13 = [+8]; uVar4 = [+0]; uVar5 = [+0xc]`, no read of `[+4]`). Row slot 1 (where
      offset 0x04's raw dword would land) is instead filled with the pointer to field_1
      (`puVar9[1] = (uint)pcVar8`) -- offset 0x04 is this table's string-offset column,
      matching client/patch-tables.md's "the client stitches the row's strings over the
      record's string-offset fields" note. width: L3 (patches_table.record_size below, and
      the decompiled reads above). name: none -- no naming source found (see naming_source
      below).
    evidence: L3
  - name: field_1
    type: cstring
    notes: >
      a NUL-terminated string read right after the 16-byte fixed part (the read cursor
      advances by 16 + strlen + 1). width/kind: L3 -- re-decompiled this pass
      (build/ghidra/handlers-c/0x09d2.c: the byte-scan loop `while (*p++ != 0)` and the
      cursor update `pos = pos_before + 0x11 + (scanned - 1)`, i.e. 16 + string length
      including the NUL); the earlier stub had this typed `unknown`, which was a placeholder
      for "computed length", not a claim the kind was unresolved. This is the row's column 2
      (offset 0x04, see field_0's note), stitched in from the wire's trailing bytes rather
      than sent as a raw string-block offset. name: none -- no naming source found.
    evidence: L3

client:
  handler:
    rva: "0x001e2810"
    registration_site_rva: "0x001e6bf8"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: OcclusionVolume.dbc
    instance: "0x10be0158"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 16
    fields: 4

naming_source:
  result: none found
  checked:
    - "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml: 0x09d2 has no strings recorded"
    - "client/symbols/ascension-extensions-2026-08-13/lua-keys.yaml: 22 builders extracted; none in this table's domain"
    - "build/corpus/ascension-ui-patch-b-stock, grepped for `OcclusionVolume`: 0 files"

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml (CustomDBCMgr load idiom and the handler's table object)"
    claim: "the packet carries one row of OcclusionVolume.dbc into the client's copy of that table"
  - level: L3
    source: "build/ghidra/handlers-c/0x09d2.c (decompiled handler, re-read this pass) and client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled; field_1 is a NUL-terminated cstring, not an unresolved width"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml, lua-keys.yaml, and build/corpus/ascension-ui-patch-b-stock (this pass)"
    claim: "no naming source for the table's 4 columns or the trailing cstring exists in any of the three"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x09d2 is named SMSG_PATCH_OCCLUSION_VOLUME"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
