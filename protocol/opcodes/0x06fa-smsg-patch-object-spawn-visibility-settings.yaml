# Generated by tools/codegen/stub_fiches.py, then hand-filled (patch-unnamed-1 sweep, 2026-09-20).
# This is a CoA-custom DBC table row with NO naming source in anything this repo holds
# (see client/patch-table-names.md). field_N names are placeholders; do not rename them
# without a cited source (lua-keys.yaml, a UI getter, or handler-strings.yaml).

id: 0x06fa
name: SMSG_PATCH_OBJECT_SPAWN_VISIBILITY_SETTINGS
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  SMSG_PATCH_OBJECT_SPAWN_VISIBILITY_SETTINGS: a clean, full-width row write into ObjectSpawnVisibilitySettings.dbc -- 7 fields, 28 bytes, matching that table's own header geometry exactly once the string column's wire-slot is counted. This replaces the file's earlier partial layout (`row: bytes_28` + one `unknown` tail field). No column name is established. The table's own snapshot row count is 0: the manifest lists no rows for it, so there is currently nothing to see this packet apply to.

layout:
  - name: field_0
    type: u32
    notes: "offset 0; bounds-checked against the table object's minId/maxId"
    evidence: L3
  - name: field_1
    type: u32
    notes: "offset 4"
    evidence: L3
  - name: field_2
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further"
    evidence: L3
  - name: field_3
    type: u32
    notes: "offset 0xc"
    evidence: L3
  - name: field_4
    type: u32
    notes: "offset 0x10"
    evidence: L3
  - name: field_5
    type: u32
    notes: "offset 0x14"
    evidence: L3
  - name: field_6
    type: u32
    notes: "offset 0x18"
    evidence: L3
  - name: field_7
    type: cstring
    notes: "NUL-terminated, scanned byte by byte; fills field_2's slot"
    evidence: L3

client:
  handler:
    rva: "0x001e2680"
    registration_site_rva: "0x001e6b7e"
    evidence: L3

patches_table:   # the client DBC table(s) this packet writes into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3 unless noted; record_size/fields/rows from client/patch-table-names.md's own header read against the ascension-live-2026-09-17 snapshot, L2)
  - dbc: ObjectSpawnVisibilitySettings.dbc
    instance: "0x10bdfe1c"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 28
    fields: 7
    rows: 0

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x06fa.c (already-decompiled handler, read by hand this pass)"
    claim: "the widths, order and destination struct of the wire fields"
  - level: L3
    source: "build/ghidra/handlers-c/0x06fa.c, full hand read, superseding the file's earlier packet_layout.py-derived partial layout"
    claim: "the record is exactly 7 fields / 28 bytes once the string slot is counted, not 'bytes_28 + 1 unknown tail field' as the file said before this pass"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (handler 0x06fa)"
    claim: "no string literal is referenced near this handler's store -- checked, empty"
    note: "rules out an event name or format string that might have hinted at a field's purpose"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06fa is named SMSG_PATCH_OBJECT_SPAWN_VISIBILITY_SETTINGS"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L1
    source: "client/patch-table-names.md (the naming-source sweep over all 228 SMSG_PATCH_* opcodes)"
    claim: "no lua-keys builder, UI getter, or DBCfmt entry names this table's columns; the reference community source offers no names for it either (bucket 'none found')"

replay: null
