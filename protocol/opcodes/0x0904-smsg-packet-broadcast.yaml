# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0904
name: SMSG_PACKET_BROADCAST
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  A packet envelope: two guids, a range and a complete inner packet (opcode and body) that the client dispatches to itself as if it had received it. The server uses it to make a client near another one replay an arbitrary opcode locally; the client drops it when either guid is its own or the two objects are farther apart than the range.

layout:
  - name: senderGuid
    type: u64
    notes: the packet is dropped when this is the active player's guid
    evidence: L3
  - name: targetGuid
    type: u64
    notes: dropped when this is the active player's guid; otherwise both are resolved through the object manager (exe 0x004d4db0, mask 0xffff)
    evidence: L3
  - name: range
    type: f32
    notes: 'squared against the distance between the two objects: the inner packet is dispatched only inside it'
    evidence: L3
  - name: innerOpcode
    type: u16
    notes: written as the first two bytes of the store handed to the dispatcher
    evidence: L3
  - name: innerSize
    type: u16
    notes: byte count of the payload that follows
    evidence: L3
  - name: innerPayload
    type: bytes_variable
    notes: innerSize bytes, copied after the opcode into a fresh CDataStore of innerSize + 2, which is handed to the exe's ClientServices::ProcessMessage (0x00631fe0) and so runs through the normal dispatch, DLL handlers included
    evidence: L3
# Read from the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20).

# Read from the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest of the body is noted as not followed.

# Widths and order, read out of the handler at 0x002295e0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x002295e0"
    registration_site_rva: "0x00228d30"
    evidence: L3

provenance:
  - level: L3
    source: "the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest of the body is noted as not followed"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0904 is named SMSG_PACKET_BROADCAST"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
