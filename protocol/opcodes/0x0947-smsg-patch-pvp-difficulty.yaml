# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0947
name: SMSG_PATCH_PVP_DIFFICULTY
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of the stock PvpDifficulty.dbc (AzerothCore already loads this table; ADR 0005), sent as the single 24-byte blob the handler at RVA 0x0022f720 reads (L3). Nothing was elided (6 cols x 4 bytes = 24 bytes matches the client header exactly). All 6 columns are named: the first is FT_SORT ('d', DBCStructure.h comments it 'id' / m_ID but PvPDifficultyEntry does not keep it as a struct member) and the other 5 are PvPDifficultyEntry's active members (L1 throughout).

layout:
  - name: id
    type: u32
    notes: 'FT_SORT (''d''): DBCStructure.h comments it //uint32 id (m_ID) but PvPDifficultyEntry does not keep it as a member'
    evidence: L1
  - name: mapId
    type: u32
    notes: active PvPDifficultyEntry member
    evidence: L1
  - name: bracketId
    type: u32
    notes: active PvPDifficultyEntry member
    evidence: L1
  - name: minLevel
    type: u32
    notes: active PvPDifficultyEntry member
    evidence: L1
  - name: maxLevel
    type: u32
    notes: active PvPDifficultyEntry member
    evidence: L1
  - name: difficulty
    type: u32
    notes: active PvPDifficultyEntry member
    evidence: L1
# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h (PvPDifficultyEntry) and DBCfmt.h (PvPDifficultyfmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md.

client:
  handler:
    rva: "0x0022f720"
    registration_site_rva: "0x00228eec"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h (PvPDifficultyEntry) and DBCfmt.h (PvPDifficultyfmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0947 is named SMSG_PATCH_PVP_DIFFICULTY"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
