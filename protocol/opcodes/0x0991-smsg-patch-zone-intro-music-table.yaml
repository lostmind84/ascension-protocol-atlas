# Generated by tools/codegen/stub_fiches.py, then completed by hand (naming-source closure pass).
id: 0x0991
name: SMSG_PATCH_ZONE_INTRO_MUSIC_TABLE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  A record patched into one of Extensions.dll's internal id-keyed containers -- looked up by id
  (RVA 0x000b2bd0), allocated fresh as a 0x14-byte heap struct when new (RVA 0x000ae5d20), and
  inserted (RVA 0x000b0b50) -- not one of the 114 CustomDBCMgr `.data` tables
  client/patch-tables.md documents: nothing in the decompiled handler references a fixed .data
  table object (no `+0xc`/`+0x10`/`+0x20` min/max/rows idiom anywhere in it, unlike 0x09d1/0x09d2
  in this same shard, which do). client/patch-table-names.md's third-party reference names
  `ZoneIntroMusicTable.dbc` for this opcode (5 fields, 20-byte record, 179 rows) and the handler's
  fixed portion happens to be the same 20 bytes -- but that binding is `L1 ref` only (unverified
  by our own extraction: this opcode is absent from
  client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml's 104-opcode CustomDBCMgr
  scan), and the byte-count match does not, by itself, prove the store target is that table. No
  source in this repository names the struct's own fields (see naming_source below).

layout:
  - name: field_0
    type: bytes_20
    notes: >
      width: L3 (block-copy read, 20 bytes; re-verified this pass against the cached decompile,
      build/ghidra/handlers-c/0x0991.c, against
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source (see naming_source below). 20 happens to equal ZoneIntroMusicTable.dbc's record_size (20
      bytes, 5 fields, per client/patch-table-names.md) -- but the store target below is not that
      table, so the match may be coincidental rather than confirming the binding.
    evidence: L3
  - name: field_1
    type: lpstring
    notes: >
      width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source.
    evidence: L3

client:
  handler:
    rva: "0x002355d0"
    registration_site_rva: "0x0022934e"
    evidence: L3

naming_source:
  result: none found
  checked:
    - "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml: 0x0991 has no strings recorded"
    - "client/symbols/ascension-extensions-2026-08-13/lua-keys.yaml: 22 builders extracted; none in this table's domain"
    - "ZoneIntroMusicTable.dbc search in build/corpus/ascension-ui-patch-b-stock (grepped for the bare table name minus \".dbc\"): 0 files"
  claimed_table:
    dbc: ZoneIntroMusicTable.dbc
    fields: 5
    record_size: 20
    rows: 179
    bound_by: "L1 ref only (client/patch-table-names.md); absent from client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml's own CustomDBCMgr scan"
    width_check: "matches the handler's fixed-portion read (20 bytes)"

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x0991.c (decompiled handler, re-read this pass) and client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order; the handler looks the id up in an internal container (RVA 0x000b2bd0), allocates a fresh 0x14-byte struct when it is new (RVA 0x000ae5d20), and inserts it (RVA 0x000b0b50) -- not one of the 114 CustomDBCMgr .data table objects patch-tables.yaml enumerates"
  - level: L1
    source: "client/patch-table-names.md (opcode 0x0991 row)"
    claim: "a third-party reference names the DBC table ZoneIntroMusicTable.dbc for this opcode; unverified by our own patch-tables.yaml scan and not treated as established here"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml, lua-keys.yaml, and build/corpus/ascension-ui-patch-b-stock (this pass)"
    claim: "no naming source for the struct's own fields exists in any of the three"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0991 is named SMSG_PATCH_ZONE_INTRO_MUSIC_TABLE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
