# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0972
name: SMSG_PATCH_CREATURE_SOUND_DATA
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_PATCH_CREATURE_SOUND_DATA by the client's own opcode table. The handler is a flat 38-dword (152-byte) block copy into an id-keyed store, with no string reads; that width and field count match CreatureSoundData.dbc's own WDBC header exactly, so this looks like a full, unmodified, all-numeric row of that table -- but the opcode is not bound in patch-tables.yaml (the DLL's CustomDBCMgr table list), so the table identity itself rests only on the opcode's own name and the matching filename in the client DBC snapshot, and no source names any of the 38 columns.

layout:
  - name: row
    type: bytes_152
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x0972.c), read by hand. packet_layout.py's own scan (the earlier, wrong stub) only followed the first two field reads before the block-copy loop; the handler is a plain 0x26 (38) dword copy loop, advancing the CDataStore cursor by exactly 0x98 (152) bytes with no string reads at all -- matching CreatureSoundData.dbc's own WDBC header exactly (38 fields, 152 bytes).

client:
  handler:
    rva: "0x0022b880"
    registration_site_rva: "0x00229171"
    evidence: L3

dbc_identity:
  dbc: CreatureSoundData.dbc
  fields: 38
  record_size: 152
  rows: 10398
  header_source: "ascension-live-2026-09-17 WDBC header, L2 (client/patch-table-names.md)"
  bound: >
    Not bound in client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml -- this opcode
    is not one of the DLL's 114 CustomDBCMgr table objects; per client/patch-tables.md it is one
    of the other 141 patch handlers, whose record is the DLL's own struct rather than a named
    table row. The table below is the one client/patch-table-names.md associates with this opcode
    by name and geometry, sourced only from a third-party reference (grade L1).
  reference_lead: "ref: partial names (L1) -- a third-party reference offers some, mostly data-derived names, unverified here"
  match: MATCH
  wire: >
    A flat 0x26-dword (38-field, 152-byte) block copy, no string reads at all -- byte-for-byte the
    table's own header (38 fields, 152 bytes).
  note: >
    This is the other opcode whose earlier, auto-generated stub had the wrong widths
    (tools/extract/packet_layout.py only followed the first two reads before the copy loop); see
    the layout's own provenance note above for the correction.

column_names: none found
  # checked: client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (no
  # string literal near the store); lua-keys.yaml (no builder in this table's domain);
  # the Ascension UI/Lua corpus (build/corpus/ascension-ui-patch-b-stock, searched for
  # the table's own base name, e.g. "CreatureSoundData").

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x0972.c), read by hand"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0972 is named SMSG_PATCH_CREATURE_SOUND_DATA"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
