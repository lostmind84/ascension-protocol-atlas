# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x090b
name: SMSG_GAME_MODE_CHANGED
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Named SMSG_GAME_MODE_CHANGED by the client's own opcode table. Layout and behaviour not
  established.

# AzerothCore calls 0x090b SMSG_COA_GAME_MODE_STATE. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout:
  - name: mask
    type: u32
    evidence: L3
# Widths and order, read out of the handler at 0x0031e990 by tools/extract/packet_layout.py.
# Names: lent by the CoA server's writer in modules/mod-coa-challenges/src/CoA.Challenges.GameModes.cpp (L1), whose widths agree with the
# client's reads position by position; a field the server writes as a literal keeps its placeholder.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x0031e990"
    registration_site_rva: "0x0031eea8"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_ChallengesUI/Tabs/GamemodesTab.lua
  - Interface/FrameXML/Ascension_BuildDraft/Ascension_BuildDraft.lua
  - Interface/FrameXML/MainMenuBarMicroButtons.lua
  - Interface/FrameXML/MainMenuBarMicroButtons.xml
  - Interface/FrameXML/Util/C_GameMode.lua
  - Interface/FrameXML/Util/WildCardUtil.lua

client_strings:   # literals the handler references, from its decompilation
  - "ASCENSION_CUSTOM_GAME_MODE_CHANGED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: ASCENSION_CUSTOM_GAME_MODE_CHANGED
    args: []
    source: "Interface/AddOns/Ascension_ChallengesUI/Tabs/GamemodesTab.lua:158"
  - event: ASCENSION_CUSTOM_GAME_MODE_CHANGED
    args: [modes]
    source: "Interface/FrameXML/Ascension_BuildDraft/Ascension_BuildDraft.lua:170"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: ASCENSION_CUSTOM_GAME_MODE_CHANG
    format: "%u"
    helper: FUN_100d0a50

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-coa-challenges/src/CoA.Challenges.Config.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.GameModes.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Review.h
    - modules/mod-coa-challenges/src/CoA.Challenges.Scripts.cpp

provenance:
  - level: L1
    source: "modules/mod-coa-challenges/src/CoA.Challenges.GameModes.cpp (the CoA server's writer, client/server-layouts.yaml)"
    claim: "the field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x090b is named SMSG_GAME_MODE_CHANGED"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
