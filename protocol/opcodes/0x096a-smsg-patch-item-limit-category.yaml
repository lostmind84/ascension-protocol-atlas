# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x096a
name: SMSG_PATCH_ITEM_LIMIT_CATEGORY
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Candidate: one row of ItemLimitCategory.dbc (bound_by: L1 ref, patch-table-names.md). The handler reads a 16-byte/4-dword block then one string (handler-fire.yaml), short of the table's full 20-column/80-byte record. id, maxCount and mode (the table's last two columns) are named, the latter two on an ascending-order-preservation assumption; one dword has no candidate and stays unnamed. The trailing lpstring is count-matched to the table's one locale block (name[16]).

layout:
  - name: id
    type: u32
    notes: 'DBC column 0 (index field, fmt ''n''); core: ItemLimitCategoryEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar4,16],[local_2c,lpstring]], one 16-byte/4-dword bulk read then one string); name L1 (core DBCStructure.h, another project''s reading of the same file).'
    evidence: L1
  - name: field_1
    type: u32
    notes: 'Second of the 4 dwords. Only 2 of the remaining 3 have a candidate core name (maxCount, mode below); this one is unaccounted. Candidate, not placed: the DBC''s ''name flags'' column (17), which ItemLimitCategoryEntryfmt marks ''x''. Width L3 only.'
    evidence: L3
  - name: maxCount
    type: u32
    notes: 'DBC column 18; core: ItemLimitCategoryEntry::maxCount, ''max allowed equipped as item or in gem slot''. Placed as the third of four dwords on the assumption that the client preserves ascending DBC order within the kept-scalar group -- maxCount and mode are the table''s last two columns, contiguous. Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
  - name: mode
    type: u32
    notes: 'DBC column 19; core: ItemLimitCategoryEntry::mode, ''0 = have, 1 = equip''. Same order-preservation inference as maxCount. Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
  - name: name
    type: lpstring
    notes: 'DBC columns 1-16; core: ItemLimitCategoryEntry has a commented-out Name[16] locale block (''m_name_lang''), the table''s only string-bearing span, matching the one observed lpstring per the map''s locale-block trap. Width L3 (handler-fire.yaml); name L1 (dead-comment name, not a stored core member).'
    evidence: L1
# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h ItemLimitCategoryEntry + DBCfmt.h ItemLimitCategoryEntryfmt (L1: another project's reading of the same client DBC file, not ours).

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# packet_layout.py had followed a call into 0x000d3680, not the handler. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x0022daa0"
    registration_site_rva: "0x002290f6"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h ItemLimitCategoryEntry + DBCfmt.h ItemLimitCategoryEntryfmt (L1: another project's reading of the same client DBC file, not ours)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "client/patch-table-names.md (the naming-sources sweep): 0x096a bound_by 'L1 ref' to ItemLimitCategory.dbc, 20 fields / 80 bytes, matching the client DBC header"
    claim: "this opcode's table is ItemLimitCategory.dbc, at L1 (only the third-party reference names it; patch-tables.yaml does not bind this opcode)"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x096a is named SMSG_PATCH_ITEM_LIMIT_CATEGORY"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
