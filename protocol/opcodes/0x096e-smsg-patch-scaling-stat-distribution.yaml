# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x096e
name: SMSG_PATCH_SCALING_STAT_DISTRIBUTION
direction: server_to_client
status: hypothetical
evidence: L0
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Candidate: one row of ScalingStatDistribution.dbc, IF that is in fact what this opcode patches -- client/patch-table-names.md grades this opcode 'L0 name' (bound_by), meaning neither patch-tables.yaml nor the third-party reference ties 0x096e to this table; only the opcode's own name text suggests it. The handler's single 88-byte bulk read does exactly equal the table's 22-column/88-byte record (client DBC header), which is consistent with the guess but does not confirm it. All 22 columns would be named by AzerothCore's own ScalingStatDistributionEntry struct if the identity holds; graded L0 throughout for that reason.

layout:
  - name: id
    type: u32
    notes: 'DBC column 0 (index field, fmt ''n''); core: ScalingStatDistributionEntry::Id. bound_by is ''L0 name'' in client/patch-table-names.md -- unlike this shard''s other opcodes, NEITHER patch-tables.yaml NOR the third-party reference binds 0x096e to ScalingStatDistribution.dbc; the table identity itself is only a guess from the opcode''s own name text (the reference does not even cover this opcode). That weakens every field below it, so names here are graded L0 (a working hypothesis), not the L1 this shard otherwise uses for core-sourced names. Width L3 (handler-fire.yaml: reads [[null,88]], one 88-byte bulk read == the full 22-column/88-byte ScalingStatDistribution.dbc record, if this is in fact that table).'
    evidence: L0
  - name: statMod_0
    type: i32
    notes: 'DBC column 1; core: ScalingStatDistributionEntry::StatMod[0] (int32). Table identity is L0 name only (see id''s note) -- graded L0.'
    evidence: L0
  - name: statMod_1
    type: i32
    notes: DBC column 2; StatMod[1]. L0, see id's note.
    evidence: L0
  - name: statMod_2
    type: i32
    notes: DBC column 3; StatMod[2]. L0, see id's note.
    evidence: L0
  - name: statMod_3
    type: i32
    notes: DBC column 4; StatMod[3]. L0, see id's note.
    evidence: L0
  - name: statMod_4
    type: i32
    notes: DBC column 5; StatMod[4]. L0, see id's note.
    evidence: L0
  - name: statMod_5
    type: i32
    notes: DBC column 6; StatMod[5]. L0, see id's note.
    evidence: L0
  - name: statMod_6
    type: i32
    notes: DBC column 7; StatMod[6]. L0, see id's note.
    evidence: L0
  - name: statMod_7
    type: i32
    notes: DBC column 8; StatMod[7]. L0, see id's note.
    evidence: L0
  - name: statMod_8
    type: i32
    notes: DBC column 9; StatMod[8]. L0, see id's note.
    evidence: L0
  - name: statMod_9
    type: i32
    notes: DBC column 10; StatMod[9]. L0, see id's note.
    evidence: L0
  - name: modifier_0
    type: u32
    notes: 'DBC column 11; core: Modifier[0] (uint32). L0, see id''s note.'
    evidence: L0
  - name: modifier_1
    type: u32
    notes: DBC column 12; Modifier[1]. L0, see id's note.
    evidence: L0
  - name: modifier_2
    type: u32
    notes: DBC column 13; Modifier[2]. L0, see id's note.
    evidence: L0
  - name: modifier_3
    type: u32
    notes: DBC column 14; Modifier[3]. L0, see id's note.
    evidence: L0
  - name: modifier_4
    type: u32
    notes: DBC column 15; Modifier[4]. L0, see id's note.
    evidence: L0
  - name: modifier_5
    type: u32
    notes: DBC column 16; Modifier[5]. L0, see id's note.
    evidence: L0
  - name: modifier_6
    type: u32
    notes: DBC column 17; Modifier[6]. L0, see id's note.
    evidence: L0
  - name: modifier_7
    type: u32
    notes: DBC column 18; Modifier[7]. L0, see id's note.
    evidence: L0
  - name: modifier_8
    type: u32
    notes: DBC column 19; Modifier[8]. L0, see id's note.
    evidence: L0
  - name: modifier_9
    type: u32
    notes: DBC column 20; Modifier[9]. L0, see id's note.
    evidence: L0
  - name: maxLevel
    type: u32
    notes: 'DBC column 21; core: ScalingStatDistributionEntry::MaxLevel. L0, see id''s note.'
    evidence: L0
# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h ScalingStatDistributionEntry + DBCfmt.h ScalingStatDistributionfmt (L0: the opcode-to-table binding itself is unconfirmed here, not just the names).

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x0022fc70"
    registration_site_rva: "0x00229135"
    evidence: L3

provenance:
  - level: L0
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h ScalingStatDistributionEntry + DBCfmt.h ScalingStatDistributionfmt (L0: the opcode-to-table binding itself is unconfirmed here, not just the names)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L0
    source: "client/patch-table-names.md (the naming-sources sweep): 0x096e bound_by 'L0 name' to ScalingStatDistribution.dbc, 22 fields / 88 bytes, matching the client DBC header"
    claim: "this opcode's table is ScalingStatDistribution.dbc, at L0 (inferred only from the opcode name itself; neither patch-tables.yaml nor the third-party reference binds this opcode)"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x096e is named SMSG_PATCH_SCALING_STAT_DISTRIBUTION"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
