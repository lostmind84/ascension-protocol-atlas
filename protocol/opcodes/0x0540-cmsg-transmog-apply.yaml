# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0540
name: CMSG_TRANSMOG_APPLY
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named CMSG_TRANSMOG_APPLY by the client's own opcode table, the first of a small
  "apply an appearance by category" cluster (0x0540, plus the non-consecutive
  0x054a ILLUSION_APPLY, 0x0552 INCARNATION_APPLY, 0x0553 SPELL_VISUAL_APPLY,
  0x0554 COSMETIC_PET_APPLY, 0x0555 COSMETIC_APPLY -- named here only for context, not
  ours to touch). The name survives in the table but nothing in this build constructs a
  packet with this opcode: no `push 0x540` / `mov r32, 0x540` occurs anywhere in
  Extensions.dll's `.text`, byte-exhaustively. The live feature runs through a single
  unified opcode instead, CMSG_APPLY_APPEARANCES (0x0697, registered and sent in this
  build -- not ours), which the client's own table already separates from this legacy
  name by 87 slots of unrelated opcodes. This is not "we have not found the sender yet";
  it is "this build has no code that could send it."

layout: []   # empty

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0540 is named CMSG_TRANSMOG_APPLY"
  - level: L3
    source: >
      tools/extract/client_sender.py's push_sites() scan, run directly against
      ~/CoaServer/client/ascension-live/Extensions.dll.ORIGINAL (sha256
      0f8d847b3adc44a963606f0cd4f7938ad6fcd6f4d87feac3131c7153bdf3bb11) for 0x0540 alone,
      2026-09-20
    claim: >
      the immediate value 0x0540 never occurs as a `push imm32` (0x68) or
      `mov r32, imm32` (0xb8-0xbf) operand anywhere in the .text section
    note: >
      Byte-exhaustive (every offset in .text is checked, not just instruction
      boundaries), so it is stronger than "no sender site was walked to a send" --
      there is no immediate-loaded candidate at all to walk from. A sender that built
      the opcode from a data table instead of a literal would still be missed; see
      open_questions.
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml and handlers.yaml"
    claim: >
      CMSG_APPLY_APPEARANCES (0x0697) has a registered sender in this build, unlike
      0x0540 -- confirming which opcode the live feature actually uses
    note: "field layout of 0x0697 is not read here; it is not one of this pass's opcodes."
  - level: L1
    source: >
      CoinThrow's capture reference, kept outside this repository at
      ~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md,
      section "Cosmetics / appearance" (the wardrobe/appearance system writeup)
    claim: >
      "0x0540-0x0559 + 0x05CD/0x05CE/0x05D1 + 0x0640-0x0642 (superseded -- no senders
      exist in the client build)"; the live feature runs through the modern appearance
      cluster (0x0697 apply, 0x069e/0x06a0 outfit save/delete, 0x06a3 visibility toggle)
      instead, and "no push32/mov-imm encodings exist in Extensions.dll code for
      0x054D-0x0558".
    note: >
      L1 per docs/EVIDENCE.md regardless of the author's own confidence. Re-derived
      independently above at L3 for this specific opcode: our own byte-exhaustive scan
      agrees with their claim.

open_questions:
  - "The immediate-value scan cannot see a sender that loads the opcode from a data
     table rather than a literal 0x540. The capture reference reports checking more
     broadly (\"the few byte matches in Ascension.exe sit inside internal table code...
     not on a path to the network layer\") and finding nothing there either, but that
     broader search was not reproduced in this pass."

related:
  - 0x0697
