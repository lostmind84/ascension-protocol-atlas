# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0621
name: SMSG_WILDCARD_ENTRY_LEARNED
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  A Wildcard entry learned: the entry whose Wildcard-store record (0x2c bytes, keyed by this id) the handler fills, its new rank, and a NUL-terminated reason; WILDCARD_ENTRY_LEARNED(internalID, newRank, preRollRank) fires with format %u%u%u, the second argument being the rank read here (proven position), the internal id looked up from the entry, the pre-roll rank taken from the store. WILDCARD_RAPID_ROLL_LEARNED adds the reason string as a fourth argument.

layout:
  - name: entryId
    type: u32
    notes: the key of the Wildcard store record the handler fills
    evidence: L3
  - name: newRank
    type: u32
    evidence: L3
  - name: reason
    type: cstring
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c, DumpC.java), its fire format and the UI's event arguments; client/symbols/ascension-extensions-2026-08-13/rank-source.md for the Wildcard store.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# the asm idiom (packet_layout.py) saw the first 2 read(s) the same way and stopped. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00a30260"
    registration_site_rva: "0x00a31592"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_CharacterAdvancement/CharacterAdvancement.lua
  - Interface/AddOns/Ascension_CharacterAdvancementSeason9/CharacterAdvancement.lua
  - Interface/AddOns/Ascension_WildCard/Dice/WildCardStartingChoice.lua
  - Interface/AddOns/Ascension_WildCard/WildCard.lua
  - Interface/FrameXML/UIParent.lua

client_strings:   # literals the handler references, from its decompilation
  - "%u%u%u"
  - "%u%u%u%s"
  - "WILDCARD_ENTRY_LEARNED"
  - "WILDCARD_RAPID_ROLL_LEARNED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: WILDCARD_ENTRY_LEARNED
    args: []
    source: "Interface/AddOns/Ascension_CharacterAdvancement/CharacterAdvancement.lua:2698"
  - event: WILDCARD_ENTRY_LEARNED
    args: [internalID, newRank, preRollRank]
    source: "Interface/AddOns/Ascension_WildCard/WildCard.lua:330"
  - event: WILDCARD_RAPID_ROLL_LEARNED
    args: [internalID, newRank, preRollRank, reason]
    source: "Interface/AddOns/Ascension_WildCard/WildCard.lua:302"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: WILDCARD_ENTRY_LEARNED
    format: "%u%u%u"
    helper: FUN_10278c90
    fields: [null, field_1, null]   # the read filling each format position

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: CharacterAdvancement.dbc
    instance: "0x10bdf63c"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 692
    fields: 179

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c, DumpC.java), its fire format and the UI's event arguments; client/symbols/ascension-extensions-2026-08-13/rank-source.md for the Wildcard store"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0621 is named SMSG_WILDCARD_ENTRY_LEARNED"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
