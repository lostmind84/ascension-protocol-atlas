# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06ba
name: SMSG_QUERY_CUSTOM_STORE_RESULT
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Named SMSG_QUERY_CUSTOM_STORE_RESULT by the client's own opcode table. Layout and behaviour not
  established.

layout:
  - name: status
    type: cstring
    evidence: L3
    notes: "server sends \"QUERY_CUSTOM_STORE_OK\""
  - name: itemCount
    type: u32
    evidence: L3
  - repeat: itemCount
    fields:
    - name: storeRecord
      type: u32[16]
      evidence: L3
      notes: "0x40 bytes per record on the client side; the server writes each item's StoreRecord fields in order"
# name from mod-ascension-compat's serializer (L1); width and order from the decompiled handler (L3)
# decompiled: FUN_101a5590 scans a NUL-terminated string at the cursor, then reads u32 count, then advances 0x40 per record

client:
  handler:
    rva: "0x001a5590"
    registration_site_rva: "0x001a597a"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/FrameXML/CustomVendor.lua

used_by_server:   # the CoA server module sends or handles this
  server_name: SMSG_QUERY_CUSTOM_STORE_RESULT
  files:
    - modules/mod-ascension-compat/src/AscensionCompat.cpp

client_strings:   # literals the handler references, from its decompilation
  - "QUERY_CUSTOM_STORE_OK"
  - "QUERY_CUSTOM_STORE_RESULT"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: QUERY_CUSTOM_STORE_RESULT
    args: [result]
    source: "Interface/FrameXML/CustomVendor.lua:227"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: QUERY_CUSTOM_STORE_RESULT
    format: "%s"
    helper: FUN_100b9620

provenance:
  - level: L3
    source: "tools/ghidra/ghidra.sh run Decomp.java <handler VA> (see client/server-layouts.md)"
    claim: "field widths and order, from the decompiled handler"
  - level: L1
    source: "mod-ascension-compat's serializer, client/server-layouts.yaml"
    claim: "field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06ba is named SMSG_QUERY_CUSTOM_STORE_RESULT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
