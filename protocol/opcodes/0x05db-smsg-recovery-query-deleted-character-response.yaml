# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05db
name: SMSG_RECOVERY_QUERY_DELETED_CHARACTER_RESPONSE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Recovery: the deleted characters found by a query. A count and per character a record this reading did not follow; RECOVERY_QUERY_RESULT(category) afterwards. The skeleton's second u32 was the record's first field.

layout:
  - name: count
    type: u32
    evidence: L3
  - repeat: count
    fields:
    - name: character
      type: bytes_variable
      notes: a deleted-character record read over some 150 lines of the handler (strings and numbers), not followed; the handler then fires RECOVERY_QUERY_RESULT with the RECOVERY_SERVICE_CATEGORY_DELETE token
      evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read by hand.

# Widths and order, read out of the handler at 0x003042f0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x003042f0"
    registration_site_rva: "0x003055fe"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "RECOVERY_SERVICE_CATEGORY_DELETE"
  - "RECOVERY_QUERY_RESULT"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: RECOVERY_QUERY_RESULT
    args: [category]
    source: "Interface/AddOns/Ascension_HelpUI/HelpMenu/HelpMenuItemRecoveryPanelMixin.lua:271"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: RECOVERY_QUERY_RESULT
    format: "%s"
    helper: FUN_100b9620
  - event: RECOVERY_QUERY_RESULT
    format: "%s"
    helper: FUN_100b9620

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05db is named SMSG_RECOVERY_QUERY_DELETED_CHARACTER_RESPONSE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
