# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0552
name: CMSG_INCARNATION_APPLY
direction: client_to_server
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named CMSG_INCARNATION_APPLY by the client's own opcode table. The opcode value appears
  nowhere else in this build: not as a 32- or 16-bit immediate in `.text`, not as a value in
  `.rdata` or `.data`, not as an arithmetic constant in the window before any `Put(u32)` that
  writes an opcode, and not as a number in the shipped Lua (the exhaustive scan recorded in
  `client/client-senders.md`, "What the scan still does not reach"). This build has no sender
  for it; layout is not established because there is nothing to read, not because nobody has
  looked. "Incarnation" is a live UI concept in this build (`Enum.IncarnationSlot`,
  `SharedXML/Enum.lua`), so the feature is not gone -- the current UI most likely applies an
  incarnation appearance through `CMSG_APPLY_APPEARANCES` (0x0697, its own fiche and the
  `0x0697` row of `client/client-senders.md`'s table -- not `client/server-layouts.md`, a
  citation error in an earlier draft: that document is scoped to opcodes the server writes,
  and 0x0697 is client-to-server) instead of this older, name-only opcode. That reading is
  `client/client-senders.md`'s own conjecture (L0), not established here.

layout: []   # unknown -- confirmed absent, not merely unexamined; see the provenance below

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0552 is named CMSG_INCARNATION_APPLY"
  - level: L3
    source: >
      client/client-senders.md (the exhaustive immediate scan of Extensions.dll across
      .text/.rdata/.data and the shipped Lua, table of the 33 opcodes with no trace at all,
      2026-09-20)
    claim: "the opcode value 0x0552 appears nowhere in this build; there is no sender for it here"
  - level: L0
    source: "client/client-senders.md, \"the older generation of a feature the current opcodes replaced\" (conjecture, explicitly marked L0 there)"
    claim: "this name-only opcode was superseded by CMSG_APPLY_APPEARANCES (0x0697)"

replay: null
