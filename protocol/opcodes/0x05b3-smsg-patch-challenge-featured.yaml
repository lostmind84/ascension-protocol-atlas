# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05b3
name: SMSG_PATCH_CHALLENGE_FEATURED
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of ChallengeFeatured.dbc: five dwords and no string. The runtime record (0x14 bytes) is a byte-for-byte copy of the wire block. The skeleton's three u32 stopped short -- the reader loads four dwords in one block plus a fifth. Dwords 2, 3 and 4 are each fed to the per-challenge notify path, so the row points at up to three challenges.

layout:
  - name: id
    type: u32
    notes: dword 0; the row key -- range-checked against minId 0x10be021c / maxId 0x10be0218, indexes the row table at 0x10be022c, then passed to FUN_10133ac0
    evidence: L3
  - name: unknown_1
    type: u32
    notes: dword 1; record +0x04. Not named.
    evidence: L3
  - name: unknown_2
    type: u32
    evidence: L3
    notes: dword 2; record +0x08. After the insert the handler walks dwords 2, 3 and 4 in a three-iteration loop and, for each non-zero value, runs the notify path (FUN_1013b8b0 then FUN_100996e0 with that value) and stamps a +2s refresh deadline -- the same shape the single-challenge tables use with their dword 1. Read as three challenge ids; inference from the loop, not a read symbol.
  - name: unknown_3
    type: u32
    evidence: L3
    notes: dword 3; record +0x0c, second iteration of that loop
  - name: unknown_4
    type: u32
    evidence: L3
    notes: dword 4; record +0x10, third iteration
# Read from the decompiled handler FUN_10138d00 (build/ghidra/handlers-c/0x05b3.c) read by hand, 2026-09-20.

# Widths and order, read out of the handler at 0x00138d00 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00138d00"
    registration_site_rva: "0x0013b6c3"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: ChallengeFeatured.dbc
    instance: "0x10be020c"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 20
    fields: 5

provenance:
  - level: L3
    source: "the decompiled handler FUN_10138d00 (build/ghidra/handlers-c/0x05b3.c) read by hand, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05b3 is named SMSG_PATCH_CHALLENGE_FEATURED"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
