# Generated by tools/codegen/stub_fiches.py, then hand-filled (patch-unnamed-1 sweep, 2026-09-20).
# This is a CoA-custom DBC table row with NO naming source in anything this repo holds
# (see client/patch-table-names.md). field_N names are placeholders; do not rename them
# without a cited source (lua-keys.yaml, a UI getter, or handler-strings.yaml).

id: 0x0680
name: SMSG_PATCH_ZONE_LIGHT
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  SMSG_PATCH_ZONE_LIGHT: a clean, full-width row write into ZoneLight.dbc -- 7 fields, 28 bytes, matching that table's own header geometry exactly. Unusually for this shard the string is the SECOND field on the wire (right after the id), not the last: the remaining 5 numeric fields are read after it, at cursor positions re-based on the string's own length. No column name is established.

layout:
  - name: field_0
    type: u32
    notes: "offset 0; bounds-checked against the table object's minId/maxId"
    evidence: L3
  - name: field_1
    type: cstring
    notes: "NUL-terminated, scanned byte by byte, immediately after the id -- not last on the wire"
    evidence: L3
  - name: field_2
    type: u32
    notes: "first u32 after the string"
    evidence: L3
  - name: field_3
    type: u32
    notes: "second u32 after the string"
    evidence: L3
  - name: field_4
    type: u32
    notes: "third u32 after the string"
    evidence: L3
  - name: field_5
    type: u32
    notes: "fourth u32 after the string"
    evidence: L3
  - name: field_6
    type: u32
    notes: "fifth u32 after the string"
    evidence: L3

client:
  handler:
    rva: "0x0021a4d0"
    registration_site_rva: "0x0021a782"
    evidence: L3

patches_table:   # the client DBC table(s) this packet writes into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3 unless noted; record_size/fields/rows from client/patch-table-names.md's own header read against the ascension-live-2026-09-17 snapshot, L2)
  - dbc: ZoneLight.dbc
    instance: "0x10bdf954"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 28
    fields: 7
    rows: 24

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x0680.c (already-decompiled handler, read by hand this pass)"
    claim: "the widths, order and destination struct of the wire fields"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (handler 0x0680)"
    claim: "no string literal is referenced near this handler's store -- checked, empty"
    note: "rules out an event name or format string that might have hinted at a field's purpose"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0680 is named SMSG_PATCH_ZONE_LIGHT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L1
    source: "client/patch-table-names.md (the naming-source sweep over all 228 SMSG_PATCH_* opcodes)"
    claim: "no lua-keys builder, UI getter, or DBCfmt entry names this table's columns; the reference community source offers no names for it either (bucket 'none found')"

replay: null
