# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0698
name: SMSG_APPLY_APPEARANCES_RESULT
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_APPLY_APPEARANCES_RESULT by the client's own opcode table. Layout and behaviour not
  established.

layout:
  - name: result
    type: cstring
    evidence: L3
    notes: "the client reads a NUL-terminated string; the server writes its `result` variable, whose type was not confirmed here"
# name from mod-ascension-compat's serializer (L1); width and order from the decompiled handler (L3)
# decompiled: FUN_100c4a50 scans a NUL-terminated string at the cursor first
# Widths and order, read out of the handler at 0x000c4a50 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x000c4a50"
    registration_site_rva: "0x000c5b8f"
    evidence: L3

used_by_server:   # the CoA server module sends or handles this
  server_name: SMSG_APPLY_APPEARANCES_RESULT
  files:
    - modules/mod-ascension-compat/src/AscensionCompat.cpp

client_strings:   # literals the handler references, from its decompilation
  - "APPLY_APPEARANCES_OK"
  - "APPLY_PENDING_APPEARANCE_RESULT"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: APPLY_PENDING_APPEARANCE_RESULT
    args: []
    source: "Interface/AddOns/Ascension_AppearanceUI/AppearanceCategorySlotButtonMixin.lua:28"
  - event: APPLY_PENDING_APPEARANCE_RESULT
    args: [applied, result]
    source: "Interface/AddOns/Ascension_AppearanceUI/AppearancePlayerModelMixin.lua:103"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: APPLY_PENDING_APPEARANCE_RESULT
    format: "%b%s"
    helper: FUN_100b96e0

provenance:
  - level: L3
    source: "tools/ghidra/ghidra.sh run Decomp.java <handler VA> (see client/server-layouts.md)"
    claim: "field widths and order, from the decompiled handler"
  - level: L1
    source: "mod-ascension-compat's serializer, client/server-layouts.yaml"
    claim: "field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0698 is named SMSG_APPLY_APPEARANCES_RESULT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
