# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x071f
name: SMSG_UNNAMED_071F   # PLACEHOLDER: the client's table answers 'unknown'; a handler exists
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One ExtraActionButtons.dbc row as the DLL keeps it: an id and five u32, out of the DBC's ten columns.

layout:
  - name: id
    type: u32
    notes: range-checked against the ExtraActionButtons.dbc table object (0x10bdffcc)
    evidence: L3
  - name: unknown_1
    type: u32
    evidence: L3
  - name: unknown_2
    type: u32
    evidence: L3
  - name: unknown_3
    type: u32
    evidence: L3
  - name: unknown_4
    type: u32
    evidence: L3
  - name: unknown_5
    type: u32
    notes: 'six u32 for a table whose rows have ten columns: a subset the DLL keeps, column mapping not read'
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read by hand; table identity from client/symbols/.../patch-tables.yaml (the instance the handler range-checks).

# Widths and order, read out of the handler at 0x001d2fe0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x001e1740"
    registration_site_rva: "0x001e6ba3"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: ExtraActionButtons.dbc
    instance: "0x10bdffcc"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 40
    fields: 10

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand; table identity from client/symbols/.../patch-tables.yaml (the instance the handler range-checks)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "the client's table has no name for 0x071f"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
