# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x09b2
name: SMSG_SPELL_ACTIVATION_HIDE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Remove the spell-activation overlay from a spell's button. The server writes u32 spellId and a u32 zero; the client's handler read sequence goes on for six more fields (u32, u32, f32, u32, u32, u32) it will not find in an 8-byte packet -- the CDataStore Gets are bounds-checked, so those reads yield nothing rather than fail. Whether the client behaves as intended with the short packet is a lab question (docs/ROADMAP.md, P4).

# AzerothCore calls 0x09b2 SMSG_COA_SPELL_ACTIVATION_HIDE. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout:
  - name: spellId
    type: u32
    evidence: L3
  - name: unknown_1
    type: u32
    notes: server writes 0
    evidence: L3
  - name: unknown_2
    type: u32
    notes: read by the client, not sent by the server
    evidence: L3
  - name: unknown_3
    type: u32
    notes: read by the client, not sent by the server
    evidence: L3
  - name: unknown_4
    type: f32
    notes: read by the client, not sent by the server
    evidence: L3
  - name: unknown_5
    type: u32
    notes: read by the client, not sent by the server
    evidence: L3
  - name: unknown_6
    type: u32
    notes: read by the client, not sent by the server
    evidence: L3
  - name: unknown_7
    type: u32
    notes: read by the client, not sent by the server
    evidence: L3
# Read from the server's writer SendSpellActivationHide (u32 spellId, u32 0) against the client's asm read sequence.

# Widths and order, read out of the handler at 0x00235840 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00235840"
    registration_site_rva: "0x002295a5"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/FrameXML/Ascension_CoAResources/CoA_SpellActivationOverlayOld.lua

client_strings:   # literals the handler references, from its decompilation
  - "SPELL_ACTIVATION_HIDE"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: SPELL_ACTIVATION_HIDE
    args: [spellID, position]
    source: "Interface/FrameXML/Ascension_CoAResources/CoA_SpellActivationOverlayOld.lua:104"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: SPELL_ACTIVATION_HIDE
    format: "%u"
    helper: FUN_10278c90
    read_slots: [0]   # indices into the 2 reads this pass saw; the layout has 8, so not field numbers
  - event: SPELL_ACTIVATION_HIDE
    format: "%u%s"
    helper: FUN_10278c90
    read_slots: [0, null]   # indices into the 2 reads this pass saw; the layout has 8, so not field numbers

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-coa-challenges/src/CoA.Challenges.Scripts.cpp

provenance:
  - level: L3
    source: "the server's writer SendSpellActivationHide (u32 spellId, u32 0) against the client's asm read sequence"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x09b2 is named SMSG_SPELL_ACTIVATION_HIDE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
