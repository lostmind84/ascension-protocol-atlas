# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0701
name: SMSG_PLAYER_GM_TICKET_INFO
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  The player's GM ticket: one ticket record (0x0709 lists its fields).

layout:
  - name: ticket
    type: bytes_variable
    notes: 'the player''s own ticket, read through the same deserializer as 0x0709, which lists the fields; PLAYER_TICKET_UPDATE afterwards.

      '
    evidence: L3
# Read from the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20); the record's member names come from the Lua table the client builds from it (GetTicketAtIndex -> FUN_10248ea0, which pushes each field by name), L3.

# Read from the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest is noted as not followed.

client:
  handler:
    rva: "0x00253dd0"
    registration_site_rva: "0x002551e6"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "PLAYER_TICKET_UPDATE"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: PLAYER_TICKET_UPDATE
    args: []
    source: "Interface/AddOns/Ascension_TicketUI/ViewTicketPanelMixin.lua:146"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: PLAYER_TICKET_UPDATE
    format: null
    helper: FUN_100b94b0

provenance:
  - level: L3
    source: "the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20); the record's member names come from the Lua table the client builds from it (GetTicketAtIndex -> FUN_10248ea0, which pushes each field by name), L3"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest is noted as not followed"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0701 is named SMSG_PLAYER_GM_TICKET_INFO"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
