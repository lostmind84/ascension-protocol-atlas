# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x059c
name: CMSG_CHALLENGE_SYNC_RESPONSE
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Answer to SMSG_CHALLENGE_SYNC_REQUEST 0x059B: the client writes one byte; the server's handler (CMSG_COA_SYNC_RESPONSE) is where its meaning is decided, not read here.

# AzerothCore calls 0x059c CMSG_COA_SYNC_RESPONSE. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout:
  - name: unknown_0
    type: u8
    evidence: L3
# Read from the client's sender (tools/ghidra/scripts/SenderC.java: PutData(&len,4) then PutData(ptr,len) is the length-prefixed string) and the CoA server's reader in mod-coa-challenges (L1 names).

# Widths and order, read out of the client's sender at 0x00143064 by tools/extract/client_sender.py.

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-ascension-compat/src/AscensionCompat.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Core.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.History.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Lifecycle.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Review.h
    - modules/mod-coa-challenges/src/CoA.Challenges.Scripts.cpp

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: SendChallengeSyncResponse
  function_rva: "0x00142f90"
  calls:
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 1177, args: ["true"]}
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 1180, args: ["false"]}
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 1209, args: ["true"]}
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 1212, args: ["false"]}

provenance:
  - level: L3
    source: "the client's sender (tools/ghidra/scripts/SenderC.java: PutData(&len,4) then PutData(ptr,len) is the length-prefixed string) and the CoA server's reader in mod-coa-challenges (L1 names)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x059c is named CMSG_CHALLENGE_SYNC_RESPONSE"

replay: null
