# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0589
name: SMSG_CALLBOARD_VIEW
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  The callboard view: a count and that many 100-byte entries. The entry's own fields were not decoded.

layout:
  - name: count
    type: u32
    notes: bounded by the reader at 42 949 672 entries; it allocates count records of 100 bytes
    evidence: L3
  - repeat: count
    fields:
    - name: entry
      type: bytes_100
      notes: one callboard entry; the reader (FUN_10116c10) fills it through per-field sub-readers whose order was not followed. CALLBOARD_VIEW_UPDATED afterwards
      evidence: L3
# Read from the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20).

# Read from the decompiled handler (build/ghidra/handlers-c), read by hand.

# Widths and order, read out of the handler at 0x00116c10 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x001194e0"
    registration_site_rva: "0x00119422"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/AscensionUI/Gossips/CallBoardCore.lua

client_strings:   # literals the handler references, from its decompilation
  - "CALLBOARD_VIEW_UPDATED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: CALLBOARD_VIEW_UPDATED
    args: []
    source: "Interface/AddOns/AscensionUI/Gossips/CallBoardCore.lua:273"

provenance:
  - level: L3
    source: "the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0589 is named SMSG_CALLBOARD_VIEW"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
