# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06fc
name: SMSG_PATCH_CREATURE_DISPLAY_INFO_GEOSET_DATA
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_PATCH_CREATURE_DISPLAY_INFO_GEOSET_DATA by the client's own opcode table. patch-tables.yaml binds this handler to CreatureDisplayInfoGeosetData.dbc's own CustomDBCMgr object; the handler reads exactly the table's 4 all-numeric fields and stores them by id. No source names any of the four.

layout:
  - name: field_0
    type: u32
    evidence: L3
  - name: field_1
    type: u32
    evidence: L3
  - name: field_2
    type: u32
    evidence: L3
  - name: field_3
    type: u32
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x06fc.c), read by hand. confirms tools/extract/packet_layout.py's reading: four individual 4-byte reads, no string, matching CreatureDisplayInfoGeosetData.dbc's own header exactly (4 fields, 16 bytes).

client:
  handler:
    rva: "0x001e1440"
    registration_site_rva: "0x001e6b8f"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: CreatureDisplayInfoGeosetData.dbc
    instance: "0x10bdfe64"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 16
    fields: 4

column_names: none found
  # checked: client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (no
  # string literal near the store); lua-keys.yaml (no builder in this table's
  # domain); the Ascension UI/Lua corpus (build/corpus/ascension-ui-patch-b-stock,
  # searched for the table's own base name) -- no getter or builder reads it by name.

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x06fc.c), read by hand"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06fc is named SMSG_PATCH_CREATURE_DISPLAY_INFO_GEOSET_DATA"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
