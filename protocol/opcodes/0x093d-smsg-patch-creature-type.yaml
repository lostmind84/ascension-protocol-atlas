# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x093d
name: SMSG_PATCH_CREATURE_TYPE
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of the stock CreatureType.dbc (AzerothCore already loads this table; ADR 0005), read by the handler
  at RVA 0x0022b9e0 as 2 dwords then 1 lpstring. This is the rare case in this shard where the wire read
  reconciles exactly with the file: CreatureType.dbc has 19 columns (client/patch-table-names.md) -- ID
  (active), the 17-column Name[16]+flag locale block (fully commented out, format char 'x' not 's'), and
  no_expirience (commented out, "no exp? critters, non-combat pets, gas cloud."). 2 numeric + 1 string is
  exactly the file's 2 non-string columns plus its 1 (disabled) locale block, with nothing left over. ID is
  named from DBCStructure.h's only active member (L1). no_expirience stays an unnamed field per the 'x' rule
  even though the core's own comment names it (by elimination it is field_1, the only other real column in
  the file). The lpstring is named Name: even though it is core 'x'-marked (disabled), it is the only
  string-type candidate in the whole record, so the identification carries no order ambiguity (L1).

layout:
  - name: ID
    type: u32
    notes: active CreatureTypeEntry member (column 0)
    evidence: L1
  - name: field_1
    type: u32
    notes: >-
      core format char 'x': skipped, no formal name. By elimination (the file's only other real column) this
      is CreatureTypeEntry's commented-out no_expirience (column 18): "no exp? critters, non-combat pets, gas
      cloud." Not asserted as the field's name per the 'x' convention (client/patch-table-names.md bucket 3).
    evidence: L1
  - name: Name
    type: lpstring
    notes: >-
      the file's only string-type column (Name[16], columns 1-16, plus its flag at 17; core 'x'-marked/
      disabled, CreatureTypeEntry comments it "//char const* Name[16]"). The only candidate in the record, so
      this identification carries no order ambiguity.
    evidence: L1
# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# the asm idiom (packet_layout.py) saw the same reads minus the strings. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x0022b9e0"
    registration_site_rva: "0x00228e53"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h (CreatureTypeEntry) and DBCfmt.h (CreatureTypefmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md: the file's 19 columns are exactly ID + Name[16]+flag (17) + no_expirience, matching the wire's 2 dwords + 1 lpstring with no leftover"
    claim: "the name ID for the leading dword and Name for the trailing lpstring; the identification (not the name) of field_1 as no_expirience"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x093d is named SMSG_PATCH_CREATURE_TYPE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
