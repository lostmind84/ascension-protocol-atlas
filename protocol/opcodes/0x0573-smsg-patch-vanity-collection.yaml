# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0573
name: SMSG_PATCH_VANITY_COLLECTION
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Three inline strings, sent with no other data. VanityCollection.dbc (76 fields, 308 bytes) has exactly 3 string columns -- artwork (+0x0c), description (+0xa8), additionalText (+0xac) -- identified by decompiling the client's own Lua-table builder for this row, FUN_1032f6e0 (VA 0x1032f6e0), whose other ~42 pushed keys (id, itemid, quality, categoryMask, dpCost/spCost/btCost, creaturePreview, spellPreview, a 24-element contentsPreview list, flags, learnedSpell, allowableClass, allowableRace) all read plain numeric fields the wire never sends, confirming the SMSG only ever patches these 3 text columns and leaves the rest of the row as the base DBC defines it. This pass could not establish which of the 3 wire strings fills which of the 3 offsets: the handler hands them to an intermediate helper (FUN_1032ec60) that fires a HOT_PATCH_VANITY_COLLECTION notification rather than storing them with a traceable 1:1 copy, so naming half the row (the column set) is as far as this pass got -- the order is for the next pass.

layout:
  - name: string_1
    type: cstring
    notes: 'the wire''s 1st of 3 inline (not length-prefixed) strings; no NUL on the wire, the handler finds the end itself. VanityCollection.dbc has exactly 3 string columns (confirmed by decompiling the Lua-table builder FUN_1032f6e0, VA 0x1032f6e0): `artwork` at record +0x0c, `description` at record +0xa8 and `additionalText` at record +0xac. Which of those three this wire position fills is not established -- the handler assembles the row through an intermediate helper (FUN_1032ec60) whose own local layout this pass did not trace far enough to pin the order down.'
    evidence: L3
  - name: string_2
    type: cstring
    notes: the wire's 2nd of 3 inline strings. See string_1 for the 3 candidate columns.
    evidence: L3
  - name: string_3
    type: cstring
    notes: the wire's 3rd of 3 inline strings. See string_1 for the 3 candidate columns.
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x0573.c) and the decompiled Lua-table builder FUN_1032f6e0 (Ghidra DecompAt.java 1032f6e0, workspace named-lua-ui), both read by hand, 2026-09-20.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# packet_layout.py had followed a call into 0x001d3a00, not the handler. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x001e55d0"
    registration_site_rva: "0x001e685b"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/FrameXML/Data/LiveUpdate.lua

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: VanityCollection.dbc
    instance: "0x10be0480"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 308
    fields: 76

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x0573.c) and the decompiled Lua-table builder FUN_1032f6e0 (Ghidra DecompAt.java 1032f6e0, workspace named-lua-ui), both read by hand, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0573 is named SMSG_PATCH_VANITY_COLLECTION"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
