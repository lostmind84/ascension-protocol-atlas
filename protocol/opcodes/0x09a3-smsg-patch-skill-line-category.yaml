# Generated by tools/codegen/stub_fiches.py, then completed by hand (naming-source closure pass).
id: 0x09a3
name: SMSG_PATCH_SKILL_LINE_CATEGORY
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  A record patched into one of Extensions.dll's internal id-keyed containers -- looked up by id
  (RVA 0x000b2020), allocated fresh as a 0x20-byte heap struct when new (RVA 0x000ae5d20), and
  inserted (RVA 0x000acba0) -- not one of the 114 CustomDBCMgr `.data` tables
  client/patch-tables.md documents: nothing in the decompiled handler references a fixed .data
  table object (no `+0xc`/`+0x10`/`+0x20` min/max/rows idiom anywhere in it, unlike 0x09d1/0x09d2
  in this same shard, which do). client/patch-table-names.md's third-party reference names
  `SkillLineCategory.dbc` for this opcode (19 fields, 76-byte record) but the handler's fixed
  portion is 12 bytes, not 76 -- the widths do not match, on top of the binding already being
  unverified (`L1 ref`, absent from our own patch-tables.yaml scan). No source in this repository
  names the struct's own fields (see naming_source below). The raw decompile
  (build/ghidra/handlers-c/0x09a3.c) is hard to follow past the compiler's stack-guard check: it
  reads as two id-keyed stores back to back, apparently a cold-path duplication artifact around
  `__security_check_cookie`. The wire read sequence below is the tool-extracted one
  (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml), which is a single coherent
  trace and is what this fiche trusts; neither visible store touches a CustomDBCMgr table object.

layout:
  - name: field_0
    type: bytes_12
    notes: >
      width: L3 (block-copy read, 12 bytes; re-verified this pass against the cached decompile,
      build/ghidra/handlers-c/0x09a3.c, against
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source (see naming_source below). 12 bytes does NOT match SkillLineCategory.dbc's claimed
      record_size (76 bytes, 19 fields, per client/patch-table-names.md) at all, which further
      undermines that already-unverified (L1) binding.
    evidence: L3
  - name: field_1
    type: lpstring
    notes: >
      width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source.
    evidence: L3
  - name: field_2
    type: lpstring
    notes: >
      width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source.
    evidence: L3
  - name: field_3
    type: lpstring
    notes: >
      width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source.
    evidence: L3
  - name: field_4
    type: lpstring
    notes: >
      width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source.
    evidence: L3

client:
  handler:
    rva: "0x00230260"
    registration_site_rva: "0x00229465"
    evidence: L3

naming_source:
  result: none found
  checked:
    - "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml: 0x09a3 has no strings recorded"
    - "client/symbols/ascension-extensions-2026-08-13/lua-keys.yaml: 22 builders extracted; none in this table's domain"
    - "SkillLineCategory.dbc search in build/corpus/ascension-ui-patch-b-stock (grepped for the bare table name minus \".dbc\"): 0 files"
  claimed_table:
    dbc: SkillLineCategory.dbc
    fields: 19
    record_size: 76
    rows: 9
    bound_by: "L1 ref only (client/patch-table-names.md); absent from client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml's own CustomDBCMgr scan"
    width_check: "does NOT match the handler's fixed-portion read (12 bytes)"

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x09a3.c (decompiled handler, re-read this pass) and client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order; the handler looks the id up in an internal container (RVA 0x000b2020), allocates a fresh 0x20-byte struct when it is new (RVA 0x000ae5d20), and inserts it (RVA 0x000acba0) -- not one of the 114 CustomDBCMgr .data table objects patch-tables.yaml enumerates"
  - level: L1
    source: "client/patch-table-names.md (opcode 0x09a3 row)"
    claim: "a third-party reference names the DBC table SkillLineCategory.dbc for this opcode; unverified by our own patch-tables.yaml scan and not treated as established here"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml, lua-keys.yaml, and build/corpus/ascension-ui-patch-b-stock (this pass)"
    claim: "no naming source for the struct's own fields exists in any of the three"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x09a3 is named SMSG_PATCH_SKILL_LINE_CATEGORY"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
