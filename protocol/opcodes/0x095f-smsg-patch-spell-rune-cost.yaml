# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x095f
name: SMSG_PATCH_SPELL_RUNE_COST
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of SpellRuneCost.dbc (bound_by: L1 ref, patch-table-names.md): the handler's single 20-byte bulk read exactly equals the table's 5-column/20-byte record (client DBC header). All 5 columns are named by AzerothCore's own SpellRuneCostEntry struct (L1, another project's reading of the same file).

layout:
  - name: id
    type: u32
    notes: 'DBC column 0 (index field, fmt ''n''); core: SpellRuneCostEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar2,20]], one 20-byte bulk read == the full 5-column/20-byte SpellRuneCost.dbc record); name L1 (core DBCStructure.h, another project''s reading of the same file).'
    evidence: L1
  - name: runeCost_blood
    type: u32
    notes: 'DBC column 1; core: SpellRuneCostEntry::RuneCost[0], ''0=blood'' per core''s comment. Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
  - name: runeCost_frost
    type: u32
    notes: 'DBC column 2; core: RuneCost[1], ''1=frost''. Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
  - name: runeCost_unholy
    type: u32
    notes: 'DBC column 3; core: RuneCost[2], ''2=unholy''. Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
  - name: runePowerGain
    type: u32
    notes: 'DBC column 4; core: SpellRuneCostEntry::runePowerGain. Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h SpellRuneCostEntry + DBCfmt.h SpellRuneCostfmt (L1: another project's reading of the same client DBC file, not ours).

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00232d60"
    registration_site_rva: "0x0022904e"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h SpellRuneCostEntry + DBCfmt.h SpellRuneCostfmt (L1: another project's reading of the same client DBC file, not ours)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "client/patch-table-names.md (the naming-sources sweep): 0x095f bound_by 'L1 ref' to SpellRuneCost.dbc, 5 fields / 20 bytes, matching the client DBC header"
    claim: "this opcode's table is SpellRuneCost.dbc, at L1 (only the third-party reference names it; patch-tables.yaml does not bind this opcode)"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x095f is named SMSG_PATCH_SPELL_RUNE_COST"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
