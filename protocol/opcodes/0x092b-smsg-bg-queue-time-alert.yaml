# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x092b
name: SMSG_BG_QUEUE_TIME_ALERT
direction: server_to_client
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_BG_QUEUE_TIME_ALERT by the client's own opcode table. Layout and behaviour not
  established.

layout:
  - name: timeInQueue
    type: u32
    evidence: L3
  - name: currentQueueID
    type: u32
    evidence: L3
  - name: suggestedQueueID
    type: u32
    evidence: L3
# Widths and order, read out of the handler at 0x000e6de0 by tools/extract/packet_layout.py.
# Names: the Lua arguments of ASCENSION_BG_QUEUE_ALERT (client/event-args.yaml, L2), at the positions the
# handler fills them from its reads (handler-fire.yaml, format %u%u%u, L3). Unnamed fields are
# read but not passed to the event.
# The structure is flat: a count followed by a repeated record appears here as
# the count plus one record's fields.

client:
  handler:
    rva: "0x000e6de0"
    registration_site_rva: "0x000e6f20"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "%u%u%u"
  - "ASCENSION_BG_QUEUE_ALERT"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: ASCENSION_BG_QUEUE_ALERT
    args: [timeInQueue, currentQueueID, suggestedQueueID]
    source: "Interface/FrameXML/Ascension_LFG/Ascension_PVP.lua:289"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: ASCENSION_BG_QUEUE_ALERT
    format: "%u%u%u"
    helper: FUN_10278c90
    fields: [field_0, field_1, field_2]   # the read filling each format position

provenance:
  - level: L2
    source: "client/event-args.yaml (ASCENSION_BG_QUEUE_ALERT) joined through client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml"
    claim: "the field names, as the UI's method named after the event declares them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x092b is named SMSG_BG_QUEUE_TIME_ALERT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
