# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06ef
name: SMSG_UPDATE_WILDCARD_MISC_DATA
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One index worth of wildcard misc data, the same five u32 as a 0x06EE item, written for the index the service currently selects (no index in the packet). The three counters are compared with what was held and each change fires its WILDCARD_REPURCHASABLE_*_CHANGED event.

layout:
  - name: unknown_0
    type: u32
    notes: stored at the wildcard service +8 for the current index
    evidence: L3
  - name: unknown_1
    type: u32
    notes: stored at +0x58
    evidence: L3
  - name: repurchasableRolls
    type: u32
    notes: compared with the value held before; a change fires WILDCARD_REPURCHASABLE_ROLLS_CHANGED(%u%u)
    evidence: L3
  - name: repurchasableAbilityRolls
    type: u32
    notes: a change fires WILDCARD_REPURCHASABLE_ABILITY_ROLLS_CHANGED
    evidence: L3
  - name: repurchasableTalentRolls
    type: u32
    notes: a change fires WILDCARD_REPURCHASABLE_TALENT_ROLLS_CHANGED
    evidence: L3
# Read from the decompiled handlers (build/ghidra/handlers-c/0x06ee.c, 0x06ef.c): five u32 per index stored in five 20-slot arrays of the wildcard service, the last three being the counters whose change fires WILDCARD_REPURCHASABLE_ROLLS_CHANGED, _ABILITY_ROLLS_CHANGED and _TALENT_ROLLS_CHANGED (0x06EF).

# Widths and order, read out of the handler at 0x00a31130 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00a31130"
    registration_site_rva: "0x00a315b4"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "WILDCARD_REPURCHASABLE_ROLLS_CHANGED"
  - "WILDCARD_REPURCHASABLE_ABILITY_ROLLS_CHANGED"
  - "WILDCARD_REPURCHASABLE_TALENT_ROLLS_CHANGED"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: WILDCARD_REPURCHASABLE_ROLLS_CHA
    format: "%u%u"
    helper: FUN_100b9560
  - event: WILDCARD_REPURCHASABLE_ABILITY_R
    format: "%u%u"
    helper: FUN_100b9560
  - event: WILDCARD_REPURCHASABLE_TALENT_RO
    format: "%u%u"
    helper: FUN_100b9560

provenance:
  - level: L3
    source: "the decompiled handlers (build/ghidra/handlers-c/0x06ee.c, 0x06ef.c): five u32 per index stored in five 20-slot arrays of the wildcard service, the last three being the counters whose change fires WILDCARD_REPURCHASABLE_ROLLS_CHANGED, _ABILITY_ROLLS_CHANGED and _TALENT_ROLLS_CHANGED (0x06EF)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06ef is named SMSG_UPDATE_WILDCARD_MISC_DATA"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
