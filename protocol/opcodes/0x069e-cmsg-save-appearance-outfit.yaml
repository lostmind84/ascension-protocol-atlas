# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x069e
name: CMSG_SAVE_APPEARANCE_OUTFIT
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Save an appearance outfit: its name, then the ids of the pending outfit. The skeleton's two u32 were the count and the first id; the string comes before them.

layout:
  - name: name
    type: cstring
    notes: SaveOutfit(name); a NEW_OUTFIT literal is special-cased before the write
    evidence: L3
  - name: count
    type: u32
    notes: the size of the pending outfit's id vector
    evidence: L3
  - repeat: count
    fields:
    - name: id
      type: u32
      notes: one entry of that vector -- the appearance ids the outfit holds, by the surrounding code, not read to the end
      evidence: L3
# Read from the decompiled sender binding (tools/ghidra/scripts/DumpC.java over the DLL), read by hand next to the UI's calls (L2).

# Widths and order, read out of the client's sender at 0x000cb1c7 by tools/extract/client_sender.py.
# A helper is called between the opcode write and the send: this is at best a
# PREFIX of the payload. An empty list here does not mean an empty packet.

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_AppearanceUI/AppearanceModelMixin.lua
  - Interface/FrameXML/StaticPopup.lua

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: SaveOutfit
  function_rva: "0x000caef0"
  calls:
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 330, args: ["outfitName"]}
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 355, args: ["name"]}

provenance:
  - level: L3
    source: "the decompiled sender binding (tools/ghidra/scripts/DumpC.java over the DLL), read by hand next to the UI's calls (L2)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x069e is named CMSG_SAVE_APPEARANCE_OUTFIT"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
