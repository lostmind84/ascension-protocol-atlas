# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x061d
name: CMSG_WILDCARD_UNLEARN_ABILITY
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Wildcard unlearn ability: one u32. The sender at 0x0017fd35 writes it and nothing else; which Lua binding it belongs to is not established.

layout:
  - name: unknown_0
    type: u32
    notes: the one u32 the sender at 0x0017fd35 writes after the opcode; the fiche's binding attribution (UnitTalentRankByID) is the nearest labelled function, not this sender
    evidence: L3
# Read from the decompiled sender at the site (DecompAt.java, 2026-09-20).

# Widths and order, read out of the client's sender at 0x0017fd35 by tools/extract/client_sender.py.

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_WildCard/WildCard.lua

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: UnitTalentRankByID
  function_rva: "0x0017f9d0"
  calls:
    - {file: "Interface/AddOns/Ascension_InspectUI/Panels/InspectBuildPanel.lua", line: 147, args: ["unit", "entry.ID", "self.activeSpec"]}
    - {file: "Interface/AddOns/Ascension_InspectUI/Panels/InspectBuildPanel.lua", line: 350, args: ["self.unit", "entry.ID", "self.specID"]}
    - {file: "Interface/LibraryXML/LibGroupTalents-1.0/LibGroupTalents-1.0.lua", line: 760, args: ["unit", "info.id", "group"]}
    - {file: "Interface/LibraryXML/LibGroupTalents-1.0/LibGroupTalents-1.0.lua", line: 1622, args: ["unit", "info.id", "active"]}

provenance:
  - level: L3
    source: "the decompiled sender at the site (DecompAt.java, 2026-09-20)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x061d is named CMSG_WILDCARD_UNLEARN_ABILITY"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
