# Generated by tools/codegen/stub_fiches.py, then hand-filled (patch-unnamed-1 sweep, 2026-09-20).
# This is a CoA-custom DBC table row with NO naming source in anything this repo holds
# (see client/patch-table-names.md). field_N names are placeholders; do not rename them
# without a cited source (lua-keys.yaml, a UI getter, or handler-strings.yaml).

id: 0x0567
name: SMSG_PATCH_CREATURE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  SMSG_PATCH_CREATURE: one packet that writes a row into TWO tables, Creature.dbc and DungeonEncounterExtra.dbc, from a handler shared with the other opcode in this pair (0x0567 and 0x0568 decompile to the identical function body, FUN_101e14f0 at RVA 0x001e14f0; each opcode is registered at its own small entry point that reaches the same code). Neither row is a full-width copy of its DBC's own record: the Creature.dbc side stores only 7 of that table's 23 fields (appended past the table's own maxId, ignoring this packet's own first field as an index), while the DungeonEncounterExtra.dbc side is a genuine full match of that table's 4 fields. No column name is established for either table.

layout:
  - name: field_0
    type: u32
    notes: "record 1 (Creature.dbc side): offset 0; not used as the row's id -- the id actually used is Creature.dbc's own maxId+1 (DAT_10be0468, an auto-increment append), not this field"
    evidence: L3
  - name: field_1
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further"
    evidence: L3
  - name: field_2
    type: u32
    notes: "offset 8"
    evidence: L3
  - name: field_3
    type: u32
    notes: "offset 0xc"
    evidence: L3
  - name: field_4
    type: u32
    notes: "offset 0x10"
    evidence: L3
  - name: field_5
    type: u32
    notes: "offset 0x14"
    evidence: L3
  - name: field_6
    type: lpstring
    notes: "via FUN_100d3680; record 1 is 7 fields (0x1c=28 bytes) allocated by FUN_10ae5d20, far short of Creature.dbc's own 23-field/92-byte row"
    evidence: L3
  - name: field_7
    type: u32
    notes: "record 2 starts here (DungeonEncounterExtra.dbc side), offset 0 of the second record; used as the min/maxId-bounded array index into the table object at 0x10bdfabc (matches DungeonEncounterExtra.dbc exactly: 4 fields, 16-byte record)"
    evidence: L3
  - name: field_8
    type: u32
    notes: "offset 4 of record 2, first half of an 8-byte load"
    evidence: L3
  - name: field_9
    type: u32
    notes: "offset 8 of record 2, second half of the same 8-byte load; read as one 8-byte load in the handler; split into two 4-byte fields here to line up with the table's own field count -- could be a single 64-bit value instead, not resolved further"
    evidence: L3
  - name: field_10
    type: u32
    notes: "offset 0xc of record 2, last of DungeonEncounterExtra.dbc's 4 fields"
    evidence: L3

client:
  handler:
    rva: "0x001e14f0"
    registration_site_rva: "0x001e679a"
    evidence: L3

patches_table:   # the client DBC table(s) this packet writes into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3 unless noted; record_size/fields/rows from client/patch-table-names.md's own header read against the ascension-live-2026-09-17 snapshot, L2)
  - dbc: Creature.dbc
    instance: "0x10be045c"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 92
    fields: 23
    rows: 127343
  - dbc: DungeonEncounterExtra.dbc
    instance: "0x10bdfabc"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 16
    fields: 4
    rows: 2069

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x0567.c (already-decompiled handler, read by hand this pass)"
    claim: "the widths, order and destination struct of the wire fields"
  - level: L3
    source: "hand-tracing both records past the swi(0x29) branch this pass (build/ghidra/handlers-c/0x0567.c and 0x0568.c, identical)"
    claim: "the decompiled C renders record 2's read behind what looks like a stack-cookie failure branch (swi(0x29), the /GS __fastfail idiom): the code after it re-reads the packet cursor from a second stack slot (iStack_50) using the exact same idiom as the top of the function, and builds a second id-bounded row matching DungeonEncounterExtra.dbc's real 4-field/16-byte geometry exactly. patch_tables.yaml independently binds this opcode to DungeonEncounterExtra.dbc as a second table on the strength of its own DAT_ global scan, not through this reasoning. Three separate opcodes in this shard show the identical shape (0x0567/0x0568 sharing one handler, and 0x06a9 independently): read here as the compiler's /GS trailer artifact and the block after it as the packet's real second half, not as unreachable/exceptional code"
    note: "not confirmed at the raw-assembly/disassembly level -- no fresh Ghidra decompile was spent isolating it this pass; stated as an open point, not a proven fact"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (handler 0x0567)"
    claim: "no string literal is referenced near this handler's store -- checked, empty"
    note: "rules out an event name or format string that might have hinted at a field's purpose"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0567 is named SMSG_PATCH_CREATURE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L1
    source: "client/patch-table-names.md (the naming-source sweep over all 228 SMSG_PATCH_* opcodes)"
    claim: "no lua-keys builder, UI getter, or DBCfmt entry names this table's columns; the reference community source offers no names for it either (bucket 'none found')"

replay: null
