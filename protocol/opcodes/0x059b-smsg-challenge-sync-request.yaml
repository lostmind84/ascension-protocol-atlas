# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x059b
name: SMSG_CHALLENGE_SYNC_REQUEST
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  The server asks the client to sync its challenge state: a timeout and a list of (id, level) pairs the client stores, then CHALLENGE_SYNC_REQUEST fired with the timeout (format %u). Names from the server's writer (L1); the client reads exactly that.

# AzerothCore calls 0x059b SMSG_COA_CHALLENGE_SYNC_REQUEST. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout:
  - name: timeoutMs
    type: u32
    evidence: L3
  - name: count
    type: u32
    notes: 0 when the server sends a removal
    evidence: L3
  - repeat: count
    fields:
    - name: id
      type: u32
      evidence: L3
    - name: level
      type: u32
      evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c, DumpC.java) and the CoA server's writer in mod-coa-challenges.

# Widths and order, read out of the handler at 0x00137db0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00137db0"
    registration_site_rva: "0x0013b7fe"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/FrameXML/UIParent.lua

client_strings:   # literals the handler references, from its decompilation
  - "CHALLENGE_SYNC_REQUEST"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: CHALLENGE_SYNC_REQUEST
    format: "%u"
    helper: FUN_10278c90
    read_slots: [0]   # indices into the 4 reads this pass saw; the layout has 3, so not field numbers

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-coa-challenges/src/CoA.Challenges.Core.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.History.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Lifecycle.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Review.h
    - modules/mod-coa-challenges/src/CoA.Challenges.Scripts.cpp

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c, DumpC.java) and the CoA server's writer in mod-coa-challenges"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x059b is named SMSG_CHALLENGE_SYNC_REQUEST"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
