# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0943
name: SMSG_PATCH_HOLIDAYS
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of the stock Holidays.dbc (AzerothCore already loads this table; ADR 0005), read by the handler at RVA 0x0022cd10 as an 8-byte read, a dword, a variable-width read (handler-fire.yaml marks its width as undetermined, likely a loop over one of HolidaysEntry's arrays), 4 more dwords, then a trailing lpstring. Only TextureFilename (the sole locale-string-free char* column in the file) is named, from DBCStructure.h (L1); the numeric portion is array-heavy and does not reconcile cleanly with HolidaysEntry's column list, so nothing else is named here.

layout:
  - name: field_0
    type: bytes_8
    notes: a single 8-byte read in the decompiled trace (local_74, handler-fire.yaml); not independently confirmed to be two separate 4-byte columns
    evidence: L3
  - name: field_1
    type: u32
    evidence: L3
  - name: field_2
    type: bytes_variable
    notes: the decompiled trace marks this read's width as variable/undetermined (local_44, handler-fire.yaml); likely a loop over one of HolidaysEntry's arrays (Duration[MAX_HOLIDAY_DURATIONS], Date[MAX_HOLIDAY_DATES] or CalendarFlags[MAX_HOLIDAY_FLAGS]), not pinned further
    evidence: L3
  - name: field_3
    type: u32
    evidence: L3
  - name: field_4
    type: u32
    evidence: L3
  - name: field_5
    type: u32
    evidence: L3
  - name: field_6
    type: u32
    evidence: L3
  - name: TextureFilename
    type: lpstring
    notes: the file's only string-type column (col 51, active HolidaysEntry member, a single non-array char*) and the only candidate, so this identification is safe even though field_0..field_6 above are not individually reconciled against HolidaysEntry's column layout.
    evidence: L1
# Read from client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence, RVA 0x0022cd10): an 8-byte read, a dword, a variable-width read, 4 more dwords and a trailing lpstring, replacing the previous flat 10-field reading from packet_layout.py, which omitted the variable-width read and the trailing string. AzerothCore src/server/shared/DataStores/DBCStructure.h (HolidaysEntry) and DBCfmt.h (Holidaysfmt) name TextureFilename as the file's only string-type column (unambiguous); HolidaysEntry's other members are mostly fixed-size arrays (Duration[10], Date[26], CalendarFlags[10]) that do not obviously explain the observed read shape, so no other field is named.

client:
  handler:
    rva: "0x0022cd10"
    registration_site_rva: "0x00228eb0"
    evidence: L3

provenance:
  - level: L1
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence, RVA 0x0022cd10): an 8-byte read, a dword, a variable-width read, 4 more dwords and a trailing lpstring, replacing the previous flat 10-field reading from packet_layout.py, which omitted the variable-width read and the trailing string. AzerothCore src/server/shared/DataStores/DBCStructure.h (HolidaysEntry) and DBCfmt.h (Holidaysfmt) name TextureFilename as the file's only string-type column (unambiguous); HolidaysEntry's other members are mostly fixed-size arrays (Duration[10], Date[26], CalendarFlags[10]) that do not obviously explain the observed read shape, so no other field is named."
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "RETRACTED for this opcode: this was packet_layout.py's flat 10-field reading (0-40 bytes), which omitted the variable-width read and the trailing string seen in handler-fire.yaml. Kept per docs/EVIDENCE.md's retraction rule; do not rely on it as the complete record."
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0943 is named SMSG_PATCH_HOLIDAYS"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
