# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06fb
name: SMSG_PATCH_GAMEOBJECT_DISPLAY_INFO_ADDON
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_PATCH_GAMEOBJECT_DISPLAY_INFO_ADDON by the client's own opcode table. patch-tables.yaml binds this handler to GameObjectDisplayInfoAddon.dbc's own CustomDBCMgr object; the handler reads exactly the table's 2 fields (an id, then a NUL-terminated string standing in for the disk row's string-offset column) and stores them by id. No source names either column.

layout:
  - name: field_0
    type: u32
    evidence: L3
  - name: field_1
    type: cstring
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x06fb.c), read by hand. packet_layout.py's own scan (the earlier stub) called the second field u32; the handler actually scans it to a NUL terminator (a do/while loop over the cursor), the cstring idiom in client/wire-path.md section 4 -- so on the wire it is a string, not a raw dword. On disk, GameObjectDisplayInfoAddon.dbc's second column is a 4-byte string-offset (patches_table below), and the client stitches the row's own string over it, per client/patch-tables.md.

client:
  handler:
    rva: "0x0020c190"
    registration_site_rva: "0x0020c392"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: GameObjectDisplayInfoAddon.dbc
    instance: "0x10bdfe40"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 8
    fields: 2

column_names: none found
  # checked: client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (no
  # string literal near the store); lua-keys.yaml (no builder in this table's
  # domain); the Ascension UI/Lua corpus (build/corpus/ascension-ui-patch-b-stock,
  # searched for the table's own base name) -- no getter or builder reads it by name.

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x06fb.c), read by hand"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06fb is named SMSG_PATCH_GAMEOBJECT_DISPLAY_INFO_ADDON"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
