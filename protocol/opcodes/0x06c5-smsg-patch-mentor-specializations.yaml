# Generated by tools/codegen/stub_fiches.py, then hand-filled (patch-unnamed-1 sweep, 2026-09-20).
# This is a CoA-custom DBC table row with NO naming source in anything this repo holds
# (see client/patch-table-names.md). field_N names are placeholders; do not rename them
# without a cited source (lua-keys.yaml, a UI getter, or handler-strings.yaml).

id: 0x06c5
name: SMSG_PATCH_MENTOR_SPECIALIZATIONS
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  SMSG_PATCH_MENTOR_SPECIALIZATIONS: writes a 3-field, 12-byte record (id, two strings) into MentorSpecializations.dbc's table object, short of that table's own 19-field, 76-byte row. Unlike the rest of this shard, two of its three fields have a naming LEAD, not a naming source: PathBecomeMentorPanelMixin.lua calls `C_Tutorial.GetMentorSpecializationInfo(specID)` and unpacks the result as `name, atlas` -- a native getter whose client-visible name matches this table exactly, returning two strings in that order, matching this record's own two strings. The getter's own names are L2 (read from client Lua); pairing them onto these two specific wire fields is this pass's own inference, graded L0 per client/patch-table-names.md's own convention for a "ui getter" lead ("L2 for the names, L0 again for 'these are the row's columns'"). Because that L0 pairing -- not the L2 name -- is the weaker claim, the fields keep their field_N placeholders rather than being renamed; the lead is recorded in the field notes and provenance instead.

layout:
  - name: field_0
    type: u32
    notes: "offset 0; bounds-checked against the table object's minId/maxId; almost certainly the specID C_Tutorial's getters take, not otherwise named"
    evidence: L3
  - name: field_1
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further"
    evidence: L3
  - name: field_2
    type: cstring
    notes: "NUL-terminated, scanned byte by byte, not FUN_100d3680. A naming LEAD, not a naming source: PathBecomeMentorPanelMixin.lua:31 calls `name, atlas = C_Tutorial.GetMentorSpecializationInfo(specID)`, a native getter whose client-visible name matches this table, returning two strings in that order. The getter's own return-value names are L2 (client Lua source); pairing them onto this specific wire position over field_3 is our own inference and stays L0 per client/patch-table-names.md's own convention for a 'ui getter' lead (L2 for the names, L0 for 'these are the row's columns') -- not renamed to `name` here because that L0 pairing, not the L2 fact, is this field's weakest link"
    evidence: L3
  - name: field_3
    type: cstring
    notes: "NUL-terminated, scanned byte by byte, not FUN_100d3680. Same getter and same caveat as field_2; if the pairing holds, this is the getter's second return value (`atlas`)"
    evidence: L3

client:
  handler:
    rva: "0x001e21d0"
    registration_site_rva: "0x001e6a54"
    evidence: L3

patches_table:   # the client DBC table(s) this packet writes into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3 unless noted; record_size/fields/rows from client/patch-table-names.md's own header read against the ascension-live-2026-09-17 snapshot, L2)
  - dbc: MentorSpecializations.dbc
    instance: "0x10be0110"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 76
    fields: 19
    rows: 10

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x06c5.c (already-decompiled handler, read by hand this pass)"
    claim: "the widths, order and destination struct of the wire fields"
  - level: L2
    source: "build/corpus/ascension-ui-patch-b-stock/Interface/AddOns/Ascension_PathToAscension/PathBecomeMentorPanelMixin.lua:31 and PathFindMentorPanelMixin.lua:15"
    claim: "C_Tutorial.GetMentorSpecializationInfo(specID) returns (name, atlas); C_Tutorial.GetMentorSpecializations() returns a list of specIDs"
    note: "found this pass while checking whether the table's name appears anywhere in the Lua corpus, per the task's second suggested check; this is the naming lead for the shard"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (handler 0x06c5)"
    claim: "no string literal is referenced near this handler's store -- checked, empty"
    note: "rules out an event name or format string that might have hinted at a field's purpose"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06c5 is named SMSG_PATCH_MENTOR_SPECIALIZATIONS"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L1
    source: "client/patch-table-names.md, row 0x06C5 ('none found -- ref: partial names (L1)')"
    claim: "the map's own sweep (lua-keys.yaml builder pairing, UI getter scan) did not surface this getter; it exists without our extraction having found it, exactly the case the task's second check exists for"

replay: null
