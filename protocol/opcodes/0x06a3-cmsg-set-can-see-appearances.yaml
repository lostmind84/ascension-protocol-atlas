# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06a3
name: CMSG_SET_CAN_SEE_APPEARANCES
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named CMSG_SET_CAN_SEE_APPEARANCES by the client's own opcode table. Layout and behaviour not
  established.

layout:
  - name: canSeeItemAppearances
    type: u8
    evidence: L3
  - name: canSeeSpellAppearances
    type: u8
    evidence: L3
# width and order from the client's sender (CDataStore PutData sizes, decompiled; L3); names from the server's reader in mod-ascension-compat (L1)
# client sender: FUN_100cb2e0
# server reader: AscensionCompat.cpp:4242: packet >> canSeeItem >> canSeeSpell, both uint8
used_by_server:   # the CoA server module sends or handles this
  server_name: CMSG_SET_CAN_SEE_APPEARANCES
  files:
    - modules/mod-ascension-compat/src/AscensionCompat.cpp

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: SetCanSeeAppearances
  function_rva: "0x000cb2e0"
  calls:
    - {file: "Interface/AddOns/Ascension_AppearanceUI/AppearanceWardrobeMixin.lua", line: 28, args: ["canSeeItemTransmog", "canSeeSpellTransmog"]}
    - {file: "Interface/AddOns/Ascension_AppearanceUI/AppearanceWardrobeMixin.lua", line: 41, args: ["canSeeItemTransmog", "canSeeSpellTransmog"]}
    - {file: "Interface/SharedXML/Util/NewCharacterSetupUtil.lua", line: 130, args: ["enableTransmog", "enableTransmog"]}

provenance:
  - level: L3
    source: "tools/ghidra/ghidra.sh run Decomp.java <sender VA> + objdump of the push/call sequence (see client/client-senders.md)"
    claim: "what the client writes, field widths and order"
  - level: L1
    source: "the server's reader (see client/client-senders.md)"
    claim: "field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06a3 is named CMSG_SET_CAN_SEE_APPEARANCES"

replay: null
