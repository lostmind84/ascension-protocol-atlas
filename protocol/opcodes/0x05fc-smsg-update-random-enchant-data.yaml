# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05fc
name: SMSG_UPDATE_RANDOM_ENCHANT_DATA
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Three u32 read unconditionally into the mystic-enchant service at +0x28, +0x2c and +0x30. The handler has no loop, so the capture reference's count/id/value reading is a naming, not a structure: its single packet carries count = 1, where the two readings are the same twelve bytes.

layout:
  - name: unknown_0
    type: u32
    evidence: L3
    notes: read with unknown_1 in one two-u32 move (pos += 8), then stored at the mystic-enchant service +0x28. The capture reference reads this as a record count and observed 1 over a 12-byte payload (L1). The handler reads a fixed three u32 and contains no loop and no comparison against this value, so at count = 1 the two readings are the same twelve bytes and their capture cannot separate them; were the server ever to send count > 1, this client would consume only the first pair and ignore the remaining (count - 1) * 8 bytes.
  - name: unknown_1
    type: u32
    evidence: L3
    notes: stored at +0x2c; the capture reference calls it id and observed 0 (L1)
  - name: unknown_2
    type: u32
    evidence: L3
    notes: read separately (pos += 4) and stored at +0x30; the capture reference calls it value and observed 1 (L1)
# Read from the decompiled handler (build/ghidra/handlers-c/0x05fc.c), read by hand. The capture reference's leading count is L1 and undecidable from a 12-byte packet. See .agents/reports/conflicts.md.

# Read from the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock).

# Widths and order, read out of the handler at 0x002e9460 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x002e9460"
    registration_site_rva: "0x002e9a5f"
    evidence: L3

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x05fc.c), read by hand"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05fc is named SMSG_UPDATE_RANDOM_ENCHANT_DATA"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L1
    source: "CoinThrow's live-session capture reference, 2026-09-20 (private, kept outside the tree; our comparison is docs/crosscheck-cointhrow-2026-09-20.md)"
    claim: "the 12-byte payload and the count/id/value naming"

replay: null
