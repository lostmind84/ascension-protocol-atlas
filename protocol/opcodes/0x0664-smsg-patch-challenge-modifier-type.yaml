# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0664
name: SMSG_PATCH_CHALLENGE_MODIFIER_TYPE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of ChallengeModifierTypes.dbc in the compact wire form: the row id, four skipped string-offset dwords, then four NUL-terminated strings written over them. The runtime record is 0x14 bytes while the client's ChallengeModifierTypes.dbc has 37 fields. Like 0x0663 the strings here are NUL-terminated, and the first three are kept as pointers into the received packet rather than copied.

layout:
  - name: id
    type: u32
    notes: dword 0, the only dword the reader loads; the row key -- range-checked against minId 0x10be02d0 / maxId 0x10be02cc, indexes the row table at 0x10be02e0, then passed to FUN_10133ac0
    evidence: L3
  - name: stringOffsets
    type: bytes_16
    notes: 'dwords 1..4, SKIPPED: the cursor jumps from +0x04 to the end of the 0x14 block. The row''s four string-offset columns; the client writes the four string pointers that follow over record +0x04 .. +0x10.'
    evidence: L3
  - name: string_1
    type: cstring
    notes: NUL-TERMINATED, not length-prefixed -- like SMSG_PATCH_CHALLENGE_MODIFIER 0x0663 and unlike the rest of the family. The first three are stored as pointers INTO the packet buffer (record +0x04, +0x08, +0x0c); only the fourth is copied out.
    evidence: L3
  - name: string_2
    type: cstring
    notes: record +0x08, a pointer into the packet buffer
    evidence: L3
  - name: string_3
    type: cstring
    notes: record +0x0c, a pointer into the packet buffer
    evidence: L3
  - name: string_4
    type: cstring
    notes: record +0x10; this one is strcpy_s'd into its own allocation. The UI resolves a modifier type through C_Challenge.GetModifierLocalization (L2); which string feeds which return value is NOT established here.
    evidence: L3
# Read from the decompiled handler FUN_10139440 (build/ghidra/handlers-c/0x0664.c) read by hand, 2026-09-20.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00139440"
    registration_site_rva: "0x0013b89d"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: ChallengeModifierTypes.dbc
    instance: "0x10be02c0"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 148
    fields: 37

provenance:
  - level: L3
    source: "the decompiled handler FUN_10139440 (build/ghidra/handlers-c/0x0664.c) read by hand, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0664 is named SMSG_PATCH_CHALLENGE_MODIFIER_TYPE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
