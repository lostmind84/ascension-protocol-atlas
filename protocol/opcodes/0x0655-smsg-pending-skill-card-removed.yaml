# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0655
name: SMSG_PENDING_SKILL_CARD_REMOVED
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Pending skill cards removed: a count and one name per removed card; the event's numbers come from the matched pending record, not the packet. Whether the record carries more than the name was not followed.

layout:
  - name: count
    type: u32
    evidence: L3
  - repeat: count
    fields:
    - name: name
      type: cstring
      notes: matched against the pending entries' names; the match fires PENDING_SKILL_CARD_REMOVED(%u%u%u) with values from the matched record (pendingIndex, cardID, ...)
      evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock).

# Widths and order, read out of the handler at 0x0031a850 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x0031a850"
    registration_site_rva: "0x0031bab5"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardUnlockFrame/SkillCardUnlockFrame.lua
  - Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardsFrame.lua
  - Interface/FrameXML/Util/SkillCardsUtil.lua

client_strings:   # literals the handler references, from its decompilation
  - "%u%u%u"
  - "PENDING_SKILL_CARD_REMOVED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: PENDING_SKILL_CARD_REMOVED
    args: [...]
    source: "Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardsFrame.lua:548"
  - event: PENDING_SKILL_CARD_REMOVED
    args: [pendingIndex, cardID]
    source: "Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardUnlockFrame/SkillCardUnlockFrame.lua:762"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: PENDING_SKILL_CARD_REMOVED
    format: "%u%u%u"
    helper: FUN_10278c90
    fields: [null, null, null]   # the read filling each format position

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0655 is named SMSG_PENDING_SKILL_CARD_REMOVED"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
