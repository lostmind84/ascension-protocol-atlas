# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0700
name: SMSG_ITEM_STAT_QUERY_RESPONSE
direction: server_to_client
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_ITEM_STAT_QUERY_RESPONSE by the client's own opcode table. Not registered
  through the normal push-handler/push-opcode/call-register path; client/client-senders.md
  places it in the same client-side query-service pairing as its request 0x06ff, without an
  RVA. Layout and behaviour not established.

layout: []   # unknown
# Checked 2026-09-20: no entry in handlers.yaml (the push-handler/push-opcode/call-register
# registration scan does not find a site for this opcode) and no hit for its name anywhere
# in the ascension-ui-patch-b-stock corpus. NOT "no client code", though:
# client/client-senders.md ("The recovery service"/query-service section) documents this
# opcode, paired with its request CMSG_ITEM_STAT_QUERY (0x06ff), as one of two
# request/response pairs registered in a client-side "query service" object -- the response
# is presumably delivered through that same service rather than the normal handler
# registrar this build's scan looks for, which is why the scan sees nothing. That document
# gives no RVA for this pair and I could not relocate one this session (see 0x06ff's
# fiche). So: client code almost certainly exists to receive this response, but neither its
# address nor its payload is established here.
#
# A third-party live-session capture (~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-
# reference-2026-09-20.md, L1, not in this repo) saw two real instances (13,496 B and
# 9,452 B) and could only characterize it as "[opaque]": dense float/vector data, one or
# more embedded POI JSON objects byte-identical in shape to opcode 0x077C's, and the ASCII
# marker "QUERY_INSTANCE_BINDS_OK". Its own reading is that this is a composite reply --
# instance-bind, POI and item-stat data concatenated in one packet, likely answering a batch
# of 0x06FF/0x06FD requests together -- which fits this fiche's query-service theory well: a
# service that batches several query kinds into one response explains a payload this
# heterogeneous far better than a single fixed-field packet would. Still L1 and
# unreproduced; no layout is written from it.

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0700 is named SMSG_ITEM_STAT_QUERY_RESPONSE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (no entry) and a corpus grep for the name (no hit), checked 2026-09-20"
    claim: "no push-handler/push-opcode/call-register registration and no corpus mention -- but see the query-service note below; this is not evidence of no client code"
  - level: L2
    source: "client/client-senders.md, 'Two more opcodes are not sent by a sender at all but registered in a query service object' (committed doc, methodology given, no RVA given for this pair; not independently re-derived this session)"
    claim: "0x06ff/0x0700 are a request/response pair driven by a client-side query-service registration, not a plain sender/handler"
  - level: L1
    source: "~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md, '0x0700 -- SMSG_ITEM_STAT_QUERY_RESPONSE (composite)' (third-party live-session capture, not in this repo, unreproduced against our binary)"
    claim: "a large (9-13 KB) composite reply mixing float/vector data, embedded POI JSON and an instance-binds marker string, not a fixed item-stat record"

replay: null
