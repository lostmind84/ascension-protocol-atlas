# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x09bf
name: SMSG_SEASONAL_COLLECTION_UNLOCKED
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Seasonal collection unlocked: no packet read is visible in the handler, and none is
  needed. `handler_0x09bf` takes no field from the `CDataStore` at all; it calls
  `FUN_10310320()` -- a lazily-constructed singleton whose vftable Ghidra itself names
  `SeasonalCollectionMgr::vftable` -- and sets the byte at `+0xa0` on it to 1. The mere
  arrival of the opcode is the whole payload. This reads as a durable one-shot unlock
  flag, not a report of *what* unlocked: the Lua-exposed getter `IsSeasonalCollectionUnlocked`
  (bound at the very same address the handler jumps through, RVA 0x003111d0) reads that
  identical `+0xa0` byte and returns it to Lua as a boolean, so the handler and the
  getter are provably reading and writing the same field, cross-checked two ways in this
  build. A companion opcode, 0x09be (handler RVA 0x10310430, not ours), sits right before
  it and is not examined here.

layout: []  # empty
# Read from the decompiled handler (build/ghidra/handlers-c/0x09bf.c) and its target
# singleton, decompiled further with `GHIDRA_WORKSPACE=cosmetics-store tools/ghidra/ghidra.sh
# run DecompAt.java 10310320` and `... DecompAt.java 103111d0`, 2026-09-20.

client:
  handler:
    rva: "0x00310e40"
    registration_site_rva: "0x003102ff"
    evidence: L3

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest is noted as not followed"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x09bf is named SMSG_SEASONAL_COLLECTION_UNLOCKED"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L3
    source: >
      `GHIDRA_WORKSPACE=cosmetics-store tools/ghidra/ghidra.sh run DecompAt.java 10310320`
      and `... DecompAt.java 103111d0`, 2026-09-20
    claim: >
      `handler_0x09bf` sets `*(char*)(FUN_10310320() + 0xa0) = 1`; `FUN_10310320()`
      lazily constructs a singleton whose vftable Ghidra names
      `SeasonalCollectionMgr::vftable`; and the function at RVA 0x003111d0 -- the exact
      address `lua-bindings.yaml` gives for `IsSeasonalCollectionUnlocked` -- reads
      `*(char*)(FUN_10310320() + 0xa0) != 0` and returns it to Lua. The handler writes
      the flag the getter reads; there is no payload field to name.
    note: >
      `client/symbols/ascension-extensions-2026-08-13/lua-bindings.yaml` line
      `IsSeasonalCollectionUnlocked: 0x003111d0` independently names the same RVA
      Ghidra's `Callers` listed as a caller of `FUN_10310320` alongside the handler
      itself, so the two tools agree without either depending on the other.

replay: null
