# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0640
name: SMSG_RECOLOR_COLLECTION_LIST
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_RECOLOR_COLLECTION_LIST by the client's own opcode table, first of "the
  recolor family" the capture reference names (0x0640-0x0642: list, added, removed --
  see the other two fiches). The client registers no handler for 0x0640. The
  registration scan (`handlers.yaml`) is exhaustive over every static `push handler;
  push opcode; call <registrar>` site across all three registrars Extensions.dll uses
  (532 total registrations, including the far-higher 0x09bf in an unrelated family --
  see that fiche), so the usual "Extensions.dll-only" caveat for a *standard* opcode
  (client/symbols/.../handlers.md) does not apply here: 0x0640 is outside AzerothCore's
  baseline entirely (protocol/known-baseline.yaml). Unlike the rest of this pass's
  opcodes there is not even a superseding name to point at: nothing in the client's Lua
  binding table or the UI corpus mentions "recolor", "dye", "tint" or a colour-collection
  concept under any name -- the live appearance-collection events are
  APPEARANCE_ADDED/APPEARANCE_REMOVED (0x069b/0x069c), which are about item appearances,
  not colours. Whatever "recolor collection" meant, this build carries no trace of the
  concept at all, not just an old opcode number for it.

layout: []   # empty

# No `client:` block: there is no handler to point at. That absence is the strongest fact
# in this fiche and it is ours -- see the L3 provenance entries below.

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0640 is named SMSG_RECOLOR_COLLECTION_LIST"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers no handler for 0x0640 in this build"
    note: >
      Cross-checked with `protocol/known-baseline.yaml`: 0x0640 is not one of
      AzerothCore's 1,306 standard opcodes (`opcode_count`), and not one of this server
      fork's own 42 additional opcodes either (`fork_opcode_count` -- SMSG_COA_*/
      CMSG_ANTICHEAT_* and the like, this checkout's own server-side additions, no more
      "AzerothCore standard" than 0x0640 itself). Corrected from an earlier draft that
      cited "1,348 standard opcodes": that number is `opcode_count + fork_opcode_count`,
      and `known-baseline.yaml` (plus its generator, `tools/extract/ac_baseline.py`) keep
      those two fields separate on purpose -- `opcodes` is pinned to the last core
      revision before this fork's own additions, `fork_opcodes` is this fork's own, and
      the linter's ADR-0005 redundancy check reads only `opcodes`. So unlike 0x0023
      (SMSG_GODMODE, a reused standard number) there is no stock Ascension.exe dispatch
      table entry to check separately -- Extensions.dll is the only place custom
      Ascension code lives, and it has none for this number.
  - level: L3
    source: >
      case-insensitive grep for "dye", "tint", "colou?r" over
      client/symbols/ascension-extensions-2026-08-13/lua-bindings.yaml and
      build/corpus/ascension-ui-patch-b-stock/, 2026-09-20
    claim: >
      no Lua binding and no UI source names a recolour/dye/tint collection concept under
      any name in this build; the only "recolor" hit anywhere is an unrelated
      character-creation flag (`recolor-newcomers` in NewCharacterSetupUtil.lua)
    note: "negative evidence: absence from a text search, not a disassembly."
  - level: L1
    source: >
      CoinThrow's capture reference, kept outside this repository at
      ~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md,
      section "Cosmetics / appearance" (the wardrobe/appearance system writeup)
    claim: >
      "0x0540-0x0559 + 0x05CD/0x05CE/0x05D1 + 0x0640-0x0642 (superseded -- no senders
      exist in the client build)", grouping "the recolor family" with the rest of the
      legacy suite without further detail.
    note: >
      L1 per docs/EVIDENCE.md; consistent with, but not identical to, our own
      handler-registration finding above (their claim is phrased in terms of senders,
      which for a server-to-client opcode is the wrong direction -- read here as
      agreement that the whole cluster is dead code).

open_questions:
  - "Did a 'recolor collection' ever exist as a shipped feature under a different name,
     or was it cut before this client build? No trace of the concept survives anywhere
     searched in this pass."

related:
  - 0x0641
  - 0x0642
