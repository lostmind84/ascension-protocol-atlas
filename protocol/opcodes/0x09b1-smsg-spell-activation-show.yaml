# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x09b1
name: SMSG_SPELL_ACTIVATION_SHOW
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Paint the spell-activation overlay (the proc glow) on the button of a spell: spell id, the overlay texture as a length-prefixed string (the server counts the NUL in the length), the overlay type, its scale and an RGB colour. The client fires SPELL_ACTIVATION_SHOW.

# AzerothCore calls 0x09b1 SMSG_COA_SPELL_ACTIVATION_SHOW. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout:
  - name: spellId
    type: u32
    evidence: L3
  - name: texture
    type: lpstring
    notes: 'the server writes size + 1: the NUL is inside the counted bytes'
    evidence: L3
  - name: overlayType
    type: u32
    evidence: L3
  - name: scale
    type: f32
    evidence: L3
  - name: red
    type: u32
    evidence: L3
  - name: green
    type: u32
    evidence: L3
  - name: blue
    type: u32
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c) and the server's writer in CoA.Challenges.Scripts.cpp SendSpellActivationShow (L1 names), whose widths agree.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# Names: lent by the CoA server's writer in modules/mod-coa-challenges/src/CoA.Challenges.Scripts.cpp (L1), whose widths agree with the
# client's reads position by position; a field the server writes as a literal keeps its placeholder.
# the asm idiom (packet_layout.py) saw the same reads minus the strings. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00235a90"
    registration_site_rva: "0x00229594"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/FrameXML/Ascension_CoAResources/Ascension_ChronomancerResource.lua
  - Interface/FrameXML/Ascension_CoAResources/Ascension_RuneMasterResource.lua
  - Interface/FrameXML/Ascension_CoAResources/CoA_SpellActivationOverlayOld.lua

client_strings:   # literals the handler references, from its decompilation
  - "%u%s%s%f%u%u%u"
  - "SPELL_ACTIVATION_SHOW"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: SPELL_ACTIVATION_SHOW
    args: [spellID, texture, position, scale, r, g, b]
    source: "Interface/FrameXML/Ascension_CoAResources/CoA_SpellActivationOverlayOld.lua:91"

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-coa-challenges/src/CoA.Challenges.Scripts.cpp

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c) and the server's writer in CoA.Challenges.Scripts.cpp SendSpellActivationShow (L1 names), whose widths agree"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "modules/mod-coa-challenges/src/CoA.Challenges.Scripts.cpp (the CoA server's writer, client/server-layouts.yaml)"
    claim: "the field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x09b1 is named SMSG_SPELL_ACTIVATION_SHOW"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
