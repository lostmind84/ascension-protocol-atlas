# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0625
name: SMSG_UPDATE_SKILL_CARD
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One skill card updated: an index, the card type name, three u32 and a byte, then SKILL_CARD_UPDATED(%u%s%u%u%u%u%u). The skeleton missed the string.

layout:
  - name: index
    type: u32
    notes: SKILL_CARD_UPDATED receives it plus one as its third argument (index)
    evidence: L3
  - name: cardType
    type: cstring
    notes: the event's second argument
    evidence: L3
  - name: unknown_2
    type: u32
    notes: with the next two u32 and the byte, the event's cardIDDeactivated, deactivated, cardIDActivated, activated (SkillCardTab.lua:489) in an order this reading could not pin
    evidence: L3
  - name: unknown_3
    type: u32
    evidence: L3
  - name: unknown_4
    type: u32
    evidence: L3
  - name: flag
    type: u8
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock).

# Widths and order, read out of the handler at 0x002f4660 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x002f4660"
    registration_site_rva: "0x002f50e4"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "%u%s%u%u%u%u%u"
  - "SKILL_CARD_UPDATED"
  - "SKILL_CARD_UNBLOCKED"
  - "SKILL_CARD_BLOCKED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: SKILL_CARD_UPDATED
    args: []
    source: "Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardsFrame.lua:563"
  - event: SKILL_CARD_UPDATED
    args: [...]
    source: "Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardTab/SkillCardTab.lua:488"
  - event: SKILL_CARD_UNBLOCKED
    args: []
    source: "Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardTab/SkillCardTab.lua:511"
  - event: SKILL_CARD_BLOCKED
    args: []
    source: "Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardTab/SkillCardTab.lua:507"

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0625 is named SMSG_UPDATE_SKILL_CARD"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
