# Generated by tools/codegen/stub_fiches.py, then completed by hand (naming-source closure pass).
id: 0x099d
name: SMSG_PATCH_WORLD_STATE_UI
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  A record patched into one of Extensions.dll's internal id-keyed containers -- looked up by id
  (RVA 0x000b2b70), allocated fresh as a 0x3c-byte heap struct when new (RVA 0x000ae5d20), and
  inserted (RVA 0x000b0930) -- not one of the 114 CustomDBCMgr `.data` tables
  client/patch-tables.md documents: nothing in the decompiled handler references a fixed .data
  table object (no `+0xc`/`+0x10`/`+0x20` min/max/rows idiom anywhere in it, unlike 0x09d1/0x09d2
  in this same shard, which do). client/patch-table-names.md's third-party reference names
  `WorldStateUI.dbc` for this opcode (63 fields, 252-byte record) but the handler's fixed portion
  is 60 bytes, not 252 -- the widths do not match, on top of the binding already being unverified
  (`L1 ref`, absent from our own patch-tables.yaml scan). No source in this repository names the
  struct's own fields (see naming_source below).

layout:
  - name: field_0
    type: bytes_60
    notes: >
      width: L3 (block-copy read, 60 bytes; re-verified this pass against the cached decompile,
      build/ghidra/handlers-c/0x099d.c, against
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source (see naming_source below). 60 bytes does NOT match WorldStateUI.dbc's claimed record_size
      (252 bytes, 63 fields, per client/patch-table-names.md) at all, which further undermines that
      already-unverified (L1) binding.
    evidence: L3
  - name: field_1
    type: lpstring
    notes: >
      width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source.
    evidence: L3
  - name: field_2
    type: lpstring
    notes: >
      width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source.
    evidence: L3
  - name: field_3
    type: lpstring
    notes: >
      width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source.
    evidence: L3
  - name: field_4
    type: lpstring
    notes: >
      width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source.
    evidence: L3
  - name: field_5
    type: lpstring
    notes: >
      width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source.
    evidence: L3
  - name: field_6
    type: lpstring
    notes: >
      width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source.
    evidence: L3

client:
  handler:
    rva: "0x002350b0"
    registration_site_rva: "0x00229408"
    evidence: L3

naming_source:
  result: none found
  checked:
    - "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml: 0x099d has no strings recorded"
    - "client/symbols/ascension-extensions-2026-08-13/lua-keys.yaml: 22 builders extracted; none in this table's domain"
    - "build/corpus/ascension-ui-patch-b-stock, grepped for `WorldStateUI`: 1 hit, and it is real: `Interface/FrameXML/WorldStateFrame.lua:141,149` calls `GetNumWorldStateUI()` and unpacks `GetWorldStateUIInfo(i)` into 11 named locals -- `uiType, state, text, icon, dynamicIcon, tooltip, dynamicTooltip, extendedUI, extendedUIState1, extendedUIState2, extendedUIState3`. This is a genuine 'ui getter' (L2 for the 11 names, per client/patch-table-names.md's own bucket-2 rule) and it is a native engine call, not Lua-side table construction, so there is no Lua body here to compare against the handler's own field order. It plausibly names members of the SAME internal container this handler writes (the shapes are close: 6 of the handler's 15 stored words are strings, and the getter's 11 values include four/five string-typed ones -- text, icon, tooltip, dynamicTooltip, and extendedUI is a string key into a lookup table), but the correspondence is not proven field-by-field (L0) and the counts do not match exactly (15 stored words vs. 11 returned values), so no field below is named from it."
  claimed_table:
    dbc: WorldStateUI.dbc
    fields: 63
    record_size: 252
    rows: 266
    bound_by: "L1 ref only (client/patch-table-names.md); absent from client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml's own CustomDBCMgr scan"
    width_check: "does NOT match the handler's fixed-portion read (60 bytes)"

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x099d.c (decompiled handler, re-read this pass) and client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order; the handler looks the id up in an internal container (RVA 0x000b2b70), allocates a fresh 0x3c-byte struct when it is new (RVA 0x000ae5d20), and inserts it (RVA 0x000b0930) -- not one of the 114 CustomDBCMgr .data table objects patch-tables.yaml enumerates"
  - level: L1
    source: "client/patch-table-names.md (opcode 0x099d row)"
    claim: "a third-party reference names the DBC table WorldStateUI.dbc for this opcode; unverified by our own patch-tables.yaml scan and not treated as established here"
  - level: L2
    source: "build/corpus/ascension-ui-patch-b-stock/Interface/FrameXML/WorldStateFrame.lua:139-149 (GetNumWorldStateUI/GetWorldStateUIInfo, this pass)"
    claim: "a genuine ui-getter exists and names 11 values -- uiType, state, text, icon, dynamicIcon, tooltip, dynamicTooltip, extendedUI, extendedUIState1, extendedUIState2, extendedUIState3 -- that plausibly belong to the SAME internal container this handler writes (not the WorldStateUI.dbc row); the correspondence to this handler's 15 stored words is not proven field-by-field (L0) and no field above is named from it"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml, lua-keys.yaml, and build/corpus/ascension-ui-patch-b-stock (this pass)"
    claim: "no naming source for the struct's own fields exists in any of the three"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x099d is named SMSG_PATCH_WORLD_STATE_UI"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
