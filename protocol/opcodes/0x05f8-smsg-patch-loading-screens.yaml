# Generated by tools/codegen/stub_fiches.py, then hand-filled (patch-unnamed-1 sweep, 2026-09-20).
# This is a CoA-custom DBC table row with NO naming source in anything this repo holds
# (see client/patch-table-names.md). field_N names are placeholders; do not rename them
# without a cited source (lua-keys.yaml, a UI getter, or handler-strings.yaml).

id: 0x05f8
name: SMSG_PATCH_LOADING_SCREENS
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  SMSG_PATCH_LOADING_SCREENS: despite the name, this handler does not touch any of the 114 CustomDBCMgr table objects -- no minId/maxId/rows-array reference anywhere in the decompiled C. It looks its key up in a hash-map-style container (FUN_100b1bd0) and inserts through a matching helper (FUN_100ab220), the pattern patch-tables.md calls out for the other 141 patch handlers that store into a DLL-internal container rather than a client DBC row. The opcode's own name happens to match LoadingScreens.dbc, a real 4-field/16-byte table in the snapshot, but nothing here ties the two together -- that binding (patch-table-names.md's 'L0 name' bucket) is exactly this: an inferred label from the opcode's own text, not a confirmed table write. No `patches_table` is claimed.

layout:
  - name: field_0
    type: u32
    notes: "offset 0; the key passed to the FUN_100b1bd0 hash-map lookup"
    evidence: L3
  - name: field_1
    type: bytes_8
    notes: "cursor advances 8 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further"
    evidence: L3
  - name: field_2
    type: u32
    notes: "offset 0xc"
    evidence: L3
  - name: field_3
    type: lpstring
    notes: "via FUN_100d3680"
    evidence: L3
  - name: field_4
    type: lpstring
    notes: "via FUN_100d3680"
    evidence: L3

client:
  handler:
    rva: "0x00228b40"
    registration_site_rva: "0x002295b6"
    evidence: L3

patches_table: []   # this handler does not write into any CustomDBCMgr table object -- see summary and provenance

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x05f8.c (already-decompiled handler, read by hand this pass)"
    claim: "the widths, order and destination struct of the wire fields"
  - level: L3
    source: "build/ghidra/handlers-c/0x05f8.c, full read"
    claim: "no DAT_10b[cd-e][0-9a-f]{4} global (the CustomDBCMgr .data range) is referenced anywhere in this handler; storage goes through FUN_100b1bd0 (lookup) / FUN_100ab220 (insert), a different container"
    note: "this is stronger than 'no naming source': the handler does not write a client DBC table row at all, by any name"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (handler 0x05f8)"
    claim: "no string literal is referenced near this handler's store -- checked, empty"
    note: "rules out an event name or format string that might have hinted at a field's purpose"
  - level: L1
    source: "client/patch-table-names.md, row 0x05F8 (bound by 'L0 name')"
    claim: "the map already flags this binding as inferred from the opcode's own text with nothing tying it to the handler; this pass confirms the handler does not touch LoadingScreens.dbc's table object (or any other)"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05f8 is named SMSG_PATCH_LOADING_SCREENS"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
