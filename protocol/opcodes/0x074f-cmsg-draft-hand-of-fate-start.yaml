# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x074f
name: CMSG_DRAFT_HAND_OF_FATE_START
direction: client_to_server
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
  - ascension-exe-2010-06-25-stock

summary: >
  Named CMSG_DRAFT_HAND_OF_FATE_START by the client's own opcode table, the first of the
  Hand of Fate run (0x074F START, 0x0750 PICK, 0x0751 SACRIFICE, 0x0752 CANCEL). Unlike its
  three neighbours, which all have a confirmed empty-or-u32 sender, no sender for 0x074F
  exists anywhere in this build: the opcode's value never appears as a written literal in
  either Extensions.dll or Ascension.exe. Layout and behaviour are not established because
  there is nothing to read a layout from -- the same negative shape as CMSG_DRAFT_ROLL
  (0x074B, see its fiche).

layout: []   # unknown -- no sender found; NOT a confirmed-empty send like 0x0645/0x064B/0x0752

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x074f is named CMSG_DRAFT_HAND_OF_FATE_START"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (tools/extract/client_sender.py, the opcode-immediate CDataStore-write scan over Extensions.dll) and client/client-senders.md ('What the scan still does not reach'), which lists 0x074F among 33 opcodes whose value appears nowhere in the build: not as a 32- or 16-bit immediate in .text, not as a value in .rdata or .data, and not as an arithmetic constant in the window before any Put(u32) opcode write"
    claim: "no sender for 0x074f exists anywhere in Extensions.dll, build ascension-extensions-2026-08-13"
  - level: L3
    source: "re-verified this session (raw byte search, no disassembler, section boundaries cross-checked against `objdump -h`): every section of Extensions.dll.ORIGINAL (.text, .rdata, .data, .rsrc, .reloc, .vm_sec) searched for the 4-byte little-endian value matching 0x0000074f found zero occurrences. The sibling opcodes 0x074E/0x0645/0x064B/0x0750/0x0751/0x0752 found 4/3/7/6/2/4 occurrences respectively by the identical per-section method -- not the uniform '2-5' an earlier pass through this fiche claimed (0x064B and 0x0750 both exceed that range; corrected here after re-counting per section rather than trusting the earlier remembered figure). The sanity check still holds for this binary: every sibling has at least one hit in Extensions.dll, 0x074F alone has none"
    claim: "corroborates the Extensions.dll absence: opcode 0x074f is written as a literal nowhere in Extensions.dll, and the search method is shown, with corrected counts, to find real literals for every sibling opcode in this specific binary"
  - level: L3
    source: "the same re-verification applied to Ascension.exe.ORIGINAL (build ascension-exe-2010-06-25-stock, image_base 0x400000, sections cross-checked against `objdump -h`): 0x074F found zero occurrences, but so did sibling 0x0751 (SendHandOfFateSacrificeSpellSelection, a confirmed real sender per lua-bindings.yaml and its own fiche) -- 0x0645/0x064B/0x074E/0x0752 found 1-3 occurrences each and 0x0750 found 29, far outside any plausible count of genuine opcode-literal writes for a single sender in this stock, pre-Ascension executable"
    claim: "the earlier 'extends it to Ascension.exe' claim does not hold: a confirmed-real sender (0x0751) also shows zero raw-byte hits in this binary, and unrelated siblings show implausibly high or low counts, so a zero count in Ascension.exe is not distinguishing evidence here one way or the other. Ascension.exe is the stock 2010 client; custom Ascension opcodes are not expected to appear in it as literals regardless of whether Extensions.dll has a sender for them. The absence claim for 0x074F is retained for Extensions.dll only -- Ascension.exe's silence is uninformative and should not be read as corroboration"
    note: >
      Skeptic re-review, 2026-09-20: the prior '2-5 occurrences each ... extends it to
      Ascension.exe' provenance entry overstated both the count and what the Ascension.exe
      half of the search shows. Downgraded to a same-level but narrower Extensions.dll
      claim, plus this separate entry stating what the Ascension.exe scan does and does not
      establish.
  - level: L1
    source: "the capture reference (CoinThrow, 2026-09-20, kept outside the tree): its own opcode table states '0x074F CMSG HoF start -- no sender found in either binary (real opcode, unused slot in this build)', and its solved-issues section retracts an earlier apparent capture of 0x074F frames as a false parse -- deflate byte runs inside a zlib-compressed content-push stream that happened to match the opcode's byte pattern, not real packets"
    claim: "independently reaches the same 'no sender in either binary' conclusion, and explains why an earlier capture pass appeared to see 0x074F traffic that turned out not to be real"
    note: >
      Attributed per docs/EVIDENCE.md, not authoritative on its own -- its "no sender"
      conclusion is corroborated above at L3 against our own copy of Extensions.dll, the
      binary where a sender would actually have to live; that is what lets the Extensions.dll
      half count as agreement rather than a bare third-party claim. Its "or in either binary"
      phrasing about Ascension.exe specifically is not independently corroborated here (see
      the Ascension.exe entry above): our own zero-count there is uninformative on its own,
      so this stays an unreproduced L1 claim for that half.
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/lua-bindings.yaml (1229 Lua-callable functions, tools/extract/lua_bindings.py over Extensions.dll), every Hand-of-Fate-named entry: CanClaimHandOfFateRewards, CancelHandOfFateSelection (0x0752), ClaimHandOfFateRewards, GetHandOfFatePickCount, GetHandOfFatePickSpellAtIndex, GetHandOfFateQuests, GetHandOfFateRewardsReleaseInfo, GetHandOfFateSacrificePickCount, GetHandOfFateSacrificePickSpellAtIndex, GetNumClaimableHandOfFateRewards, GetNumClaimedHandOfFateRewards, GetSelectedHandOfFateSpell, HasHandOfFatePick, HasHandOfFateSacrificePick, SendHandOfFateSacrificeSpellSelection (0x0751), SendHandOfFateSpellSelection (0x0750)"
    claim: "no binding in the complete list opens or starts a Hand of Fate selection -- every Hand-of-Fate binding is either a read-only getter/predicate or one of the three sends already accounted for (0x0750/0x0751/0x0752); there is no unaccounted-for binding left for a computed-opcode sender to hide behind, the way FUN_100dd6e0 hid 0x0749/0x074A"
    note: >
      Narrows, not closes, the open question below: this rules out a client-side Lua
      trigger, computed or literal, but not a send issued directly from C++ game code
      outside the Lua binding surface.

open_questions:
  - "Whether the Hand of Fate 'start' step happens on the wire at all in this build, or the
     server enters that sub-state on its own (e.g. as a side effect of CMSG_DRAFT_START at
     0x0749, or of the SMSG_DRAFT_STATE push at 0x0753) without the client ever announcing
     it. No Lua binding, computed opcode or otherwise, has been found to send 0x074F, so the
     client-side half of this is closed; what the server fork expects to receive is a
     question about our own fork's C++ implementation, not the client's wire protocol, and
     is out of this atlas's scope for that reason -- not ADR 0005, which governs which
     *opcodes* get fiches (standard vs. Ascension-custom) and does not speak to this axis;
     0x074f is itself a custom, non-baseline opcode, so ADR 0005 if anything argues for
     documenting it here, not against going further."

replay: null
