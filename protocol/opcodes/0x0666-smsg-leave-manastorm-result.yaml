# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0666
name: SMSG_LEAVE_MANASTORM_RESULT
direction: server_to_client
status: proven
evidence: L5
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Received on 2026-09-19 through RegisterPacket: one NUL-terminated string, LEAVE_MANASTORM_NOT_ACTIVE.

layout:
  - name: result
    type: cstring
    evidence: L5
# name from mod-ascension-compat's AscensionManastormProtocol.h / AscensionManastorm.cpp (L1); width and order from the decompiled handler (L3)
# decompiled: FUN_102a1d40 scans a NUL-terminated string; SendResult(LeaveResult, ...)
# Widths and order, read out of the handler at 0x002a1d40 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x002a1d40"
    registration_site_rva: "0x002a460a"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_Manastorm/ManastormQueue.lua

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-ascension-compat/src/AscensionManastormProtocol.h

client_strings:   # literals the handler references, from its decompilation
  - "LEAVE_MANASTORM_RESULT"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: LEAVE_MANASTORM_RESULT
    format: "%s"
    helper: FUN_100b9620

provenance:
  - level: L5
    source: "tools/replay/manastorm_roundtrip.py --pair all --control, run on slot 1 on 2026-09-19 (client/lua-packet-api.md)"
    claim: "one NUL-terminated LEAVE_MANASTORM_* string, received by the client's handler"
  - level: L3
    source: "tools/ghidra/ghidra.sh run Decomp.java <handler VA> (see client/server-layouts.md)"
    claim: "field widths and order, from the decompiled handler"
  - level: L1
    source: "mod-ascension-compat's Manastorm serializers, client/server-layouts.yaml"
    claim: "field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0666 is named SMSG_LEAVE_MANASTORM_RESULT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay:
  script: tools/replay/manastorm_roundtrip.py
  asserts:
    - "the payload is one NUL-terminated string starting with LEAVE_MANASTORM_"
  asserts_measured:
    - "48 bytes read after a 0x0665: 'LEAVE_MANASTORM_NOT_ACTIVE', NUL at 26"
  last_run: { date: 2026-09-20, result: pass, build: coa-extensions-rev4-2026-09-14,
              dataset: ascension-live-2026-09-17, slot: 1 }
