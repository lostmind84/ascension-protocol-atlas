# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06ff
name: CMSG_ITEM_STAT_QUERY
direction: client_to_server
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named CMSG_ITEM_STAT_QUERY by the client's own opcode table. Not sent through the normal
  PutData sender path; client/client-senders.md places it in a client-side query-service
  registration alongside its response 0x0700, without an RVA. Layout and behaviour not
  established.

layout: []   # unknown
# Checked 2026-09-20: no entry in senders.yaml (the client_sender.py CDataStore
# ctor/PutData/Finalize/Send scan does not find a site for this opcode) and no hit for its
# name anywhere in the ascension-ui-patch-b-stock corpus. NOT "no client code", though:
# client/client-senders.md ("The recovery service"/query-service section) documents this
# opcode, paired with its response SMSG_ITEM_STAT_QUERY_RESPONSE (0x0700), as one of two
# request/response pairs registered in a client-side "query service" object rather than
# sent through the normal PutData sender path -- which is exactly why the two mechanical
# scans above see nothing. That document names the registration pattern (`mov ds:[X],
# request; mov ds:[X+4], response`, one of two forms it describes) but gives no RVA for
# this specific pair, and I could not relocate one within this session (checked
# lua-bindings.yaml/lua-keys.yaml for an "ItemStat*" service binding -- nothing closer than
# the unrelated tooltip binding GetScalingItemStats). So: client code almost certainly
# exists and drives this opcode, but neither its address nor its payload is established
# here.
#
# A third-party live-session capture (~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-
# reference-2026-09-20.md, L1, not in this repo) claims an 8-byte payload, seen live several
# times: `{u32 item_id; u32 slot_or_type}`, with slot_or_type = 2 in every sample. Consistent
# with the query-service theory above (a request carrying an item id makes sense for
# "item stat query"), but unreproduced against this build's binary or Lua, so it stays L1
# and does not become a layout here.

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06ff is named CMSG_ITEM_STAT_QUERY"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (no entry) and a corpus grep for the name (no hit), checked 2026-09-20"
    claim: "no PutData-style sender and no corpus mention -- but see the query-service note below; this is not evidence of no client code"
  - level: L2
    source: "client/client-senders.md, 'Two more opcodes are not sent by a sender at all but registered in a query service object' (committed doc, methodology given, no RVA given for this pair; not independently re-derived this session)"
    claim: "0x06ff/0x0700 are a request/response pair driven by a client-side query-service registration, not a plain sender/handler"
  - level: L1
    source: "~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md, '0x06FF -- CMSG_ITEM_STAT_QUERY' (third-party live-session capture, not in this repo, unreproduced against our binary)"
    claim: "an 8-byte payload {u32 item_id; u32 slot_or_type}, slot_or_type = 2 in every sample"

replay: null
