# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0660
name: SMSG_MANASTORM_ACTIVE_DATA
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_MANASTORM_ACTIVE_DATA by the client's own opcode table. Layout and behaviour not
  established.

layout:
  - name: depth
    type: u32
    evidence: L3
    notes: "0 when no run is active"
  - name: scene
    type: u32
    evidence: L3
    notes: "stage, or endgameStage when mode >= 4"
  - name: type
    type: cstring
    evidence: L3
    notes: "Types.at(mode): the mode's name"
  - name: caches
    type: u32
    evidence: L3
    notes: "server sends 0"
  - name: chance
    type: f32
    evidence: L3
    notes: "CacheChance(...) / 100.0f"
  - name: item
    type: u32
    evidence: L3
    notes: "CacheForLevel(...)"
# name from mod-ascension-compat's AscensionManastormProtocol.h / AscensionManastorm.cpp (L1); width and order from the decompiled handler (L3)
# decompiled: FUN_102a1e90 reads u32, u32, a NUL-terminated string, u32, f32 (movss), u32 -- exactly WriteActive's six operands in order
# Widths and order, read out of the handler at 0x002a1e90 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x002a1e90"
    registration_site_rva: "0x002a4651"
    evidence: L3

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-ascension-compat/src/AscensionManastormProtocol.h

client_strings:   # literals the handler references, from its decompilation
  - "ACTIVE_MANASTORM_UPDATED"
  - "MANASTORM_CACHE_INFO_UPDATED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: ACTIVE_MANASTORM_UPDATED
    args: []
    source: "Interface/AddOns/Ascension_Manastorm/Manastorm.lua:174"
  - event: ACTIVE_MANASTORM_UPDATED
    args: [previousLevel, currentLevel]
    source: "Interface/AddOns/Ascension_UIDevelopmentTools/EventTrace/EventArgNames.lua:338"
  - event: MANASTORM_CACHE_INFO_UPDATED
    args: [numCaches, chanceForAdditionalCache, itemEntry]
    source: "Interface/AddOns/Ascension_Manastorm/Manastorm.lua:190"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: ACTIVE_MANASTORM_UPDATED
    format: "%u%u"
    helper: FUN_100b9560

provenance:
  - level: L3
    source: "tools/ghidra/ghidra.sh run Decomp.java <handler VA> (see client/server-layouts.md)"
    claim: "field widths and order, from the decompiled handler"
  - level: L1
    source: "mod-ascension-compat's Manastorm serializers, client/server-layouts.yaml"
    claim: "field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0660 is named SMSG_MANASTORM_ACTIVE_DATA"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
