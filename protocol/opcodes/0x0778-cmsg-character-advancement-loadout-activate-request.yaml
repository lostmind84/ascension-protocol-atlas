# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0778
name: CMSG_CHARACTER_ADVANCEMENT_LOADOUT_ACTIVATE_REQUEST
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One string: the argument of ActivateLoadout.

layout:
  - name: uuid
    type: cstring
    notes: 'the binding''s string argument, NUL-terminated (FUN_100b97a0): ActivateLoadout(loadout.UUID)'
    evidence: L3
# Read from the decompiled sender binding (DumpC.java over the DLL, 2026-09-20), read next to the UI's calls (L2).

# Widths and order, read out of the client's sender at 0x0017407a by tools/extract/client_sender.py.
# A helper is called between the opcode write and the send: this is at best a
# PREFIX of the payload. An empty list here does not mean an empty packet.

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: ActivateLoadout
  function_rva: "0x00173fd0"
  calls:
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 1505, args: ["uuid"]}
    - {file: "Interface/FrameXML/Util/TalentLoadoutUtil.lua", line: 16, args: ["uuid"]}
    - {file: "Interface/FrameXML/Util/TalentLoadoutUtil.lua", line: 17, args: ["uuid"]}
    - {file: "Interface/FrameXML/Util/TalentLoadoutUtil.lua", line: 22, args: ["loadout.UUID"]}

provenance:
  - level: L3
    source: "the decompiled sender binding (DumpC.java over the DLL, 2026-09-20), read next to the UI's calls (L2)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0778 is named CMSG_CHARACTER_ADVANCEMENT_LOADOUT_ACTIVATE_REQUEST"

replay: null
