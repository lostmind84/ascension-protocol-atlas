# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05b2
name: SMSG_PATCH_CHALLENGE_CONDITION
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of ChallengeConditions.dbc, patched into the client's table: ten dwords then the ConditionType token as a length-prefixed string. The runtime record is dword-equal to the wire block (0x28 bytes) with the string pointer written over dword 5, which the reader skips. Six of the ten dwords are named: the row key, ElseGroup, ConditionValue1-3 and NegativeCondition, from the offsets the detailedChallenge condition builder at 0x1013c7de reads.

layout:
  - name: id
    type: u32
    notes: dword 0; the row key -- range-checked against minId 0x10bdfd30 / maxId 0x10bdfd2c and used to index the row table at 0x10bdfd40, then passed to the row-insert FUN_10133ac0
    evidence: L3
  - name: unknown_1
    type: u32
    notes: dword 1; stored at record +0x04. Tested against zero after the store; when non-zero the handler runs the notify path (FUN_1013b8b0 then FUN_100996e0) with it and stamps a +2s refresh deadline. Not named.
    evidence: L3
  - name: unknown_2
    type: u32
    evidence: L3
    notes: dword 2; stored at record +0x08 and, when non-zero, passed alone to FUN_10135180 at the end of the handler -- the same call every element table in this family makes with its own dword 2 (0x05B6, 0x05B7, 0x05B8, 0x05B9, 0x0663). Read as the owning challenge whose cached model is invalidated; the name is inference, not a read symbol.
  - name: unknown_3
    type: u32
    notes: dword 3; stored at record +0x0c. Not named.
    evidence: L3
  - name: elseGroup
    type: u32
    notes: dword 4; stored at record +0x10, which the detailedChallenge condition builder at 0x1013c7de reads as the Lua key ElseGroup (lua-keys.yaml, a string literal in this build). ChallengeUtil.lua:66 compares conditionA.ElseGroup (L2).
    evidence: L3
  - name: unknown_5
    type: bytes_4
    notes: 'dword 5 is SKIPPED by the reader: the cursor jumps from +0x10 to +0x18. It is the row''s string-offset column, and the client overwrites record +0x14 with the pointer to the string that follows the block.'
    evidence: L3
  - name: conditionValue1
    type: u32
    notes: dword 6; record +0x18 = the builder's ConditionValue1. ChallengeExtendedInfoMixin.lua:290 reads data.ConditionValue1 (L2). Used as a float by the UI's formatters.
    evidence: L3
  - name: conditionValue2
    type: u32
    notes: dword 7; record +0x1c = ConditionValue2 (L2 at ChallengeExtendedInfoMixin.lua:290)
    evidence: L3
  - name: conditionValue3
    type: u32
    notes: dword 8; record +0x20 = ConditionValue3 (L2 at ChallengeExtendedInfoMixin.lua:290)
    evidence: L3
  - name: negativeCondition
    type: u32
    notes: dword 9; record +0x24 = NegativeCondition, used as a boolean (ChallengeExtendedInfoMixin.lua:323, L2)
    evidence: L3
  - name: conditionType
    type: lpstring
    notes: u32 length then that many bytes, no NUL (FUN_100d3680). Copied and stored at record +0x14, over the skipped dword 5; the builder reads +0x14 as ConditionType. A token, e.g. CHALLENGE_CONDITIONS_TYPE_COMPLETE_CHALLENGE (ChallengeExtendedInfoMixin.lua:292, L2).
    evidence: L3
# Read from the decompiled handler FUN_101385c0 (build/ghidra/handlers-c/0x05b2.c) read by hand, and client/symbols/ascension-extensions-2026-08-13/lua-keys.yaml (key names are string literals in this build), 2026-09-20. The capture reference (~/CoaServer/reference/cointhrow, L1 for us) reports the same field list for this row (0 ID, 4 ElseGroup, string ConditionType, 6-8 ConditionValue1/2/3, 9 NegativeCondition, 1-3 unresolved) and names the builder site 0x1013C7DE. Every part of that we could re-derive here from our own binary and the client's Lua, so the fields above are graded L3 and L2 on our own evidence rather than carried at L1.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# packet_layout.py had followed a call into 0x000d3680, not the handler. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x001385c0"
    registration_site_rva: "0x0013b6b2"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: ChallengeConditions.dbc
    instance: "0x10bdfd20"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 40
    fields: 10

provenance:
  - level: L3
    source: "the decompiled handler FUN_101385c0 (build/ghidra/handlers-c/0x05b2.c) read by hand, and client/symbols/ascension-extensions-2026-08-13/lua-keys.yaml (key names are string literals in this build), 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml (CustomDBCMgr load idiom and the handler's table object)"
    claim: "the packet carries one row of ChallengeConditions.dbc into the client's copy of that table"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05b2 is named SMSG_PATCH_CHALLENGE_CONDITION"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
