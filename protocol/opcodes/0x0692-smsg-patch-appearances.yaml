# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0692
name: SMSG_PATCH_APPEARANCES
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of Appearances.dbc: 17 dwords (68 bytes) copied straight off the wire, of which the one at +0x08 is a throwaway placeholder overwritten by a trailing cstring. Two of the 17 fields are named from the native getter C_Appearance.GetAppearanceDisplayInfo (VA 0x100c8090, called at UIParent.lua:1787), which reads exactly these two offsets and binds to this table's own row array (confirmed by its minId/maxId/rows addresses matching Appearances.dbc's CustomDBCMgr object): displayType (the string) and displayID. The getter's other two Lua return values (the discarded 3rd, and displayName) come from a separate lookup joined on displayID, not from this record, so they name no column here.

layout:
  - name: id
    type: u32
    notes: record +0x00, the row key -- range-checked against minId 0x10bdfcc4 / maxId 0x10bdfcc0, indexes the row table at 0x10bdfcd4, then passed to FUN_10133ac0.
    evidence: L3
  - name: unknown_1
    type: u32
    notes: record +0x04. Not read by GetAppearanceDisplayInfo (VA 0x100c8090), the only getter checked that binds to this table's row array (DAT_10bdfcd4 matches Appearances.dbc's instance exactly). Not named.
    evidence: L3
  - name: unknown_2
    type: bytes_4
    notes: 'record +0x08, SKIPPED by every reader: sent on the wire as a throwaway placeholder dword, then overwritten by the trailing string below.'
    evidence: L3
  - name: displayID
    type: u32
    notes: 'record +0x0c. GetAppearanceDisplayInfo''s second return value (VA 0x100c8090: `*(undefined4 *)(iVar1 + 0xc)`). Lua destructures it as `displayID` (`local displayType, displayID, _, displayName = C_Appearance.GetAppearanceDisplayInfo(appearanceID)`, UIParent.lua:1787).'
    evidence: L3
  - name: unknown_4
    type: u32
    notes: record +0x10. Not read by GetAppearanceDisplayInfo. Not named.
    evidence: L3
  - name: unknown_5
    type: u32
    notes: record +0x14. Not read by GetAppearanceDisplayInfo. Not named.
    evidence: L3
  - name: unknown_6
    type: u32
    notes: record +0x18. Not read by GetAppearanceDisplayInfo. Not named.
    evidence: L3
  - name: unknown_7
    type: u32
    notes: record +0x1c. Not read by GetAppearanceDisplayInfo. Not named.
    evidence: L3
  - name: unknown_8
    type: u32
    notes: record +0x20. Not read by GetAppearanceDisplayInfo. Not named.
    evidence: L3
  - name: unknown_9
    type: u32
    notes: record +0x24. Not read by GetAppearanceDisplayInfo. Not named.
    evidence: L3
  - name: unknown_10
    type: u32
    notes: record +0x28. Not read by GetAppearanceDisplayInfo. Not named.
    evidence: L3
  - name: unknown_11
    type: u32
    notes: record +0x2c. Not read by GetAppearanceDisplayInfo. Not named.
    evidence: L3
  - name: unknown_12
    type: u32
    notes: record +0x30. Not read by GetAppearanceDisplayInfo. Not named.
    evidence: L3
  - name: unknown_13
    type: u32
    notes: record +0x34. Not read by GetAppearanceDisplayInfo. Not named.
    evidence: L3
  - name: unknown_14
    type: u32
    notes: record +0x38. Not read by GetAppearanceDisplayInfo. Not named.
    evidence: L3
  - name: unknown_15
    type: u32
    notes: record +0x3c. Not read by GetAppearanceDisplayInfo. Not named.
    evidence: L3
  - name: unknown_16
    type: u32
    notes: record +0x40, the record's last dword before the trailing string.
    evidence: L3
  - name: displayType
    type: cstring
    notes: 'record +0x08, over the skipped dword. GetAppearanceDisplayInfo''s first return value, read from this exact offset (VA 0x100c8090: `*(undefined1 **)(iVar1 + 8)`, pushed with the default-empty-string idiom -- a string, e.g. "APPEARANCE_DISPLAY_TYPE_ITEM"). Lua destructures it as `displayType` (UIParent.lua:1787). The getter''s 3rd and 4th returns (Lua''s discarded `_` and `displayName`) are NOT read from this record -- they come from a separate item/creature-display lookup joined on displayID, so they name no column here.'
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x0692.c) and the decompiled native getter C_Appearance.GetAppearanceDisplayInfo (Ghidra DecompAt.java 100c8090, workspace named-lua-ui), both read by hand, 2026-09-20.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x001dced0"
    registration_site_rva: "0x001e692d"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: Appearances.dbc
    instance: "0x10bdfcb4"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 68
    fields: 17

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x0692.c) and the decompiled native getter C_Appearance.GetAppearanceDisplayInfo (Ghidra DecompAt.java 100c8090, workspace named-lua-ui), both read by hand, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml (CustomDBCMgr load idiom and the handler's table object)"
    claim: "the packet carries one row of Appearances.dbc into the client's copy of that table"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0692 is named SMSG_PATCH_APPEARANCES"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
