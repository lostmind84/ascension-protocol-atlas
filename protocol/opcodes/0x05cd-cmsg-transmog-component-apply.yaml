# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05cd
name: CMSG_TRANSMOG_COMPONENT_APPLY
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named CMSG_TRANSMOG_COMPONENT_APPLY by the client's own opcode table, with its own
  small SMSG pair immediately after it in the table (0x05ce SMSG_TRANSMOG_COMPONENT_UPDATE,
  0x05d1 SMSG_TRANSMOG_COMPONENT_SUCCESS -- see those fiches; 0x05cf/0x05d0 sit unnamed
  between them). Nothing in this build constructs a packet with this opcode: no
  `push 0x5cd` / `mov r32, 0x5cd` occurs anywhere in Extensions.dll's `.text`,
  byte-exhaustively. The capture reference groups it with the rest of the legacy suite
  as superseded but does not name a specific modern-cluster opcode that replaces the
  "component" concept the way it does for outfits or the visibility toggle; what a
  "transmog component" was (a sub-slot of an appearance? an ingredient item?) is not
  established by anything read in this pass.

layout: []   # empty

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05cd is named CMSG_TRANSMOG_COMPONENT_APPLY"
  - level: L3
    source: >
      tools/extract/client_sender.py's push_sites() scan, run directly against
      ~/CoaServer/client/ascension-live/Extensions.dll.ORIGINAL (sha256
      0f8d847b3adc44a963606f0cd4f7938ad6fcd6f4d87feac3131c7153bdf3bb11) for 0x05cd alone,
      2026-09-20
    claim: >
      the immediate value 0x05cd never occurs as a `push imm32` (0x68) or
      `mov r32, imm32` (0xb8-0xbf) operand anywhere in the .text section
    note: >
      Byte-exhaustive (every offset in .text, not just instruction boundaries). A sender
      that built the opcode from a data table instead of a literal would still be
      missed; see open_questions.
  - level: L1
    source: >
      CoinThrow's capture reference, kept outside this repository at
      ~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md,
      section "Cosmetics / appearance" (the wardrobe/appearance system writeup)
    claim: >
      "0x0540-0x0559 + 0x05CD/0x05CE/0x05D1 + 0x0640-0x0642 (superseded -- no senders
      exist in the client build)"; names `CMSG_TRANSMOG_APPLY`/`ILLUSION_APPLY`/
      `INCARNATION_APPLY`/`SPELL_VISUAL_APPLY`/`COSMETIC*_APPLY`/`TRANSMOG_OUTFIT_*`/
      `TRANSMOG_COMPONENT_APPLY` and "the recolor family" together as carrying "the old
      numbering of the appearance system", without a specific modern-opcode mapping for
      the component sub-concept.
    note: >
      L1 per docs/EVIDENCE.md regardless of the author's own confidence. Re-derived
      independently above at L3 for this specific opcode: our own byte-exhaustive scan
      agrees with their claim of no sender.

open_questions:
  - "What was a 'transmog component'? Neither source read in this pass identifies a
     modern-cluster equivalent for this concept the way it does for outfits (0x069e/
     0x06a0) or the visibility toggle (0x06a3)."

related:
  - 0x05ce
  - 0x05d1
