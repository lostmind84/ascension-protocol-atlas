# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0663
name: SMSG_PATCH_CHALLENGE_MODIFIER
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of ChallengeModifiers.dbc: eight dwords then a NUL-terminated modifier-type token -- the family's only cstring, every other challenge patch packet length-prefixes its strings. The runtime record is dword-equal to the wire block (0x20 bytes) with the string pointer written over dword 4, which the reader skips.

layout:
  - name: id
    type: u32
    notes: dword 0; the row key -- range-checked against minId 0x10be02ac / maxId 0x10be02a8, indexes the row table at 0x10be02bc, then passed to FUN_10133ac0
    evidence: L3
  - name: unknown_1
    type: u32
    notes: dword 1; record +0x04. Gates the notify path. Not named.
    evidence: L3
  - name: unknown_2
    type: u32
    evidence: L3
    notes: dword 2; record +0x08 and, when non-zero, the sole argument to FUN_10135180 -- the family-wide invalidate call. Inference.
  - name: unknown_3
    type: u32
    notes: dword 3; record +0x0c. Not named.
    evidence: L3
  - name: unknown_4
    type: bytes_4
    notes: 'dword 4 is SKIPPED: the cursor jumps from +0x0c to +0x14. The string''s offset column; the pointer is written over record +0x10.'
    evidence: L3
  - name: unknown_5
    type: u32
    notes: dword 5; record +0x14 -- loaded together with dword 6 in one 8-byte move
    evidence: L3
  - name: unknown_6
    type: u32
    notes: dword 6; record +0x18
    evidence: L3
  - name: unknown_7
    type: u32
    notes: dword 7; record +0x1c
    evidence: L3
  - name: modifierType
    type: cstring
    notes: 'NUL-TERMINATED, not length-prefixed: the handler scans from the end of the 0x20 block to the NUL and advances the cursor past it. The only string in the challenge patch family that is not an lpstring. Stored at record +0x10 over dword 4; a token the UI resolves through C_Challenge.GetModifierLocalization (L2).'
    evidence: L3
# Read from the decompiled handler FUN_10139200 (build/ghidra/handlers-c/0x0663.c) read by hand, 2026-09-20. The capture reference (~/CoaServer/reference/cointhrow, L1 for us) counts Modifiers 8/8 fields and calls the row raw. Our own read confirms eight dwords and a 0x20 record (L3), and adds what their note does not mention: a string column at dword 4, carried on the wire as a NUL-terminated string.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00139200"
    registration_site_rva: "0x0013b88c"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: ChallengeModifiers.dbc
    instance: "0x10be029c"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 32
    fields: 8

provenance:
  - level: L3
    source: "the decompiled handler FUN_10139200 (build/ghidra/handlers-c/0x0663.c) read by hand, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml (CustomDBCMgr load idiom and the handler's table object)"
    claim: "the packet carries one row of ChallengeModifiers.dbc into the client's copy of that table"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0663 is named SMSG_PATCH_CHALLENGE_MODIFIER"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
