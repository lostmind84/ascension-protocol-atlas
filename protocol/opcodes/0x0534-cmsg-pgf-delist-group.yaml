# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0534
name: CMSG_PGF_DELIST_GROUP
direction: client_to_server
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named CMSG_PGF_DELIST_GROUP by the client's own opcode table. No sender exists for this
  opcode in this build: the value appears nowhere in `.text` as a 32- or 16-bit immediate,
  nowhere in `.rdata`/`.data`, and nowhere in the shipped Lua (client/client-senders.md's
  exhaustive scan), and it is absent from every symbol dump (senders/handlers/layouts/
  handler-fire/handler-strings.yaml). Layout is not established; there is no code to read
  it from. client/client-senders.md notes, unverified (L0), that this may be the older
  generation of a feature the live CMSG_GROUP_FINDER_* opcodes (0x057E/0x057F) replaced.

layout: []   # unknown

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0534 is named CMSG_PGF_DELIST_GROUP"
  - level: L3
    source: "client/client-senders.md (opcode value absent from .text/.rdata/.data and the shipped Lua; also absent from client/symbols/ascension-extensions-2026-08-13/{senders,handlers,layouts,handler-fire,handler-strings}.yaml)"
    claim: "no sender or handler for this opcode exists in this client build"

replay: null
