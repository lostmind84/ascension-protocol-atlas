# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x092a
name: SMSG_PATCH_SPELL
direction: server_to_client
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_PATCH_SPELL by the client's own opcode table. One row of the stock Spell.dbc (AzerothCore
  already loads this table; ADR 0005), sent as an opaque 680-byte blob (RVA 0x00231de0, no per-field split
  available) followed by 4 lpstrings. Spell.dbc's own file has exactly 4 locale-string blocks (SpellName,
  Rank, Description, ToolTip -- the last two fully commented out of SpellEntry) and 234 columns total; the
  numeric arithmetic closes exactly (234 - 4x16 string slots = 170 dwords = 680 bytes, matching the blob),
  which is a stronger reconciliation than most opcodes in this bucket get. Even so, this fiche does not
  decompose the 680-byte blob into named columns or the 4 lpstrings into SpellName/Rank/Description/ToolTip:
  the blob is a single un-individuated read, so nothing establishes that the internal byte order was not
  reshuffled during packing even though the total closes, and a sibling table (SkillLine, 0x0933) that looks
  structurally identical has a string-block count that does NOT reconcile with its lpstring count (3 vs 4),
  which is direct evidence that this project's assumption "one lpstring per locale block, in file order"
  fails at least once and should not be trusted anywhere it is not independently confirmed. DBCStructure.h
  (SpellEntry) and DBCfmt.h (SpellEntryfmt) were checked; no naming source safe enough to assert per-field was
  found. Read against the real decompiled handler (build/ghidra/handlers-c/0x092a.c): confirms the blob is
  one 170-dword (0xaa) copy loop and the 4 strings are 4 separate, sequential FUN_100d3680 calls in a fixed
  order -- consistent with, and no stronger than, the structure already in this layout. The same function
  also inspects and conditionally rewrites 3 pairs of fields after the copy (comparing one member of each
  pair against 0x6b/0x6c and the other against 30), a plausible fit for 3 of SpellEntry's MAX_SPELL_EFFECTS
  arrays, but Ghidra's declared locals for that logic do not reliably map to fixed byte offsets inside the
  680-byte blob (the copy loop's destination array is declared shorter than the copy itself and spills into
  adjacent locals in a way this pass could not safely resolve to specific columns), so nothing further is
  asserted from it.

layout:
  - name: field_0
    type: bytes_680
    evidence: L3
  - name: field_1
    type: lpstring
    evidence: L3
  - name: field_2
    type: lpstring
    evidence: L3
  - name: field_3
    type: lpstring
    evidence: L3
  - name: field_4
    type: lpstring
    evidence: L3
# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# packet_layout.py had followed a call into 0x000d3680, not the handler. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00231de0"
    registration_site_rva: "0x00228d3f"
    evidence: L3

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x092a is named SMSG_PATCH_SPELL"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
