# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x095c
name: SMSG_PATCH_GROUND_EFFECT_TEXTURE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_PATCH_GROUND_EFFECT_TEXTURE by the client's own opcode table. The opcode is not bound
  in patch-tables.yaml -- the DLL's own custom-table list does not mention this opcode;
  GroundEffectTexture.dbc is the table patch-table-names.md associates with it by name and geometry
  (grade L1). The handler's registered entry point lies inside a decompiled function shared with
  another opcode in this shard, so whether it reads a full GroundEffectTexture.dbc row is not
  established. No source names any of the table's columns.

layout:
  - name: field_0
    type: bytes_76
    evidence: L3
  - name: field_1
    type: lpstring
    evidence: L3
  - name: field_2
    type: lpstring
    evidence: L3
# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x0022cb80"
    registration_site_rva: "0x00229021"
    evidence: L3

dbc_identity:
  dbc: GroundEffectTexture.dbc
  fields: 11
  record_size: 44
  rows: 38436
  header_source: "ascension-live-2026-09-17 WDBC header, L2 (client/patch-table-names.md)"
  bound: >
    Not bound in client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml -- this opcode
    is not one of the DLL's 114 CustomDBCMgr table objects; per client/patch-tables.md it is one
    of the other 141 patch handlers, whose record is the DLL's own struct rather than a named
    table row. The table below is the one client/patch-table-names.md associates with this opcode
    by name and geometry, sourced only from a third-party reference (grade L1).
  reference_lead: "ref: stock (wowdev, L1) -- a third-party reference calls this a published stock Wrath table, unverified here"
  match: UNCERTAIN
  wire: >
    Registered at 0x0022cb80. Ghidra's function recovery places both this and 0x095d inside one
    decompiled function starting earlier, at 0x1022c8e0 (build/ghidra/handlers-c/0x095c.c and
    0x095d.c are identical text) -- so neither opcode's registered address is that function's own
    start, and a third, unidentified caller may own the true entry. From the top, the visible path
    reads 76 bytes plus one lpstring, matching neither this table nor 0x095d's. Nested inside a
    branch reached only conditionally, and reached again inside a second, deeper branch, is a
    block of ten 4-byte reads plus one 8-byte read (44 bytes, all numeric, no string) -- matching
    this table's own header (11 fields, 44 bytes) plausibly, by size, but which entry point
    actually reaches that code is not established.
  note: >
    0x095c's registered address is the larger of the two (0x0022cb80 vs 0x095d's 0x0022ca70),
    consistent with owning the deeper of the two nested branches, but that is an inference from
    address ordering, not a read from 0x095c's own entry.

column_names: none found
  # checked: client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (no
  # string literal near the store); lua-keys.yaml (no builder in this table's domain);
  # the Ascension UI/Lua corpus (build/corpus/ascension-ui-patch-b-stock, searched for
  # the table's own base name, e.g. "GroundEffectTexture").

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x095c is named SMSG_PATCH_GROUND_EFFECT_TEXTURE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
