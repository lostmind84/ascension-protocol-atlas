# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x09d0
name: SMSG_CHARACTER_CUSTOMIZATION_UNLOCKS
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Character customization unlocks: a u32 the client never reads. The handler clears its unlock container and ignores the payload.

layout:
  - name: unknown_0
    type: u32
    notes: 'Four bytes on the wire, always zero in the capture, and the handler does not read them: it clears the client''s customization-unlock container and returns. So the packet has a payload the client ignores.

      '
    evidence: L3
# Read from CoinThrow's capture reference 2026-09-20 (docs/sources/, L1) read next to our own decompilation; see docs/crosscheck-cointhrow-2026-09-20.md.

# Read from the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest is noted as not followed.

client:
  handler:
    rva: "0x0032e300"
    registration_site_rva: "0x0032e6c2"
    evidence: L3

provenance:
  - level: L3
    source: "CoinThrow's capture reference 2026-09-20 (docs/sources/, L1) read next to our own decompilation; see docs/crosscheck-cointhrow-2026-09-20.md"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest is noted as not followed"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x09d0 is named SMSG_CHARACTER_CUSTOMIZATION_UNLOCKS"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
