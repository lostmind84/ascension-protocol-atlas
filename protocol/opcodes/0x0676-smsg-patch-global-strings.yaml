# Generated by tools/codegen/stub_fiches.py, then hand-filled (patch-unnamed-1 sweep, 2026-09-20).
# This is a CoA-custom DBC table row with NO naming source in anything this repo holds
# (see client/patch-table-names.md). field_N names are placeholders; do not rename them
# without a cited source (lua-keys.yaml, a UI getter, or handler-strings.yaml).

id: 0x0676
name: SMSG_PATCH_GLOBAL_STRINGS
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  SMSG_PATCH_GLOBAL_STRINGS: writes a 4-field, 16-byte record (id, one u32, two strings) into GlobalStrings.dbc's table object -- far short of that table's own 20-field, 80-byte row. Strings are read with FUN_1008baf0, a different helper from the FUN_100d3680 used by most of this shard's opcodes. After storing the row the handler conditionally calls a hook through the pointer table (DAT_10bc90a0) when a flag bit in the second field is set, passing the two strings through -- some sort of client-side announcement, not traced further. No column name is established.

layout:
  - name: field_0
    type: u32
    notes: "offset 0; the id bounds-checked against the table object's minId/maxId"
    evidence: L3
  - name: field_1
    type: u32
    notes: "offset 4; bit 0 (after an optional >>1 shift gated on *DAT_10bcb458) selects whether the post-store hook fires"
    evidence: L3
  - name: field_2
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further"
    evidence: L3
  - name: field_3
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further"
    evidence: L3
  - name: field_4
    type: lpstring
    notes: "via FUN_1008baf0, not FUN_100d3680"
    evidence: L3
  - name: field_5
    type: lpstring
    notes: "via FUN_1008baf0, not FUN_100d3680; passed to the post-store hook (DAT_10bc90a0) together with field_4 when field_1's bit is set"
    evidence: L3

client:
  handler:
    rva: "0x0025ad00"
    registration_site_rva: "0x0025af16"
    evidence: L3

patches_table:   # the client DBC table(s) this packet writes into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3 unless noted; record_size/fields/rows from client/patch-table-names.md's own header read against the ascension-live-2026-09-17 snapshot, L2)
  - dbc: GlobalStrings.dbc
    instance: "0x10bdfae0"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 80
    fields: 20
    rows: 14357

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x0676.c (already-decompiled handler, read by hand this pass)"
    claim: "the widths, order and destination struct of the wire fields"
  - level: L3
    source: "build/ghidra/handlers-c/0x0676.c, the post-store block"
    claim: "a hook through the exe pointer table (DAT_10bc90a0) fires conditionally on field_1's low bit, passed both strings"
    note: "the hook's own purpose was not traced; recorded because it is the only opcode in this shard with visible post-store side effects"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (handler 0x0676)"
    claim: "no string literal is referenced near this handler's store -- checked, empty"
    note: "rules out an event name or format string that might have hinted at a field's purpose"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0676 is named SMSG_PATCH_GLOBAL_STRINGS"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L1
    source: "client/patch-table-names.md (the naming-source sweep over all 228 SMSG_PATCH_* opcodes)"
    claim: "no lua-keys builder, UI getter, or DBCfmt entry names this table's columns; the reference community source offers no names for it either (bucket 'none found')"

replay: null
