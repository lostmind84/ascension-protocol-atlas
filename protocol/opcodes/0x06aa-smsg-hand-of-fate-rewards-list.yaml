# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06aa
name: SMSG_HAND_OF_FATE_REWARDS_LIST
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  The Hand of Fate rewards list, and the only compressed packet found so far: a u32 uncompressed size then a deflate stream the client inflates with zlib before parsing. The inflated content was not decoded.

layout:
  - name: uncompressedSize
    type: u32
    notes: the size the payload inflates to; the reader allocates that much
    evidence: L3
  - name: deflateStream
    type: bytes_variable
    notes: the rest of the packet, inflated by the exe's zlib uncompress (0x00778180) before anything is parsed. The inflated bytes are the rewards list proper, whose records were not read here
    evidence: L3
# Read from the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20).

# Read from the decompiled handler (build/ghidra/handlers-c), read as far as its visible reads; the rest is noted as not followed.

client:
  handler:
    rva: "0x00226ff0"
    registration_site_rva: "0x002275a2"
    evidence: L3

provenance:
  - level: L3
    source: "the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read as far as its visible reads; the rest is noted as not followed"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06aa is named SMSG_HAND_OF_FATE_REWARDS_LIST"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
