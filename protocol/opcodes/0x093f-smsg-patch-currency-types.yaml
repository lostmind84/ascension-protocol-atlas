# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x093f
name: SMSG_PATCH_CURRENCY_TYPES
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of the stock CurrencyTypes.dbc (AzerothCore already loads this table; ADR 0005), sent as the single 16-byte blob the handler at RVA 0x0022bc20 reads (L3). Nothing was elided (4 cols x 4 bytes = 16 bytes matches the client header exactly). ItemId ('n', struct comment: used as the real index) and BitIndex are CurrencyTypesEntry's only active members and are named; the file's own ID (col 0) and Category (col 2) are core 'x' columns with no name (L1 throughout).

layout:
  - name: field_0
    type: u32
    notes: 'core format char ''x''/''X'' at this column: skipped, no name in DBCStructure.h'
    evidence: L1
  - name: ItemId
    type: u32
    notes: 'active CurrencyTypesEntry member (''n''/FT_IND); struct comment: used as real index'
    evidence: L1
  - name: field_2
    type: u32
    notes: 'core format char ''x''/''X'' at this column: skipped, no name in DBCStructure.h'
    evidence: L1
  - name: BitIndex
    type: u32
    notes: active CurrencyTypesEntry member
    evidence: L1
# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h (CurrencyTypesEntry) and DBCfmt.h (CurrencyTypesfmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md.

client:
  handler:
    rva: "0x0022bc20"
    registration_site_rva: "0x00228e71"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h (CurrencyTypesEntry) and DBCfmt.h (CurrencyTypesfmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x093f is named SMSG_PATCH_CURRENCY_TYPES"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
