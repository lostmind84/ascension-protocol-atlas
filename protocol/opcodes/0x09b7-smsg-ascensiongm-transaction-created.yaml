# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x09b7
name: SMSG_ASCENSIONGM_TRANSACTION_CREATED
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  A GM transaction created: a nine-u32 record. Partially followed.

layout:
  - name: transaction
    type: bytes_36
    notes: nine u32 read into a transaction record by a helper, then an event (FUN_100d0a50); names not followed
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest of the body is noted as not followed.

# Widths and order, read out of the handler at 0x00a13fa0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00a14460"
    registration_site_rva: "0x00a13e3e"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "ASCENSION_CUSTOM_GM_TRANSACTION_CREATED"
  - "ASCENSION_CUSTOM_GM_TRANSACTION_LIST_UPDATED"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: ASCENSION_CUSTOM_GM_TRANSACTION_
    format: "%u"
    helper: FUN_100d0a50
  - event: ASCENSION_CUSTOM_GM_TRANSACTION_
    format: "%b"
    helper: FUN_100d0bd0

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest of the body is noted as not followed"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x09b7 is named SMSG_ASCENSIONGM_TRANSACTION_CREATED"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
