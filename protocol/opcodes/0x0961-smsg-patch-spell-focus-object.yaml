# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0961
name: SMSG_PATCH_SPELL_FOCUS_OBJECT
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Candidate: one row of SpellFocusObject.dbc (bound_by: L1 ref, patch-table-names.md). The handler reads two dwords then one string (handler-fire.yaml). id (column 0) is named -- it is the only column AzerothCore's SpellFocusObjectEntry struct stores at all -- and the trailing lpstring is count-matched to the table's one locale block (Name[16]); the second dword has no naming source and stays unnamed.

layout:
  - name: id
    type: u32
    notes: 'DBC column 0 (index field, fmt ''n''); core: SpellFocusObjectEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar5,8],[local_2c,lpstring]], two dwords then one string); name L1 (core DBCStructure.h, another project''s reading of the same file). ID is the ONLY column AzerothCore''s SpellFocusObjectEntry struct names at all (the whole rest of the table, columns 1-17, is one commented-out Name[16] locale block + a flags column) -- 8 fixed bytes is short of even that minimal shape (4 bytes), so the second dword has no candidate at all.'
    evidence: L1
  - name: field_1
    type: u32
    notes: 'Second dword of the handler''s read sequence. No naming source: AzerothCore''s SpellFocusObjectEntry struct stores nothing besides ID (see id''s note). Width L3 only.'
    evidence: L3
  - name: name
    type: lpstring
    notes: 'The focus object''s display name: SpellFocusObject.dbc''s only other content is Name[16] (DBC columns 1-16, a 16-locale block AzerothCore''s fmt marks ''x'' and never stores), and the handler reads exactly one trailing lpstring -- count matches, per the map''s locale-block trap (client/patch-table-names.md): the wire carries only the primary locale. Width L3 (handler-fire.yaml); name L1 (candidate, from the DBC''s own commented locale block, not a stored core member).'
    evidence: L1
# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h SpellFocusObjectEntry + DBCfmt.h SpellFocusObjectfmt (L1: another project's reading of the same client DBC file, not ours).

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# packet_layout.py had followed a call into 0x000d3680, not the handler. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00231cd0"
    registration_site_rva: "0x0022906c"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h SpellFocusObjectEntry + DBCfmt.h SpellFocusObjectfmt (L1: another project's reading of the same client DBC file, not ours)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "client/patch-table-names.md (the naming-sources sweep): 0x0961 bound_by 'L1 ref' to SpellFocusObject.dbc, 18 fields / 72 bytes, matching the client DBC header"
    claim: "this opcode's table is SpellFocusObject.dbc, at L1 (only the third-party reference names it; patch-tables.yaml does not bind this opcode)"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0961 is named SMSG_PATCH_SPELL_FOCUS_OBJECT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
