# Generated by tools/codegen/stub_fiches.py, then hand-filled (patch-unnamed-1 sweep, 2026-09-20).
# This is a CoA-custom DBC table row with NO naming source in anything this repo holds
# (see client/patch-table-names.md). field_N names are placeholders; do not rename them
# without a cited source (lua-keys.yaml, a UI getter, or handler-strings.yaml).

id: 0x06f6
name: SMSG_PATCH_SPELL_ACTIVATION_OVERLAYS
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  SMSG_PATCH_SPELL_ACTIVATION_OVERLAYS: a clean, full-width row write into SpellActivationOverlays.dbc -- 14 fields, 56 bytes, matching that table's own header geometry exactly once the two string columns' wire-slots are counted. This replaces the file's earlier partial layout (`row: bytes_56` + one `unknown` tail field), which undercounted the fields and left the second string unaccounted for. No column name is established.

layout:
  - name: field_0
    type: u32
    notes: "offset 0; bounds-checked against the table object's minId/maxId"
    evidence: L3
  - name: field_1
    type: u32
    notes: "offset 4"
    evidence: L3
  - name: field_2
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (1st of two non-adjacent gaps)"
    evidence: L3
  - name: field_3
    type: u32
    notes: "offset 0xc"
    evidence: L3
  - name: field_4
    type: u32
    notes: "offset 0x10"
    evidence: L3
  - name: field_5
    type: u32
    notes: "offset 0x14"
    evidence: L3
  - name: field_6
    type: u32
    notes: "offset 0x18"
    evidence: L3
  - name: field_7
    type: u32
    notes: "offset 0x1c, first half of an 8-byte load"
    evidence: L3
  - name: field_8
    type: u32
    notes: "offset 0x20, second half; read as one 8-byte load in the handler; split into two 4-byte fields here to line up with the table's own field count -- could be a single 64-bit value instead, not resolved further"
    evidence: L3
  - name: field_9
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (2nd of two non-adjacent gaps)"
    evidence: L3
  - name: field_10
    type: u32
    notes: "offset 0x28"
    evidence: L3
  - name: field_11
    type: u32
    notes: "offset 0x2c"
    evidence: L3
  - name: field_12
    type: u32
    notes: "offset 0x30"
    evidence: L3
  - name: field_13
    type: u32
    notes: "offset 0x34"
    evidence: L3
  - name: field_14
    type: cstring
    notes: "1st of 2, NUL-terminated, scanned byte by byte; fills field_2's slot"
    evidence: L3
  - name: field_15
    type: cstring
    notes: "2nd of 2; fills field_9's slot"
    evidence: L3

client:
  handler:
    rva: "0x001e3430"
    registration_site_rva: "0x001e6b6d"
    evidence: L3

patches_table:   # the client DBC table(s) this packet writes into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3 unless noted; record_size/fields/rows from client/patch-table-names.md's own header read against the ascension-live-2026-09-17 snapshot, L2)
  - dbc: SpellActivationOverlays.dbc
    instance: "0x10bdfdb0"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 56
    fields: 14
    rows: 1405

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x06f6.c (already-decompiled handler, read by hand this pass)"
    claim: "the widths, order and destination struct of the wire fields"
  - level: L3
    source: "build/ghidra/handlers-c/0x06f6.c, full hand read, superseding the file's earlier packet_layout.py-derived partial layout"
    claim: "the record is exactly 14 fields / 56 bytes once the two string slots are counted, not 'bytes_56 + 1 unknown tail field' as the file said before this pass"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (handler 0x06f6)"
    claim: "no string literal is referenced near this handler's store -- checked, empty"
    note: "rules out an event name or format string that might have hinted at a field's purpose"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06f6 is named SMSG_PATCH_SPELL_ACTIVATION_OVERLAYS"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L1
    source: "client/patch-table-names.md (the naming-source sweep over all 228 SMSG_PATCH_* opcodes)"
    claim: "no lua-keys builder, UI getter, or DBCfmt entry names this table's columns; the reference community source offers no names for it either (bucket 'none found')"

replay: null
