# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0940
name: SMSG_PATCH_DUNGEON_ENCOUNTER
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of the stock DungeonEncounter.dbc (AzerothCore already loads this table; ADR 0005), read by the
  handler at RVA 0x0022bcc0 as 6 dwords then 1 lpstring. id (leading, the universal 'n' convention, and
  DungeonEncounterEntry's own first column) and encounterName (the file's only locale-string block,
  encounterName[16], and the only string-type candidate) are named from DBCStructure.h (L1). The 5 numeric
  fields in between do not reconcile: DungeonEncounterEntry names only 3 more active columns (mapId,
  difficulty, encounterIndex; orderIndex is commented out) against 5 observed reads, so none of them is
  individually named here. Read against the real decompiled handler (build/ghidra/handlers-c/0x0940.c):
  confirms a clean sequential 6-dwords-then-1-lpstring shape (no branching), and shows the string is read
  before the 6th dword is stored into the final record, so that dword is not simply "the last of the prefix"
  -- it is stored at a position after the string, suggesting a value from past encounterName's own column
  range (col5-20) rather than an extra prefix column. Column arithmetic doesn't close cleanly enough to name
  it (the file has 23 columns total: id/mapId/difficulty/orderIndex(x)/encounterIndex/encounterName[16]/
  nameFlags(x)/spellIconID(x) -- 7 non-string columns against the 6 observed, off by one), so it stays unnamed.

layout:
  - name: id
    type: u32
    notes: active DungeonEncounterEntry member (column 0, 'n')
    evidence: L1
  - name: field_1
    type: u32
    evidence: L3
  - name: field_2
    type: u32
    evidence: L3
  - name: field_3
    type: u32
    evidence: L3
  - name: field_4
    type: u32
    evidence: L3
  - name: field_5
    type: u32
    evidence: L3
  - name: encounterName
    type: lpstring
    notes: >-
      the file's only locale-string block (encounterName[16], columns 5-20, active DungeonEncounterEntry
      member) and the only string-type candidate in the record, so this identification is safe even though
      field_1..field_5 above are not individually reconciled against DungeonEncounterEntry's column layout.
    evidence: L1
# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# the asm idiom (packet_layout.py) saw the same reads minus the strings. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x0022bcc0"
    registration_site_rva: "0x00228e80"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h (DungeonEncounterEntry) and DBCfmt.h (DungeonEncounterfmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md: column 0 ('n') is id; the file's only locale-string block is encounterName[16], the only string-type candidate"
    claim: "the names id and encounterName"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0940 is named SMSG_PATCH_DUNGEON_ENCOUNTER"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
