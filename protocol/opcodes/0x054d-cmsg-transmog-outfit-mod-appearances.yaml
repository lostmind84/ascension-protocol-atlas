# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x054d
name: CMSG_TRANSMOG_OUTFIT_MOD_APPEARANCES
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named CMSG_TRANSMOG_OUTFIT_MOD_APPEARANCES by the client's own opcode table: one of
  five consecutive TRANSMOG_OUTFIT_* CMSGs (0x054d-0x0551 -- MOD_APPEARANCES, MOD_NAME,
  APPLY_ID, REMOVE_ID, CREATE_ID), an old per-outfit CRUD surface. Nothing in this build
  constructs a packet with this opcode: no `push 0x54d` / `mov r32, 0x54d` occurs
  anywhere in Extensions.dll's `.text`, byte-exhaustively. The live feature collapses the
  same five actions into two verbs, CMSG_SAVE_APPEARANCE_OUTFIT (0x069e, registered and
  sent in this build -- not ours; the capture reference describes it as `{cstring name;
  u32 n; u32 appearanceId[n]}`, one call that both creates and edits an outfit by name)
  and CMSG_DELETE_APPEARANCE_OUTFIT (0x06a0, also live). This is not "we have not found
  the sender yet"; it is "this build has no code that could send it."

layout: []   # empty

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x054d is named CMSG_TRANSMOG_OUTFIT_MOD_APPEARANCES"
  - level: L3
    source: >
      tools/extract/client_sender.py's push_sites() scan, run directly against
      ~/CoaServer/client/ascension-live/Extensions.dll.ORIGINAL (sha256
      0f8d847b3adc44a963606f0cd4f7938ad6fcd6f4d87feac3131c7153bdf3bb11) for 0x054d alone,
      2026-09-20
    claim: >
      the immediate value 0x054d never occurs as a `push imm32` (0x68) or
      `mov r32, imm32` (0xb8-0xbf) operand anywhere in the .text section
    note: >
      Byte-exhaustive (every offset in .text, not just instruction boundaries). A sender
      that built the opcode from a data table instead of a literal would still be
      missed; see open_questions.
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml and handlers.yaml"
    claim: >
      CMSG_SAVE_APPEARANCE_OUTFIT (0x069e) and CMSG_DELETE_APPEARANCE_OUTFIT (0x06a0)
      both have registered senders in this build, unlike the whole 0x054d-0x0551 CRUD
      cluster -- confirming which pair of opcodes the live outfit feature actually uses
    note: "field layout of 0x069e/0x06a0 is not read here; neither is one of this pass's opcodes."
  - level: L1
    source: >
      CoinThrow's capture reference, kept outside this repository at
      ~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md,
      section "Cosmetics / appearance" (the wardrobe/appearance system writeup)
    claim: >
      "0x0540-0x0559 + 0x05CD/0x05CE/0x05D1 + 0x0640-0x0642 (superseded -- no senders
      exist in the client build)"; outfit save/delete moved to 0x069e/0x06a0
      (`C_AppearanceOutfit.SaveOutfit`, sender at 0x100cb1c8; `DeleteOutfit`, at
      0x100c7b6e), and "no push32/mov-imm encodings exist in Extensions.dll code for
      0x054D-0x0558".
    note: >
      L1 per docs/EVIDENCE.md regardless of the author's own confidence. Re-derived
      independently above at L3 for this specific opcode: our own byte-exhaustive scan
      agrees with their claim.

open_questions:
  - "The immediate-value scan cannot see a sender that loads the opcode from a data
     table rather than a literal 0x54d. Not reproduced here: the capture reference's
     broader byte search of Ascension.exe, which it reports finding only unrelated
     internal table/record math there, not a network-layer path."

related:
  - 0x069e
  - 0x06a0
