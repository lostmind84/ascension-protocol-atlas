# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0651
name: CMSG_ENTER_MANASTORM
direction: client_to_server
status: proven
evidence: L5
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Round trip proven on 2026-09-19 through the client's Lua packet API: a 4-byte payload is answered
  with 0x0652, an 8-byte one is not. The client's own sender (FUN_102a68f0) stays read at L3: the
  proven bytes came from CreatePacket, not from the Manastorm UI.

layout:
  - name: depth
    type: u32
    evidence: L5
# width and order from the client's sender (CDataStore PutData sizes, decompiled; L3); names from the server's reader in mod-ascension-compat (L1)
# client sender: FUN_102a68f0
# server reader: AscensionManastorm.cpp:283-286: size must be 4; depth = read<uint32>(0)
used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_Manastorm/ManastormQueue.lua
  - Interface/AddOns/Ascension_Manastorm/ManastormQueue.xml
  - Interface/AddOns/Ascension_UIDevelopmentTools/EventTrace/EventArgNames.lua

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-ascension-compat/src/AscensionManastormProtocol.h

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: Enter
  function_rva: "0x002a68f0"
  calls:
    - {file: "Interface/AddOns/Ascension_Manastorm/ManastormQueue.lua", line: 189, args: ["self.selectedLevel"]}

provenance:
  - level: L5
    source: "tools/replay/manastorm_roundtrip.py, run on slot 1 on 2026-09-19 (client/lua-packet-api.md)"
    claim: "the server accepts a 4-byte payload (u32 depth) and answers with 0x0652"
  - level: L3
    source: "tools/ghidra/ghidra.sh run Decomp.java <sender VA> + objdump of the push/call sequence (see client/client-senders.md)"
    claim: "what the client writes, field widths and order"
  - level: L1
    source: "the server's reader (see client/client-senders.md)"
    claim: "field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0651 is named CMSG_ENTER_MANASTORM"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay:
  script: tools/replay/manastorm_roundtrip.py
  asserts:
    - "the server accepts a 4-byte payload (u32 depth) and answers with 0x0652"
    - "the server ignores a payload of another size (control: 8 bytes, no reply)"
  asserts_measured:
    - "pksend 0x0651 u32:1 -> 0x0652 ENTER_MANASTORM_TOO_LOW_PLAYER_LEVEL within 2 s"
    - "pksend 0x0651 u32:1 u32:0 -> no 0x0652 within 3 s"
  last_run: { date: 2026-09-20, result: pass, build: coa-extensions-rev4-2026-09-14,
              dataset: ascension-live-2026-09-17, slot: 1 }
