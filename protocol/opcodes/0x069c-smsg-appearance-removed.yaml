# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x069c
name: SMSG_APPEARANCE_REMOVED
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  An appearance removed from the collection: the appearance id and a second u32, then APPEARANCE_UNCOLLECTED(appearanceID). The mirror of 0x069B.

layout:
  - name: appearanceId
    type: u32
    notes: APPEARANCE_UNCOLLECTED carries it as arg1 (UIParent.lua:1796)
    evidence: L3
  - name: unknown_1
    type: u32
    notes: passed with the id into the appearance collection; 0x069B (appearance added) carries sourceItem in this position per the server writer, not confirmed for this packet
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x069c.c) and UIParent.lua's event handler (L2).

# Widths and order, read out of the handler at 0x000c4930 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x000c4930"
    registration_site_rva: "0x000c5bd3"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "APPEARANCE_UNCOLLECTED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: APPEARANCE_UNCOLLECTED
    args: []
    source: "Interface/AddOns/Ascension_AppearanceUI/AppearanceCollectionMixin.lua:350"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: APPEARANCE_UNCOLLECTED
    format: null
    helper: FUN_100b9560

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: Appearances.dbc
    instance: "0x10bdfcb4"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 68
    fields: 17

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x069c.c) and UIParent.lua's event handler (L2)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x069c is named SMSG_APPEARANCE_REMOVED"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
