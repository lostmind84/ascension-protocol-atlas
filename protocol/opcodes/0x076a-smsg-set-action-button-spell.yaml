# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x076a
name: SMSG_SET_ACTION_BUTTON_SPELL
direction: server_to_client
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Named SMSG_SET_ACTION_BUTTON_SPELL by the client's own opcode table. Layout and behaviour not
  established.

layout:
  - name: button
    type: u8
    evidence: L3
  - name: spell
    type: u32
    evidence: L3
# Widths and order, read out of the handler at 0x000d42f0 by tools/extract/packet_layout.py.
# Names: the Lua arguments of SET_ACTION_BUTTON_SPELL_PAYLOAD (client/event-args.yaml, L2), at the positions the
# handler fills them from its reads (handler-fire.yaml, format %u%u, L3). Unnamed fields are
# read but not passed to the event.
# The structure is flat: a count followed by a repeated record appears here as
# the count plus one record's fields.

client:
  handler:
    rva: "0x000d42f0"
    registration_site_rva: "0x000d4d71"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/FrameXML/Util/C_Spell.lua

client_strings:   # literals the handler references, from its decompilation
  - "SET_ACTION_BUTTON_SPELL_PAYLOAD"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: SET_ACTION_BUTTON_SPELL_PAYLOAD
    args: [button, spell]
    source: "Interface/FrameXML/Util/C_Spell.lua:371"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: SET_ACTION_BUTTON_SPELL_PAYLOAD
    format: "%u%u"
    helper: FUN_10278c90
    fields: [field_0, field_1]   # the read filling each format position

provenance:
  - level: L2
    source: "client/event-args.yaml (SET_ACTION_BUTTON_SPELL_PAYLOAD) joined through client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml"
    claim: "the field names, as the UI's method named after the event declares them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x076a is named SMSG_SET_ACTION_BUTTON_SPELL"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
