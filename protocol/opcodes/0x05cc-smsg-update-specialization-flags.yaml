# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05cc
name: SMSG_UPDATE_SPECIALIZATION_FLAGS
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Specialization flags changed: two u32 the handler passes, in order, to SPECIALIZATION_FLAGS_UPDATED with format %u%u (the mapping is the handler's), and the second alone to ASCENSION_CUSTOM_GAME_MODE_CHANGED(modes). The names come from the events' names, not from a declared Lua parameter list.

layout:
  - name: specializationId
    type: u32
    notes: first %u of SPECIALIZATION_FLAGS_UPDATED; named after the event
    evidence: L3
  - name: flags
    type: u32
    notes: second %u, and the `modes` of ASCENSION_CUSTOM_GAME_MODE_CHANGED
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c, DumpC.java), its fire format and the UI's event arguments.

# Widths and order, read out of the handler at 0x0031eca0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x0031eca0"
    registration_site_rva: "0x0031eeb9"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "SPECIALIZATION_FLAGS_UPDATED"
  - "ASCENSION_CUSTOM_GAME_MODE_CHANGED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: ASCENSION_CUSTOM_GAME_MODE_CHANGED
    args: []
    source: "Interface/AddOns/Ascension_ChallengesUI/Tabs/GamemodesTab.lua:158"
  - event: ASCENSION_CUSTOM_GAME_MODE_CHANGED
    args: [modes]
    source: "Interface/FrameXML/Ascension_BuildDraft/Ascension_BuildDraft.lua:170"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: SPECIALIZATION_FLAGS_UPDATED
    format: "%u%u"
    helper: FUN_10278c90
    fields: [field_0, field_1]   # the read filling each format position
  - event: ASCENSION_CUSTOM_GAME_MODE_CHANG
    format: "%u"
    helper: FUN_100d0a50

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c, DumpC.java), its fire format and the UI's event arguments"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05cc is named SMSG_UPDATE_SPECIALIZATION_FLAGS"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
