# Generated by tools/codegen/stub_fiches.py, then hand-filled (patch-unnamed-1 sweep, 2026-09-20).
# This is a CoA-custom DBC table row with NO naming source in anything this repo holds
# (see client/patch-table-names.md). field_N names are placeholders; do not rename them
# without a cited source (lua-keys.yaml, a UI getter, or handler-strings.yaml).

id: 0x056a
name: SMSG_PATCH_ITEM_DISPLAY_INFO_COLLECTIONS
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  SMSG_PATCH_ITEM_DISPLAY_INFO_COLLECTIONS: patch_tables.yaml does not bind this opcode to any table (it is absent from its handlers: map, even though the decompiled C this binding is built from was on disk). Hand-tracing the handler shows it DOES follow the standard CustomDBCMgr idiom -- minId/maxId-bounded lookup into a pointer array at +0x20 of a table object -- but at instance address 0x10bdff3c, which our own name-getter/instance-pairing scan never names: it falls in a one-slot gap between SpellAlternativePowerType.dbc (0x10bdff18) and LFGActivities.dbc (0x10bdff60), exactly one 0x24-byte span short. The stored record is 8 fields / 32 bytes, which matches ItemDisplayInfoCollections.dbc's own header (8 fields, 32-byte record) exactly -- real corroboration of the third party's claim that this is that table, but not proof: our own extraction still does not name the address this handler actually touches, so the table identity stays L1 and the column names stay unestablished either way.

layout:
  - name: field_0
    type: u32
    notes: "offset 0; the id bounds-checked against minId/maxId of the table object at 0x10bdff3c"
    evidence: L3
  - name: field_1
    type: u32
    notes: "offset 4, first half of an 8-byte load"
    evidence: L3
  - name: field_2
    type: u32
    notes: "offset 8, second half of the same load; read as one 8-byte load in the handler; split into two 4-byte fields here to line up with the table's own field count -- could be a single 64-bit value instead, not resolved further"
    evidence: L3
  - name: field_3
    type: u32
    notes: "offset 0xc"
    evidence: L3
  - name: field_4
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (first of two consecutive gaps)"
    evidence: L3
  - name: field_5
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (second of two consecutive gaps)"
    evidence: L3
  - name: field_6
    type: u32
    notes: "offset 0x18"
    evidence: L3
  - name: field_7
    type: u32
    notes: "offset 0x1c"
    evidence: L3
  - name: field_8
    type: lpstring
    notes: "via FUN_100d3680"
    evidence: L3
  - name: field_9
    type: lpstring
    notes: "via FUN_100d3680"
    evidence: L3

client:
  handler:
    rva: "0x001e1b50"
    registration_site_rva: "0x001e67cd"
    evidence: L3

patches_table:   # the client DBC table(s) this packet writes into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3 unless noted; record_size/fields/rows from client/patch-table-names.md's own header read against the ascension-live-2026-09-17 snapshot, L2)
  - dbc: ItemDisplayInfoCollections.dbc
    instance: "0x10bdff3c"   # hand-traced this pass (DAT_10bdff5c/4c/48 = rows/minId/maxId of a table object at base+0x20/+0x10/+0xc); NOT present in patch-tables.yaml's own handlers: map -- this table name is the map's L1 third-party claim, corroborated only by the matching 8-field/32-byte geometry, not independently confirmed at this address
    record_size: 32
    fields: 8
    rows: 4704

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x056a.c (already-decompiled handler, read by hand this pass)"
    claim: "the widths, order and destination struct of the wire fields"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml `tables:` list, checked for a name at instance 0x10bdff3c this pass"
    claim: "no table name is recorded at this instance address -- confirmed gap between two named neighbours"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (handler 0x056a)"
    claim: "no string literal is referenced near this handler's store -- checked, empty"
    note: "rules out an event name or format string that might have hinted at a field's purpose"
  - level: L1
    source: "client/patch-table-names.md, row 0x056A (bound by 'L1 ref')"
    claim: "the third-party reference names this opcode ITEM_DISPLAY_INFO_COLLECTIONS; its 8-field/32-byte claim matches the header read here independently, but the address it is bound to (0x10bc9000, a different, already-named instance in our own extraction) does not match the address this handler actually touches (0x10bdff3c)"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x056a is named SMSG_PATCH_ITEM_DISPLAY_INFO_COLLECTIONS"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
