# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06e2
name: SMSG_INSPECT_CHARACTER_ADVANCEMENT_RESULT
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Result of inspecting a character's Advancement. A CA_INSPECT_* token; on CA_INSPECT_OK the inspected character's GUID, its active slot and slot count, then one complete known-entries block per slot -- the same 0x0726 body, read by the same deserializer (FUN_10166760). On any other token the packet is the string alone.

layout:
  - name: result
    type: cstring
    notes: 'A CA_INSPECT_* token, matched in source order against a table of six strings starting at CA_INSPECT_OK (PTR_s_CA_INSPECT_OK_10b34640). The handler fires INSPECT_CHARACTER_ADVANCEMENT_RESULT(result). Only index 0, CA_INSPECT_OK, reads further: on every other token nothing after the string is touched, so a failure is the string alone. An unmatched token falls through to the handler''s "Unexpected Value: {}" path, which also reads nothing more.

      '
    evidence: L3
  - name: targetGuid
    type: u64
    notes: 'CA_INSPECT_OK only. Read as two u32 -- low at payload +0x00 of the body, high at +0x04 -- and recombined as CONCAT44(high, low). The pair keys the inspect-target lookup FUN_1016f700 and the recombined value is compared against the local player''s GUID at the end of the handler before it refreshes the frame.

      '
    evidence: L3
  - name: activeSlot
    type: u32
    notes: 'CA_INSPECT_OK only, body +0x08. Passed to FUN_101499e0 -- the same setter that 0x0725''s first field feeds, which that fiche proves at L5 to be the zero-based active specialization slot. Here it is the inspected character''s.

      '
    evidence: L3
  - name: slotCount
    type: u32
    notes: 'CA_INSPECT_OK only, body +0x0c. The loop bound: the handler iterates index 0..slotCount-1, fetching each slot container with FUN_10149480(index) and creating it with FUN_10149380(index) when absent, and reads one known-entries block per iteration. 0x0725''s second field (slotCount) feeds the twin setter FUN_101499f0. If a container can be neither fetched nor created the handler abandons the packet.

      '
    evidence: L3
  - repeat: slotCount
    fields:
    - name: count
      type: u32
      notes: 'One complete known-entries block per slot, read by FUN_10166760 (RVA 0x00166760) -- literally the same call 0x0726''s handler makes at 0x001713c4, with the same packet cursor. The block is a u32 count followed by that many 21-byte records, and the result is stored into the slot with FUN_10158010, again as in 0x0726.

        '
      evidence: L3
    - repeat: count
      fields:
      - name: entryId
        type: u32
        notes: record +0x04 of the 0x20-byte CAKnownEntry
        evidence: L3
      - name: rank
        type: u32
        notes: record +0x08
        evidence: L3
      - name: marker
        type: u32
        notes: record +0x0c
        evidence: L3
      - name: flag
        type: u8
        notes: record +0x10, the lock byte
        evidence: L3
      - name: buildTime
        type: u32
        notes: record +0x18
        evidence: L3
      - name: reserved
        type: u32
        notes: 'Record +0x1c; reads 5 and 6 are adjacent u32 taken in one pair of moves. Widths, order and struct offsets are the shared deserializer''s, read from its instructions (client/symbols/ascension-extensions-2026-08-13/0x0726-handler.md). What each field MEANS, and how each is graded, belongs to 0x0726: there entryId and flag are L5, rank L3, and marker, buildTime and reserved L1. Nothing here raises them.

          '
        evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x06e2.c) traced with tools/helper_trace.py and read by hand, 2026-09-20. The success path was followed to the end this time: the per-slot body is the same deserializer 0x0726's handler calls (FUN_10166760, RVA 0x00166760).

# Read from the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest of the body is noted as not followed.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x0016fe70"
    registration_site_rva: "0x00172275"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_InspectUI/Panels/InspectBuildPanel.lua
  - Interface/LibraryXML/LibTalentQuery-1.0/LibTalentQuery-1.0.lua

client_strings:   # literals the handler references, from its decompilation
  - "Unexpected Value: {}"
  - "CA_INSPECT_OK"
  - "INSPECT_CHARACTER_ADVANCEMENT_RESULT"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: INSPECT_CHARACTER_ADVANCEMENT_RESULT
    args: [result]
    source: "Interface/AddOns/Ascension_InspectUI/Panels/InspectBuildPanel.lua:52"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: INSPECT_CHARACTER_ADVANCEMENT_RE
    format: "%s"
    helper: FUN_10278c90
    read_slots: [null]   # indices into the 4 reads this pass saw; the layout has 1, so not field numbers

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: CharacterAdvancement.dbc
    instance: "0x10bdf63c"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 692
    fields: 179

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x06e2.c) traced with tools/helper_trace.py and read by hand, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest of the body is noted as not followed"
    claim: "SUPERSEDED 2026-09-20: this entry recorded the earlier pass that stopped at the result token and left the body as bytes_variable. The body has since been followed to the end of the success path; kept here because history is the point."
  - level: L1
    source: "the capture reference (CoinThrow, 2026-09-20, kept outside the tree -- see docs/crosscheck-cointhrow-2026-09-20.md)"
    claim: "the inspect result is a result string, the target GUID and per-slot state using the same 21-byte entry record; a failure is the string only"
    note: >
      CORROBORATION, NOT THE SOURCE. The reference says this in one prose line; the
      layout above was re-derived from our own handler before the line was believed,
      and the two readings agree on every element, including that only the success
      token reads past the string. What the reference adds is confidence, not a field.
      Their claim is silent about the activeSlot/slotCount pair, which is ours.
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06e2 is named SMSG_INSPECT_CHARACTER_ADVANCEMENT_RESULT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
