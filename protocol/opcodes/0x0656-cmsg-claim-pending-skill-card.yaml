# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0656
name: CMSG_CLAIM_PENDING_SKILL_CARD
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Claim pending skill cards: a count and per card a name and two u32; ClaimAllPendingSkillCards sends one such packet per pending group. The skeleton had one record without the count.

layout:
  - name: count
    type: u32
    notes: the binding sends one packet per pending group; the count is that group's card vector size (0x20-byte records)
    evidence: L3
  - repeat: count
    fields:
    - name: name
      type: cstring
      notes: written with its NUL through PutData(len + 1)
      evidence: L3
    - name: unknown_1
      type: u32
      notes: record +0x18
      evidence: L3
    - name: unknown_2
      type: u32
      notes: record +0x1c
      evidence: L3
# Read from the decompiled sender binding (tools/ghidra/scripts/DumpC.java over the DLL, 2026-09-20), read by hand next to the UI's calls (L2).

# Widths and order, read out of the client's sender at 0x0031c2c3 by tools/extract/client_sender.py.
# A helper is called between the opcode write and the send: this is at best a
# PREFIX of the payload. An empty list here does not mean an empty packet.

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: ClaimAllPendingSkillCards
  function_rva: "0x0031c1a0"
  calls: []   # no call in the stock UI corpus

provenance:
  - level: L3
    source: "the decompiled sender binding (tools/ghidra/scripts/DumpC.java over the DLL, 2026-09-20), read by hand next to the UI's calls (L2)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0656 is named CMSG_CLAIM_PENDING_SKILL_CARD"

replay: null
