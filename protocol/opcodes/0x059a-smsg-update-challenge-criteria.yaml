# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x059a
name: SMSG_UPDATE_CHALLENGE_CRITERIA
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  A challenge criteria entry: eight u32 and a done byte, the client reading exactly that (the skeleton's two u64 were pairs of u32). Names from the fork's AppendCriteriaEntry/BuildCriteriaEntry, which it derived from this handler.

# AzerothCore calls 0x059a SMSG_COA_CHALLENGE_CRITERIA_UPDATED. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout:
  - name: reserved
    type: u32
    notes: the fork sends 0 (slot a)
    evidence: L3
  - name: challengeId
    type: u32
    notes: the event's first argument
    evidence: L3
  - name: level
    type: u32
    notes: the event's second argument
    evidence: L3
  - name: requirementType
    type: u32
    notes: 'index into the client''s CHALLENGE_REQUIREMENT_TYPE_* table (the fork''s note: the handler indexes it with the u32 at wire +0xc); the event''s %s'
    evidence: L3
  - name: value1
    type: u32
    notes: 'slots e, f, g, h: the fork sends target, gate level, progress and 0; the event carries three of them as %f. The requirement row these come from calls them MiscValue1/2/3 -- the name the client''s own requirement builder at 0x1013c1a1 uses for record +0x14/+0x18/+0x1c (lua-keys.yaml, L3) and the name the UI reads off a requirement (ChallengeItemMixin.lua:490, L2). The event''s own argument names stay value1..3 (Ascension_TrackerHeader.lua, L2).'
    evidence: L3
  - name: value2
    type: u32
    evidence: L3
  - name: value3
    type: u32
    evidence: L3
  - name: unknown_7
    type: u32
    evidence: L3
  - name: done
    type: u8
    notes: the event fired is CHALLENGE_CRITERIA_UPDATED, _COMPLETED or _FAILED
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c) next to mod-coa-challenges' writer (the fork's reconstruction, L1 for names).

# Read from the decompiled handler (build/ghidra/handlers-c, DumpC.java).

# Widths and order, read out of the handler at 0x00137030 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00137030"
    registration_site_rva: "0x0013b7ed"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "%u%u%s%f%f%f"
  - "CHALLENGE_REQUIREMENT_TYPE_NONE"
  - "CHALLENGE_CRITERIA_UPDATED"
  - "CHALLENGE_CRITERIA_FAILED"
  - "CHALLENGE_CRITERIA_COMPLETED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: CHALLENGE_CRITERIA_UPDATED
    args: []
    source: "Interface/AddOns/Ascension_ChallengesUI/ChallengesUI.lua:151"
  - event: CHALLENGE_CRITERIA_FAILED
    args: [challengeID, level, requirementType, value1, value2, value3]
    source: "Interface/FrameXML/Ascension_TrackerHeader/Ascension_TrackerHeader.lua:91"
  - event: CHALLENGE_CRITERIA_COMPLETED
    args: [challengeID, level, requirementType, value1, value2, value3]
    source: "Interface/FrameXML/Ascension_TrackerHeader/Ascension_TrackerHeader.lua:78"

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-coa-challenges/src/CoA.Challenges.History.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Lifecycle.cpp
    - modules/mod-coa-challenges/src/CoAChallengeParse.h
    - modules/mod-coa-challenges/src/CoAChallengeTests.cpp

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: ChallengeRequirements.dbc
    instance: "0x10be02e4"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 36
    fields: 9

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c) next to mod-coa-challenges' writer (the fork's reconstruction, L1 for names)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c, DumpC.java)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x059a is named SMSG_UPDATE_CHALLENGE_CRITERIA"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
