# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x096b
name: SMSG_PATCH_ITEM_DISPLAY_INFO
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of ItemDisplayInfo.dbc (bound_by: L1 ref, patch-table-names.md): the handler's 100-byte bulk read exactly equals the table's 25-column/100-byte record (client DBC header), followed by 14 separate trailing strings (recounted directly from build/ghidra/handlers-c/0x096b.c: FUN_100d3680 is called 14 times in unconditional code, not 13 as an earlier pass of this fiche had it). AzerothCore's ItemDisplayInfoEntry struct stores only ID and inventoryIcon (a string-offset field within the 100-byte block, per client/patch-tables.md's on-disk-offset-vs-wire-string distinction) -- named here; the other 23 dword positions are dead-commented only ('m_modelName[2]', 'm_modelTexture[2]', 'm_geosetGroup[3]', etc.) and stay unnamed, and which of the 14 trailing strings (if any) supplies inventoryIcon's actual text is not established.

layout:
  - name: id
    type: u32
    notes: 'DBC column 0 (index field, fmt ''n''); core: ItemDisplayInfoEntry::ID. Width L3 (handler-fire.yaml: reads [[null,100], then 14 lpstrings], one 100-byte bulk read == the full 25-column/100-byte ItemDisplayInfo.dbc record (client DBC header), followed by 14 separate strings); name L1 (core DBCStructure.h, another project''s reading of the same file).'
    evidence: L1
  - name: field_1
    type: u32
    notes: 'DBC column 1. AzerothCore''s ItemDisplayInfoEntry struct only ever names ID and inventoryIcon; every other column is a dead comment, not a stored member, so per the ONE RULE this stays unnamed. Dead comment: ''m_modelName[2]'' (this is the first of that 2-element run). Width L3 only.'
    evidence: L3
  - name: field_2
    type: u32
    notes: DBC column 2, modelName[2] continued (dead comment). Width L3 only, no naming source.
    evidence: L3
  - name: field_3
    type: u32
    notes: 'DBC column 3. Dead comment: ''m_modelTexture[2]'', first element. Width L3 only, no naming source.'
    evidence: L3
  - name: field_4
    type: u32
    notes: DBC column 4, modelTexture[2] continued (dead comment). Width L3 only, no naming source.
    evidence: L3
  - name: inventoryIcon
    type: u32
    notes: 'DBC column 5; core: ItemDisplayInfoEntry::inventoryIcon (''char const* inventoryIcon'', m_inventoryIcon), the one string AzerothCore actually stores. On disk (and in this 100-byte block) it is a string-offset field, per client/patch-tables.md''s own distinction between ''a string offset on disk and a string on the wire'' -- the record''s dword here is not itself readable text; the actual characters arrive as one of the 14 trailing strings, and which of the 14 is not established (see field_25''s note). Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
  - name: field_6
    type: u32
    notes: DBC column 6, part of a 3-element geosetGroup dead comment ('m_geosetGroup[3]'). Width L3 only, no naming source.
    evidence: L3
  - name: field_7
    type: u32
    notes: DBC column 7, geosetGroup[3] continued (dead comment). Width L3 only, no naming source.
    evidence: L3
  - name: field_8
    type: u32
    notes: DBC column 8, geosetGroup[3] continued (dead comment). Width L3 only, no naming source.
    evidence: L3
  - name: field_9
    type: u32
    notes: 'DBC column 9. Dead comment: ''m_flags''. Width L3 only, no naming source.'
    evidence: L3
  - name: field_10
    type: u32
    notes: 'DBC column 10. Dead comment: ''m_spellVisualID''. Width L3 only, no naming source.'
    evidence: L3
  - name: field_11
    type: u32
    notes: 'DBC column 11. Dead comment: ''m_groupSoundIndex''. Width L3 only, no naming source.'
    evidence: L3
  - name: field_12
    type: u32
    notes: DBC column 12, part of a 2-element helmetGeosetVis dead comment ('m_helmetGeosetVis[2]'). Width L3 only, no naming source.
    evidence: L3
  - name: field_13
    type: u32
    notes: DBC column 13, helmetGeosetVis[2] continued (dead comment). Width L3 only, no naming source.
    evidence: L3
  - name: field_14
    type: u32
    notes: DBC column 14, part of a 2-element texture dead comment ('m_texture[2]'). Width L3 only, no naming source.
    evidence: L3
  - name: field_15
    type: u32
    notes: DBC column 15, texture[2] continued (dead comment). Width L3 only, no naming source.
    evidence: L3
  - name: field_16
    type: u32
    notes: DBC column 16, part of an 8-element itemVisual dead comment ('m_itemVisual[8]'). Width L3 only, no naming source.
    evidence: L3
  - name: field_17
    type: u32
    notes: DBC column 17, itemVisual[8] continued. Width L3 only, no naming source.
    evidence: L3
  - name: field_18
    type: u32
    notes: DBC column 18, itemVisual[8] continued. Width L3 only, no naming source.
    evidence: L3
  - name: field_19
    type: u32
    notes: DBC column 19, itemVisual[8] continued. Width L3 only, no naming source.
    evidence: L3
  - name: field_20
    type: u32
    notes: DBC column 20, itemVisual[8] continued. Width L3 only, no naming source.
    evidence: L3
  - name: field_21
    type: u32
    notes: DBC column 21, itemVisual[8] continued. Width L3 only, no naming source.
    evidence: L3
  - name: field_22
    type: u32
    notes: DBC column 22, itemVisual[8] continued. Width L3 only, no naming source.
    evidence: L3
  - name: field_23
    type: u32
    notes: DBC column 23, itemVisual[8] continued (last element). Width L3 only, no naming source.
    evidence: L3
  - name: field_24
    type: u32
    notes: 'DBC column 24. Dead comment: ''m_particleColorID''. Width L3 only, no naming source.'
    evidence: L3
  - name: field_25
    type: lpstring
    notes: 1st of 14 trailing strings. AzerothCore names only one string column in this table (inventoryIcon, DBC column 5 -- see that field's note), but says nothing about the other real Blizzard string columns (modelName, modelTexture, texture, and possibly itemVisual, all only dead-commented, untyped in those comments) that the client evidently also sends as text here, given 14 strings for 1 core-named string field. Which of these 14, if any, is inventoryIcon's actual text is not established. Width L3 only, no naming source.
    evidence: L3
  - name: field_26
    type: lpstring
    notes: 2nd of 14 trailing strings. Same caveat as field_25 -- unidentified. Width L3 only.
    evidence: L3
  - name: field_27
    type: lpstring
    notes: 3rd of 14 trailing strings. Same caveat as field_25. Width L3 only.
    evidence: L3
  - name: field_28
    type: lpstring
    notes: 4th of 14 trailing strings. Same caveat as field_25. Width L3 only.
    evidence: L3
  - name: field_29
    type: lpstring
    notes: 5th of 14 trailing strings. Same caveat as field_25. Width L3 only.
    evidence: L3
  - name: field_30
    type: lpstring
    notes: 6th of 14 trailing strings. Same caveat as field_25. Width L3 only.
    evidence: L3
  - name: field_31
    type: lpstring
    notes: 7th of 14 trailing strings. Same caveat as field_25. Width L3 only.
    evidence: L3
  - name: field_32
    type: lpstring
    notes: 8th of 14 trailing strings. Same caveat as field_25. Width L3 only.
    evidence: L3
  - name: field_33
    type: lpstring
    notes: 9th of 14 trailing strings. Same caveat as field_25. Width L3 only.
    evidence: L3
  - name: field_34
    type: lpstring
    notes: 10th of 14 trailing strings. Same caveat as field_25. Width L3 only.
    evidence: L3
  - name: field_35
    type: lpstring
    notes: 11th of 14 trailing strings. Same caveat as field_25. Width L3 only.
    evidence: L3
  - name: field_36
    type: lpstring
    notes: 12th of 14 trailing strings. Same caveat as field_25. Width L3 only.
    evidence: L3
  - name: field_37
    type: lpstring
    notes: 13th of 14 trailing strings. Same caveat as field_25. Width L3 only.
    evidence: L3
  - name: field_38
    type: lpstring
    notes: '14th of 14 trailing strings. SKEPTIC CORRECTION (core-2 re-review): the reviewed layout stopped at 13 trailing lpstrings (field_25..field_37), but build/ghidra/handlers-c/0x096b.c calls FUN_100d3680 FOURTEEN times in unconditional straight-line code right after the 25-dword bulk read -- apppcStack_158, _140, _128, _110, _f8, _e0, _c8, _b0, _98, _80, _68, _50, _38, _20, 14 distinct destination buffers, confirmed by the handler''s own per-call counter (uStack_15c, incremented 0..13) and by the 14 pcStack_* destination pointers it fills -- not 13. This 14th string was omitted from the layout entirely by the reviewed pass. Same caveat as field_25 -- unidentified, no naming source. Width L3 only.'
    evidence: L3
# Read from build/ghidra/handlers-c/0x096b.c, re-read by the core-2 skeptic pass (counted FUN_100d3680 calls directly). Corrects every '13 trailing strings' reference in this fiche to 14 (the true FUN_100d3680 call count) and adds field_38.

# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h ItemDisplayInfoEntry + DBCfmt.h ItemDisplayTemplateEntryfmt (L1: another project's reading of the same client DBC file, not ours).

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# packet_layout.py had followed a call into 0x000d3680, not the handler. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x0022cf90"
    registration_site_rva: "0x00229108"
    evidence: L3

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x096b.c, re-read by the core-2 skeptic pass (counted FUN_100d3680 calls directly)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h ItemDisplayInfoEntry + DBCfmt.h ItemDisplayTemplateEntryfmt (L1: another project's reading of the same client DBC file, not ours)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "client/patch-table-names.md (the naming-sources sweep): 0x096b bound_by 'L1 ref' to ItemDisplayInfo.dbc, 25 fields / 100 bytes, matching the client DBC header"
    claim: "this opcode's table is ItemDisplayInfo.dbc, at L1 (only the third-party reference names it; patch-tables.yaml does not bind this opcode)"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x096b is named SMSG_PATCH_ITEM_DISPLAY_INFO"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
