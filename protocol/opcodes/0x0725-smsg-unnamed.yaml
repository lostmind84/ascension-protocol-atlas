# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0725
name: SMSG_UNNAMED_0725   # PLACEHOLDER: the client's table answers 'unknown'; a handler exists
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Unnamed in the client's own opcode table, but the client registers a
  handler for 0x0725 -- there is code behind it. Direction inferred from
  that registration. Layout and behaviour not established.

layout:
  - name: slot
    type: u32
    evidence: L5
    notes: 'Zero-based. The handler fires ASCENSION_CA_SPECIALIZATION_ACTIVE_ID_CHANGED with slot + 1: forged 7 -> event 8, forged 2 -> event 3 (2026-09-19). #4027 sends 0 -- ''slot 0 of 1''. What the shim-overridden GetActiveChrSpec answers was not read. The value goes to the setter FUN_101499e0, which is also where SMSG 0x06E2''s third u32 lands for an inspected character; the capture reference calls this field active_slot and reports a live value of 0, which agrees.

      '
  - name: slotCount
    type: u32
    evidence: L3
    notes: '#4027 sends 1; forged 9 with no observable effect on the event. The value goes to the twin setter FUN_101499f0, and SMSG 0x06E2''s fourth u32 -- the bound of its per-slot loop -- is the same quantity for an inspected character, which is the clearest statement of what it counts: how many specialization slots exist. The capture reference calls it slot_count and reports a live value of 1.

      '
# Read from the capture reference (CoinThrow, 2026-09-20, kept outside the tree -- see docs/crosscheck-cointhrow-2026-09-20.md). Only the notes changed; slot keeps its L5 and slotCount its L3.

# name from mod-ascension-compat's serializer (L1); width and order from the decompiled handler (L3)
# decompiled: FUN_10171d90 reads exactly two u32 and passes them on

client:
  handler:
    rva: "0x00171d90"
    registration_site_rva: "0x0017222e"
    evidence: L3

used_by_server:   # the CoA server module sends or handles this
  server_name: SMSG_CHARACTER_ADVANCEMENT_ACTIVE_SPEC
  files:
    - modules/mod-ascension-compat/src/AscensionCompat.cpp

client_strings:   # literals the handler references, from its decompilation
  - "ASCENSION_CA_SPECIALIZATION_ACTIVE_ID_CHANGED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: ASCENSION_CA_SPECIALIZATION_ACTIVE_ID_CHANGED
    args: [specID]
    source: "Interface/AddOns/AscensionUI/CharacterAdvancement/CASpecListMixin.lua:41"
  - event: ASCENSION_CA_SPECIALIZATION_ACTIVE_ID_CHANGED
    args: []
    source: "Interface/AddOns/Ascension_CoATalents/CoATalentFrame.lua:246"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: ASCENSION_CA_SPECIALIZATION_ACTI
    format: "%u"
    helper: FUN_100d0a50

provenance:
  - level: L1
    source: "the capture reference (CoinThrow, 2026-09-20, kept outside the tree -- see docs/crosscheck-cointhrow-2026-09-20.md)"
    claim: "the two u32 are active_slot and slot_count; the packet must arrive before 0x0726, whose handler is said to store nothing until this container exists"
    note: >
      The NAMES agree with ours and with what the lab proved, so they add confidence and
      no level. The ORDERING claim we could not reproduce and it is not what our
      handlers show. 0x0726's handler (RVA 0x00171260) asks FUN_10149470 for the active
      slot index, calls FUN_10149480 for its container and, when that returns null,
      CREATES it with FUN_10149380 and asks again; only if the second fetch also fails
      does it abandon the packet. So on this build 0x0726 does not need a prior 0x0725
      to have a container -- it makes one. What it cannot make up is the active slot
      INDEX FUN_10149470 returns, and what that index is before any 0x0725 has arrived
      is not established here. The lab never tested the two packets in isolation: the
      character had logged in normally, so a 0x0725 had already been sent. Their claim
      may still be true of a real server's behaviour; it is not visible as a hard gate
      in the code. Re-testing it is one forge on a fresh session.
  - level: L5
    source: "tools/replay/ca_vertical.py --check spec, slot 1, 2026-09-19"
    claim: "two u32; the first is a zero-based slot and the event carries it plus one"
    note: >
      Forged with .debug send opcode (1829 uint32 7 uint32 9); the event's arguments were
      recorded with CoaProbe evwatch because the CoA compat shim overrides GetActiveChrSpec.
  - level: L3
    source: "tools/ghidra/ghidra.sh run Decomp.java <handler VA> (see client/server-layouts.md)"
    claim: "field widths and order, from the decompiled handler"
  - level: L1
    source: "mod-ascension-compat's serializer, client/server-layouts.yaml"
    claim: "field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "the client's table has no name for 0x0725"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

open_questions:
  - "Is a 0x0725 really required before a 0x0726 takes effect? The capture reference says
     so; 0x0726's handler creates the slot container itself when it is missing, so the
     only thing 0x0725 can be supplying is the active slot index. Forge a 0x0726 on a
     session that has had no 0x0725 and see whether IsKnownID moves."

replay:
  script: tools/replay/ca_vertical.py
  asserts:
    - "ASCENSION_CA_SPECIALIZATION_ACTIVE_ID_CHANGED fires with slot + 1 after a 0x0725 (u32 slot, u32 slotCount)"
  asserts_measured:
    - "u32 7, u32 9 -> event args [8]"
  last_run: { date: 2026-09-20, result: pass, build: coa-extensions-rev4-2026-09-14,
              dataset: ascension-live-2026-09-17, slot: 1 }
