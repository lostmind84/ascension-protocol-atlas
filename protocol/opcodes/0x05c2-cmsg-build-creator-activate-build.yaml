# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05c2
name: CMSG_BUILD_CREATOR_ACTIVATE_BUILD
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Activate a build-creator build: the build id as a string and two booleans. The skeleton missed the string.

layout:
  - name: buildId
    type: cstring
    notes: 'ActivateBuild(BuildCreatorUtil.GetPendingBuildID(), true, true): the id travels as a NUL-terminated string (FUN_100b97a0)'
    evidence: L3
  - name: flag_1
    type: u8
    notes: the second Lua argument
    evidence: L3
  - name: flag_2
    type: u8
    notes: the third
    evidence: L3
# Read from the decompiled sender binding (tools/ghidra/scripts/DumpC.java over the DLL, 2026-09-20), read by hand next to the UI's calls (L2).

# Widths and order, read out of the client's sender at 0x001018a0 by tools/extract/client_sender.py.
# A helper is called between the opcode write and the send: this is at best a
# PREFIX of the payload. An empty list here does not mean an empty packet.

used_by:   # UI source naming this opcode's event
  - Interface/FrameXML/StaticPopup.lua

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: ActivateBuild
  function_rva: "0x00101740"
  calls:
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 474, args: ["unpack(args)"]}
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 488, args: ["unpack(args)"]}
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 502, args: ["unpack(args)"]}
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 574, args: ["BuildCreatorUtil.GetPendingBuildID()", "true", "true"]}
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 588, args: ["BuildCreatorUtil.GetPendingBuildID()", "true", "true"]}
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 1001, args: ["unpack(args)"]}

provenance:
  - level: L3
    source: "the decompiled sender binding (tools/ghidra/scripts/DumpC.java over the DLL, 2026-09-20), read by hand next to the UI's calls (L2)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05c2 is named CMSG_BUILD_CREATOR_ACTIVATE_BUILD"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
