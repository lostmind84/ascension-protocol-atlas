# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x053b
name: CMSG_ASCENSIONGM_TICKET_LIST_REQUEST
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Request the GM ticket list: no payload, the opcode alone. Sent through the shared helper
  FUN_100e08b0(opcode), which constructs a CDataStore, writes only the opcode and sends --
  the same helper client/client-senders.md names for CMSG_CA_UNLEARN_SPELL_ALL and
  CMSG_TAXI_REQUEST_EARLY_LANDING. No Lua binding for the sender was found (see the
  retraction in provenance: an earlier pass named the wrong one), so what UI action
  triggers the send is not established.

layout: []  # empty
# Widths and order, read out of the client's sender at 0x00a4f080 by tools/extract/client_sender.py.
# No PutData between the opcode write and the send: the packet is empty. Confirmed by
# decompiling the sender directly (DecompAt.java 10a4f080, 2026-09-20, GHIDRA_WORKSPACE=social-a):
#   FUN_100e08b0(0x53b); FUN_100e6190(); func_0x100e6210(); FUN_101928b0(); func_0x101929d0();
#   FUN_10114160(param_1,param_2,param_3,param_4);
# One call into the opcode-write/finalize/send helper (FUN_100e08b0) and no PutData anywhere
# in the function.

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-ascension-compat/src/AscensionCompat.cpp
  notes: >
    the fork only carries this opcode in a name table for logging unrecognised extension
    packets (ExtensionOpcodeName/EXTENSION_OPCODES); it does not parse or act on the packet.

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "the decompiled sender (DecompAt.java 10a4f080, 2026-09-20, GHIDRA_WORKSPACE=social-a): opcode write through FUN_100e08b0, then Finalize and Send with no PutData between them"
    claim: "the layout: an empty payload"
  - level: L3
    source: "client/client-senders.md (\"Two helpers wrap the same calls and are modelled\": FUN_100e08b0(opcode) is named for this exact opcode among CMSG_CA_UNLEARN_SPELL_ALL and CMSG_TAXI_REQUEST_EARLY_LANDING)"
    claim: "the sender goes through the opcode-write/finalize/send helper with no payload"
  - level: L0
    source: "client_binding as generated by tools/extract/sender_bindings.py (before 2026-09-20)"
    claim: "RETRACTED. Named OpenAscensionWikiURL (function_rva 0x00a4ee00) as the Lua binding that sends this packet, with its two ChatFrame.lua wiki-command calls as the UI evidence. The extractor's own fallback rule -- when no exact function boundary is known for the sender, take the nearest registered Lua binding below the sender site, provided no int3 padding lies between them (tools/extract/sender_bindings.py) -- landed on an unrelated function 0x280 bytes earlier. Decompiling both addresses (DecompAt.java 10a4f080 10a4ee00, 2026-09-20) shows they are two separate, self-terminated functions: 0x00a4ee00 only builds the string \"https://project-ascension.fandom...\" and calls a function pointer to open it (matching the wiki slash-command calls at ChatFrame.lua:1623/1626) and never touches a CDataStore or writes 0x53b; 0x00a4f080 is the actual sender, and Callers.java found zero static callers of it, so the real trigger is unknown. Kept here, not deleted, per docs/EVIDENCE.md's retraction rule."
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x053b is named CMSG_ASCENSIONGM_TICKET_LIST_REQUEST"

replay: null
