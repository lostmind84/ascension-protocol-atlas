# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0578
name: SMSG_UPDATE_OBJECT_ADDON
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  An addon-side object update: a u64 guid as two u32, then three u32 filed against that guid. The guid's high word selects the updater.

layout:
  - name: guidLow
    type: u32
    evidence: L3
  - name: guidHigh
    type: u32
    notes: 'dispatch on the high word: below 0x10000000 (a player) and 0xF100xxxx go to two object updaters (FUN_102cdcf0, FUN_102cdaf0); 0x1F50xxxx and other values return without reading'
    evidence: L3
  - name: unknown_2
    type: u32
    evidence: L3
  - name: unknown_3
    type: u32
    evidence: L3
  - name: unknown_4
    type: u32
    notes: the updater (FUN_102ce0c0) reads a u32, then two more, and files them in a hash map keyed by the guid; the fields' meaning was not read
    evidence: L3
# Read from the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20).

# Read from the decompiled handler (build/ghidra/handlers-c), read by hand.

# Widths and order, read out of the handler at 0x002cdfb0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x002cdfb0"
    registration_site_rva: "0x002cdf84"
    evidence: L3

provenance:
  - level: L3
    source: "the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0578 is named SMSG_UPDATE_OBJECT_ADDON"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
