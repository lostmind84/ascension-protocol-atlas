# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05ba
name: SMSG_LIST_BROKEN_CHALLENGE_RULES
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  The rules currently broken in the player's challenge, sent whole: a count then one length-prefixed rule-type token per entry; the handler rebuilds its list.

layout:
  - name: count
    type: u32
    evidence: L3
  - repeat: count
    fields:
    - name: rule
      type: lpstring
      notes: 'u32 length then that many bytes, no NUL. A CHALLENGE_RULES_TYPE_* token, the same vocabulary SMSG_PATCH_CHALLENGE_RULE 0x05B8 carries in its string column and C_Challenge.GetRuleLocalization / HasBrokenChallengeRule resolve (ChallengeUtil.lua and Ascension_ChallengesUI, L2). The capture reference reports a live login packet of 2,107 bytes carrying count = 47 and lists those 47 tokens; that is L1 for us and is not reproduced here. Our own evidence gives the shape, not the vocabulary''s size.

        '
      evidence: L3
# Read from the decompiled handler FUN_10138170 (build/ghidra/handlers-c/0x05ba.c), its fire format and the UI's event arguments, 2026-09-20. The capture reference (~/CoaServer/reference/cointhrow, L1 for us) reports this packet observed live at 2,107 bytes with count = 47, and enumerates the 47 CHALLENGE_RULES_TYPE_* tokens. The layout it implies is the one already recorded here from the binary (L3); the count and the token list are its capture and stay L1 -- we have no capture and did not re-derive the enumeration from this build.

# Read from the decompiled handler (build/ghidra/handlers-c, DumpC.java), its fire format and the UI's event arguments.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00138170"
    registration_site_rva: "0x0013b845"
    evidence: L3

provenance:
  - level: L3
    source: "the decompiled handler FUN_10138170 (build/ghidra/handlers-c/0x05ba.c), its fire format and the UI's event arguments, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c, DumpC.java), its fire format and the UI's event arguments"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05ba is named SMSG_LIST_BROKEN_CHALLENGE_RULES"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
