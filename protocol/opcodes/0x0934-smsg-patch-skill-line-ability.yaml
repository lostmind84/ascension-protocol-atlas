# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0934
name: SMSG_PATCH_SKILL_LINE_ABILITY
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of the stock SkillLineAbility.dbc (AzerothCore already loads this table; ADR 0005), sent as the single 56-byte blob the handler at RVA 0x00230100 reads (L3). Nothing was elided (14 cols x 4 bytes = 56 bytes matches the client header exactly), so the blob is decomposed here one dword per column in file order. 10 of 14 columns are active SkillLineAbilityEntry members and named from DBCStructure.h; ExcludeRace, ExcludeClass and the 2-wide CharacterPoints are core 'x' columns with no name (L1 throughout: only the total width was independently decompiled).

layout:
  - name: ID
    type: u32
    notes: active SkillLineAbilityEntry member
    evidence: L1
  - name: SkillLine
    type: u32
    notes: active SkillLineAbilityEntry member
    evidence: L1
  - name: Spell
    type: u32
    notes: active SkillLineAbilityEntry member
    evidence: L1
  - name: RaceMask
    type: u32
    notes: active SkillLineAbilityEntry member
    evidence: L1
  - name: ClassMask
    type: u32
    notes: active SkillLineAbilityEntry member
    evidence: L1
  - name: field_5
    type: u32
    notes: 'core format char ''x''/''X'' at this column: skipped, no name in DBCStructure.h'
    evidence: L1
  - name: field_6
    type: u32
    notes: 'core format char ''x''/''X'' at this column: skipped, no name in DBCStructure.h'
    evidence: L1
  - name: MinSkillLineRank
    type: u32
    notes: active SkillLineAbilityEntry member
    evidence: L1
  - name: SupercededBySpell
    type: u32
    notes: active SkillLineAbilityEntry member
    evidence: L1
  - name: AcquireMethod
    type: u32
    notes: active SkillLineAbilityEntry member
    evidence: L1
  - name: TrivialSkillLineRankHigh
    type: u32
    notes: active SkillLineAbilityEntry member
    evidence: L1
  - name: TrivialSkillLineRankLow
    type: u32
    notes: active SkillLineAbilityEntry member
    evidence: L1
  - name: field_12
    type: u32
    notes: 'core format char ''x''/''X'' at this column: skipped, no name in DBCStructure.h'
    evidence: L1
  - name: field_13
    type: u32
    notes: 'core format char ''x''/''X'' at this column: skipped, no name in DBCStructure.h'
    evidence: L1
# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h (SkillLineAbilityEntry) and DBCfmt.h (SkillLineAbilityfmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md.

client:
  handler:
    rva: "0x00230100"
    registration_site_rva: "0x00228dc9"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h (SkillLineAbilityEntry) and DBCfmt.h (SkillLineAbilityfmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0934 is named SMSG_PATCH_SKILL_LINE_ABILITY"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
