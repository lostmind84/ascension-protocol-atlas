# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x090f
name: SMSG_POI_ADD
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Add a POI: two u32, two strings, three u32 and two floats, matching the %s%s%u%u%u%f%f the handler fires. The two floats are the position the record keeps.

layout:
  - name: unknown_0
    type: u32
    evidence: L3
  - name: unknown_1
    type: u32
    evidence: L3
  - name: name
    type: cstring
    notes: the first of the two %s the handler fires
    evidence: L3
  - name: description
    type: cstring
    notes: the second %s
    evidence: L3
  - name: unknown_4
    type: u32
    evidence: L3
  - name: unknown_5
    type: u32
    evidence: L3
  - name: unknown_6
    type: u32
    evidence: L3
  - name: x
    type: f32
    notes: the %f%f of the fired event; stored at record +0x50 and +0x54
    evidence: L3
  - name: y
    type: f32
    evidence: L3
# Read from the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20).

# Read from the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest of the body is noted as not followed.

# Widths and order, read out of the handler at 0x002cf1f0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x002cf1f0"
    registration_site_rva: "0x002cf070"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "%s%s%u%u%u%f%f"
  - "ASCENSION_POI_ADD"

provenance:
  - level: L3
    source: "the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest of the body is noted as not followed"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x090f is named SMSG_POI_ADD"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
