# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0911
name: SMSG_POI_CLEAR
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  POI clear: no payload. The handler takes no packet parameter at all (FUN_102cf6a0(void)); it clears the POI map and formats an event.

layout: []  # empty
# Read from the decompiled handler (build/ghidra/handlers-c/0x0911.c): the function has no CDataStore argument.

# Read from the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20).

# Read from the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest is noted as not followed.

client:
  handler:
    rva: "0x002cf6a0"
    registration_site_rva: "0x002cf08e"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "ASCENSION_POI_CLEAR"

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x0911.c): the function has no CDataStore argument"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md, '090F/0910/0911 | POI add / remove / clear (0x102CF1F0/0x102CF7E0/0x102CF6A0; ASCENSION_POI_*)' (third-party, not in this repo)"
    claim: "independently names the same handler address (0x102CF6A0 = this fiche's 0x002cf6a0 + image base) for the 'clear' member of the add/remove/clear trio and gives it no payload -- corroborates the empty layout"
  - level: L3
    source: "the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest is noted as not followed"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0911 is named SMSG_POI_CLEAR"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
