# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05be
name: SMSG_PATCH_CHALLENGE_CONDITION_TYPE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of ChallengeConditionTypes.dbc in the compact wire form: the row id, eight skipped string-offset dwords, then eight length-prefixed strings written over them. Structurally identical to SMSG_PATCH_CHALLENGE_REQUIREMENT_TYPE 0x05BC. The runtime record is 0x24 bytes while the client's ChallengeConditionTypes.dbc has 73 fields, so the wire row is a projection of the file.

layout:
  - name: id
    type: u32
    notes: dword 0, the only dword the reader loads; the row key -- range-checked against minId 0x10bdfd54 / maxId 0x10bdfd50, indexes the row table at 0x10bdfd64, then passed to FUN_10133ac0
    evidence: L3
  - name: stringOffsets
    type: bytes_32
    notes: 'dwords 1..8, SKIPPED: the cursor jumps from +0x04 to the end of the 0x24 block. The row''s eight string-offset columns; the client writes the eight string pointers that follow over record +0x04 .. +0x20.'
    evidence: L3
  - name: string_1
    type: lpstring
    notes: u32 length then that many bytes, no NUL; copied and stored at record +0x04
    evidence: L3
  - name: string_2
    type: lpstring
    notes: record +0x08
    evidence: L3
  - name: string_3
    type: lpstring
    notes: record +0x0c
    evidence: L3
  - name: string_4
    type: lpstring
    notes: record +0x10
    evidence: L3
  - name: string_5
    type: lpstring
    notes: record +0x14
    evidence: L3
  - name: string_6
    type: lpstring
    notes: record +0x18
    evidence: L3
  - name: string_7
    type: lpstring
    notes: record +0x1c
    evidence: L3
  - name: string_8
    type: lpstring
    notes: record +0x20. The UI resolves a condition type through C_Challenge.GetConditionLocalization; which string feeds which return value is NOT established here.
    evidence: L3
# Read from the decompiled handler FUN_101387c0 (build/ghidra/handlers-c/0x05be.c) read by hand, 2026-09-20.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# packet_layout.py had followed a call into 0x000d3680, not the handler. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x001387c0"
    registration_site_rva: "0x0013b751"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: ChallengeConditionTypes.dbc
    instance: "0x10bdfd44"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 292
    fields: 73

provenance:
  - level: L3
    source: "the decompiled handler FUN_101387c0 (build/ghidra/handlers-c/0x05be.c) read by hand, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05be is named SMSG_PATCH_CHALLENGE_CONDITION_TYPE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
