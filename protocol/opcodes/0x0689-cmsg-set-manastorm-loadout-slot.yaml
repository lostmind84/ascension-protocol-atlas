# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0689
name: CMSG_SET_MANASTORM_LOADOUT_SLOT
direction: client_to_server
status: proven
evidence: L5
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Round trip proven on 2026-09-19 through the client's Lua packet API (the bytes came from
  CreatePacket, not from the Manastorm UI; the client's own sender stays L3).

layout:
  - name: depth
    type: u32
    evidence: L5
  - name: spellId
    type: u32
    evidence: L5
# width and order from the client's sender (CDataStore PutData sizes, decompiled; L3); names from the server's reader in mod-ascension-compat (L1)
# client sender: FUN_102a6ac0
# server reader: AscensionManastorm.cpp:284-287: size must be 8; depth = read(0), spell = read(4)
used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-ascension-compat/src/AscensionManastormProtocol.h

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: SetLoadoutSpellAtIndex
  function_rva: "0x002a6ac0"
  calls:
    - {file: "Interface/AddOns/Ascension_Manastorm/ManastormLoadoutFlyout.lua", line: 30, args: ["self.button:GetID()", "button.spell.spellID"]}

provenance:
  - level: L5
    source: "tools/replay/manastorm_roundtrip.py --pair all --control, run on slot 1 on 2026-09-19 (client/lua-packet-api.md)"
    claim: "the server accepts (u32, u32) and answers 0x068A with the first u32 echoed; a 4-byte payload is ignored"
  - level: L3
    source: "tools/ghidra/ghidra.sh run Decomp.java <sender VA> + objdump of the push/call sequence (see client/client-senders.md)"
    claim: "what the client writes, field widths and order"
  - level: L1
    source: "the server's reader (see client/client-senders.md)"
    claim: "field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0689 is named CMSG_SET_MANASTORM_LOADOUT_SLOT"

replay:
  script: tools/replay/manastorm_roundtrip.py
  asserts:
    - "the server accepts an 8-byte payload (u32, u32) and answers with 0x068A echoing the first u32"
    - "the server ignores a 4-byte payload (control)"
  asserts_measured:
    - "pksend 0x0689 u32:99 u32:0 -> 0x068A slot=99 SET_MANASTORM_LOADOUT_OUT_OF_RANGE within 2 s"
    - "pksend 0x0689 u32:99 -> no 0x068A within 3 s"
  last_run: { date: 2026-09-20, result: pass, build: coa-extensions-rev4-2026-09-14,
              dataset: ascension-live-2026-09-17, slot: 1 }
