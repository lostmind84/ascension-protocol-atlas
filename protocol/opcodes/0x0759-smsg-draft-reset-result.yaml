# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0759
name: SMSG_DRAFT_RESET_RESULT
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Answer to a draft-mode request: the handler fires DRAFT_RESULT with one string it reads through a callee. Only the string is established; whether the packet carries more is not (the earlier width reading belonged to a callee shared with 0x0753).

layout:
  - name: result
    type: cstring
    notes: read in a callee; the only argument of the fire
    evidence: L3
# Read from the decompiled handler's fire (DRAFT_RESULT with format %s, handler-fire.yaml); the string is read in a callee, and the asm idiom's earlier 14-field reading came from following that callee into the draft-state parser.

# Widths and order, read out of the handler at 0x00225970 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00225970"
    registration_site_rva: "0x0022754a"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "Draft result {}: {}"
  - "DRAFT_RESULT"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: DRAFT_RESULT
    format: "%s"
    helper: FUN_100b9620

provenance:
  - level: L3
    source: "the decompiled handler's fire (DRAFT_RESULT with format %s, handler-fire.yaml); the string is read in a callee, and the asm idiom's earlier 14-field reading came from following that callee into the draft-state parser"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0759 is named SMSG_DRAFT_RESET_RESULT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
