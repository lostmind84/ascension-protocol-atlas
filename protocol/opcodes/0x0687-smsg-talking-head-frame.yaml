# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0687
name: SMSG_TALKING_HEAD_FRAME
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  A talking-head display: two texts, three sizes, two textures, an animation id and a loop flag. The skeleton missed the four strings.

layout:
  - name: header
    type: cstring
    evidence: L3
  - name: body
    type: cstring
    evidence: L3
  - name: headerSize
    type: u32
    evidence: L3
  - name: bodySize
    type: u32
    evidence: L3
  - name: duration
    type: u32
    evidence: L3
  - name: portrait
    type: cstring
    evidence: L3
  - name: art
    type: cstring
    evidence: L3
  - name: animId
    type: u32
    evidence: L3
  - name: loopAnim
    type: u8
    notes: names from TALKING_HEAD_FRAME_DISPLAY(header, body, headerSize, bodySize, duration, portrait, art, animID, loopAnim), TalkingHeadFrame.lua:188; the handler reads exactly that sequence (%s%s%u%u%u%s%s%u%b)
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock).

# Widths and order, read out of the handler at 0x000d45a0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x000d45a0"
    registration_site_rva: "0x000d4d08"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_UIDevelopmentTools/EventTrace/EventArgNames.lua
  - Interface/FrameXML/TalkingHeadFrame.lua

client_strings:   # literals the handler references, from its decompilation
  - "%s%s%u%u%u%s%s%u%b"
  - "TALKING_HEAD_FRAME_DISPLAY"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: TALKING_HEAD_FRAME_DISPLAY
    args: [header, body, headerSize, bodySize, duration, portrait, art, animID, loopAnim]
    source: "Interface/FrameXML/TalkingHeadFrame.lua:188"
  - event: TALKING_HEAD_FRAME_DISPLAY
    args: [headerText, bodyText, headerFontSize, bodyFontSize, duration, portrait, textureKit, animationID, loopAnimation]
    source: "Interface/AddOns/Ascension_UIDevelopmentTools/EventTrace/EventArgNames.lua:347"

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0687 is named SMSG_TALKING_HEAD_FRAME"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
