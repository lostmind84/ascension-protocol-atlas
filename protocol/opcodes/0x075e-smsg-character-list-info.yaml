# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x075e
name: SMSG_CHARACTER_LIST_INFO
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_CHARACTER_LIST_INFO by the client's own opcode table. Layout and behaviour not
  established.

layout:
  - name: maxActive
    type: u32
    evidence: L3
    notes: "the client offers a new character while active < max"
  - name: total
    type: u32
    evidence: L3
    notes: "server sends 0"
  - name: active
    type: u32
    evidence: L3
    notes: "server sends 0"
  - name: inactive
    type: u32
    evidence: L3
    notes: "server sends 0"
  - repeat: characters (until the packet ends)
    fields:
    - name: guid
      type: u32
      evidence: L3
      notes: "character GUID low part"
    - name: active
      type: u8
      evidence: L3
    - name: online
      type: u8
      evidence: L3
    - name: level
      type: u8
      evidence: L3
    - name: race
      type: u8
      evidence: L3
    - name: class
      type: u8
      evidence: L3
    - name: gender
      type: u8
      evidence: L3
    - name: zone
      type: u32
      evidence: L3
    - name: name
      type: cstring
      evidence: L3
# name from mod-ascension-compat's serializer (L1); width and order from the decompiled handler (L3)
# client reads u32 x5, u8 x6, u32, then a string the extractor cannot see; the server's per-character fields line up once guid is u32 and active is u8

client:
  handler:
    rva: "0x00190d60"
    registration_site_rva: "0x00191192"
    evidence: L3

used_by_server:   # the CoA server module sends or handles this
  server_name: SMSG_ASCENSION_CHARACTER_LIST_INFO
  files:
    - modules/mod-ascension-compat/src/AscensionCharacterSelection.cpp
    - modules/mod-ascension-compat/src/AscensionCharacterSelection.h

client_strings:   # literals the handler references, from its decompilation
  - "CHARACTER_LIST_UPDATED"

provenance:
  - level: L3
    source: "tools/ghidra/ghidra.sh run Decomp.java <handler VA> (see client/server-layouts.md)"
    claim: "field widths and order, from the decompiled handler"
  - level: L1
    source: "mod-ascension-compat's serializer, client/server-layouts.yaml"
    claim: "field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x075e is named SMSG_CHARACTER_LIST_INFO"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
