# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x054f
name: CMSG_TRANSMOG_OUTFIT_APPLY_ID
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named CMSG_TRANSMOG_OUTFIT_APPLY_ID by the client's own opcode table: one of five
  consecutive TRANSMOG_OUTFIT_* CMSGs (0x054d-0x0551 -- MOD_APPEARANCES, MOD_NAME,
  APPLY_ID, REMOVE_ID, CREATE_ID), an old per-outfit CRUD surface; this one applies a
  saved outfit by its numeric id. Nothing in this build constructs a packet with this
  opcode: no `push 0x54f` / `mov r32, 0x54f` occurs anywhere in Extensions.dll's `.text`,
  byte-exhaustively. The live feature applies appearances through the unified
  CMSG_APPLY_APPEARANCES (0x0697, registered and sent in this build -- not ours; `u32
  count` + that many `u32 appearanceId`, the array indexed by appearance category), not
  through an outfit-id lookup. This is not "we have not found the sender yet"; it is
  "this build has no code that could send it."

layout: []   # empty

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x054f is named CMSG_TRANSMOG_OUTFIT_APPLY_ID"
  - level: L3
    source: >
      tools/extract/client_sender.py's push_sites() scan, run directly against
      ~/CoaServer/client/ascension-live/Extensions.dll.ORIGINAL (sha256
      0f8d847b3adc44a963606f0cd4f7938ad6fcd6f4d87feac3131c7153bdf3bb11) for 0x054f alone,
      2026-09-20
    claim: >
      the immediate value 0x054f never occurs as a `push imm32` (0x68) or
      `mov r32, imm32` (0xb8-0xbf) operand anywhere in the .text section
    note: >
      Byte-exhaustive (every offset in .text, not just instruction boundaries). A sender
      that built the opcode from a data table instead of a literal would still be
      missed; see open_questions.
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml and handlers.yaml"
    claim: >
      CMSG_APPLY_APPEARANCES (0x0697) has a registered sender in this build, unlike the
      whole 0x054d-0x0551 CRUD cluster -- confirming the live feature applies
      appearances by array, not by outfit id
    note: "field layout of 0x0697 is not read here; it is not one of this pass's opcodes."
  - level: L1
    source: >
      CoinThrow's capture reference, kept outside this repository at
      ~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md,
      section "Cosmetics / appearance" (the wardrobe/appearance system writeup)
    claim: >
      "0x0540-0x0559 + 0x05CD/0x05CE/0x05D1 + 0x0640-0x0642 (superseded -- no senders
      exist in the client build)"; apply moved to 0x0697 (`CMSG_APPLY_APPEARANCES`: `u32
      count` + `count` x `u32 appearanceId`), and "no push32/mov-imm encodings exist in
      Extensions.dll code for 0x054D-0x0558".
    note: >
      L1 per docs/EVIDENCE.md regardless of the author's own confidence. Re-derived
      independently above at L3 for this specific opcode: our own byte-exhaustive scan
      agrees with their claim.

open_questions:
  - "The immediate-value scan cannot see a sender that loads the opcode from a data
     table rather than a literal 0x54f. Not reproduced here: the capture reference's
     broader byte search of Ascension.exe, which it reports finding only unrelated
     internal table/record math there, not a network-layer path."

related:
  - 0x0697
