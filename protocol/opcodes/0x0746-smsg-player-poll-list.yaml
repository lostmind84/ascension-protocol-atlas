# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0746
name: SMSG_PLAYER_POLL_LIST
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  The player poll list: a count and that many 176-byte poll records. The record's own fields were not decoded.

layout:
  - name: count
    type: u32
    notes: read by the poll manager's deserializer (FUN_102d62e0), which logs 'Received {} polls'
    evidence: L3
  - repeat: count
    fields:
    - name: poll
      type: bytes_176
      notes: one 0xb0-byte poll record, built by FUN_102d6f80 and FUN_102d6de0; its fields were not decoded. The handler then fires PLAYER_POLL_NEW_POLL for the first unanswered poll
      evidence: L3
# Read from the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20).

# Read from the decompiled handler (build/ghidra/handlers-c/0x0746.c), read by hand.

# Widths and order, read out of the handler at 0x002d62e0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x002d7d90"
    registration_site_rva: "0x002d8564"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "PlayerPollMgr::HandlePlayerPollListOpcode: Received {} polls for opcode {}"
  - "PLAYER_POLL_NEW_POLL"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: PLAYER_POLL_NEW_POLL
    format: "%u"
    helper: FUN_100d0a50

provenance:
  - level: L3
    source: "the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x0746.c), read by hand"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0746 is named SMSG_PLAYER_POLL_LIST"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
