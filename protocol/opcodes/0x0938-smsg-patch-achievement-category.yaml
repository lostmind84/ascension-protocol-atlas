# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0938
name: SMSG_PATCH_ACHIEVEMENT_CATEGORY
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of the stock Achievement_Category.dbc (AzerothCore already loads this table; ADR 0005). Reading the actual decompiled handler (build/ghidra/handlers-c/0x0938.c, RVA 0x00229850) by hand: ID and parentCategory (AchievementCategoryEntry's only 2 active members) are the leading dwords, then a length-prefixed blob (memcpy, not the lpstring helper, then a transform of unconfirmed purpose) whose repeat count is provably exactly 1 -- the loop's exit test is a stack-address comparison exactly 24 bytes (one element) past its start, computed here from the declared locals -- then 1 trailing dword. 2 + 17 (one collapsed locale block, the disabled name[16]+flag) + 1 = 20, the file's full column count, with nothing left over. ID and parentCategory are named from DBCStructure.h (L1); the trailing dword is AchievementCategoryEntry's commented-out sortOrder (column 19), identified but kept as an unnamed field_N per the 'x' convention.

layout:
  - name: ID
    type: u32
    notes: active AchievementCategoryEntry member (column 0)
    evidence: L1
  - name: parentCategory
    type: u32
    notes: active AchievementCategoryEntry member (column 1, -1 for a main category)
    evidence: L1
  - repeat: 1
    fields:
    - name: field_2
      type: bytes_variable
      notes: 'a u32 length then that many raw bytes (memcpy, NOT the FUN_100d3680 lpstring idiom used elsewhere in this bucket), the result passed through FUN_10086e30 (transform of unconfirmed purpose) and freed, not stored verbatim. The repeat count is confirmed at exactly 1 by stack layout: the loop writes a 24-byte element starting at the vector object''s own address, and its exit test compares against a fixed local variable exactly 24 bytes further up the frame -- one iteration exactly spans that gap (the same reasoning as 0x0935 and 0x0937). By column arithmetic (2 prefix + 1x17 collapsed here + 1 more read after it = 20, the file''s full column count) this occupies the file''s disabled name[16]+flag locale block (columns 2-18) -- an inference from arithmetic, not a direct read, so no name is asserted.'
      evidence: L3
  - name: field_3
    type: u32
    notes: core format char 'x' (column 19, sortOrder, per DBCStructure.h's comment "//uint32 sortOrder; // 19"); fully commented out, no formal name asserted
    evidence: L1
# Read from build/ghidra/handlers-c/0x0938.c (the actual decompiled handler, RVA 0x00229850, read and traced by hand), cross-checked against AzerothCore src/server/shared/DataStores/DBCStructure.h (AchievementCategoryEntry) and DBCfmt.h (AchievementCategoryfmt). Confirms and refines the pre-existing repeat-block sketch (which already had the right shape) with a proven count of 1 and the arithmetic that identifies its contents.

client:
  handler:
    rva: "0x00229850"
    registration_site_rva: "0x00228e05"
    evidence: L3

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x0938.c (the actual decompiled handler, RVA 0x00229850, read and traced by hand), cross-checked against AzerothCore src/server/shared/DataStores/DBCStructure.h (AchievementCategoryEntry) and DBCfmt.h (AchievementCategoryfmt). Confirms and refines the pre-existing repeat-block sketch (which already had the right shape) with a proven count of 1 and the arithmetic that identifies its contents."
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h (AchievementCategoryEntry) and DBCfmt.h (AchievementCategoryfmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md: AchievementCategoryEntry has exactly 2 active members (ID, parentCategory), matching the 2 leading dwords handler-fire.yaml reads before the repeat block"
    claim: "the names ID and parentCategory for the two leading dwords"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0938 is named SMSG_PATCH_ACHIEVEMENT_CATEGORY"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
