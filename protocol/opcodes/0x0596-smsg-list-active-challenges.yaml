# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0596
name: SMSG_LIST_ACTIVE_CHALLENGES
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  The player's active challenges, sent whole on login and after every change; the handler rebuilds its cache from it and fires CHALLENGE_ACTIVE_LIST_CHANGED with no arguments. One 26-byte record per challenge, block-copied by the client. Names of the two meaningful fields come from the server's writer (L1); the server writes constants into the other five.

# AzerothCore calls 0x0596 SMSG_COA_CHALLENGE_ACTIVE_LIST. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout:
  - name: count
    type: u32
    evidence: L3
  - repeat: count
    fields:
    - name: unknown_0
      type: u32
      notes: server writes 0
      evidence: L3
    - name: challengeId
      type: u32
      evidence: L3
    - name: level
      type: u32
      evidence: L3
    - name: unknown_3
      type: u32
      notes: server writes 1
      evidence: L3
    - name: unknown_4
      type: u32
      notes: server writes 0
      evidence: L3
    - name: unknown_5
      type: u32
      notes: server writes 0
      evidence: L3
    - name: unknown_6
      type: u16
      notes: server writes 0
      evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c, DumpC.java) and the CoA server's writer in mod-coa-challenges.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# the asm idiom (packet_layout.py) saw the first 1 read(s) the same way and stopped. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00138050"
    registration_site_rva: "0x0013b7a6"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "CHALLENGE_ACTIVE_LIST_CHANGED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: CHALLENGE_ACTIVE_LIST_CHANGED
    args: []
    source: "Interface/AddOns/Ascension_ChallengesUI/ChallengesUI.lua:143"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: CHALLENGE_ACTIVE_LIST_CHANGED
    format: null
    helper: FUN_100b94b0

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-coa-challenges/src/CoA.Challenges.Core.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.History.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Lifecycle.cpp

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c, DumpC.java) and the CoA server's writer in mod-coa-challenges"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0596 is named SMSG_LIST_ACTIVE_CHALLENGES"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
