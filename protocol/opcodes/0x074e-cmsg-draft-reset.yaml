# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x074e
name: CMSG_DRAFT_RESET
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Reset the draft: no payload, the opcode alone. Sent by the Lua binding RedraftAbilities.

layout: []  # empty
# Read from the decompiled sender binding (DumpC.java over the DLL, 2026-09-20): opcode write, then Finalize and Send with no data write between them.

# Widths and order, read out of the client's sender at 0x00223e26 by tools/extract/client_sender.py.
# No PutData between the opcode write and the send: the packet is empty.

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/AscensionUI/Gossips/PrestigeMode.lua

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: RedraftAbilities
  function_rva: "0x00223dc0"
  calls:
    - {file: "Interface/AddOns/Ascension_Draft/DraftActionButtonMixin.lua", line: 113, args: []}
    - {file: "Interface/AddOns/Ascension_Draft/Draft.lua", line: 369, args: []}

provenance:
  - level: L3
    source: "the decompiled sender binding (DumpC.java over the DLL, 2026-09-20): opcode write, then Finalize and Send with no data write between them"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x074e is named CMSG_DRAFT_RESET"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"
  - level: L1
    source: "the capture reference (CoinThrow, 2026-09-20, kept outside the tree)"
    claim: "draft reset (0x074E) is empty, sender at Extensions.dll VA 0x10223E26"
    note: >
      Corroborates the L3 finding above exactly -- 0x10223E26 minus image_base
      0x10000000 is RVA 0x00223e26, the same sender site tools/extract/client_sender.py
      recorded independently (inside RedraftAbilities, function_rva 0x00223dc0). No
      field changes level: this is agreement, not new proof.

replay: null
