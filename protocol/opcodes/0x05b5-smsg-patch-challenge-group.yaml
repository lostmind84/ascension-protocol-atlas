# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05b5
name: SMSG_PATCH_CHALLENGE_GROUP
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of ChallengeGroups.dbc: three dwords and no string; the runtime record (0x0c bytes) is a byte-for-byte copy of the wire block. This is the shortest packet in the challenge patch family. Dword 1 is passed to the group-refresh routine FUN_10135130.

layout:
  - name: id
    type: u32
    notes: dword 0; the row key -- range-checked against minId 0x10be0240 / maxId 0x10be023c, indexes the row table at 0x10be0250, then passed to FUN_10133ac0
    evidence: L3
  - name: unknown_1
    type: u32
    evidence: L3
    notes: dword 1; record +0x04, and passed unconditionally to FUN_10135130 after the insert -- the same routine SMSG_PATCH_CHALLENGE 0x0591 calls with a challenge id and SMSG_PATCH_CHALLENGE_GROUP_REWARD does not. A key the group layer re-reads; the name is inference.
  - name: unknown_2
    type: u32
    notes: dword 2; record +0x08. Not named.
    evidence: L3
# Read from the decompiled handler FUN_10138e50 (build/ghidra/handlers-c/0x05b5.c) read by hand, 2026-09-20. The capture reference (~/CoaServer/reference/cointhrow, L1 for us) counts Groups 3/3 fields. Our own read confirms the count from the binary (three dwords, a 0x0c record): L3.

# Widths and order, read out of the handler at 0x00138e50 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00138e50"
    registration_site_rva: "0x0013b6e8"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: ChallengeGroups.dbc
    instance: "0x10be0230"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 12
    fields: 3

provenance:
  - level: L3
    source: "the decompiled handler FUN_10138e50 (build/ghidra/handlers-c/0x05b5.c) read by hand, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05b5 is named SMSG_PATCH_CHALLENGE_GROUP"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
