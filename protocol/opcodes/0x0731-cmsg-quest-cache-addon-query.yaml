# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0731
name: CMSG_QUEST_CACHE_ADDON_QUERY
direction: client_to_server
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named CMSG_QUEST_CACHE_ADDON_QUERY by the client's own opcode table. Not sent through the
  normal PutData sender path; client/client-senders.md places it in a client-side
  query-service registration alongside its response 0x0732, without an RVA. Layout and
  behaviour not established.

layout: []   # unknown
# Checked 2026-09-20: not in senders.yaml (no CDataStore ctor/PutData/Finalize/Send site
# found for this opcode) and not in handlers.yaml. NOT "no client code", though:
# client/client-senders.md ("The recovery service"/query-service section) documents this
# opcode, paired with its response SMSG_QUEST_CACHE_ADDON_QUERY_RESPONSE (0x0732), as one of
# two request/response pairs registered in a client-side "query service" object rather than
# sent through the normal PutData path -- consistent with the scan finding nothing here.
# That document gives no RVA for this pair; I could not relocate one this session.
#
# A scratch candidate at VA 0x1006de8c (a leftover build/ghidra/missing-senders.txt, not
# part of the documented extraction pipeline, and unrelated to the query-service claim
# above) turned out to be a dead end on inspection: Callers.java finds zero references to
# that address, so it is not a real function entry, and the fragment Ghidra disassembles
# from it (`FUN_101af030(buf, 0x731)` then a string-buffer free) does not touch a
# CDataStore in the documented +0x14/+4 way -- the 0x731 there reads as an unrelated
# immediate (most likely a buffer-size constant), not this opcode. Recorded so the same
# dead end isn't re-chased.

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0731 is named CMSG_QUEST_CACHE_ADDON_QUERY"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (no entry), checked 2026-09-20"
    claim: "no PutData-style sender -- but see the query-service note below; this is not evidence of no client code"
  - level: L2
    source: "client/client-senders.md, 'Two more opcodes are not sent by a sender at all but registered in a query service object' (committed doc, methodology given, no RVA given for this pair; not independently re-derived this session)"
    claim: "0x0731/0x0732 are a request/response pair driven by a client-side query-service registration, not a plain sender/handler"
  - level: L0
    source: "build/ghidra/missing-senders.txt (a scratch file, not part of the documented extraction pipeline) claiming a sender site at VA 0x1006de8c"
    claim: "REJECTED. Checked by hand 2026-09-20 (Callers.java 1006de8c: zero references; DecompAt.java 1006de8c: the resulting fragment is not a CDataStore sender). Not usable as evidence."

replay: null
