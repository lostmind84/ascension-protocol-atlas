# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0800
name: CMSG_MULTIPLE_MOVES_BETTER
direction: client_to_server
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named CMSG_MULTIPLE_MOVES_BETTER by the client's own opcode table -- not the same opcode as
  AzerothCore's own SMSG_MULTIPLE_MOVES (0x51E, "uncompressed version of SMSG_COMPRESSED_MOVES",
  `src/server/game/Server/Protocol/Opcodes.h:1340`), and not a duplicate of it: different
  number, different direction, and this one is client-to-server. `client/client-senders.md`
  states plainly that this opcode "is a name with no trace either": not a 32- or 16-bit
  immediate in `.text`, not a value in `.rdata`/`.data`, not an arithmetic constant before a
  `Put(u32)` opcode write, and not a number in the shipped Lua. This build has no sender for
  it; layout is not established because there is nothing to read, not because nobody has
  looked. The three files this fiche used to cite under `used_by_server` were checked by hand
  and are false positives of a bare-literal scan: `0x0800` in each is an unrelated bitmask
  (`GameModeDef`'s `BuildDraft` bit and `REGEN_CAST_RANGE`), never an opcode. See the
  retraction in provenance.

layout: []   # unknown -- confirmed absent, not merely unexamined; see the provenance below

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0800 is named CMSG_MULTIPLE_MOVES_BETTER"
  - level: L3
    source: >
      client/client-senders.md ("What the scan still does not reach": "0x0800
      CMSG_MULTIPLE_MOVES_BETTER is a name with no trace either", 2026-09-20)
    claim: "the opcode value 0x0800 appears nowhere in this build; there is no sender for it here"
  - level: L1
    source: >
      retraction, checked by hand 2026-09-20: modules/mod-ascension-compat/src/
      AscensionCharacterSelection.cpp:87 (a comment listing config bit values),
      modules/mod-coa-challenges/src/CoA.Challenges.Review.h:158 (`GameModeDef` row
      `{"BuildDraft", ..., 0x0800}`) and modules/mod-coa-challenges/src/
      CoA.Challenges.Scripts.cpp:526 (`REGEN_CAST_RANGE = 0x800`)
    claim: >
      this fiche previously listed these three files under `used_by_server`, generated from a
      bare-literal scan (client/server-usage.yaml warns "a bare literal can be a false
      positive"); all three are that false positive -- 0x0800 there is a config/game-mode
      bitmask or an unrelated named constant, never CMSG_MULTIPLE_MOVES_BETTER. The server
      does not use this opcode.
  - level: L1
    source: "azerothcore-wotlk-coa src/server/game/Server/Protocol/Opcodes.h:1340, src/server/game/Entities/Player/Player.cpp:12139 (AzerothCore's own SMSG_MULTIPLE_MOVES = 0x51E)"
    claim: "0x51E is a different, unrelated opcode that merely shares part of the name"

replay: null
