# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0730
name: SMSG_CALLBOARD_CACHE_CONFIG_UPDATE
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  The callboard cache config is a list, not a struct: the handler move-assigns a three-pointer container out of its parse helper, so a u32 count then that many 24-byte records. The capture reference's 1,132-byte payload is 4 + 47 x 24 exactly; the 28-byte block this fiche used to claim divides nothing.

layout:
  - name: count
    type: u32
    evidence: L3
    notes: 'The handler at RVA 0x00116190 reads nothing itself: it move-assigns a three-pointer container (begin/end/capacity) out of the parse helper at RVA 0x00115400 and nulls the source, so this packet is a variable-length list and not the fixed 28-byte struct this fiche claimed before. The extractor found seven u32 reads in that helper (layouts.yaml), which is this count plus one record''s six columns. The capture reference reports count = 47 over a 1,132-byte payload: 4 + 47 * 24 = 1132 exactly, while no whole number of 28-byte blocks fits 1,132 bytes (L1).'
  - repeat: count
    fields:
    - name: entryId
      type: u32
      evidence: L3
      notes: |
        record +0x00. The parse helper at RVA 0x00115400 reads a u32 count, then per record six
        u32 at +0x00, +0x04, +0x08, +0x0c, +0x10 and +0x14, advancing the cursor by 0x18 -- so the
        record is twenty-four bytes of six u32, read here and not taken from anyone's capture.
        What the column MEANS is still L1 below.
        The capture reference reports 47 consecutive values descending 691 -> 645, and that the key is not a Manastorm.dbc row (44 of the 47 ids exist there but the columns disagree, and 654/671/691 are absent). Whether it keys a callboard npc/quest or an ability is not established. The capture reference reports 47 consecutive values descending 691 -> 645, and that the key is not a Manastorm.dbc row (44 of the 47 ids exist there but the columns disagree, and 654/671/691 are absent). Whether it keys a callboard npc/quest or an ability is not established.
    - name: unknown_1
      type: u32
      evidence: L1
      notes: record +0x04; the capture reference reports zero in every row
    - name: groupId
      type: u32
      evidence: L1
      notes: record +0x08; the capture reference reports nine distinct sparse values (130, 132, 134, 135, 137, 139, 155, 500, ...)
    - name: unknown_3
      type: u32
      evidence: L1
      notes: record +0x0c; the capture reference reports a small descending counter, eleven distinct values from 83 down to 0
    - name: itemId
      type: u32
      evidence: L1
      notes: 'record +0x10. The capture reference reports an Item.dbc id: nine distinct values, 1397886 and the contiguous custom block 1615000-1615009, both in class 15 (miscellaneous).'
    - name: cost
      type: u32
      evidence: L1
      notes: record +0x14; the capture reference reports a cost ladder of fourteen distinct values from 300 down to 15
# Read from the decompiled handler (build/ghidra/handlers-c/0x0730.c), read by hand, with the parse helper's read widths from client/symbols/ascension-extensions-2026-08-13/layouts.yaml. The record's 24-byte split and the column roles are the capture reference's (L1); decompiling the parse helper FUN_10115400 (RVA 0x00115400) is the one Ghidra run that would promote them. See .agents/reports/conflicts.md.

# Read from the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock).

# Widths and order, read out of the handler at 0x00115400 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00116190"
    registration_site_rva: "0x001161f2"
    evidence: L3

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x0730.c), read by hand, with the parse helper's read widths from client/symbols/ascension-extensions-2026-08-13/layouts.yaml"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
    note: "superseded: this is the reading that gave the single 28-byte block of seven u32; the handler move-assigns a container, so the packet is a list"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
    note: "the seven u32 are the parse helper's reads at RVA 0x00115400 flattened in source order, not seven fields of a flat packet"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0730 is named SMSG_CALLBOARD_CACHE_CONFIG_UPDATE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L1
    source: "CoinThrow's live-session capture reference, 2026-09-20 (private, kept outside the tree; our comparison is docs/crosscheck-cointhrow-2026-09-20.md)"
    claim: "the 1,132-byte payload, count = 47, the 24-byte record and the six column roles"

replay: null
