# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0926
name: SMSG_CA_AVAILABLE_CREDITS_UPDATE
direction: server_to_client
status: proven
evidence: L5
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  A credit balance the Character Advancement UI spends (ability and talent unlearns, per CharacterAdvancementCostUtil.lua): one u8 credit type and its u32 amount, stored in the client's credits map keyed by the type. The Lua read is CA_GetCreditAmount(type) by name; that binding's body was not followed to the same map here.

layout:
  - name: creditType
    type: u8
    evidence: L5
    notes: 'The key of the client''s credits map; forged 1 -> CA_GetCreditAmount(1) changed, other types did not. The client names the four values itself: Enum.ResetCreditType = {AbilityReset = 1, TalentReset = 2, AbilityUnlearn = 3, TalentUnlearn = 4} (Interface/SharedXML/Enum.lua:275, corpus ascension-ui-patch-b-stock, L2), and CharacterAdvancementCostUtil.lua passes exactly those constants to CA_GetCreditAmount. So 1 and 2 count resets, 3 and 4 count unlearns, ability and talent respectively. The capture reference independently reports the range as 1-4 naming the same enum, with a live sample of 1 -> 0, 2 -> 0, 3 -> 2, 4 -> 3 (L1); the enum itself we read here, so the naming is L2, not L1.

      '
  - name: amount
    type: u32
    evidence: L5
    notes: 'Forged 777 -> CA_GetCreditAmount(1) answered 777 (was 0). The UI divides the gold part of a reset cost by this counter (CharacterAdvancementCostUtil.lua:99, CalculateResetCostGold(level, CA_GetCreditAmount(resetCreditType))), so the value scales what a reset costs.

      '
# Read from Interface/SharedXML/Enum.lua:275 in the UI corpus ascension-ui-patch-b-stock (Enum.ResetCreditType), read with the handler. Only the creditType note changed; both fields keep the L5 the replay gave them.

# Read from the decompiled handler (build/ghidra/handlers-c, DumpC.java): FUN_101a1970 looks the u8 up in the credits map and the u32 is stored there.

# Widths and order, read out of the handler at 0x001a1c00 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x001a1c00"
    registration_site_rva: "0x001a1c74"
    evidence: L3

provenance:
  - level: L2
    source: "Interface/SharedXML/Enum.lua:275 in the UI corpus ascension-ui-patch-b-stock (Enum.ResetCreditType), read with the handler"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L5
    source: "tools/replay/ca_vertical.py --check credits, slot 1, 2026-09-19"
    claim: "u8 creditType then u32 amount; CA_GetCreditAmount(creditType) answers amount"
    note: "forged with .debug send opcode (2342 uint8 1 uint32 777); read through CoaProbe eval"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c, DumpC.java): FUN_101a1970 looks the u8 up in the credits map and the u32 is stored there"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0926 is named SMSG_CA_AVAILABLE_CREDITS_UPDATE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay:
  script: tools/replay/ca_vertical.py
  asserts:
    - "CA_GetCreditAmount(1) answers the forged amount after a 0x0926 (u8 1, u32 777)"
  asserts_measured:
    - "CA_GetCreditAmount(1): 0 -> 777"
  last_run: { date: 2026-09-20, result: pass, build: coa-extensions-rev4-2026-09-14,
              dataset: ascension-live-2026-09-17, slot: 1 }
