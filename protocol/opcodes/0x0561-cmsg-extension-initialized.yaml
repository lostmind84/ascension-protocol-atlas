# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0561
name: CMSG_EXTENSION_INITIALIZED
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  The DLL announces itself after the handshake: two literal u32, 0 and 1. No argument reaches them.

layout:
  - name: constant_0
    type: u32
    notes: always 0 (Put(u32) of a literal)
    evidence: L3
  - name: constant_1
    type: u32
    notes: always 1
    evidence: L3
# Read from the decompiled sender function (FuncAt.java + DumpC.java over the DLL, 2026-09-20), read by hand.

# Widths and order, read out of the client's sender at 0x00a73bc9 by tools/extract/client_sender.py.

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-ascension-compat/src/AscensionCompat.cpp

provenance:
  - level: L3
    source: "the decompiled sender function (FuncAt.java + DumpC.java over the DLL, 2026-09-20), read by hand"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0561 is named CMSG_EXTENSION_INITIALIZED"

replay: null
