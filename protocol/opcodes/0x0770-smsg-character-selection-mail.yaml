# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0770
name: SMSG_CHARACTER_SELECTION_MAIL
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_CHARACTER_SELECTION_MAIL by the client's own opcode table. Layout and behaviour not
  established.

layout:
  - name: listIndex
    type: u32
    evidence: L3
  - name: hasMail
    type: u8
    evidence: L3
  - name: hasStoreMail
    type: u8
    evidence: L3
    notes: "server always sends 0: not tracked by this fork"
# name from mod-ascension-compat's serializer (L1); width and order from the decompiled handler (L3)
# server writes 6 bytes; client reads u32, u8, u8

client:
  handler:
    rva: "0x000d39a0"
    registration_site_rva: "0x000d4dda"
    evidence: L3

used_by_server:   # the CoA server module sends or handles this
  server_name: SMSG_ASCENSION_CHARACTER_SELECTION_MAIL
  files:
    - modules/mod-ascension-compat/src/AscensionCharacterSelection.cpp
    - modules/mod-ascension-compat/src/AscensionCharacterSelection.h

provenance:
  - level: L3
    source: "tools/ghidra/ghidra.sh run Decomp.java <handler VA> (see client/server-layouts.md)"
    claim: "field widths and order, from the decompiled handler"
  - level: L1
    source: "mod-ascension-compat's serializer, client/server-layouts.yaml"
    claim: "field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0770 is named SMSG_CHARACTER_SELECTION_MAIL"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
