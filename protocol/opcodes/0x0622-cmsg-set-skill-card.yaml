# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0622
name: CMSG_SET_SKILL_CARD
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Set a skill card: a selector, the card type name, the index and the card id (0 to clear). The skeleton missed the string.

layout:
  - name: unknown_0
    type: u32
    notes: the value FUN_1016f580 returns -- the same per-index selector the wildcard misc data uses
    evidence: L3
  - name: cardType
    type: cstring
    notes: RemoveCardAtIndex(cardType, index)
    evidence: L3
  - name: index
    type: u32
    evidence: L3
  - name: cardId
    type: u32
    notes: 0 in this binding, which removes the card; other senders of 0x0622 were not read
    evidence: L3
# Read from the decompiled sender binding (tools/ghidra/scripts/DumpC.java over the DLL, 2026-09-20), read by hand next to the UI's calls (L2).

# Widths and order, read out of the client's sender at 0x002f7895 by tools/extract/client_sender.py.
# A helper is called between the opcode write and the send: this is at best a
# PREFIX of the payload. An empty list here does not mean an empty packet.

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_SkillCards/SkillCard/SkillCard.lua
  - Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardTab/SkillCardTab.lua
  - Interface/FrameXML/Util/SkillCardsUtil.lua

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: RemoveCardAtIndex
  function_rva: "0x002f77f0"
  calls:
    - {file: "Interface/FrameXML/StaticPopup.lua", line: 4710, args: ["unpack(data)"]}
    - {file: "Interface/FrameXML/Util/SkillCardsUtil.lua", line: 206, args: ["cardType", "index"]}
    - {file: "Interface/FrameXML/Util/SkillCardsUtil.lua", line: 214, args: ["cardType", "index"]}
    - {file: "Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardTab/SkillCardTab.lua", line: 134, args: ["unpack(data)"]}
    - {file: "Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardTab/SkillCardTab.lua", line: 138, args: ["unpack(data)"]}

provenance:
  - level: L3
    source: "the decompiled sender binding (tools/ghidra/scripts/DumpC.java over the DLL, 2026-09-20), read by hand next to the UI's calls (L2)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0622 is named CMSG_SET_SKILL_CARD"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
