# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0599
name: SMSG_LIST_CHALLENGE_CRITERIA
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  The criteria of the player's challenges, sent whole; the handler stores each 33-byte record at +0xc of a cache entry keyed by its second u32 and fires CHALLENGE_CRITERIA_LIST_CHANGED. The record is the one SMSG_UPDATE_CHALLENGE_CRITERIA 0x059A carries once, and the fields here are that fiche's: eight u32 and a done byte (33 = 8x4 + 1). The skeleton's two u64 were pairs of u32, the same misreading corrected on 0x059A.

# AzerothCore calls 0x0599 SMSG_COA_CHALLENGE_CRITERIA_LIST. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout:
  - name: count
    type: u32
    evidence: L3
  - repeat: count
    fields:
    - name: reserved
      type: u32
      notes: the fork sends 0
      evidence: L3
    - name: challengeId
      type: u32
      notes: the key the client hashes (FNV-1a) to find the cache entry
      evidence: L3
    - name: level
      type: u32
      evidence: L3
    - name: requirementType
      type: u32
      notes: index into the client's CHALLENGE_REQUIREMENT_TYPE_* table
      evidence: L3
    - name: value1
      type: u32
      notes: the requirement row calls the three MiscValue1/2/3 (ChallengeItemMixin.lua:490, L2); the criteria event delivers them as value1..3
      evidence: L3
    - name: value2
      type: u32
      evidence: L3
    - name: value3
      type: u32
      evidence: L3
    - name: unknown_7
      type: u32
      evidence: L3
    - name: done
      type: u8
      notes: the 33-byte record ends on this byte
      evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x0599.c); the record's fields taken from fiche 0x059A, read out of handler FUN_10137030, 2026-09-20.

# Read from the decompiled handler (build/ghidra/handlers-c, DumpC.java); the server writes only the count in its own chain.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# the asm idiom (packet_layout.py) saw the first 1 read(s) the same way and stopped. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00136e80"
    registration_site_rva: "0x0013b7dc"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "CHALLENGE_CRITERIA_LIST_CHANGED"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: CHALLENGE_CRITERIA_LIST_CHANGED
    format: null
    helper: FUN_100b94b0

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-coa-challenges/src/CoA.Challenges.History.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Lifecycle.cpp

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x0599.c); the record's fields taken from fiche 0x059A, read out of handler FUN_10137030, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c, DumpC.java); the server writes only the count in its own chain"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0599 is named SMSG_LIST_CHALLENGE_CRITERIA"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
