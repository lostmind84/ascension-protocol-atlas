# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x09bc
name: SMSG_REALM_INFO
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Realm parameters the client keeps in one struct (FUN_1008e2d0) and exposes through C_Realm-style getters: realmId at +4, expansion at +8 (GetRealmMaxLevel maps it to 25/35/45/55), a maintenance flag at +0x18 gated by the first u8 flag at +0x40, the auction cut rate at +0x1c. The nine leading fields are the ones azerothcore-wotlk-coa#4128 reproduced; the handler goes on to read eight u8 flags, two NUL-terminated strings, a u8 and a conditional trailing u32 not decoded here. Widths from the asm idiom (floats by movss); names from the getters (L3); the unnamed fields are stored but no getter read here consumes them.

layout:
  - name: realmId
    type: u32
    notes: "struct +4; GetRealmId; forged 4242 -> GetRealmId() 4242"
    evidence: L5
  - name: expansion
    type: u32
    notes: >
      struct +8; GetRealmExpansion; forged 2 -> GetRealmExpansion() 2. GetRealmMaxLevel()
      answered 80 for it, which is not among the 25/35/45/55 read in the getter's
      decompile: that mapping does not describe the value 2, and is left as read.
    evidence: L5
  - name: unknown_2
    type: f32
    notes: struct +0xc
    evidence: L3
  - name: unknown_3
    type: f32
    notes: struct +0x10
    evidence: L3
  - name: unknown_4
    type: f32
    notes: struct +0x14
    evidence: L3
  - name: maintenance
    type: u32
    notes: "stored as a bool at +0x18; GetRealmMaintenance returns it when flags_0 is set; forged 1 with flags_0=1 -> true"
    evidence: L5
  - name: auctionCutRate
    type: f32
    notes: "struct +0x1c; GetRealmAuctionCutRate; forged 0.5 -> 0.5"
    evidence: L5
  - name: unknown_7
    type: f32
    notes: struct +0x20
    evidence: L3
  - name: unknown_8
    type: u32
    notes: struct +0x24
    evidence: L3
  - name: flags_0
    type: u8
    notes: struct +0x40; gates GetRealmMaintenance
    evidence: L3
  - name: flags_1
    type: u8
    notes: struct +0x41
    evidence: L3
  - name: flags_2
    type: u8
    notes: struct +0x42
    evidence: L3
  - name: flags_3
    type: u8
    notes: struct +0x43
    evidence: L3
  - name: flags_4
    type: u8
    notes: struct +0x44
    evidence: L3
  - name: flags_5
    type: u8
    notes: struct +0x45
    evidence: L3
  - name: flags_6
    type: u8
    notes: struct +0x46
    evidence: L3
  - name: flags_7
    type: u8
    notes: struct +0x47
    evidence: L3
  - name: string_0
    type: cstring
    notes: NUL-scanned; copied into a std::string
    evidence: L3
  - name: string_1
    type: cstring
    evidence: L3
  - name: unknown_19
    type: u8
    notes: struct +0x48
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c, DumpC.java) and the Lua getters GetRealmId/GetRealmExpansion/GetRealmAuctionCutRate/GetRealmMaintenance/GetRealmMaxLevel (DecompAt.java 102fcc70..102fcd20), which read the struct the handler fills.

# Widths and order, read out of the handler at 0x002fc6c0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x002fc6c0"
    registration_site_rva: "0x002fc5e0"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/GlueXML/GlueParent.lua

client_strings:   # literals the handler references, from its decompilation
  - ";EREALM_ALERT_MAINTENANCE"

provenance:
  - level: L5
    source: "tools/replay/ca_vertical.py --check realm, slot 1, 2026-09-19"
    claim: "the positions and widths of realmId, expansion, maintenance (with flags_0) and auctionCutRate"
    note: >
      Forged with .debug send opcode: 2492 uint32 4242 uint32 2 float 0.125 float 0.25
      float 0.375 uint32 1 float 0.5 float 0.625 uint32 9 uint8 1 uint8 0 x7 string alpha
      string beta uint8 0. The getters answered 4242, 2, 0.5, true; GetRealmMaxLevel 80.
      The three unnamed floats, unknown_7/8, the strings and the trailing u8 were sent with
      distinct values but nothing read them back: they stay L3.
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c, DumpC.java) and the Lua getters GetRealmId/GetRealmExpansion/GetRealmAuctionCutRate/GetRealmMaintenance/GetRealmMaxLevel (DecompAt.java 102fcc70..102fcd20), which read the struct the handler fills"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x09bc is named SMSG_REALM_INFO"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay:
  script: tools/replay/ca_vertical.py
  asserts:
    - "GetRealmId, GetRealmExpansion, GetRealmAuctionCutRate and GetRealmMaintenance answer the forged values"
  asserts_measured:
    - "4242, 2, 0.5, true; GetRealmMaxLevel 80"
  last_run: { date: 2026-09-20, result: pass, build: coa-extensions-rev4-2026-09-14,
              dataset: ascension-live-2026-09-17, slot: 1 }
