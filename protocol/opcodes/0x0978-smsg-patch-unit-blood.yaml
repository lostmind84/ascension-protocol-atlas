# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0978
name: SMSG_PATCH_UNIT_BLOOD
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_PATCH_UNIT_BLOOD by the client's own opcode table. The opcode is not bound in patch-
  tables.yaml -- the DLL's own custom-table list does not mention this opcode; UnitBlood.dbc is the
  table patch-table-names.md associates with it by name and geometry (grade L0). The handler's
  registered entry point lies inside a decompiled function shared with another opcode in this shard,
  so whether it reads a full UnitBlood.dbc row is not established. No source names any of the
  table's columns.

layout:
  - name: field_0
    type: bytes_12
    evidence: L3
  - name: field_1
    type: lpstring
    evidence: L3
  - name: field_2
    type: lpstring
    evidence: L3
  - name: field_3
    type: lpstring
    evidence: L3
  - name: field_4
    type: lpstring
    evidence: L3
  - name: field_5
    type: lpstring
    evidence: L3
  - name: field_6
    type: lpstring
    evidence: L3
# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# packet_layout.py had followed a call into 0x000d3680, not the handler. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00234000"
    registration_site_rva: "0x002291ce"
    evidence: L3

dbc_identity:
  dbc: UnitBlood.dbc
  fields: 10
  record_size: 40
  rows: 21
  header_source: "ascension-live-2026-09-17 WDBC header, L2 (client/patch-table-names.md)"
  bound: >
    Not bound in client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, and not named
    by any reference either. The table below is inferred purely from the opcode's own name
    matching a same-named file in the client DBC snapshot (grade L0) -- the weakest kind of
    identity claim this atlas records.
  match: UNCERTAIN
  wire: >
    Registered at 0x00234000. Ghidra's function recovery places it inside a decompiled function
    that actually starts at 0x10233ef0 -- not this opcode's own registration -- so some other,
    unidentified caller owns the visible top of the function (a 2-field, 8-byte-plus-one-string
    record unrelated to UnitBlood.dbc's own geometry). Deeper in, reached only through a branch
    the extraction treats as unreachable, is a block that reads several 4-byte fields and then
    five more lpstrings in a row -- roughly five numeric identifiers and five string columns,
    which is at least directionally consistent with this table's own header (10 fields, 40 bytes;
    UnitBlood.dbc is known elsewhere to carry several texture-filename columns) -- but the exact
    count did not reconcile cleanly on this read (the tool that scanned this build's handlers,
    client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, measured a 12-byte leading
    block here against the ~20 bytes read by hand), and which entry point 0x0978 itself actually
    reaches is not established.
  note: >
    Kept at the width already on record. Flagged, not resolved.

column_names: none found
  # checked: client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (no
  # string literal near the store); lua-keys.yaml (no builder in this table's domain);
  # the Ascension UI/Lua corpus (build/corpus/ascension-ui-patch-b-stock, searched for
  # the table's own base name, e.g. "UnitBlood").

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0978 is named SMSG_PATCH_UNIT_BLOOD"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
