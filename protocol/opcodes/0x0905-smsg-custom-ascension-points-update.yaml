# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0905
name: SMSG_CUSTOM_ASCENSION_POINTS_UPDATE
direction: server_to_client
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_CUSTOM_ASCENSION_POINTS_UPDATE by the client's own opcode table. Layout and behaviour not
  established.

layout: []   # unknown
# Checked 2026-09-20: no entry in handlers.yaml (the push-handler/push-opcode/call-register
# registration scan does not find a site for this opcode) and no hit for its name anywhere
# in the ascension-ui-patch-b-stock corpus. client_code: none -- this build has no code
# behind the name (AGENTS.md rule 4); do not infer a layout from it.
#
# A third-party live-session capture (~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-
# reference-2026-09-20.md, L1, not in this repo) claims a 6-byte payload seen twice
# pre-character-list: `{u8 flag=1; u8 kind; u32 value}`, kinds 2/4 -> values 12/20. That
# capture is from a real realm and this build has none of the code that would receive it --
# either a different client build handled it, or this build's handler exists somewhere this
# session's two scans do not reach. Recorded as an attributed, unreproduced L1 hint, not a
# layout: I could not re-derive it from this build's binary or Lua, so it does not move
# past L1 (EVIDENCE.md: a third-party claim stays L1 here even when its author reports live
# verification).

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0905 is named SMSG_CUSTOM_ASCENSION_POINTS_UPDATE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (no entry) and a corpus grep for the name (no hit), checked 2026-09-20"
    claim: "no client code handles this opcode in this build"
  - level: L1
    source: "~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md, '0x0905 -- SMSG_CUSTOM_ASCENSION_POINTS_UPDATE' (third-party live-session capture, not in this repo, unreproduced against our binary)"
    claim: "a 6-byte payload {u8 flag; u8 kind; u32 value}, observed twice (kind 2 -> value 12, kind 4 -> value 20) just before the character list on a real realm"

replay: null
