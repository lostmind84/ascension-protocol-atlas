# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x065b
name: SMSG_CHARACTER_ADVANCEMENT_UNLOCK_ENTRY_RESULT
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Result of unlocking a Character Advancement entry: the internal id and a result string. The skeleton missed the string.

layout:
  - name: internalId
    type: u32
    evidence: L3
  - name: result
    type: cstring
    notes: CHARACTER_ADVANCEMENT_UNLOCK_ENTRY_RESULT(result, internalID)
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read by hand.

# Widths and order, read out of the handler at 0x00170dc0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00170dc0"
    registration_site_rva: "0x00172264"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_CharacterAdvancement/Templates/CASpellButton.lua
  - Interface/AddOns/Ascension_CharacterAdvancementSeason9/Templates/CASpellButton.lua
  - Interface/AddOns/Ascension_WildCard/Dice/WildCardStartingChoice.lua
  - Interface/FrameXML/SpellListItem.lua

client_strings:   # literals the handler references, from its decompilation
  - "CHARACTER_ADVANCEMENT_UNLOCK_ENTRY_RESULT"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: CHARACTER_ADVANCEMENT_UNLOCK_ENTRY_RESULT
    args: [result, internalID]
    source: "Interface/AddOns/Ascension_CharacterAdvancement/Templates/CASpellButton.lua:650"
  - event: CHARACTER_ADVANCEMENT_UNLOCK_ENTRY_RESULT
    args: []
    source: "Interface/AddOns/Ascension_WildCard/Dice/WildCardStartingChoice.lua:586"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: CHARACTER_ADVANCEMENT_UNLOCK_ENT
    format: "%s%u"
    helper: FUN_1011a5b0

provenance:
  - level: L1
    source: "the capture reference (CoinThrow, 2026-09-20, kept outside the tree -- see docs/crosscheck-cointhrow-2026-09-20.md)"
    claim: "0x065A/0x065B are the CMSG/SMSG unlock-entry pair behind a tree node's native unlock button"
    note: >
      ADDS the request side, which we did not have: this result answers a CMSG the
      reference numbers one below it, and it describes the pair as the tree node's native
      lock and unlock buttons. We hold no fiche for that CMSG and have not looked for its
      sender, so the pairing is unverified here -- but it is a concrete place to look, and
      it agrees with the UI calls already listed under used_by.
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x065b is named SMSG_CHARACTER_ADVANCEMENT_UNLOCK_ENTRY_RESULT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
