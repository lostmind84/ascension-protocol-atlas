# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x053c
name: CMSG_ASCENSIONGM_TICKET_CHAT_REQUEST
direction: client_to_server
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named CMSG_ASCENSIONGM_TICKET_CHAT_REQUEST by the client's own opcode table. No sender
  exists for this opcode in this build: the value appears nowhere in `.text` as a 32- or
  16-bit immediate, nowhere in `.rdata`/`.data`, and nowhere in the shipped Lua
  (client/client-senders.md's exhaustive scan), and it is absent from every symbol dump
  (senders/handlers/layouts/handler-fire/handler-strings.yaml). Layout is not established;
  there is no code to read it from. Unlike the sibling CMSG_ASCENSIONGM_TICKET_LIST_REQUEST
  (0x053B), which has a live empty-payload sender, this ticket-chat opcode has none.

layout: []   # unknown

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x053c is named CMSG_ASCENSIONGM_TICKET_CHAT_REQUEST"
  - level: L3
    source: "client/client-senders.md (opcode value absent from .text/.rdata/.data and the shipped Lua; also absent from client/symbols/ascension-extensions-2026-08-13/{senders,handlers,layouts,handler-fire,handler-strings}.yaml)"
    claim: "no sender or handler for this opcode exists in this client build"

replay: null
