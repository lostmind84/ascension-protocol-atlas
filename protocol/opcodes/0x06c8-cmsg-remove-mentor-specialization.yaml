# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06c8
name: CMSG_REMOVE_MENTOR_SPECIALIZATION
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Remove a mentor specialization: the binding RemoveMentorSpecialization writes one u32; no call in the stock UI corpus names its argument, so the field is named after the binding.

layout:
  - name: specializationId
    type: u32
    notes: named after the binding; no UI call seen
    evidence: L3
# Read from the Lua binding that sends it (client_binding: one argument, one write) and the UI's calls of it.

# Widths and order, read out of the client's sender at 0x00a1b780 by tools/extract/client_sender.py.

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_PathToAscension/PathBecomeMentorPanelMixin.lua

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: RemoveMentorSpecialization
  function_rva: "0x00a1b710"
  calls: []   # no call in the stock UI corpus

provenance:
  - level: L3
    source: "the Lua binding that sends it (client_binding: one argument, one write) and the UI's calls of it"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06c8 is named CMSG_REMOVE_MENTOR_SPECIALIZATION"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
