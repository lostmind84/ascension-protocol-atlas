# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x065e
name: SMSG_MANASTORM_COMPLETED_LEVEL
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_MANASTORM_COMPLETED_LEVEL by the client's own opcode table. Layout and behaviour not
  established.

layout:
  - name: depth
    type: u32
    evidence: L3
    notes: "run.encounter->depth"
# name from mod-ascension-compat's AscensionManastormProtocol.h / AscensionManastorm.cpp (L1); width and order from the decompiled handler (L3)
# decompiled: FUN_102a2990 reads a single u32
# Widths and order, read out of the handler at 0x002a2990 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x002a2990"
    registration_site_rva: "0x002a462c"
    evidence: L3

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-ascension-compat/src/AscensionManastormProtocol.h

client_strings:   # literals the handler references, from its decompilation
  - "%s%s%s"
  - "MANASTORM_LEVEL_COMPLETED"
  - "MANASTORM_LEVEL_UNLOCKED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: MANASTORM_LEVEL_COMPLETED
    args: []
    source: "Interface/AddOns/Ascension_Manastorm/Manastorm.lua:194"
  - event: MANASTORM_LEVEL_COMPLETED
    args: [level]
    source: "Interface/AddOns/Ascension_UIDevelopmentTools/EventTrace/EventArgNames.lua:335"
  - event: MANASTORM_LEVEL_UNLOCKED
    args: [icon, title, description]
    source: "Interface/FrameXML/Notifications/ToastNotificationSystem.lua:303"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: MANASTORM_LEVEL_COMPLETED
    format: "%u"
    helper: FUN_10278c90
    fields: [field_0]   # the read filling each format position
  - event: MANASTORM_LEVEL_UNLOCKED
    format: "%s%s%s"
    helper: FUN_10278c90
    fields: [null, null, null]   # the read filling each format position

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: ManastormMessages.dbc
    instance: "0x10be00a4"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 156
    fields: 39

provenance:
  - level: L3
    source: "tools/ghidra/ghidra.sh run Decomp.java <handler VA> (see client/server-layouts.md)"
    claim: "field widths and order, from the decompiled handler"
  - level: L1
    source: "mod-ascension-compat's Manastorm serializers, client/server-layouts.yaml"
    claim: "field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x065e is named SMSG_MANASTORM_COMPLETED_LEVEL"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
