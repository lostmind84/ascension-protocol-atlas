# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x057c
name: CMSG_GROUP_FINDER_CREATE_LISTING
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Create a group-finder listing: a u32 list, one u32, three bytes, two length-prefixed strings, two bytes, a u32 and a byte -- the same body as 0x057D. The skeleton had the list as two fields. No stock UI call carries argument names, so the fields stay positional.

layout:
  - name: count
    type: u32
    notes: size of a u32 vector the binding builds from its arguments
    evidence: L3
  - repeat: count
    fields:
    - name: id
      type: u32
      evidence: L3
  - name: unknown_1
    type: u32
    evidence: L3
  - name: unknown_2
    type: u8
    evidence: L3
  - name: unknown_3
    type: u8
    evidence: L3
  - name: unknown_4
    type: u8
    evidence: L3
  - name: string_0
    type: lpstring
    notes: u32 length then the bytes (lpstring)
    evidence: L3
  - name: string_1
    type: lpstring
    notes: u32 length then the bytes (lpstring)
    evidence: L3
  - name: unknown_7
    type: u8
    evidence: L3
  - name: unknown_8
    type: u8
    evidence: L3
  - name: unknown_9
    type: u32
    evidence: L3
  - name: unknown_10
    type: u8
    evidence: L3
# Read from the decompiled sender binding (tools/ghidra/scripts/DumpC.java over the DLL, 2026-09-20), read by hand next to the UI's calls (L2).

# Widths and order, read out of the client's sender at 0x00272889 by tools/extract/client_sender.py.

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: CreateListing
  function_rva: "0x00272650"
  calls: []   # no call in the stock UI corpus

provenance:
  - level: L3
    source: "the decompiled sender binding (tools/ghidra/scripts/DumpC.java over the DLL, 2026-09-20), read by hand next to the UI's calls (L2)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x057c is named CMSG_GROUP_FINDER_CREATE_LISTING"

replay: null
