# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0954
name: SMSG_PATCH_SPELL_DIFFICULTY
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of SpellDifficulty.dbc (bound_by: L1 ref, patch-table-names.md): the handler's single 20-byte bulk read exactly equals the table's 5-column/20-byte record (client DBC header). All 5 columns are named by AzerothCore's own SpellDifficultyEntry struct (L1, another project's reading of the same file).

layout:
  - name: id
    type: u32
    notes: 'DBC column 0 (index field, fmt ''n''); core: SpellDifficultyEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar2,20]], one 20-byte bulk read == the full 5-column/20-byte SpellDifficulty.dbc record); name L1 (core DBCStructure.h, another project''s reading of the same file).'
    evidence: L1
  - name: spellId_10n
    type: i32
    notes: 'DBC column 1; core: SpellDifficultyEntry::SpellID[0] (int32), ''10N'' instance mode per core''s comment. Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
  - name: spellId_25n
    type: i32
    notes: 'DBC column 2; core: SpellDifficultyEntry::SpellID[1] (int32), ''25N''. Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
  - name: spellId_10h
    type: i32
    notes: 'DBC column 3; core: SpellDifficultyEntry::SpellID[2] (int32), ''10H''. Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
  - name: spellId_25h
    type: i32
    notes: 'DBC column 4; core: SpellDifficultyEntry::SpellID[3] (int32), ''25H''. Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h SpellDifficultyEntry + DBCfmt.h SpellDifficultyfmt (L1: another project's reading of the same client DBC file, not ours).

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00231b00"
    registration_site_rva: "0x00228fa6"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h SpellDifficultyEntry + DBCfmt.h SpellDifficultyfmt (L1: another project's reading of the same client DBC file, not ours)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "client/patch-table-names.md (the naming-sources sweep): 0x0954 bound_by 'L1 ref' to SpellDifficulty.dbc, 5 fields / 20 bytes, matching the client DBC header"
    claim: "this opcode's table is SpellDifficulty.dbc, at L1 (only the third-party reference names it; patch-tables.yaml does not bind this opcode)"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0954 is named SMSG_PATCH_SPELL_DIFFICULTY"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
