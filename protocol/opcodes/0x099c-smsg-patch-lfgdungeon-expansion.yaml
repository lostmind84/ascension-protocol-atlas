# Generated by tools/codegen/stub_fiches.py, then completed by hand (naming-source closure pass).
id: 0x099c
name: SMSG_PATCH_LFGDUNGEON_EXPANSION
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  A record patched into one of Extensions.dll's internal id-keyed containers -- looked up by id
  (RVA 0x000b1a50), allocated fresh as a 0x20-byte heap struct when new (RVA 0x000ae5d20), and
  inserted (RVA 0x000aa9a0) -- not one of the 114 CustomDBCMgr `.data` tables
  client/patch-tables.md documents: nothing in the decompiled handler references a fixed .data
  table object (no `+0xc`/`+0x10`/`+0x20` min/max/rows idiom anywhere in it, unlike 0x09d1/0x09d2
  in this same shard, which do). The third-party reference in client/patch-table-names.md names
  `LFGDungeonExpansion.dbc` for this opcode, but no file of that name exists in either client-DBC
  snapshot this atlas holds ('Disagreements found' #4 there) -- there is no table to compare
  against in the first place. No source in this repository names the struct's own fields (see
  naming_source below).

layout:
  - name: field_0
    type: bytes_32
    notes: >
      width: L3 (block-copy read, 32 bytes; re-verified this pass against the cached decompile,
      build/ghidra/handlers-c/0x099c.c, against
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source (see naming_source below). The third-party reference names LFGDungeonExpansion.dbc for
      this opcode, but no file of that name exists in either client-DBC snapshot this atlas holds
      (client/patch-table-names.md, 'Disagreements found' #4): there is no table geometry to compare
      this record against at all.
    evidence: L3

client:
  handler:
    rva: "0x0022e3a0"
    registration_site_rva: "0x002293f9"
    evidence: L3

naming_source:
  result: none found
  checked:
    - "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml: 0x099c has no strings recorded"
    - "client/symbols/ascension-extensions-2026-08-13/lua-keys.yaml: 22 builders extracted; none in this table's domain"
    - "LFGDungeonExpansion.dbc search in build/corpus/ascension-ui-patch-b-stock (grepped for the bare table name minus \".dbc\"): 0 files"
  claimed_table:
    dbc: LFGDungeonExpansion.dbc
    fields: null   # file not present in either client-DBC snapshot (see summary)
    record_size: null
    rows: null
    bound_by: "L1 ref only (client/patch-table-names.md); the file itself does not exist in ascension-live-2026-09-17 or cointhrow-2026-09-09"

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x099c.c (decompiled handler, re-read this pass) and client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order; the handler looks the id up in an internal container (RVA 0x000b1a50), allocates a fresh 0x20-byte struct when it is new (RVA 0x000ae5d20), and inserts it (RVA 0x000aa9a0) -- not one of the 114 CustomDBCMgr .data table objects patch-tables.yaml enumerates"
  - level: L1
    source: "client/patch-table-names.md (opcode 0x099c row)"
    claim: "a third-party reference names the DBC table LFGDungeonExpansion.dbc for this opcode; unverified by our own patch-tables.yaml scan and not treated as established here"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml, lua-keys.yaml, and build/corpus/ascension-ui-patch-b-stock (this pass)"
    claim: "no naming source for the struct's own fields exists in any of the three"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x099c is named SMSG_PATCH_LFGDUNGEON_EXPANSION"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
