# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06ed
name: SMSG_PATCH_APPEARANCE_DETAILS
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of AppearanceDetails.dbc: id, a dword, a cstring (over a skipped placeholder dword), then a fourth dword -- only 20 of the table's 84 bytes (5 of its 21 fields), the row allocation size the handler uses. Despite sharing decompiled code with 0x0691 (Ghidra put both opcodes' handlers in one function at 0x101dca00), this opcode's own entry point (registration RVA 0x001e6b4b, reached via the label at 0x101dcd30) only ever touches AppearanceDetails.dbc's row array (0x10bdfa4c matches its CustomDBCMgr object exactly) -- AppearanceCategories.dbc, which patch-tables.yaml also lists for this opcode, is written by 0x0691's own entry point, not this one. No column names were found this pass: patch-table-names.md's cited 'ui getter FrameXML/UIParent.lua:1787' is C_Appearance.GetAppearanceDisplayInfo, which this pass confirmed (by its minId/maxId/rows addresses) reads Appearances.dbc's row array, not AppearanceDetails.dbc's -- it names no field here. A second, name-shaped candidate, C_Appearance.GetAppearanceDetails (VA 0x100c7f60), was also checked and reads an unrelated 20-byte-stride cache at a different base address, not this table either.

layout:
  - name: id
    type: u32
    notes: record +0x00, the row key -- range-checked against minId 0x10bdfa3c / maxId 0x10bdfa38, indexes the row table at 0x10bdfa4c, then passed to FUN_10133ac0. 0x10bdfa4c is AppearanceDetails.dbc's own rows pointer (instance 0x10bdfa2c + 0x20), confirming this handler writes AppearanceDetails.dbc and not AppearanceCategories.dbc.
    evidence: L3
  - name: unknown_1
    type: u32
    notes: record +0x04. No naming source located this pass (see summary). Not named.
    evidence: L3
  - name: unknown_2
    type: bytes_4
    notes: 'record +0x08, SKIPPED by the reader: sent on the wire as a throwaway placeholder dword, then overwritten by the trailing cstring below.'
    evidence: L3
  - name: unknown_3
    type: u32
    notes: record +0x0c. No naming source located this pass. Not named.
    evidence: L3
  - name: unknown_4
    type: u32
    notes: record +0x10, the last field this opcode writes. AppearanceDetails.dbc's own header reports 21 fields / 84 bytes; only these first 5 (20 bytes) are ever touched by this SMSG -- the allocation size passed to the row constructor is literally 0x14 (20), not the table's full 84-byte record.
    evidence: L3
  - name: string_1
    type: cstring
    notes: record +0x08, over the skipped dword. The row's only string column, sent as a plain inline cstring (not length-prefixed). No naming source located this pass.
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x06ed.c, the LAB_101dcd29 block specific to this opcode's own registration site) and the decompiled candidate getters C_Appearance.GetAppearanceDisplayInfo (VA 0x100c8090) and C_Appearance.GetAppearanceDetails (VA 0x100c7f60) (Ghidra DecompAt.java, workspace named-lua-ui), all read by hand, 2026-09-20.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x001dcd30"
    registration_site_rva: "0x001e6b4b"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: AppearanceCategories.dbc
    instance: "0x10be005c"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 104
    fields: 25
  - dbc: AppearanceDetails.dbc
    instance: "0x10bdfa2c"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 84
    fields: 21

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x06ed.c, the LAB_101dcd29 block specific to this opcode's own registration site) and the decompiled candidate getters C_Appearance.GetAppearanceDisplayInfo (VA 0x100c8090) and C_Appearance.GetAppearanceDetails (VA 0x100c7f60) (Ghidra DecompAt.java, workspace named-lua-ui), all read by hand, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06ed is named SMSG_PATCH_APPEARANCE_DETAILS"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
