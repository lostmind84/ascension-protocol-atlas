# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x094f
name: SMSG_PATCH_SPELL_CATEGORY
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of SpellCategory.dbc (bound_by: L1 ref, patch-table-names.md): the handler's two dwords exactly equal the table's 2-column/8-byte record (client DBC header). Both columns are named by AzerothCore's own SpellCategoryEntry struct (L1, another project's reading of the same file).

layout:
  - name: id
    type: u32
    notes: 'DBC column 0 (index field, fmt ''n''); core: SpellCategoryEntry::Id. Width L3 (handler-fire.yaml: reads [[uVar2,4],[uVar3,4]], two dwords == the full 2-column/8-byte SpellCategory.dbc record); name L1 (core DBCStructure.h, another project''s reading of the same file).'
    evidence: L1
  - name: flags
    type: u32
    notes: 'DBC column 1; core: SpellCategoryEntry::Flags. Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h SpellCategoryEntry + DBCfmt.h SpellCategoryfmt (L1: another project's reading of the same client DBC file, not ours).

# Widths and order, read out of the handler at 0x002317d0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x002317d0"
    registration_site_rva: "0x00228f58"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h SpellCategoryEntry + DBCfmt.h SpellCategoryfmt (L1: another project's reading of the same client DBC file, not ours)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L1
    source: "client/patch-table-names.md (the naming-sources sweep): 0x094f bound_by 'L1 ref' to SpellCategory.dbc, 2 fields / 8 bytes, matching the client DBC header"
    claim: "this opcode's table is SpellCategory.dbc, at L1 (only the third-party reference names it; patch-tables.yaml does not bind this opcode)"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x094f is named SMSG_PATCH_SPELL_CATEGORY"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
