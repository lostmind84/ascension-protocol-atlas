# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0706
name: SMSG_CLOSE_GM_TICKET_RESULT
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Result of closing a ticket: one result string. The skeleton's two strings were the same read on the handler's two branches (GM view vs player view), which fire CLOSE_GM_TICKET_RESULT or CLOSE_PLAYER_TICKET_RESULT.

layout:
  - name: result
    type: cstring
    notes: one string; the handler reads it on both of its branches -- the GM branch fires CLOSE_GM_TICKET_RESULT(result), the player branch CLOSE_PLAYER_TICKET_RESULT(result), chosen by a flag of the ticket service object (+8), not by the packet
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read next to the ticket UI's event handlers (L2, corpus ascension-ui-patch-b-stock).

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# packet_layout.py had followed a call into 0x0008c870, not the handler. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00253800"
    registration_site_rva: "0x00255219"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "CLOSE_GM_TICKET_RESULT"
  - "CLOSE_PLAYER_TICKET_RESULT"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: CLOSE_PLAYER_TICKET_RESULT
    args: [result]
    source: "Interface/AddOns/Ascension_TicketUI/TicketUI.lua:91"
  - event: CLOSE_PLAYER_TICKET_RESULT
    args: []
    source: "Interface/AddOns/Ascension_TicketUI/ViewTicketPanelMixin.lua:181"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: CLOSE_GM_TICKET_RESULT
    format: "%s"
    helper: FUN_100b9620
  - event: CLOSE_PLAYER_TICKET_RESULT
    format: "%s"
    helper: FUN_100b9620

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read next to the ticket UI's event handlers (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0706 is named SMSG_CLOSE_GM_TICKET_RESULT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
