# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06b1
name: SMSG_TUTORIAL_REWARDED
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  A tutorial was rewarded: one u32 handed to TUTORIAL_ENTRY_UPDATE(%u); the tutorial id by the opcode's name and the sibling request 0x06A8 (no Lua parameter declared).

layout:
  - name: tutorialId
    type: u32
    notes: by the opcode's name
    evidence: L3
# Read from the decompiled handler and its fire (format %u, the read at position 0 proven by handler-fire.yaml).

# Widths and order, read out of the handler at 0x00a18aa0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00a18aa0"
    registration_site_rva: "0x00a18cfb"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "TUTORIAL_ENTRY_UPDATE"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: TUTORIAL_ENTRY_UPDATE
    format: "%u"
    helper: FUN_10278c90
    fields: [field_0]   # the read filling each format position

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: Tutorial.dbc
    instance: "0x10bdf6f0"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 372
    fields: 92

provenance:
  - level: L3
    source: "the decompiled handler and its fire (format %u, the read at position 0 proven by handler-fire.yaml)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06b1 is named SMSG_TUTORIAL_REWARDED"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
