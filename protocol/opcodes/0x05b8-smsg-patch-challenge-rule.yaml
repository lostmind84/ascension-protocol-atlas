# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05b8
name: SMSG_PATCH_CHALLENGE_RULE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of ChallengeRules.dbc: five dwords then the rule-type token as a length-prefixed string. The runtime record is dword-equal to the wire block (0x14 bytes) with the string pointer written over dword 4, which the reader skips. Only the row key and the string are named; no Lua builder reads this record field by field, the model exposes rules as a plain id array.

layout:
  - name: id
    type: u32
    notes: dword 0; the row key -- range-checked against minId 0x10be0360 / maxId 0x10be035c, indexes the row table at 0x10be0370, then passed to FUN_10133ac0
    evidence: L3
  - name: unknown_1
    type: u32
    notes: dword 1; record +0x04. Gates the notify path. Not named.
    evidence: L3
  - name: unknown_2
    type: u32
    evidence: L3
    notes: dword 2; record +0x08 and, when non-zero, the sole argument to FUN_10135180 -- the family-wide invalidate call. Inference.
  - name: unknown_3
    type: u32
    notes: dword 3; record +0x0c. Not named.
    evidence: L3
  - name: unknown_4
    type: bytes_4
    notes: 'dword 4 is SKIPPED by the reader: the cursor jumps from +0x0c to the end of the 0x14 block. It is the row''s string-offset column; the client writes the string pointer over record +0x10.'
    evidence: L3
  - name: ruleType
    type: lpstring
    notes: u32 length then that many bytes, no NUL (FUN_100d3680). Stored at record +0x10 over the skipped dword 4. A CHALLENGE_RULES_TYPE_* token -- the same vocabulary SMSG_LIST_BROKEN_CHALLENGE_RULES 0x05BA carries and C_Challenge.GetRuleLocalization resolves (ChallengeUtil.lua, L2).
    evidence: L3
# Read from the decompiled handler FUN_1013a670 (build/ghidra/handlers-c/0x05b8.c) read by hand, 2026-09-20. The capture reference (~/CoaServer/reference/cointhrow, L1 for us) reports '5 fields, raw + 1 string; dword 4 = the rule-type token string'. The skipped-dword-4 half of that re-derives from our own decompilation (L3); 'raw' does not -- the wire row here is five dwords while our patch-tables.yaml and the client's DBC give ChallengeRules.dbc five fields too, so raw and compact coincide for this table and the claim is untestable rather than confirmed.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# packet_layout.py had followed a call into 0x000d3680, not the handler. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x0013a670"
    registration_site_rva: "0x0013b71b"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: ChallengeRules.dbc
    instance: "0x10be0350"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 20
    fields: 5

provenance:
  - level: L3
    source: "the decompiled handler FUN_1013a670 (build/ghidra/handlers-c/0x05b8.c) read by hand, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml (CustomDBCMgr load idiom and the handler's table object)"
    claim: "the packet carries one row of ChallengeRules.dbc into the client's copy of that table"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05b8 is named SMSG_PATCH_CHALLENGE_RULE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
