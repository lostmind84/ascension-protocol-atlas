# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x09c7
name: CMSG_MISSILE_FIRE_POSITION
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Ten fields written from a caller-supplied struct (`param_1`, a `longlong*`, so its fields
  sit at 8-byte-aligned offsets): two full 8-byte slots (guidA, guidB), one lone u32 from the
  high half of a third slot, one lone byte from the low byte of a fourth slot, then three
  more 8-byte slots each split into two consecutive u32 writes (low dword, high dword). The
  server only logs and discards the payload (`DescribePacketPayload`, never parsed), so no
  field name below is server-confirmed; the struct-offset grouping is read straight from the
  decompiled sender and is the most this fiche can say without the sender's own caller.

layout:
  - name: guidA
    type: u64
    evidence: L3
    notes: "param_1[0] (offset 0x00-0x07), sent whole via one PutData(8). Likely a packed GUID given the width, not confirmed as one."
  - name: guidB
    type: u64
    evidence: L3
    notes: "param_1[1] (offset 0x08-0x0f), sent whole via one PutData(8)."
  - name: field_2
    type: u32
    evidence: L3
    notes: >
      *(u32*)(param_1 + 0x1c). Skeptic correction, 2026-09-20 (independent full
      re-decompilation of FUN_10322810, GHIDRA_WORKSPACE=verify-unclassified): the previous
      note claimed the slot's low dword (0x18-0x1b) "is read by the sender but never sent".
      That does not survive -- the re-decompiled function contains no reference to offset
      0x18 anywhere, not even a silent read; only 0x1c is touched. Whether 0x1c is genuinely
      the high half of an 8-byte value at 0x18-0x1f (by analogy with guidA/guidB's addressing,
      which is plausible given param_1's 8-byte-aligned layout) or an independent field with
      0x18-0x1b simply unused/padding by the caller is not established either way. Meaning
      not established.
  - name: field_3
    type: u8
    evidence: L3
    notes: >
      the low byte of param_1[4] (offset 0x20); the other 7 bytes of that slot are read but
      never sent. Meaning not established.
  - repeat: 6
    fields:
    - name: field
      type: u32
      evidence: L3
      notes: >
        Six PutData(4) calls, but not six independent fields: they are three 8-byte slots
        (param_1[7] at 0x38, param_1[8] at 0x40, param_1[9] at 0x48) each split into a low
        dword then a high dword by two consecutive calls. Whether each pair is one 64-bit
        value (a timestamp? a second GUID?) or two unrelated u32s is not established; only
        the pairing itself -- same slot, consecutive writes -- is read from the decompilation.
        Between field_3's slot and this group, param_1's offsets 0x28-0x37 (two more 8-byte
        slots) are never sent at all.
# width and order from the client's sender (DecompAt.java 10322810, GHIDRA_WORKSPACE=unclassified,
# 2026-09-20; matches the automated extraction in senders.yaml: fields [u64, u64, u32, u8, u32,
# u32, u32, u32, u32, u32]); names are placeholders -- the server's reader does not parse the
# payload, so there is no L1 name to lend here, unlike most fiches in this file.
# client sender: FUN_10322810 (opcode write at 0x0032297c, per senders.yaml's sender_site_rva)
# server reader: modules/mod-ascension-compat/src/AscensionCompat.cpp:5026: logged with
#   DescribePacketPayload, not parsed (the server's own comment there says the payload
#   "carries client projectile rendering coordinates", but that is the module author's gloss,
#   not a parsed field -- see provenance)
used_by_server:   # the CoA server module sends or handles this
  server_name: CMSG_MISSILE_FIRE_POSITION
  files:
    - modules/mod-ascension-compat/src/AscensionCompat.cpp

provenance:
  - level: L3
    source: "tools/ghidra/ghidra.sh run DecompAt.java 10322810 (GHIDRA_WORKSPACE=unclassified, 2026-09-20), full decompilation of the sender"
    claim: >
      the field widths, order and struct-offset grouping: two full 8-byte slots, a lone u32
      at a third slot's high half, a lone byte at a fourth slot's low byte, a gap of two
      unsent 8-byte slots, then three more 8-byte slots each split into two u32 writes
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence: [u64, u64, u32, u8, u32, u32, u32, u32, u32, u32])"
    claim: "the field widths and their order, as the client writes them -- cross-checked against the full decompilation above and in agreement"
  - level: L1
    source: "modules/mod-ascension-compat/src/AscensionCompat.cpp:5023-5031 (the CoA server's comment, not a parse)"
    claim: "the packet carries client-side projectile rendering coordinates for the missile already cast; the server needs none of it because AzerothCore already resolved the cast"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x09c7 is named CMSG_MISSILE_FIRE_POSITION"
  - level: L3
    source: >
      Skeptic re-check: tools/ghidra/ghidra.sh run DecompAt.java 10322810
      (GHIDRA_WORKSPACE=verify-unclassified, 2026-09-20), an independent full
      re-decompilation of the same sender
    claim: >
      retraction of one sub-claim on field_2: no instruction or decompiled reference to
      offset 0x18 (the field_2 slot's presumed low dword) exists anywhere in the function.
      The rest of the layout re-verifies exactly: guidA at 0x00 (u64), guidB at 0x08 (u64),
      field_2 at 0x1c (u32, the only reference to that slot), field_3 as the low byte of the
      8-byte load at 0x20, a real gap at 0x28-0x37 (param_1[5]/param_1[6], never referenced),
      then three low/high u32 pairs at 0x38/0x3c, 0x40/0x44 and 0x48/0x4c -- matching
      senders.yaml's [u64, u64, u32, u8, u32, u32, u32, u32, u32, u32] field by field.

replay: null
