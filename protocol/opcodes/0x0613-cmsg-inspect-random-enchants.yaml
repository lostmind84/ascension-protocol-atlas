# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0613
name: CMSG_INSPECT_RANDOM_ENCHANTS
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Inspect another player's random enchants: the binding Inspect(unit, ...) resolves the unit token to its GUID and sends the 8 bytes (InspectFrame.lua calls it with self.unit). The server answers with the inspected enchants (0x0614/0x0615 by name).

layout:
  - name: unitGuid
    type: u64
    evidence: L3
# Read from the decompiled binding (build/ghidra/senders-c, DumpC.java over the sender's function) and the UI's calls of it (client_binding).

# Widths and order, read out of the client's sender at 0x002ee0ec by tools/extract/client_sender.py.

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: Inspect
  function_rva: "0x002edf00"
  calls:
    - {file: "Interface/AddOns/Ascension_InspectUI/InspectFrame.lua", line: 197, args: ["self.unit", "true"]}

provenance:
  - level: L3
    source: "the decompiled binding (build/ghidra/senders-c, DumpC.java over the sender's function) and the UI's calls of it (client_binding)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0613 is named CMSG_INSPECT_RANDOM_ENCHANTS"

replay: null
