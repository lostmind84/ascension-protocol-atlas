# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06e0
name: SMSG_OPEN_COLLECTOR_CACHE_RESULT
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_OPEN_COLLECTOR_CACHE_RESULT by the client's own opcode table, the very next
  slot after CMSG_OPEN_COLLECTOR_CACHE (0x06df -- not ours, but very much alive: the Lua
  binding `OpenCollectorCache` at RVA 0x00194830 constructs a `CDataStore`, writes opcode
  0x6df, `PutData`s one caller-supplied `u32`, `Finalize`s and sends -- read directly from
  its decompilation in this pass). The natural reading is that 0x06e0 is that request's
  server-to-client answer, and the "Collector Cache" feature itself is real and patched
  into this client (four DBC tables -- `CollectorCacheTypes`/`RarityTypes`/`RarityRates`/
  `Items` -- loaded via the SMSG_PATCH_* stream at 0x06d9-0x06dc, plus Lua getters
  `GetCollectorCacheItemInfo`/`GetCollectorCacheItems`/`GetCollectorCacheRarityRatesInfo`/
  `GetCollectorCacheRarityTypeInfo`/`GetCollectorCacheTypeInfo`/`GetNumCollectorCacheItems`).
  But this specific opcode, the result, has no handler registered in this build: whatever
  the request opens, the client does not read a reply on 0x06e0 to learn about it. Either
  the result already lives in the patched DBC data the getters above read (so "opening"
  the cache needs no live reply), or it arrives some other way this pass did not trace
  (an item delivery, a UI refresh triggered off something else).

layout: []   # empty

# No `client:` block: there is no handler to point at. That absence is the strongest fact
# in this fiche and it is ours -- see the L3 provenance entries below.

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06e0 is named SMSG_OPEN_COLLECTOR_CACHE_RESULT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers no handler for 0x06e0 in this build"
    note: >
      Cross-checked with `protocol/known-baseline.yaml`: 0x06e0 is not one of
      AzerothCore's 1,306 standard opcodes (`opcode_count`), and not one of this server
      fork's own 42 additional opcodes either (`fork_opcode_count` -- SMSG_COA_*/
      CMSG_ANTICHEAT_* and the like, this checkout's own server-side additions, no more
      "AzerothCore standard" than 0x06e0 itself). Corrected from an earlier draft that
      cited "1,348 standard opcodes": that number is `opcode_count + fork_opcode_count`,
      and `known-baseline.yaml` (plus its generator, `tools/extract/ac_baseline.py`) keep
      those two fields separate on purpose -- `opcodes` is pinned to the last core
      revision before this fork's own additions, `fork_opcodes` is this fork's own, and
      the linter's ADR-0005 redundancy check reads only `opcodes`. So there is no stock
      Ascension.exe dispatch table entry to check separately -- Extensions.dll is the
      only place custom Ascension code lives, and it has none for this number.
  - level: L3
    source: >
      client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml and
      lua-bindings.yaml (`OpenCollectorCache` -> RVA 0x00194830), decompiled with
      `GHIDRA_WORKSPACE=cosmetics-store tools/ghidra/ghidra.sh run DecompAt.java 10194830`
      2026-09-20
    claim: >
      the Lua-exposed `OpenCollectorCache` constructs a `CDataStore`, writes opcode
      0x6df (`(*DAT_10bc90cc)(0x6df)`), `PutData`s one `u32` field taken from its
      argument, `Finalize`s and sends via `ClientServices::Send` -- matching
      `senders.yaml`'s own recovered entry for 0x06df (`fields: [u32]`) -- and returns
      without registering any callback or reading a reply
    note: >
      0x06df is not one of this pass's assigned opcodes and is not modified here; cited
      only as context for why 0x06e0's absence is notable rather than merely "unnamed
      slot, ignore".
  - level: L1
    source: >
      CoinThrow's capture reference, kept outside this repository at
      ~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md,
      section "Other families"
    claim: "\"06DF\"/\"06E0\" | CMSG/SMSG | open collector cache `{u32}` / result (no handler located)"
    note: >
      L1 per docs/EVIDENCE.md, but this is the closest match in this whole pass between
      the capture reference's claim and our own re-derivation: their CMSG field width
      (`{u32}`) matches what we read directly out of the sender's decompilation above,
      and their "no handler located" for the SMSG matches our own handlers.yaml scan
      exactly. Independent methods, same two facts.

open_questions:
  - "How does the client learn the cache's contents if not through 0x06e0? The patched
     DBC tables (0x06d9-0x06dc) already carry static type/rarity/item data; whether
     'opening' a cache needs any live reply at all, or whether the result travels on a
     different opcode entirely (an item push, a currency/inventory update), is not
     traced in this pass."
