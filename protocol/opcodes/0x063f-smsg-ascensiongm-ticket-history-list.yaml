# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x063f
name: SMSG_ASCENSIONGM_TICKET_HISTORY_LIST
direction: server_to_client
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_ASCENSIONGM_TICKET_HISTORY_LIST by the client's own opcode table. No handler
  is registered for this opcode anywhere in this build's handler-registration scan (532
  opcodes registered in total, this one not among them), and it is likewise absent from
  the read-sequence, handler-fire and handler-strings dumps. Layout is not established;
  there is no code to read it from. Its request, CMSG_ASCENSIONGM_TICKET_HISTORY_REQUEST
  (0x063E), likewise has no sender in this build (see that fiche). The server module does
  not send this opcode either (absent from client/server-layouts.md's 29 verified sends).

layout: []   # unknown

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x063f is named SMSG_ASCENSIONGM_TICKET_HISTORY_LIST"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan across every registrar call site in the DLL; 532 opcodes registered, this one absent) and the same build's layouts.yaml/handler-fire.yaml/handler-strings.yaml, also absent"
    claim: "no handler for this opcode is registered in this client build"

replay: null
