# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05ca
name: SMSG_CHALLENGE_CREATOR_QUERY_COMPLETION_RESPONSE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Answer to CMSG_CHALLENGE_CREATOR_QUERY_COMPLETION: the trial id, a count, then that many completion entries the client parses in a callee (0x88 bytes each per the fork's Opcodes.h comment), then TRIAL_COMPLETION_LIST_CHANGED(trialID) fired with format %s. The server's writer agrees on the first two fields.

# AzerothCore calls 0x05ca SMSG_COA_TRIAL_QUERY_RESULT. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout:
  - name: trialId
    type: lpstring
    evidence: L3
  - name: count
    type: u32
    evidence: L3
  - repeat: count
    fields:
    - name: entry
      type: unknown
      notes: 0x88 bytes per the fork's comment; parsed in a callee, shape not read here
      evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c, DumpC.java) and the CoA server's writer in mod-coa-challenges; the record size is the server repository's own comment in Opcodes.h (L1).

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# the asm idiom (packet_layout.py) saw the same reads minus the strings. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00123c10"
    registration_site_rva: "0x001242e5"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "TRIAL_COMPLETION_LIST_CHANGED"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: TRIAL_COMPLETION_LIST_CHANGED
    format: "%s"
    helper: FUN_10278c90
    fields: [null]   # the read filling each format position

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-coa-challenges/src/CoA.Challenges.History.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Scripts.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Trials.cpp

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c, DumpC.java) and the CoA server's writer in mod-coa-challenges; the record size is the server repository's own comment in Opcodes.h (L1)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05ca is named SMSG_CHALLENGE_CREATOR_QUERY_COMPLETION_RESPONSE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
