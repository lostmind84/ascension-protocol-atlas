# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0648
name: SMSG_SKILL_CARD_COLLECTION_LIST
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  The skill-card collection: a u32, a 96-byte block of 24 u32 copied into the skill-card service, a count, then that many 12-byte entries. Byte-exact against the capture reference's 440-byte payload (4 + 96 + 4 + 28 x 12); what the crosscheck called a conflict was its own reading of their struct[28] as a 28-byte record, where they mean 28 records of 12 bytes.

layout:
  - name: unknown_0
    type: u32
    evidence: L3
    notes: read first and stored at the skill-card service +0x00; the handler empties the collection container (end = begin) before reading it. The capture reference reports the whole leading 100 bytes zero in its sample (L1).
  - name: block
    type: bytes_96
    evidence: L3
    notes: 24 u32 copied one at a time into the skill-card service at +0x3c..+0x9c (the loop runs puVar6 from iVar5+0x3c to iVar5+0x9c, 0x60/4 = 24 iterations)
  - name: count
    type: u32
    evidence: L3
    notes: the entry count; the handler skips the loop entirely when it is zero. The capture reference observed 28, and 4 + 96 + 4 + 28 * 12 = 440 is its whole payload (L1).
  - repeat: count
    fields:
    - name: cardId
      type: u32
      evidence: L1
      notes: entry +0x00. The handler copies the entry's first eight bytes in a single 8-byte move, so this split into two u32 is the capture reference's; it reports 17 of its 28 ids resolving in SkillCard.dbc as kill/skill card rows, the other 11 sitting above the extracted table's id range.
    - name: unknown_1
      type: u32
      evidence: L1
      notes: entry +0x04; the capture reference reports zero in every row
    - name: copies
      type: u32
      evidence: L3
      notes: 'entry +0x08, read as its own u32 before the cursor advances by 0xc. The name is the capture reference''s: it observed 1 for 23 rows and 3 for the last 5, which is what makes it a copy count rather than a boundary artifact (L1).'
# Read from the decompiled handler (build/ghidra/handlers-c/0x0648.c), read by hand. The entry's first eight bytes are taken in one move, so the card-id/zero split inside them is the capture reference's (L1). See .agents/reports/conflicts.md.

# Read from the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock).

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x0031b3f0"
    registration_site_rva: "0x0031ba6e"
    evidence: L3

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x0648.c), read by hand"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0648 is named SMSG_SKILL_CARD_COLLECTION_LIST"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L1
    source: "CoinThrow's live-session capture reference, 2026-09-20 (private, kept outside the tree; our comparison is docs/crosscheck-cointhrow-2026-09-20.md)"
    claim: "the 440-byte payload, count = 28, the entry's card-id/zero/copies columns and the SkillCard.dbc hits"

replay: null
