# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x064a
name: SMSG_PATCH_CHARACTER_ADVANCEMENT
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One CharacterAdvancement.dbc row, patched into the client's own copy of the catalogue: an id the handler range-checks against the table object, then the row's columns as a long mixture of u32 and NUL-terminated strings. Only the prefix below has been read; the handler assembles a 179-column record and the rest of it is not followed. The capture reference reports the packet as unused on a live realm, the client loading its catalogue from its own DBC instead.

layout:
  - name: id
    type: u32
    notes: 'The row key. The handler keeps this first read and range-checks it before writing: it tests DAT_10bdf64c <= id <= DAT_10bdf648 with the row array at DAT_10bdf65c, which are +0x10, +0xc and +0x20 of the table object at 0x10bdf63c -- the instance client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml names CharacterAdvancement.dbc. That is the same range-check-then-replace-or-append pattern as the other patch handlers in this block.

      '
    evidence: L3
  - name: string_0
    type: cstring
    notes: 'Immediately after the id. NUL-scanned, its length measured, then copied with strcpy_s into a buffer the handler allocates for the row. Which DBC column it is was not read.

      '
    evidence: L3
  - name: block_0
    type: bytes_56
    notes: 'Fourteen consecutive u32. The decompiler takes them in three groups -- an 8-byte move at +0x00, a u32 at +0x08, then +0x0c..+0x18 and +0x1c..+0x2c, then +0x30 and +0x34 -- but the reads are contiguous and the cursor advances exactly 0x38 before the next string. Column mapping not read, so they are left as one block rather than fourteen invented names.

      '
    evidence: L3
  - name: string_1
    type: cstring
    notes: the second NUL-scanned string, copied the same way
    evidence: L3
  - name: body
    type: bytes_variable
    notes: 'NOT FOLLOWED. From here the handler continues for another ~700 decompiled lines: more strings, u32 runs, a group of u8 at one point, and several 8-byte reads, before assembling the whole thing into the row and firing CHARACTER_ADVANCEMENT_ENTRY_PATCHED. Mapping it onto the 179 columns of the 692-byte record is its own task and no part of it is claimed here.

      '
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x064a.c) traced with tools/helper_trace.py and read by hand as far as its first string pair, 2026-09-20. A PREFIX ONLY. The handler is 1055 decompiled lines assembling a 179-column row; everything after string_1 is not followed.

# Widths and order, read out of the handler at 0x001dd650 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x001dd650"
    registration_site_rva: "0x001e68f7"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "%b%s%s%u"
  - "ASCENSION_ENTRY_UPDATED"
  - "ASCENSION_KNOWN_ENTRY_UPDATED"
  - "CHARACTER_ADVANCEMENT_ENTRY_PATCHED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: ASCENSION_KNOWN_ENTRY_UPDATED
    args: [internalID]
    source: "Interface/AddOns/Ascension_ForcedPrimaryStat/PrimaryStat.lua:247"
  - event: ASCENSION_KNOWN_ENTRY_UPDATED
    args: []
    source: "Interface/FrameXML/Util/DraftUtil.lua:233"
  - event: CHARACTER_ADVANCEMENT_ENTRY_PATCHED
    args: [isNew, class, tab, entryID]
    source: "Interface/AddOns/Ascension_TalentUI/TalentTreeBase.lua:359"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: ASCENSION_ENTRY_UPDATED
    format: null
    helper: FUN_100d0a50
  - event: ASCENSION_KNOWN_ENTRY_UPDATED
    format: null
    helper: FUN_100b9560
  - event: ASCENSION_ENTRY_UPDATED
    format: null
    helper: FUN_100d0a50
  - event: ASCENSION_KNOWN_ENTRY_UPDATED
    format: null
    helper: FUN_100b9560

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: CharacterAdvancement.dbc
    instance: "0x10bdf63c"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 692
    fields: 179

provenance:
  - level: L1
    source: "the capture reference (CoinThrow, 2026-09-20, kept outside the tree -- see docs/crosscheck-cointhrow-2026-09-20.md)"
    claim: "the packet is unused on a live realm: the client loads the Character Advancement catalogue from its own DBC"
    note: >
      Not reproducible here and it changes no field. It is worth recording because it
      explains why no capture of this packet exists to check the prefix above against,
      and it lowers the priority of finishing the 179-column map: nothing observed has
      ever sent one of these. The handler is present and complete either way.
  - level: L3
    source: "build/ghidra/handlers-c/0x064a.c line 729 read against client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml"
    claim: "the leading u32 is range-checked against the CharacterAdvancement.dbc table object at 0x10bdf63c, so it is the row key"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x064a.c) traced with tools/helper_trace.py and read by hand as far as its first string pair, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x064a is named SMSG_PATCH_CHARACTER_ADVANCEMENT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
