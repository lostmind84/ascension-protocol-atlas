# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0672
name: SMSG_PLAYER_DATA
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Player data: the client reads one byte and keeps it. The capture shows a four-byte payload, so the packet may carry more than the client consumes.

layout:
  - name: unknown_0
    type: u8
    notes: 'The handler reads exactly one byte and stores it in the DLL global at 0x10d3d74a, then returns. The capture reference reports a four-byte payload holding a zero count; both hold if the server sends four bytes and the client consumes one, so the wire size is not settled here -- the client''s reading is.

      '
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), re-read 2026-09-20 against the capture reference.

# Read from the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock).

# Widths and order, read out of the handler at 0x00a4f4d0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00a4f4d0"
    registration_site_rva: "0x00a678ae"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/FrameXML/Util/C_NoviceNetwork.lua

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), re-read 2026-09-20 against the capture reference"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0672 is named SMSG_PLAYER_DATA"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
