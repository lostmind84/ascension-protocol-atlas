# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0722
name: SMSG_UNNAMED_0722   # PLACEHOLDER: the client's table answers 'unknown'; a handler exists
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One CharacterAdvancementEssence.dbc row, nine u32 in column order: the handler stores field i at slot i of a 0x24-byte record, replacing the row with that id or appending it. Column names are the CoA server module's reading of the same DBC (AscensionCoATalentData.cpp), not the client's.

layout:
  - name: id
    type: u32
    notes: CharacterAdvancementEssence.dbc column 0; the row key, range-checked against the table then stored at slot 0
    evidence: L3
  - name: level
    type: u32
    notes: 'column 1 (server: ESSENCE_LEVEL)'
    evidence: L3
  - name: key
    type: u32
    notes: 'column 2 (server: ESSENCE_KEY)'
    evidence: L3
  - name: flags_0
    type: u32
    notes: 'column 3 (server: ESSENCE_FLAGS)'
    evidence: L3
  - name: flags_1
    type: u32
    notes: column 4
    evidence: L3
  - name: flags_2
    type: u32
    notes: column 5
    evidence: L3
  - name: flags_3
    type: u32
    notes: column 6
    evidence: L3
  - name: abilityEssence
    type: u32
    notes: 'column 7 (server: ESSENCE_AE)'
    evidence: L3
  - name: talentEssence
    type: u32
    notes: 'column 8 (server: ESSENCE_TE)'
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x0722.c): nine u32 reads stored at record slots 0..8; column names from mod-ascension-compat AscensionCoATalentData.cpp (ESSENCE_* indexes, L1).

# Widths and order, read out of the handler at 0x001dd510 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x001dd510"
    registration_site_rva: "0x001e6bc5"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: CharacterAdvancementEssence.dbc
    instance: "0x10bdfc6c"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 36
    fields: 9

provenance:
  - level: L1
    source: "the capture reference (CoinThrow, 2026-09-20, kept outside the tree -- see docs/crosscheck-cointhrow-2026-09-20.md)"
    claim: "0x0722 is the essence candidate of the 0x0721-0x0724 block; handler 0x101DD510"
    note: >
      CORROBORATION, no level change. We read this handler filling the table object at
      0x10bdfc6c, which patch-tables.yaml names CharacterAdvancementEssence.dbc, and the
      column names come from the CoA module's reading of the same DBC. Their independent
      guess lands on the same table from the other side. Their handler address converts to
      exactly our registered handler RVA for this opcode (subtract ImageBase 0x10000000),
      and the same holds for all four of 0x0721-0x0724. That is a useful side result: for
      this block their addresses and ours describe the same code, which is not true
      everywhere -- docs/crosscheck-cointhrow-2026-09-20.md records six of their 232
      addresses that do not reproduce on our build.
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x0722.c): nine u32 reads stored at record slots 0..8; column names from mod-ascension-compat AscensionCoATalentData.cpp (ESSENCE_* indexes, L1)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "the client's table has no name for 0x0722"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
