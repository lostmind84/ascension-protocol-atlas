# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0908
name: SMSG_MYTHIC_PLUS_DUNGEON_STATE_CHANGED
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Mythic+ dungeon state: eight bytes, a state byte and two u32; the state selects the countdown, started or complete event.

layout:
  - name: unknown_0
    type: bytes_8
    notes: eight bytes read first
    evidence: L3
  - name: state
    type: u8
    notes: 5 is compared for MYTHIC_PLUS_COMPLETE(onTime); the other branches fire MYTHIC_PLUS_COUNTDOWN_STARTED(10) and MYTHIC_PLUS_STARTED
    evidence: L3
  - name: unknown_2
    type: u32
    evidence: L3
  - name: unknown_3
    type: u32
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock).

# Widths and order, read out of the handler at 0x002bb630 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x002bb630"
    registration_site_rva: "0x002bb40f"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "MYTHIC_PLUS_COUNTDOWN_STARTED"
  - "MYTHIC_PLUS_STARTED"
  - "MYTHIC_PLUS_COMPLETE"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: MYTHIC_PLUS_COUNTDOWN_STARTED
    args: []
    source: "Interface/AddOns/Ascension_MythicPlus/MythicPlusObjectiveTracker.lua:277"
  - event: MYTHIC_PLUS_COUNTDOWN_STARTED
    args: [seconds]
    source: "Interface/FrameXML/Util/C_Keystones.lua:300"
  - event: MYTHIC_PLUS_STARTED
    args: []
    source: "Interface/AddOns/Ascension_MythicPlus/MythicPlusObjectiveTracker.lua:271"
  - event: MYTHIC_PLUS_COMPLETE
    args: [onTime]
    source: "Interface/AddOns/Ascension_MythicPlus/MythicPlusCompleteBanner.lua:128"
  - event: MYTHIC_PLUS_COMPLETE
    args: [success]
    source: "Interface/FrameXML/Util/C_Keystones.lua:319"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: null
    format: "%u"
    helper: FUN_10278c90
    read_slots: [null]   # indices into the 4 reads this pass saw; the layout has 3, so not field numbers
  - event: null
    format: "%b"
    helper: FUN_10278c90
    read_slots: [1]   # indices into the 4 reads this pass saw; the layout has 3, so not field numbers

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0908 is named SMSG_MYTHIC_PLUS_DUNGEON_STATE_CHANGED"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
