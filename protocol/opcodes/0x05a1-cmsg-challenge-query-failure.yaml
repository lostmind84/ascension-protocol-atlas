# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05a1
name: CMSG_CHALLENGE_QUERY_FAILURE
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  A failures request the client sends empty; the CoA server's handler reads nothing from the payload and answers 0x05A2. The empty shape is confirmed on the client side too: `tools/extract/client_sender.py` located the send site at 0x00142f19 and recorded zero `PutData` calls between the opcode write and the send (L3, `senders.yaml`), independent of the server's own L1 reading. (`client/client-senders.md`'s separate hand-curated write-up of twelve opcodes does not cover this one; that document's silence is not evidence either way, and this fiche does not depend on it.)

# AzerothCore calls 0x05a1 CMSG_COA_QUERY_FAILURES. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout: []   # empty packet: the server's handler reads nothing from it (a deactivate tolerates an absent string)

# Widths and order, read out of the client's sender at 0x00142f19 by tools/extract/client_sender.py.
# No PutData between the opcode write and the send: the packet is empty.

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-ascension-compat/src/AscensionCompat.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Scripts.cpp

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: QueryChallengeFailures
  function_rva: "0x00142ec0"
  calls:
    - {file: "Interface/FrameXML/WorldMapFrame.lua", line: 246, args: []}

provenance:
  - level: L1
    source: "mod-coa-challenges CoA.Challenges.Scripts.cpp (the server's handler reads no field)"
    claim: "the packet is empty"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05a1 is named CMSG_CHALLENGE_QUERY_FAILURE"

replay: null
