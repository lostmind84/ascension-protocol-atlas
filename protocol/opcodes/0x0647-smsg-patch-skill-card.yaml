# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0647
name: SMSG_PATCH_SKILL_CARD
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of SkillCard.dbc: 12 dwords (48 bytes), of which the 4 at +0x14..+0x20 arrive as throwaway placeholders and are then overwritten by 4 length-prefixed strings the wire sends right after. All 12 fields are named from the client's own Lua-table builder for this row, FUN_1009bc60 (VA 0x1009bc60, client/symbols/.../lua-keys.yaml's f_1009bc60), which pushes each one by an explicit key literal read at the matching record offset; two of its key literals (Rank at +0x08, Type at +0x14) are .rdata strings the automatic extractor did not capture. lua-keys.yaml's own offset for CollectedRank (16) is a false pairing: FUN_1009bc60 shows CollectedRank is computed from a separate per-player hashmap, not read from the record, so that key is not one of this row's columns.

layout:
  - name: id
    type: u32
    notes: record +0x00, the row key -- range-checked against minId 0x10bdf508 / maxId 0x10bdf504, indexes the row table at 0x10bdf518, then passed to FUN_10133ac0.
    evidence: L3
  - name: CardID
    type: u32
    notes: 'record +0x04. Name and offset from the Lua-table builder FUN_1009bc60 (VA 0x1009bc60): `SetKey(param_1,"CardID"); SetInt(param_1,*(param_2+4))`.'
    evidence: L3
  - name: Rank
    type: u32
    notes: record +0x08. Key literal at 0x10b28c38 ("Rank", read back from the .rdata bytes), pushed by FUN_1009bc60 from `*(param_2+8)`.
    evidence: L3
  - name: ItemID
    type: u32
    notes: 'record +0x0c. FUN_1009bc60: `SetKey(param_1,"ItemID"); SetInt(param_1,*(param_2+0xc))`.'
    evidence: L3
  - name: SpellID
    type: u32
    notes: 'record +0x10. FUN_1009bc60: `SetKey(param_1,"SpellID"); SetInt(param_1,*(param_2+0x10))`; matches lua-keys.yaml''s `f_1009bc60` entry `{key: SpellID, offset: 16}`.'
    evidence: L3
  - name: Type
    type: cstring
    notes: record +0x14, the wire's 1st of 4 trailing strings, overwriting the placeholder dword read at this offset. Key literal at 0x10b28708 ("Type", read back from the .rdata bytes), pushed by FUN_1009bc60 from the string pointer at `*(param_2+0x14)`. lua-keys.yaml's automatic extractor never found this key (it only paired offset 0x10 with SpellID and, wrongly, with the unrelated runtime value CollectedRank -- see summary).
    evidence: L3
  - name: Expansion
    type: cstring
    notes: 'record +0x18, the wire''s 2nd trailing string. FUN_1009bc60: `SetKey(param_1,"Expansion"); SetString(param_1,*(param_2+0x18))`; matches lua-keys.yaml.'
    evidence: L3
  - name: Class
    type: cstring
    notes: 'record +0x1c, the wire''s 3rd trailing string. FUN_1009bc60: `SetKey(param_1,"Class"); SetString(param_1,*(param_2+0x1c))`; matches lua-keys.yaml.'
    evidence: L3
  - name: Quality
    type: cstring
    notes: 'record +0x20, the wire''s 4th trailing string -- sent as text (e.g. a quality name), not a numeric quality enum. FUN_1009bc60: `SetKey(param_1,"Quality"); SetString(param_1,*(param_2+0x20))`; matches lua-keys.yaml.'
    evidence: L3
  - name: IsWildcard
    type: u32
    notes: 'record +0x24, read as a C++ bool by the getter (`*(int *)(param_2+0x24) != 0`). FUN_1009bc60: `SetKey(param_1,"IsWildcard")`; matches lua-keys.yaml offset 36.'
    evidence: L3
  - name: IsDraftMode
    type: u32
    notes: 'record +0x28, read as a bool. FUN_1009bc60: `SetKey(param_1,"IsDraftMode")`; matches lua-keys.yaml offset 40.'
    evidence: L3
  - name: IsStarterCard
    type: u32
    notes: 'record +0x2c, read as a bool. FUN_1009bc60: `SetKey(param_1,"IsStarterCard")`; matches lua-keys.yaml offset 44. The record ends here (0x30 = 48 bytes = the 12 fields of SkillCard.dbc); IsCollected, CollectedProgress, CollectedRank, QualityCost and MaxRank, which FUN_1009bc60 also pushes, are computed from player state and a separate hashmap lookup keyed by SpellID, not read from this record.'
    evidence: L3
# Read from the decompiled Lua-table builder FUN_1009bc60 (Ghidra DecompAt.java 1009bc60, workspace named-lua-ui) and the decompiled handler (build/ghidra/handlers-c/0x0647.c), both read by hand; two key-literal strings resolved by reading Extensions.dll.ORIGINAL's .rdata bytes at their VAs (0x10b28c38, 0x10b28708), 2026-09-20.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x001e2f10"
    registration_site_rva: "0x001e68e6"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "unordered_map/set too long"

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: SkillCard.dbc
    instance: "0x10bdf4f8"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 48
    fields: 12

provenance:
  - level: L3
    source: "the decompiled Lua-table builder FUN_1009bc60 (Ghidra DecompAt.java 1009bc60, workspace named-lua-ui) and the decompiled handler (build/ghidra/handlers-c/0x0647.c), both read by hand; two key-literal strings resolved by reading Extensions.dll.ORIGINAL's .rdata bytes at their VAs (0x10b28c38, 0x10b28708), 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml (CustomDBCMgr load idiom and the handler's table object)"
    claim: "the packet carries one row of SkillCard.dbc into the client's copy of that table"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0647 is named SMSG_PATCH_SKILL_CARD"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
