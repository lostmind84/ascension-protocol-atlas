# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x064b
name: CMSG_WILDCARD_REROLL_UNLOCKED_STARTING_ABILITIES
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Reroll the unlocked starting abilities: no payload, the opcode alone. Sent by the Lua binding
  RerollUnlockedStartingAbilities, and only when a client-side gate passes: the binding first
  reads a shared state struct through FUN_10a315d0 and compares two of its u32 fields
  (offsets +0x198 and +0x19c); the packet is built and sent only when they are equal, otherwise
  the binding returns false to Lua without sending anything. The same struct and the same
  equality check gate the neighbouring 0x0643 CMSG_WILDCARD_ROLL_ABILITIES sender. Field
  semantics at +0x198/+0x19c are not established -- this only says the send is conditional, not
  what the condition means.

layout: []  # empty
# Read from the decompiled sender binding (DumpC.java over the DLL, 2026-09-20): opcode write, then Finalize and Send with no data write between them.

# Widths and order, read out of the client's sender at 0x00a2da9f by tools/extract/client_sender.py.
# No PutData between the opcode write and the send: the packet is empty.

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_WildCard/Dice/WildCardDice.lua
  - Interface/AddOns/Ascension_WildCard/Dice/WildCardStartingChoice.lua

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: RerollUnlockedStartingAbilities
  function_rva: "0x00a2da20"
  calls:
    - {file: "Interface/AddOns/Ascension_WildCard/Dice/WildCardStartingChoice.lua", line: 517, args: []}
    - {file: "Interface/AddOns/Ascension_WildCard/Dice/WildCardDice.lua", line: 1402, args: []}

provenance:
  - level: L3
    source: "the decompiled sender binding (DumpC.java over the DLL, 2026-09-20): opcode write, then Finalize and Send with no data write between them"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "build/ghidra/senders-c/0x064b.c (FUN_10a2da20, DumpC.java): `iVar2 = FUN_10a315d0(...); if (*(int*)(iVar2+0x198) == *(int*)(iVar2+0x19c)) { ...send... } else { return-false, no send }`, compared against build/ghidra/senders-c/0x0643.c (FUN_10a2dbe0) which gates its own send on the identical `FUN_10a315d0(); *(iVar3+0x198) == *(iVar3+0x19c)` check alongside three other client-side guards"
    claim: "the send is conditional on a client-side check shared with 0x0643's sender; the packet itself, when sent, still carries no payload"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x064b is named CMSG_WILDCARD_REROLL_UNLOCKED_STARTING_ABILITIES"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
