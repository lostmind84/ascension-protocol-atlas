# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x090c
name: SMSG_DISCORD_INITIALIZE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Discord initialize: no payload. The handler calls the Discord service's accessors and reads nothing from the store.

layout: []  # empty
# Read from the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20).

# Read from the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest is noted as not followed.

client:
  handler:
    rva: "0x00220ec0"
    registration_site_rva: "0x00220b3d"
    evidence: L3

provenance:
  - level: L3
    source: "the deserialization helper decompiled and traced (tools/helper_trace.py over DumpC.java's output, 2026-09-20)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md, the Discord paragraph naming handlers 0x10220E80/0x10220EC0/0x10221060 (third-party, not in this repo), checked 2026-09-20"
    claim: "CHECKED, NOT A CONTRADICTION. 0x10220EC0 matches this fiche's handler RVA exactly, corroborating the address. That paragraph also lists a payload shape 'initialize result {u32}', which at first glance could be read as claiming this opcode has a u32 payload -- it does not: our own opcodes.yaml names 0x052E CMSG_DISCORD_INITIALIZE_RESULT, a CLIENT-TO-SERVER opcode, and the paragraph's four payload shapes (join request u64, user-id update u64, activity join cstring, initialize result u32) line up one-for-one with the four CMSG_DISCORD_* senders (0x0529/0x052A/0x052B/0x052E), not with the three SMSG handler addresses it happens to list in the same sentence. Cross-checked against our own name table before writing this down."
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read as far as its first reads; the rest is noted as not followed"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x090c is named SMSG_DISCORD_INITIALIZE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
