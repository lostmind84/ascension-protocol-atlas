# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0971
name: SMSG_PATCH_CREATURE_SPELL_DATA
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of CreatureSpellData.dbc (bound_by: L1 ref, patch-table-names.md): the handler's single 36-byte bulk read exactly equals the table's 9-column/36-byte record (client DBC header). 5 of 9 columns are named by AzerothCore's own CreatureSpellDataEntry struct (L1, another project's reading of the same file); the other 4 are columns the core's format string marks 'x' (skipped) and never names.

layout:
  - name: id
    type: u32
    notes: 'DBC column 0 (index field, fmt ''n''); core: CreatureSpellDataEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar3,36]], one 36-byte bulk read == the full 9-column/36-byte CreatureSpellData.dbc record); name L1 (core DBCStructure.h, another project''s reading of the same file).'
    evidence: L1
  - name: spellId_0
    type: u32
    notes: 'DBC column 1; core: CreatureSpellDataEntry::spellId[0] (MAX_CREATURE_SPELL_DATA_SLOT=4). Width L3; name L1 (core DBCStructure.h).'
    evidence: L1
  - name: spellId_1
    type: u32
    notes: DBC column 2; spellId[1]. Width L3; name L1 (core DBCStructure.h).
    evidence: L1
  - name: spellId_2
    type: u32
    notes: DBC column 3; spellId[2]. Width L3; name L1 (core DBCStructure.h).
    evidence: L1
  - name: spellId_3
    type: u32
    notes: DBC column 4; spellId[3]. Width L3; name L1 (core DBCStructure.h).
    evidence: L1
  - name: field_5
    type: u32
    notes: DBC column 5, part of availability[4]; CreatureSpellDatafmt marks columns 5-8 'x' (skipped). Core comments the array but never stores it -- unnamed. Width L3 only.
    evidence: L3
  - name: field_6
    type: u32
    notes: DBC column 6, availability[4] continued, 'x'. Width L3 only, no naming source.
    evidence: L3
  - name: field_7
    type: u32
    notes: DBC column 7, availability[4] continued, 'x'. Width L3 only, no naming source.
    evidence: L3
  - name: field_8
    type: u32
    notes: DBC column 8, availability[4] continued, 'x'. Width L3 only, no naming source.
    evidence: L3
# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h CreatureSpellDataEntry + DBCfmt.h CreatureSpellDatafmt (L1: another project's reading of the same client DBC file, not ours).

# Widths and order, read out of the handler at 0x0022b940 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x0022b940"
    registration_site_rva: "0x00229162"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h CreatureSpellDataEntry + DBCfmt.h CreatureSpellDatafmt (L1: another project's reading of the same client DBC file, not ours)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "client/patch-table-names.md (the naming-sources sweep): 0x0971 bound_by 'L1 ref' to CreatureSpellData.dbc, 9 fields / 36 bytes, matching the client DBC header"
    claim: "this opcode's table is CreatureSpellData.dbc, at L1 (only the third-party reference names it; patch-tables.yaml does not bind this opcode)"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0971 is named SMSG_PATCH_CREATURE_SPELL_DATA"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
