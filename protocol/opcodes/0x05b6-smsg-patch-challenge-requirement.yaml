# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05b6
name: SMSG_PATCH_CHALLENGE_REQUIREMENT
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of ChallengeRequirements.dbc: nine dwords then the RequirementType token as a length-prefixed string. The runtime record is dword-equal to the wire block (0x24 bytes) with the string pointer written over dword 4, which the reader skips. Five dwords are named from the offsets the detailedChallenge requirement builder at 0x1013c1a1 reads: the row key, RequirementType (the string) and MiscValue1-3.

# AzerothCore calls 0x05b6 SMSG_COA_CHALLENGE_REQUIREMENT. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout:
  - name: id
    type: u32
    notes: dword 0; the row key -- range-checked against minId 0x10be02f4 / maxId 0x10be02f0, indexes the row table at 0x10be0304, then passed to FUN_10133ac0
    evidence: L3
  - name: unknown_1
    type: u32
    notes: dword 1; record +0x04. Gates the notify path (non-zero runs FUN_1013b8b0 / FUN_100996e0 and stamps a +2s refresh deadline). Not named.
    evidence: L3
  - name: unknown_2
    type: u32
    evidence: L3
    notes: dword 2; record +0x08 and, when non-zero, the sole argument to FUN_10135180 at the end of the handler -- the family-wide invalidate call. Read as the owning challenge; inference, not a read symbol.
  - name: unknown_3
    type: u32
    notes: dword 3; record +0x0c. Not named.
    evidence: L3
  - name: unknown_4
    type: bytes_4
    notes: 'dword 4 is SKIPPED by the reader: the cursor jumps from +0x0c to +0x14. It is the row''s string-offset column, and the client writes the string pointer over record +0x10.'
    evidence: L3
  - name: miscValue1
    type: u32
    notes: dword 5; record +0x14 = the Lua key MiscValue1 (detailedChallenge requirement builder at 0x1013c1a1, lua-keys.yaml). ChallengeItemMixin.lua:490 reads requirement.MiscValue1 (L2). The UI treats the three as floats via ChallengeUtil.ApplyMiscValueFormatting.
    evidence: L3
  - name: miscValue2
    type: u32
    notes: dword 6; record +0x18 = MiscValue2 (ChallengeItemMixin.lua:490, L2)
    evidence: L3
  - name: miscValue3
    type: u32
    notes: dword 7; record +0x1c = MiscValue3 (ChallengeItemMixin.lua:490, L2)
    evidence: L3
  - name: unknown_8
    type: u32
    notes: dword 8; record +0x20. No builder reads it. Not named.
    evidence: L3
  - name: requirementType
    type: lpstring
    notes: u32 length then that many bytes, no NUL (FUN_100d3680). Copied and stored at record +0x10, over the skipped dword 4; the builder reads +0x10 as RequirementType. A token the UI resolves through C_Challenge.GetRequirementLocalization (ChallengeUtil.lua:4, L2).
    evidence: L3
# Read from the decompiled handler FUN_10139cd0 (build/ghidra/handlers-c/0x05b6.c) read by hand, and client/symbols/ascension-extensions-2026-08-13/lua-keys.yaml, 2026-09-20. The capture reference (~/CoaServer/reference/cointhrow, L1 for us) reports the same list (0 ID, 4 RequirementType stored at runtime +0x10, 5-7 MiscValue1/2/3, 1-3 and 8 unresolved) and names the builder 0x1013C1A1 and this handler 0x10139CD0. All of it re-derives from our own decompilation and our own extracted Lua keys, so it is graded L3/L2 here instead of being carried at L1.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# packet_layout.py had followed a call into 0x000d3680, not the handler. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00139cd0"
    registration_site_rva: "0x0013b6f9"
    evidence: L3

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-coa-challenges/src/CoA.Challenges.History.cpp

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: ChallengeRequirements.dbc
    instance: "0x10be02e4"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 36
    fields: 9

provenance:
  - level: L3
    source: "the decompiled handler FUN_10139cd0 (build/ghidra/handlers-c/0x05b6.c) read by hand, and client/symbols/ascension-extensions-2026-08-13/lua-keys.yaml, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml (CustomDBCMgr load idiom and the handler's table object)"
    claim: "the packet carries one row of ChallengeRequirements.dbc into the client's copy of that table"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05b6 is named SMSG_PATCH_CHALLENGE_REQUIREMENT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
