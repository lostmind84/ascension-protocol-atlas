# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x074b
name: CMSG_DRAFT_ROLL
direction: client_to_server
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
  - ascension-exe-2010-06-25-stock

summary: >
  Named CMSG_DRAFT_ROLL by the client's own opcode table, in the same run as
  CMSG_DRAFT_START/STOP/SELECT/SKIP/RESET (0x0749-0x074E). Layout not established --
  but unlike its neighbours, no sender for it exists anywhere in this build: it is
  absent from the complete 1229-entry Lua binding list, unmatched by the
  opcode-immediate sender scan, and the one place its raw opcode value appears in
  Ascension.exe is inside an unrelated function, not a packet send (see provenance).
  This is the same negative shape 0x0749/0x074A have when scanned by immediate alone
  (they DO have a sender, just a computed one -- see their fiches); 0x074B has not
  been found to have one at all, computed or otherwise.

layout: []   # unknown -- no sender or handler found to read a layout from

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x074b is named CMSG_DRAFT_ROLL"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/lua-bindings.yaml (1229 Lua-callable functions, tools/extract/lua_bindings.py over Extensions.dll) and senders.yaml (the opcode-immediate CDataStore-write scan, tools/extract/client_sender.py)"
    claim: "no Lua binding named for the draft feature sends 0x074B, and the opcode never appears as a Put(u32)-preceding immediate in Extensions.dll -- the same absence the static scan reports for the computed 0x0749/0x074A senders, except FUN_100dd6e0 recovers those two by hand (see their fiches) and no equivalent has been found for 0x074B"
    note: >
      Negative evidence from a complete and a bounded-but-thorough source respectively,
      not proof that no sender exists anywhere (a third, still-uncomputed opcode
      pattern cannot be ruled out the way the immediate scan rules out a literal one).
  - level: L3
    source: "tools/ghidra/ghidra.sh run DecompAt.java 59f787, GHIDRA_WORKSPACE=draft-builds GHIDRA_PROGRAM=Ascension.exe.ORIGINAL (build ascension-exe-2010-06-25-stock, image_base 0x400000, so RVA 0x0019f787)"
    claim: "VA 0x0059f787 in Ascension.exe -- the address the capture reference names as the only place the byte pattern for 0x074B turns up in this executable -- decompiles to the middle of sub_59f787, a function that builds a formatted string from Player_C-derived fields and repeatedly calls a two-argument double-pushing helper (FUN_0084e2a0); it has no CDataStore construction, no opcode-then-PutData sequence and no call to the client's packet-send helpers. This is not a packet sender."
    note: >
      Re-derives, independently and at L3, the capture reference's own claim below --
      exactly the case docs/EVIDENCE.md asks for: their L1 observation is confirmed
      against our binary rather than trusted on their word.
  - level: L1
    source: "the capture reference (CoinThrow, 2026-09-20, kept outside the tree)"
    claim: "draft roll (0x074B) has no sender in this build, matching draft start/stop; the hit for its opcode value in Ascension.exe at VA 0x59F787 is a numeric argument, not a send"
    note: >
      Attributed, not authoritative for us per docs/EVIDENCE.md. Corroborated above at
      L3 for the Ascension.exe half of the claim; the capture reference is WRONG about
      0x0749/0x074A having no sender at all in this build -- FUN_100dd6e0 (Lua
      ToggleDraftMode) sends both via a computed opcode -- so its blanket "no sender"
      phrasing for the whole 0x0749-0x074B run is not by itself trusted for 0x074B
      either; it is trusted here only where our own scan and decompile agree with it.

open_questions:
  - "Whether CMSG_DRAFT_ROLL has a sender at all in this build, computed the way
     0x0749/0x074A are (FUN_100dd6e0 never writes the literal 0x749; it derives it from
     a boolean). The immediate scan and the full Lua-binding list both come back empty,
     but neither rules out a third computed-opcode pattern nobody has looked for yet. If
     the draft run has a client-side roll/reroll action with no visible sender, that
     would be the place to look; none has been found in the stock UI corpus."

replay: null
