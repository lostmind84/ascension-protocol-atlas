# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0724
name: SMSG_UNNAMED_0724   # PLACEHOLDER: the client's table answers 'unknown'; a handler exists
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One CharacterAdvancementTabTypes.dbc row, reduced to the id and two strings: the DLL keeps a compact record, not the 19-column DBC row. The first string is the tab name the CoA server module reads from column 1.

layout:
  - name: id
    type: u32
    notes: CharacterAdvancementTabTypes.dbc column 0; range-checked against the table
    evidence: L3
  - name: name
    type: cstring
    notes: column 1 per the server module (the tab token it upper-cases); copied into the row
    evidence: L3
  - name: string_1
    type: cstring
    notes: a second string the handler copies; which column is not read here
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x0724.c): u32 then two NUL-scanned strings; the name column from mod-ascension-compat AscensionCoATalentData.cpp (L1).

# Widths and order, read out of the handler at 0x001dea90 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x001dea90"
    registration_site_rva: "0x001e6be7"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: CharacterAdvancementTabTypes.dbc
    instance: "0x10bdfd68"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 76
    fields: 19

provenance:
  - level: L1
    source: "the capture reference (CoinThrow, 2026-09-20, kept outside the tree -- see docs/crosscheck-cointhrow-2026-09-20.md)"
    claim: "0x0721-0x0724 are Character Advancement data-record updates keyed by an id; handler 0x101DEA90"
    note: >
      Consistent with our reading -- CharacterAdvancementTabTypes.dbc is a Character
      Advancement table and the handler is keyed by the id it range-checks. They claim no
      payload for it. Their handler address converts to exactly our registered handler RVA
      for this opcode (subtract ImageBase 0x10000000), and the same holds for all four of
      0x0721-0x0724. That is a useful side result: for this block their addresses and ours
      describe the same code, which is not true everywhere -- docs/crosscheck-
      cointhrow-2026-09-20.md records six of their 232 addresses that do not reproduce on
      our build.
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x0724.c): u32 then two NUL-scanned strings; the name column from mod-ascension-compat AscensionCoATalentData.cpp (L1)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "the client's table has no name for 0x0724"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
