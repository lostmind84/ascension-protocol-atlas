# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0649
name: SMSG_SKILL_CARD_COLLECTED
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Skill cards collected: a count, then 9-byte records keyed by two u32; each fires SKILL_CARD_COLLECTED or SKILL_CARD_AUTO_REVEALED(cardID, rank) from the looked-up card.

layout:
  - name: count
    type: u32
    evidence: L3
  - repeat: count
    fields:
    - name: unknown_0
      type: u32
      notes: with the next u32 forms the FNV-1a key of the card looked up in the service's map; the event's cardID and rank come from that record
      evidence: L3
    - name: unknown_1
      type: u32
      evidence: L3
    - name: flag
      type: u8
      notes: chooses between SKILL_CARD_COLLECTED and SKILL_CARD_AUTO_REVEALED by the branch structure; not verified
      evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock).

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x0031b050"
    registration_site_rva: "0x0031ba82"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "SKILL_CARD_AUTO_REVEALED"
  - "SKILL_CARD_COLLECTED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: SKILL_CARD_AUTO_REVEALED
    args: [...]
    source: "Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardsFrame.lua:538"
  - event: SKILL_CARD_AUTO_REVEALED
    args: [cardID, rank]
    source: "Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardUnlockFrame/SkillCardUnlockFrame.lua:752"
  - event: SKILL_CARD_AUTO_REVEALED
    args: []
    source: "Interface/FrameXML/Util/SkillCardsUtil.lua:323"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: SKILL_CARD_AUTO_REVEALED
    format: "%u%u"
    helper: FUN_10278c90
    read_slots: [null, null]   # indices into the 4 reads this pass saw; the layout has 2, so not field numbers
  - event: SKILL_CARD_COLLECTED
    format: "%u%u"
    helper: FUN_10278c90
    read_slots: [null, null]   # indices into the 4 reads this pass saw; the layout has 2, so not field numbers

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0649 is named SMSG_SKILL_CARD_COLLECTED"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
