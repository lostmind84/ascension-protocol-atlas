# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0930
name: SMSG_PATCH_SPELL_MISSILE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_PATCH_SPELL_MISSILE by the client's own opcode table. The handler reads exactly 60 bytes (15 four-byte fields, the last two read together as one 8-byte load) and hands them to an id-keyed store; that width and field count match SpellMissile.dbc's own WDBC header exactly, so this looks like a full, unmodified row of that table -- but the opcode is not bound in patch-tables.yaml (the DLL's CustomDBCMgr table list), so the table identity itself rests only on the opcode's own name and the matching filename in the client DBC snapshot, and no source names any of the 15 columns.

layout:
  - name: field_0
    type: u32
    evidence: L3
  - name: field_1
    type: u32
    evidence: L3
  - name: field_2
    type: u32
    evidence: L3
  - name: field_3
    type: u32
    evidence: L3
  - name: field_4
    type: u32
    evidence: L3
  - name: field_5
    type: u32
    evidence: L3
  - name: field_6
    type: u32
    evidence: L3
  - name: field_7
    type: u32
    evidence: L3
  - name: field_8
    type: u32
    evidence: L3
  - name: field_9
    type: u32
    evidence: L3
  - name: field_10
    type: u32
    evidence: L3
  - name: field_11
    type: u32
    evidence: L3
  - name: field_12
    type: u32
    evidence: L3
  - name: field_13
    type: u32
    evidence: L3
  - name: field_14
    type: u32
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x0930.c), read by hand. packet_layout.py's own scan (the earlier, wrong stub) only followed 3 of the 15 field reads; the handler advances the CDataStore cursor by exactly 0x3c (60) bytes -- 13 individual 4-byte reads plus one 8-byte (2-field) read -- matching SpellMissile.dbc's own header (15 fields, 60 bytes) exactly, field for field, all numeric.

client:
  handler:
    rva: "0x00232830"
    registration_site_rva: "0x00228d8a"
    evidence: L3

dbc_identity:
  dbc: SpellMissile.dbc
  fields: 15
  record_size: 60
  rows: 170
  header_source: "ascension-live-2026-09-17 WDBC header, L2 (client/patch-table-names.md)"
  bound: >
    Not bound in client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml -- this opcode
    is not one of the DLL's 114 CustomDBCMgr table objects; per client/patch-tables.md it is one
    of the other 141 patch handlers, whose record is the DLL's own struct rather than a named
    table row. The table below is the one client/patch-table-names.md associates with this opcode
    by name and geometry, sourced only from a third-party reference (grade L1).
  reference_lead: "ref: stock (wowdev, L1) -- a third-party reference calls this a published stock Wrath table, unverified here"
  match: MATCH
  wire: >
    Thirteen individual 4-byte reads plus one 8-byte (2-field) read, all numeric, cursor advancing
    exactly 0x3c (60) bytes -- field-for-field and byte-for-byte the table's own header (15
    fields, 60 bytes).
  note: >
    This is the opcode whose earlier, auto-generated stub had the wrong widths
    (tools/extract/packet_layout.py only followed 3 of the 15 reads); see the layout's own
    provenance note above for the correction.

column_names: none found
  # checked: client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (no
  # string literal near the store); lua-keys.yaml (no builder in this table's domain);
  # the Ascension UI/Lua corpus (build/corpus/ascension-ui-patch-b-stock, searched for
  # the table's own base name, e.g. "SpellMissile").

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x0930.c), read by hand"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0930 is named SMSG_PATCH_SPELL_MISSILE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
