# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0948
name: SMSG_PATCH_SKILL_RACE_CLASS_INFO
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of the stock SkillRaceClassInfo.dbc (AzerothCore already loads this table; ADR 0005), sent as the single 32-byte blob the handler at RVA 0x002305d0 reads (L3). Nothing was elided (8 cols x 4 bytes = 32 bytes matches the client header exactly). 6 of 8 columns are named: ID is FT_SORT ('d', commented out of the struct but named in its comment) and SkillID/RaceMask/ClassMask/Flags/SkillTierID are active members; MinLevel and SkillCostIndex are core 'x' columns with no formal name even though a disused comment names them too (L1 throughout).

layout:
  - name: ID
    type: u32
    notes: 'FT_SORT (''d''): DBCStructure.h comments it //uint32 ID but SkillRaceClassInfoEntry does not keep it as a member'
    evidence: L1
  - name: SkillID
    type: u32
    notes: active SkillRaceClassInfoEntry member
    evidence: L1
  - name: RaceMask
    type: u32
    notes: active SkillRaceClassInfoEntry member
    evidence: L1
  - name: ClassMask
    type: u32
    notes: active SkillRaceClassInfoEntry member
    evidence: L1
  - name: Flags
    type: u32
    notes: active SkillRaceClassInfoEntry member
    evidence: L1
  - name: field_5
    type: u32
    notes: 'core format char ''x''/''X'' at this column: skipped, no name in DBCStructure.h'
    evidence: L1
  - name: SkillTierID
    type: u32
    notes: active SkillRaceClassInfoEntry member
    evidence: L1
  - name: field_7
    type: u32
    notes: 'core format char ''x''/''X'' at this column: skipped, no name in DBCStructure.h'
    evidence: L1
# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h (SkillRaceClassInfoEntry) and DBCfmt.h (SkillRaceClassInfofmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md.

client:
  handler:
    rva: "0x002305d0"
    registration_site_rva: "0x00228efb"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h (SkillRaceClassInfoEntry) and DBCfmt.h (SkillRaceClassInfofmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0948 is named SMSG_PATCH_SKILL_RACE_CLASS_INFO"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
