# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06bd
name: SMSG_CUSTOM_STORE_TYPE_LIST
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  The custom store's type list: a count, then per type three u32 and two strings, packed into a record for the store UI. The skeleton's two u32 were the count and the first field.

layout:
  - name: count
    type: u32
    evidence: L3
  - repeat: count
    fields:
    - name: unknown_0
      type: u32
      evidence: L3
    - name: unknown_1
      type: u32
      evidence: L3
    - name: unknown_2
      type: u32
      evidence: L3
    - name: string_0
      type: cstring
      evidence: L3
    - name: string_1
      type: cstring
      notes: the five values are packed into one record (FUN_101a2f00) the store UI reads; which is the type id and which the display name was not followed
      evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x06bd.c), read by hand.

# Widths and order, read out of the handler at 0x001a51a0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x001a51a0"
    registration_site_rva: "0x001a599c"
    evidence: L3

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x06bd.c), read by hand"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06bd is named SMSG_CUSTOM_STORE_TYPE_LIST"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
