# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0909
name: SMSG_MYTHIC_PLUS_PROGRESS_UPDATE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  The mythic+ progress packet: a count of changed encounters, then that many encounter IDs (each merged into the tracked-encounter map and fired as MYTHIC_PLUS_ENCOUNTER_UPDATE), then a trash-kill count and a champion-kill count, each fired as its own event only when it differs from what the client already holds.

layout:
  - name: encounterCount
    type: u32
    notes: read first (local_28 in the decompile) and used only as the bound of the do-while loop that follows; never itself passed to a Lua event.
    evidence: L3
  - repeat: encounterCount
    fields:
    - name: encounterID
      type: u32
      notes: one u32 per iteration (uVar12/local_1c); looked up in, and if absent inserted into, the MythicPlusMgr singleton's tracked-encounter map (FUN_100d14a0, FUN_10193460), then formatted with FUN_10278c90(&DAT_10b2c8f8 "%u", uVar12) and fired as MYTHIC_PLUS_ENCOUNTER_UPDATE with this same value -- matches the event's sole Lua argument, encounterID (Interface/AddOns/Ascension_MythicPlus/ MythicPlusObjectiveTracker.lua:297).
      evidence: L3
  - name: trashDead
    type: u32
    notes: 'read once, unconditionally, right after the loop (iVar7 at LAB_102bbbf4); compared against the MythicPlusMgr singleton''s stored value at +0xc and, only on a change, written there, then FUN_10087440("MYTHIC_PLUS_TRASH_UPDATE") and a fire through the wrapper FUN_100b9560 (whose own arguments the decompile hides). The event''s sole Lua argument is trashDead (MythicPlusObjectiveTracker.lua:289), and this read is the only packet field that gates that fire, which is why it carries that name here. Caveat: the value actually formatted before the fire (FUN_100c0270/FUN_101d0500) is read back from the SAME singleton''s +0x18, a different field than the +0xc this read is stored to -- so whether the wrapper ends up sending this packet''s raw value, or something else cached at +0x18, is not established from this decompile alone.'
    evidence: L3
  - name: championsDead
    type: u32
    notes: 'same shape as trashDead, one field later: read unconditionally, compared against and conditionally stored into the singleton''s +0x10, then FUN_10087440( "MYTHIC_PLUS_CHAMPIONS_UPDATE") and the same wrapper fire. Lua argument championsDead (MythicPlusObjectiveTracker.lua:293). Same +0x18 caveat as trashDead.'
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c/0x0909.c), read by hand and cross-checked with tools/helper_trace.py and the auto-extracted read/fire sequence in client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml.

# Superseded: the widths/order tools/extract/packet_layout.py read out of the handler were
# correct (a count, a repeated u32, then two trailing u32s -- see handler-fire.yaml's own
# `reads: [[local_28, 4], {repeat: local_28, reads: [[uVar12, 4]]}, [iVar7, 4], [iVar7, 4]]`),
# but an earlier pass of this fiche flattened that into four same-level fields
# (field_0/encounterID/field_2/field_3) instead of writing the repeat block. Corrected
# 2026-09-20 by reading the handler C directly.

client:
  handler:
    rva: "0x002bb850"
    registration_site_rva: "0x002bb41e"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "MYTHIC_PLUS_TRASH_UPDATE"
  - "MYTHIC_PLUS_CHAMPIONS_UPDATE"
  - "MYTHIC_PLUS_ENCOUNTER_UPDATE"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: MYTHIC_PLUS_TRASH_UPDATE
    args: [trashDead]
    source: "Interface/AddOns/Ascension_MythicPlus/MythicPlusObjectiveTracker.lua:289"
  - event: MYTHIC_PLUS_CHAMPIONS_UPDATE
    args: [championsDead]
    source: "Interface/AddOns/Ascension_MythicPlus/MythicPlusObjectiveTracker.lua:293"
  - event: MYTHIC_PLUS_ENCOUNTER_UPDATE
    args: [encounterID]
    source: "Interface/AddOns/Ascension_MythicPlus/MythicPlusObjectiveTracker.lua:297"

client_fire:   # what the handler fires; corrected 2026-09-20 against build/ghidra/handlers-c/0x0909.c (L3) -- see the retraction in provenance
  - event: MYTHIC_PLUS_ENCOUNTER_UPDATE
    format: "%u"
    helper: FUN_10278c90
    fields: [encounterID]   # the read filling the format position
  - event: MYTHIC_PLUS_TRASH_UPDATE
    format: null   # hidden by the wrapper; see trashDead's note
    helper: FUN_100b9560
  - event: MYTHIC_PLUS_CHAMPIONS_UPDATE
    format: null   # hidden by the wrapper; see championsDead's note
    helper: FUN_100b9560

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c/0x0909.c), read by hand and cross-checked with tools/helper_trace.py and the auto-extracted read/fire sequence in client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md, '0909/090A ... mythic+ progress/timer ... (0x102BB850/0x102BBDB0); events MYTHIC_PLUS_{STARTED,COMPLETE,ENCOUNTER_UPDATE,TRASH_UPDATE,CHAMPIONS_UPDATE,TIMER_UPDATE}' (third-party, not in this repo)"
    claim: "independently names the same handler address (0x102BB850 = this fiche's 0x002bb850 + image base) and lists MYTHIC_PLUS_TRASH_UPDATE and MYTHIC_PLUS_CHAMPIONS_UPDATE among its events -- corroborates the corrected client_fire above, not the retracted one"
  - level: L0
    source: "an earlier pass of this fiche (before 2026-09-20)"
    claim: "RETRACTED. The layout was a flat field_0/encounterID/field_2/field_3, and client_fire labeled all three fires MYTHIC_PLUS_ENCOUNTER_UPDATE. Both were wrong: the last two reads are trailing fields (not loop-body fields), and their fires are MYTHIC_PLUS_TRASH_UPDATE and MYTHIC_PLUS_CHAMPIONS_UPDATE -- tools/extract/handler_fire.py's EVENT_LIT regex only matches FUN_10086e30(name, offset) calls, so it never saw the plain FUN_10087440(\"MYTHIC_PLUS_TRASH_UPDATE\") / FUN_10087440(\"MYTHIC_PLUS_CHAMPIONS_UPDATE\") literals in this handler and kept re-using the last event name it had matched (MYTHIC_PLUS_ENCOUNTER_UPDATE) for both later wrapper fires. handler-fire.yaml's `reads:` list was already correct (count, repeat, then two trailing reads); only its `fires:` labels and this fiche's flattened layout were wrong."
  - level: L2
    source: "client/event-args.yaml (MYTHIC_PLUS_ENCOUNTER_UPDATE) joined through client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml"
    claim: "the field names, as the UI's method named after the event declares them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0909 is named SMSG_MYTHIC_PLUS_PROGRESS_UPDATE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
