# Generated by tools/codegen/stub_fiches.py, then completed by hand (naming-source closure pass).
id: 0x09d1
name: SMSG_PATCH_CHR_CLASSES_ROLES
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of ChrClassesRoles.dbc, written straight into the client's own copy of that table
  (patch-tables.yaml's CustomDBCMgr scan binds this opcode to it, and the decompile confirms
  the idiom: the row goes into the table object's rows array at (id - minId), bounds-checked
  against minId/maxId, exactly as client/patch-tables.md describes). The 11 fields are read
  off the wire in a flat sequential order (offsets 0x00..0x28) but the client re-orders 5 of
  them when it stores the row (see field_3/field_4/field_6/field_8/field_9 below) -- the
  table's own column order is therefore not the wire order. No source in this repository
  names any of the 11 columns: checked handler-strings.yaml, lua-keys.yaml and the UI corpus
  (see naming_source below).

layout:
  - name: field_0
    type: u32
    notes: "wire offset 0x00; stored at row slot 0 (the bounds-check id). width/order: L3. name: none -- no naming source."
    evidence: L3
  - name: field_1
    type: u32
    notes: "wire offset 0x04; stored at row slot 1, unmoved. width/order: L3. name: none -- no naming source."
    evidence: L3
  - name: field_2
    type: u32
    notes: "wire offset 0x08; stored at row slot 2, unmoved. width/order: L3. name: none -- no naming source."
    evidence: L3
  - name: field_3
    type: u32
    notes: >
      wire offset 0x0c; stored at row slot 5, not slot 3 -- the client re-orders this field
      when it writes the row (build/ghidra/handlers-c/0x09d1.c, re-read this pass:
      `puVar14[5] = uVar6`, where uVar6 is this field). width/order: L3. name: none -- no
      naming source.
    evidence: L3
  - name: field_4
    type: u32
    notes: >
      wire offset 0x10; stored at row slot 8, not slot 4 (`puVar14[8] = uVar7`). width/order:
      L3. name: none -- no naming source.
    evidence: L3
  - name: field_5
    type: u32
    notes: >
      wire offset 0x14; stored at row slot 3, not slot 5 (`puVar14[3] = uVar8`). width/order:
      L3. name: none -- no naming source.
    evidence: L3
  - name: field_6
    type: u32
    notes: >
      wire offset 0x18; stored at row slot 6, unmoved (`puVar14[6] = uVar9`). width/order:
      L3. name: none -- no naming source.
    evidence: L3
  - name: field_7
    type: u32
    notes: >
      wire offset 0x1c; stored at row slot 9, not slot 7 (`puVar14[9] = uVar10`). width/order:
      L3. name: none -- no naming source.
    evidence: L3
  - name: field_8
    type: u32
    notes: >
      wire offset 0x20; stored at row slot 4, not slot 8 (`puVar14[4] = uVar11`). width/order:
      L3. name: none -- no naming source.
    evidence: L3
  - name: field_9
    type: u32
    notes: >
      wire offset 0x24; stored at row slot 7, not slot 9 (`puVar14[7] = uVar12`). width/order:
      L3. name: none -- no naming source.
    evidence: L3
  - name: field_10
    type: u32
    notes: "wire offset 0x28; stored at row slot 10, unmoved. width/order: L3. name: none -- no naming source."
    evidence: L3

client:
  handler:
    rva: "0x001e0400"
    registration_site_rva: "0x001e68b0"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: ChrClassesRoles.dbc
    instance: "0x10bdfa50"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 44
    fields: 11

naming_source:
  result: none found
  checked:
    - "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml: 0x09d1 has no strings recorded"
    - "client/symbols/ascension-extensions-2026-08-13/lua-keys.yaml: 22 builders extracted; none in this table's domain (class/role)"
    - "build/corpus/ascension-ui-patch-b-stock, grepped for `ChrClassesRoles`: 0 files"

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml (CustomDBCMgr load idiom and the handler's table object)"
    claim: "the packet carries one row of ChrClassesRoles.dbc into the client's copy of that table"
  - level: L3
    source: "build/ghidra/handlers-c/0x09d1.c (decompiled handler, re-read this pass) and client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their wire order (flat, offsets 0x00-0x28), and the row-slot each one is stored at, which is not the same order for 5 of the 11 fields"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml, lua-keys.yaml, and build/corpus/ascension-ui-patch-b-stock (this pass)"
    claim: "no naming source for the table's 11 columns exists in any of the three"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x09d1 is named SMSG_PATCH_CHR_CLASSES_ROLES"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
