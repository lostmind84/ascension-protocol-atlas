# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0760
name: SMSG_CHARACTER_DEACTIVATE_RESULT
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Named SMSG_CHARACTER_DEACTIVATE_RESULT by the client's own opcode table. Layout and behaviour not
  established.

layout:
  - name: result
    type: cstring
    evidence: L3
    notes: "sent by SendResult(session, opcode, result): one status string"
# name from mod-ascension-compat's SendResult helper (L1); width and order from the decompiled handler (L3)
# decompiled: FUN_10190a30 scans a NUL-terminated string at the cursor and hands it to a std::string
# Widths and order, read out of the handler at 0x00190a30 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00190a30"
    registration_site_rva: "0x001911b4"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/GlueXML/CharacterSelect.lua

used_by_server:   # the CoA server module sends or handles this
  server_name: SMSG_ASCENSION_CHARACTER_DEACTIVATE_RESULT
  files:
    - modules/mod-ascension-compat/src/AscensionCharacterSelection.cpp
    - modules/mod-ascension-compat/src/AscensionCharacterSelection.h

client_strings:   # literals the handler references, from its decompilation
  - "DEACTIVATE_CHARACTER_OK"
  - "CHARACTER_DEACTIVATE_RESULT"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: CHARACTER_DEACTIVATE_RESULT
    format: "%b%s"
    helper: FUN_100b96e0

provenance:
  - level: L3
    source: "tools/ghidra/ghidra.sh run DecompAt.java <handler VA> (see client/server-layouts.md)"
    claim: "the handler reads one NUL-terminated string"
  - level: L1
    source: "AscensionCharacterSelection.cpp SendResult, client/server-layouts.yaml"
    claim: "field name"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0760 is named SMSG_CHARACTER_DEACTIVATE_RESULT"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
