# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0703
name: CMSG_CREATE_GM_TICKET
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Create a GM ticket: the binding CreateTicket(priority, category, title, content, characterName) starts with two bytes; the strings that follow go through a helper the scan does not follow (prefix only).

layout:
  - name: priority
    type: u8
    evidence: L3
  - name: category
    type: u8
    evidence: L3
# Read from the Lua binding that sends it (client_binding) and the UI's calls of it; the client's writes typed by tools/extract/client_sender.py.

# Widths and order, read out of the client's sender at 0x00255b2f by tools/extract/client_sender.py.
# A helper is called between the opcode write and the send: this is at best a
# PREFIX of the payload. An empty list here does not mean an empty packet.

client_binding:   # the Lua binding whose function sends this packet (client/symbols/ascension-extensions-2026-08-13/sender-bindings.yaml, L3) and the UI's calls (L2)
  name: CreateTicket
  function_rva: "0x00255a50"
  calls:
    - {file: "Interface/AddOns/Ascension_TicketUI/CreateTicketPanelMixin.lua", line: 154, args: ["priority", "category", "title", "content", "characterName"]}

provenance:
  - level: L3
    source: "the Lua binding that sends it (client_binding) and the UI's calls of it; the client's writes typed by tools/extract/client_sender.py"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/senders.yaml (client sender PutData sequence)"
    claim: "the field widths and their order, as the client writes them"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0703 is named CMSG_CREATE_GM_TICKET"

replay: null
