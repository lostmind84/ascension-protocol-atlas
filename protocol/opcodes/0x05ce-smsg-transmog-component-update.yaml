# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05ce
name: SMSG_TRANSMOG_COMPONENT_UPDATE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_TRANSMOG_COMPONENT_UPDATE by the client's own opcode table, sitting right
  after CMSG_TRANSMOG_COMPONENT_APPLY (0x05cd) and before SMSG_TRANSMOG_COMPONENT_SUCCESS
  (0x05d1, with two unnamed opcodes 0x05cf/0x05d0 between them) -- the natural reading is
  a server push of a changed component followed by a success ack for the apply, but
  nothing in this build has code to send either half of that: the client registers no
  handler for 0x05ce. The registration scan (`handlers.yaml`) is exhaustive over every
  static `push handler; push opcode; call <registrar>` site across all three registrars
  Extensions.dll uses (532 total registrations, including the far-higher 0x09bf in an
  unrelated family -- see that fiche), so the usual "Extensions.dll-only" caveat for a
  *standard* opcode (client/symbols/.../handlers.md) does not apply here: 0x05ce is
  outside AzerothCore's baseline entirely (protocol/known-baseline.yaml), so stock
  Ascension.exe code has no notion of it either -- there is nowhere else a handler could
  live.

layout: []   # empty

# No `client:` block: there is no handler to point at. That absence is the strongest fact
# in this fiche and it is ours -- see the L3 provenance entries below.

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05ce is named SMSG_TRANSMOG_COMPONENT_UPDATE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers no handler for 0x05ce in this build"
    note: >
      Cross-checked with `protocol/known-baseline.yaml`: 0x05ce is not one of
      AzerothCore's 1,306 standard opcodes (`opcode_count`), and not one of this server
      fork's own 42 additional opcodes either (`fork_opcode_count` -- SMSG_COA_*/
      CMSG_ANTICHEAT_* and the like, this checkout's own server-side additions, no more
      "AzerothCore standard" than 0x05ce itself). Corrected from an earlier draft that
      cited "1,348 standard opcodes": that number is `opcode_count + fork_opcode_count`,
      and `known-baseline.yaml` (plus its generator, `tools/extract/ac_baseline.py`) keep
      those two fields separate on purpose -- `opcodes` is pinned to the last core
      revision before this fork's own additions, `fork_opcodes` is this fork's own, and
      the linter's ADR-0005 redundancy check reads only `opcodes`. So unlike 0x0023
      (SMSG_GODMODE, a reused standard number) there is no stock Ascension.exe dispatch
      table entry to check separately -- Extensions.dll is the only place custom
      Ascension code lives, and it has none for this number.
  - level: L1
    source: >
      CoinThrow's capture reference, kept outside this repository at
      ~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md,
      section "Cosmetics / appearance" (the wardrobe/appearance system writeup)
    claim: >
      "0x0540-0x0559 + 0x05CD/0x05CE/0x05D1 + 0x0640-0x0642 (superseded -- no senders
      exist in the client build)", grouping this SMSG with CMSG_TRANSMOG_COMPONENT_APPLY
      (0x05cd) as part of the legacy suite.
    note: "L1 per docs/EVIDENCE.md; consistent with, but not identical to, our own handler-registration finding above (their claim is phrased in terms of senders, which for a server-to-client opcode is the wrong direction -- read here as agreement that the whole cluster is dead code)."

open_questions:
  - "Was this the reply half of a component apply/update exchange with 0x05cd, or an
     unrelated push? Nothing in this build reads it either way, so the pairing is a
     numbering inference, not established behaviour."

related:
  - 0x05cd
  - 0x05d1
