# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0559
name: CMSG_TRANSMOG_SPEC_SET_UPDATE
direction: client_to_server
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named CMSG_TRANSMOG_SPEC_SET_UPDATE by the client's own opcode table, the last member
  of the legacy 0x0540-0x0559 range. Unlike its neighbours this opcode does have one
  raw byte match for a `mov esi, 0x559` encoding in Extensions.dll's `.text` (RVA
  0x4e1010), but it is not real code: Ghidra's own analysis at that address reports
  "Control flow encountered bad instruction data" and halts, meaning the disassembly
  before and after it does not resolve to valid instructions either -- the match is a
  coincidental five-byte pattern inside non-code bytes, not a sender. No walkable
  `push 0x559` -> `Put(u32)` -> `Send` sequence exists there or anywhere else in the
  section. The capture reference does not name a specific modern-cluster opcode that
  replaces this one (unlike the rest of the legacy suite, which it maps to named
  successors); it only places 0x0559 inside the same superseded numeric range as a
  whole. What "spec" refers to here -- talent spec, a per-loadout appearance set, or
  something else -- is not established by anything read in this pass.

layout: []   # empty

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0559 is named CMSG_TRANSMOG_SPEC_SET_UPDATE"
  - level: L3
    source: >
      tools/extract/client_sender.py's push_sites() scan, run directly against
      ~/CoaServer/client/ascension-live/Extensions.dll.ORIGINAL (sha256
      0f8d847b3adc44a963606f0cd4f7938ad6fcd6f4d87feac3131c7153bdf3bb11) for 0x0559 alone,
      2026-09-20, followed by `GHIDRA_WORKSPACE=cosmetics-store tools/ghidra/ghidra.sh run
      DecompAt.java 104e1010`
    claim: >
      the byte scan finds one `mov esi, 0x559` encoding at RVA 0x4e1010, but Ghidra
      decompiles that address as `halt_baddata()` with "Control flow encountered bad
      instruction data" -- it is not a real instruction, so there is no sender for this
      opcode in this build
    note: >
      Byte-exhaustive (every offset in .text is checked, not just instruction
      boundaries), which is exactly why it can surface a coincidental match; the Ghidra
      check is what rules this one out rather than leaving it as an unresolved hit.
  - level: L1
    source: >
      CoinThrow's capture reference, kept outside this repository at
      ~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md,
      section "Cosmetics / appearance" (the wardrobe/appearance system writeup)
    claim: >
      "0x0540-0x0559 + 0x05CD/0x05CE/0x05D1 + 0x0640-0x0642 (superseded -- no senders
      exist in the client build)", grouping 0x0559 into the legacy range without naming
      a specific modern replacement for it the way it does for the rest of the suite.
    note: "L1 per docs/EVIDENCE.md; consistent with, but less specific than, our own L3 finding above."

open_questions:
  - "What does 'spec' name here? Neither the client's own table nor the capture
     reference gives a modern-cluster successor for this one opcode, unlike the rest of
     the legacy suite."
