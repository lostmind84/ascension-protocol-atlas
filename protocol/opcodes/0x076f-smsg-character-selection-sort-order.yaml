# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x076f
name: SMSG_CHARACTER_SELECTION_SORT_ORDER
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Named SMSG_CHARACTER_SELECTION_SORT_ORDER by the client's own opcode table. Layout and behaviour not
  established.

layout:
  - name: sortOrder
    type: cstring
    evidence: L3
# name from mod-ascension-compat's serializer (L1); width and order from the decompiled handler (L3)
# decompiled: FUN_100d3a00 scans one NUL-terminated string at the cursor and hands it to a std::string

client:
  handler:
    rva: "0x000d3a00"
    registration_site_rva: "0x000d4dc9"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/GlueXML/CharacterSelect.lua

used_by_server:   # the CoA server module sends or handles this
  server_name: SMSG_ASCENSION_CHARACTER_SORT_ORDER
  files:
    - modules/mod-ascension-compat/src/AscensionCharacterSelection.cpp
    - modules/mod-ascension-compat/src/AscensionCharacterSelection.h

provenance:
  - level: L3
    source: "tools/ghidra/ghidra.sh run Decomp.java <handler VA> (see client/server-layouts.md)"
    claim: "field widths and order, from the decompiled handler"
  - level: L1
    source: "mod-ascension-compat's serializer, client/server-layouts.yaml"
    claim: "field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x076f is named SMSG_CHARACTER_SELECTION_SORT_ORDER"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
