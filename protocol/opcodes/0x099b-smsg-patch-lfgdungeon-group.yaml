# Generated by tools/codegen/stub_fiches.py, then completed by hand (naming-source closure pass).
id: 0x099b
name: SMSG_PATCH_LFGDUNGEON_GROUP
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  A record patched into one of Extensions.dll's internal id-keyed containers -- looked up by id
  (RVA 0x000b1a80), allocated fresh as a 0x14-byte heap struct when new (RVA 0x000ae5d20), and
  inserted (RVA 0x000aaab0) -- not one of the 114 CustomDBCMgr `.data` tables
  client/patch-tables.md documents: nothing in the decompiled handler references a fixed .data
  table object (no `+0xc`/`+0x10`/`+0x20` min/max/rows idiom anywhere in it, unlike 0x09d1/0x09d2
  in this same shard, which do). client/patch-table-names.md's third-party reference names
  `LFGDungeonGroup.dbc` for this opcode (21 fields, 84-byte record) but the handler's fixed
  portion is 8 bytes, not 84 -- the widths do not match, on top of the binding already being
  unverified (`L1 ref`, absent from our own patch-tables.yaml scan). No source in this repository
  names the struct's own fields (see naming_source below).

layout:
  - name: field_0
    type: u32
    notes: >
      width: L3 (block-copy read, 8 bytes; re-verified this pass against the cached decompile,
      build/ghidra/handlers-c/0x099b.c, against
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming
      source (see naming_source below). 8 bytes does NOT match LFGDungeonGroup.dbc's claimed
      record_size (84 bytes, 21 fields, per client/patch-table-names.md) at all, which further
      undermines that already-unverified (L1) binding.
    evidence: L3
  - name: field_1
    type: u32
    notes: >
      width: L3 -- part of the same fixed 8-byte block as field_0 (wire offset 0x04);
      re-verified this pass against build/ghidra/handlers-c/0x099b.c and
      client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml. name: none -- no
      naming source.
    evidence: L3

client:
  handler:
    rva: "0x0022e460"
    registration_site_rva: "0x002293ea"
    evidence: L3

naming_source:
  result: none found
  checked:
    - "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml: 0x099b has no strings recorded"
    - "client/symbols/ascension-extensions-2026-08-13/lua-keys.yaml: 22 builders extracted; none in this table's domain"
    - "LFGDungeonGroup.dbc search in build/corpus/ascension-ui-patch-b-stock (grepped for the bare table name minus \".dbc\"): 0 files"
  claimed_table:
    dbc: LFGDungeonGroup.dbc
    fields: 21
    record_size: 84
    rows: 18
    bound_by: "L1 ref only (client/patch-table-names.md); absent from client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml's own CustomDBCMgr scan"
    width_check: "does NOT match the handler's fixed-portion read (8 bytes)"

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x099b.c (decompiled handler, re-read this pass) and client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order; the handler looks the id up in an internal container (RVA 0x000b1a80), allocates a fresh 0x14-byte struct when it is new (RVA 0x000ae5d20), and inserts it (RVA 0x000aaab0) -- not one of the 114 CustomDBCMgr .data table objects patch-tables.yaml enumerates"
  - level: L1
    source: "client/patch-table-names.md (opcode 0x099b row)"
    claim: "a third-party reference names the DBC table LFGDungeonGroup.dbc for this opcode; unverified by our own patch-tables.yaml scan and not treated as established here"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml, lua-keys.yaml, and build/corpus/ascension-ui-patch-b-stock (this pass)"
    claim: "no naming source for the struct's own fields exists in any of the three"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x099b is named SMSG_PATCH_LFGDUNGEON_GROUP"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
