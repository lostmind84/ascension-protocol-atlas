# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0933
name: SMSG_PATCH_SKILL_LINE
direction: server_to_client
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_PATCH_SKILL_LINE by the client's own opcode table. One row of the stock SkillLine.dbc
  (AzerothCore already loads this table; ADR 0005), sent as an opaque 12-byte blob (RVA 0x00230370, no
  per-field split available) followed by 4 lpstrings. SkillLineEntry has 4 active numeric members (id,
  categoryId, spellIcon, canLink) but only 12 bytes (3 dwords) are read before the strings, and the file has
  only 3 locale-string blocks (name, kept; description and alternateVerb, both commented out) against 4
  lpstrings observed on the wire -- neither count reconciles. This is the concrete counter-example that rules
  out "prefix = every numeric column, then one lpstring per locale block, in file order" as a safe general
  assumption for this bucket (see 0x092a's note): whatever this handler actually does, it is not that. No
  naming source was found that is safe to assert here. DBCStructure.h (SkillLineEntry) and DBCfmt.h
  (SkillLinefmt) were checked; nothing in the layout is renamed. Read against the real decompiled handler
  (build/ghidra/handlers-c/0x0933.c): the 12-byte prefix does resolve cleanly (id, then categoryId read but
  its value discarded by this handler -- not stored, though it is still consumed off the wire -- then
  skillCostID, matching the file's first 3 columns exactly), but the rest of the function is far more complex
  than "4 lpstrings": Ghidra shows what looks like an early return after the first string for one code path,
  and, reached from what reads as an exception-handler continuation, a second block that reads 5 more dwords
  and 3 more lpstrings before building the final 8-word record. Whether that second block is a genuine part
  of every read of this packet, or an artifact of how Ghidra represented this function's exception handling,
  is not established by this pass -- the summary above is left as "no naming source found" for the strings
  pending a dedicated re-read of this specific function, which needs more care than this sweep's budget.

layout:
  - name: field_0
    type: bytes_12
    evidence: L3
  - name: field_1
    type: lpstring
    evidence: L3
  - name: field_2
    type: lpstring
    evidence: L3
  - name: field_3
    type: lpstring
    evidence: L3
  - name: field_4
    type: lpstring
    evidence: L3
# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# packet_layout.py had followed a call into 0x000d3680, not the handler. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00230370"
    registration_site_rva: "0x00228dba"
    evidence: L3

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0933 is named SMSG_PATCH_SKILL_LINE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
