# Generated by tools/codegen/stub_fiches.py, then hand-filled (patch-unnamed-1 sweep, 2026-09-20).
# This is a CoA-custom DBC table row with NO naming source in anything this repo holds
# (see client/patch-table-names.md). field_N names are placeholders; do not rename them
# without a cited source (lua-keys.yaml, a UI getter, or handler-strings.yaml).

id: 0x0569
name: SMSG_PATCH_ITEM_ADDON
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  SMSG_PATCH_ITEM_ADDON: writes into ItemAddon.dbc's table object, but not as a plain row copy. The handler reads one id plus 12 numeric fields and 2 strings off the wire, looks up any existing record for that id (FUN_101a2690), and if one exists copies 16 words (64 bytes) of it into a local buffer before passing both the wire fields and that copy to a merge helper (FUN_102096d0) that this pass did not trace further. Either way the record this opcode ultimately holds is at most 16 fields (64 bytes) -- far short of ItemAddon.dbc's own 48-field, 192-byte row. No column name is established.

layout:
  - name: field_0
    type: u32
    notes: "offset 0; the id passed to the existing-record lookup and to the merge helper"
    evidence: L3
  - name: field_1
    type: bytes_8
    notes: "cursor advances 8 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further"
    evidence: L3
  - name: field_2
    type: u32
    notes: "offset 0xc"
    evidence: L3
  - name: field_3
    type: u32
    notes: "offset 0x10"
    evidence: L3
  - name: field_4
    type: u32
    notes: "offset 0x14"
    evidence: L3
  - name: field_5
    type: u32
    notes: "offset 0x18"
    evidence: L3
  - name: field_6
    type: u32
    notes: "offset 0x1c"
    evidence: L3
  - name: field_7
    type: u32
    notes: "offset 0x20"
    evidence: L3
  - name: field_8
    type: u32
    notes: "offset 0x24"
    evidence: L3
  - name: field_9
    type: u32
    notes: "offset 0x28"
    evidence: L3
  - name: field_10
    type: u32
    notes: "offset 0x2c"
    evidence: L3
  - name: field_11
    type: u32
    notes: "offset 0x30"
    evidence: L3
  - name: field_12
    type: u32
    notes: "offset 0x34"
    evidence: L3
  - name: field_13
    type: lpstring
    notes: "via FUN_100d3680"
    evidence: L3
  - name: field_14
    type: lpstring
    notes: "via FUN_100d3680"
    evidence: L3

client:
  handler:
    rva: "0x001e1800"
    registration_site_rva: "0x001e67bc"
    evidence: L3

patches_table:   # the client DBC table(s) this packet writes into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3 unless noted; record_size/fields/rows from client/patch-table-names.md's own header read against the ascension-live-2026-09-17 snapshot, L2)
  - dbc: ItemAddon.dbc
    instance: "0x10be0438"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 192
    fields: 48
    rows: 563770

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x0569.c (already-decompiled handler, read by hand this pass)"
    claim: "the widths, order and destination struct of the wire fields"
  - level: L3
    source: "build/ghidra/handlers-c/0x0569.c, the lookup/merge tail (FUN_101a2690, FUN_102096d0)"
    claim: "an existing record (16 words) is read and merged with the wire fields rather than the wire fields simply overwriting the row"
    note: "FUN_102096d0's field-by-field merge semantics were not traced; only the wire's own widths are claimed at L3"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (handler 0x0569)"
    claim: "no string literal is referenced near this handler's store -- checked, empty"
    note: "rules out an event name or format string that might have hinted at a field's purpose"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0569 is named SMSG_PATCH_ITEM_ADDON"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L1
    source: "client/patch-table-names.md (the naming-source sweep over all 228 SMSG_PATCH_* opcodes)"
    claim: "no lua-keys builder, UI getter, or DBCfmt entry names this table's columns; the reference community source offers no names for it either (bucket 'none found')"

replay: null
