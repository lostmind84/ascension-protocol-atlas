# Generated by tools/codegen/stub_fiches.py, then hand-filled (patch-unnamed-1 sweep, 2026-09-20).
# This is a CoA-custom DBC table row with NO naming source in anything this repo holds
# (see client/patch-table-names.md). field_N names are placeholders; do not rename them
# without a cited source (lua-keys.yaml, a UI getter, or handler-strings.yaml).

id: 0x06ac
name: SMSG_PATCH_TUTORIAL_KEYWORDS
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  SMSG_PATCH_TUTORIAL_KEYWORDS: writes a 17-field, 68-byte record (2 numeric fields, 7 strings, then 8 more numeric fields) into TutorialKeywords.dbc's table object -- the widest reduced record in this shard by string count, still well short of that table's own 64-field, 260-byte row. No column name is established.

layout:
  - name: field_0
    type: u32
    notes: "offset 0; bounds-checked against the table object's minId/maxId"
    evidence: L3
  - name: field_1
    type: u32
    notes: "offset 4"
    evidence: L3
  - name: field_2
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (1st of 7 consecutive gaps)"
    evidence: L3
  - name: field_3
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (2nd of 7)"
    evidence: L3
  - name: field_4
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (3rd of 7)"
    evidence: L3
  - name: field_5
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (4th of 7)"
    evidence: L3
  - name: field_6
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (5th of 7)"
    evidence: L3
  - name: field_7
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (6th of 7)"
    evidence: L3
  - name: field_8
    type: bytes_4
    notes: "cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (7th of 7)"
    evidence: L3
  - name: field_9
    type: u32
    notes: "offset 0x24"
    evidence: L3
  - name: field_10
    type: u32
    notes: "offset 0x28"
    evidence: L3
  - name: field_11
    type: u32
    notes: "offset 0x2c"
    evidence: L3
  - name: field_12
    type: u32
    notes: "offset 0x30"
    evidence: L3
  - name: field_13
    type: u32
    notes: "offset 0x34"
    evidence: L3
  - name: field_14
    type: u32
    notes: "offset 0x38"
    evidence: L3
  - name: field_15
    type: u32
    notes: "offset 0x3c"
    evidence: L3
  - name: field_16
    type: u32
    notes: "offset 0x40"
    evidence: L3
  - name: field_17
    type: cstring
    notes: "1st of 7, NUL-terminated, scanned byte by byte; not FUN_100d3680"
    evidence: L3
  - name: field_18
    type: cstring
    notes: "2nd of 7"
    evidence: L3
  - name: field_19
    type: cstring
    notes: "3rd of 7"
    evidence: L3
  - name: field_20
    type: cstring
    notes: "4th of 7"
    evidence: L3
  - name: field_21
    type: cstring
    notes: "5th of 7"
    evidence: L3
  - name: field_22
    type: cstring
    notes: "6th of 7"
    evidence: L3
  - name: field_23
    type: cstring
    notes: "7th of 7"
    evidence: L3

client:
  handler:
    rva: "0x001e44c0"
    registration_site_rva: "0x001e69eb"
    evidence: L3

patches_table:   # the client DBC table(s) this packet writes into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3 unless noted; record_size/fields/rows from client/patch-table-names.md's own header read against the ascension-live-2026-09-17 snapshot, L2)
  - dbc: TutorialKeywords.dbc
    instance: "0x10bdf714"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 260
    fields: 64
    rows: 483

provenance:
  - level: L3
    source: "build/ghidra/handlers-c/0x06ac.c (already-decompiled handler, read by hand this pass)"
    claim: "the widths, order and destination struct of the wire fields"
  - level: L3
    source: "build/ghidra/handlers-c/0x06ac.c, the 7-string tail and the destination struct fill"
    claim: "the 7 cstrings occupy struct slots 2-8 (between field_1 and field_9), matching the 7 consecutive wire gaps at offsets 8-0x23"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-strings.yaml (handler 0x06ac)"
    claim: "no string literal is referenced near this handler's store -- checked, empty"
    note: "rules out an event name or format string that might have hinted at a field's purpose"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06ac is named SMSG_PATCH_TUTORIAL_KEYWORDS"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L1
    source: "client/patch-table-names.md (the naming-source sweep over all 228 SMSG_PATCH_* opcodes)"
    claim: "no lua-keys builder, UI getter, or DBCfmt entry names this table's columns; the reference community source offers no names for it either (bucket 'none found')"

replay: null
