# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0580
name: SMSG_GROUP_FINDER_CREATE_LISTING_RESPONSE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Answer to a group-finder create-listing request: a u32 result code, 0 meaning ok, then -- on success only -- one length-prefixed string the handler reads with FUN_100d3680 and hands to GROUP_FINDER_ACTIVITY_CREATED(%s); on failure the code indexes the CREATE_LISTING_* table (client_enums) and GROUP_FINDER_ACTIVITY_CREATION_FAILED fires with that string. The string after a success is what the handler passes to the event: the listing's identity, not read further.

layout:
  - name: result
    type: u32
    notes: 0 = ok; otherwise an index into the client_enums table
    evidence: L3
  - name: listing
    type: lpstring
    notes: present only when result is 0
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c, DumpC.java), the result-code table it indexes (client_enums) and the UI's event arguments.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# the asm idiom (packet_layout.py) saw the same reads minus the strings. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x002709b0"
    registration_site_rva: "0x00272400"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "UNEXPECTED_ENUM_VALUE_{}"
  - "GROUP_FINDER_ACTIVITY_CREATED"
  - "CREATE_LISTING_OK"
  - "GROUP_FINDER_ACTIVITY_CREATION_FAILED"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: GROUP_FINDER_ACTIVITY_CREATED
    format: "%s"
    helper: FUN_100b9620
  - event: GROUP_FINDER_ACTIVITY_CREATION_F
    format: "%s"
    helper: FUN_100b9620

client_enums:   # string tables the handler indexes by a packet value (client/symbols/ascension-extensions-2026-08-13/result-tables.yaml, L3)
  - field: result   # value i means values[i]; the handler checks i < 5
    table_va: "0x10b40440"
    values: [CREATE_LISTING_OK, CREATE_LISTING_ALREADY_LISTED, CREATE_LISTING_NOT_LEADER, CREATE_LISTING_IN_GROUP, CREATE_LISTING_INVALID_LISTING]

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c, DumpC.java), the result-code table it indexes (client_enums) and the UI's event arguments"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0580 is named SMSG_GROUP_FINDER_CREATE_LISTING_RESPONSE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
