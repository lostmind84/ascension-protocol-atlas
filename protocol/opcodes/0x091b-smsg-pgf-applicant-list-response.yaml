# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x091b
name: SMSG_PGF_APPLICANT_LIST_RESPONSE
direction: server_to_client
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_PGF_APPLICANT_LIST_RESPONSE by the client's own opcode table. Not among the 532 handlers the
  exhaustive push/push/call registrar scan of `.text` finds
  (`tools/extract/opcode_handlers.py`, three registrars,
  `client/symbols/ascension-extensions-2026-08-13/handlers.yaml`); this build registers
  no handler for it, and the server modules in this fork never send it either
  (`client/server-usage.yaml` has no entry for it, not even a false-positive literal).
  Layout is not established because there is nothing to read, not because nobody has
  looked.

layout: []   # unknown -- confirmed absent, not merely unexamined; see the provenance below

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x091b is named SMSG_PGF_APPLICANT_LIST_RESPONSE"
  - level: L3
    source: >
      client/symbols/ascension-extensions-2026-08-13/handlers.yaml
      (tools/extract/opcode_handlers.py's exhaustive push-imm32/push-imm32/call-rel32 scan
      of the whole .text section, 532 handlers found across three registrars)
    claim: "opcode 0x091b is not among the registered handlers; this build has no client-side handler for it"
  - level: L3
    source: "client/server-usage.yaml (server_usage.py's literal scan of modules/mod-ascension-compat and modules/mod-coa-challenges, 2026-09-20)"
    claim: "opcode 0x091b has no entry at all, not even a false-positive bare-literal match; the CoA server fork never references it"

replay: null
