# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x058d
name: SMSG_UPDATE_CONFIGS
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  The challenge config sync: five keyed sections -- ints, bools, two float sections and an optional list section -- each a count then (key, value) pairs; the client diffs them against what it holds and fires INT/BOOL/RATE_CONFIG_UPDATED(key, old, new). The fork's writer only sends the first section's shape.

# AzerothCore calls 0x058d SMSG_COA_CONFIG. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout:
  - name: intCount
    type: u32
    evidence: L3
  - repeat: intCount
    fields:
    - name: key
      type: lpstring
      notes: u32 length then the bytes
      evidence: L3
    - name: value
      type: u32
      notes: INT_CONFIG_UPDATED(key, old, new) when it changed
      evidence: L3
  - name: boolCount
    type: u32
    evidence: L3
  - repeat: boolCount
    fields:
    - name: key
      type: lpstring
      evidence: L3
    - name: value
      type: u8
      notes: BOOL_CONFIG_UPDATED(key, old, new)
      evidence: L3
  - name: rateCount
    type: u32
    evidence: L3
  - repeat: rateCount
    fields:
    - name: key
      type: lpstring
      evidence: L3
    - name: value
      type: f32
      notes: RATE_CONFIG_UPDATED(key, old, new)
      evidence: L3
  - name: rateCount2
    type: u32
    evidence: L3
  - repeat: rateCount2
    fields:
    - name: key
      type: lpstring
      evidence: L3
    - name: value
      type: f32
      notes: a second float section with the same event
      evidence: L3
  - name: listSection
    type: bytes_variable
    notes: 'only when 8 or more bytes remain: a count, then per entry a key, a u32 count and that many u32 -- list-valued configs, the tail not followed to the end'
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock).

# Widths and order, read out of the handler at 0x00196c90 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00196c90"
    registration_site_rva: "0x00198222"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "INT_CONFIG_UPDATED"
  - "%s%u%u"
  - "%s%b%b"
  - "%s%f%f"
  - "unordered_map/set too long"
  - "BOOL_CONFIG_UPDATED"
  - "FLOAT_CONFIG_UPDATED"
  - "RATE_CONFIG_UPDATED"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: INT_CONFIG_UPDATED
    format: "%s%u%u"
    helper: FUN_10278c90
    read_slots: [6, 16]   # indices into the 20 reads this pass saw; the layout has 7, so not field numbers
  - event: BOOL_CONFIG_UPDATED
    format: "%s%b%b"
    helper: FUN_10278c90
    read_slots: [0, null]   # indices into the 20 reads this pass saw; the layout has 7, so not field numbers
  - event: RATE_CONFIG_UPDATED
    format: "%s%f%f"
    helper: FUN_10278c90
    read_slots: [0, null, 11]   # indices into the 20 reads this pass saw; the layout has 7, so not field numbers

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-coa-challenges/src/CoA.Challenges.Core.cpp

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x058d is named SMSG_UPDATE_CONFIGS"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
