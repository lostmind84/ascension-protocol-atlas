# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0932
name: SMSG_PATCH_ITEM
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of the stock Item.dbc (AzerothCore already loads this table; ADR 0005). All 8 columns are active ItemEntry members, so all 8 are named here from DBCStructure.h/Itemfmt (L1, another project's reading of the same file, not independently reproduced from client Lua or a fresh decompile) while the widths were independently read per-field from the client's own handler at RVA 0x0022d960 (L3, see client/symbols/.../handler-fire.yaml). The fiche grades L1 because the weakest claim (the names) is L1.

layout:
  - name: ID
    type: u32
    notes: active ItemEntry member
    evidence: L1
  - name: ClassID
    type: u32
    notes: active ItemEntry member
    evidence: L1
  - name: SubclassID
    type: u32
    notes: active ItemEntry member
    evidence: L1
  - name: SoundOverrideSubclassID
    type: i32
    notes: active ItemEntry member (int32)
    evidence: L1
  - name: Material
    type: i32
    notes: active ItemEntry member (int32)
    evidence: L1
  - name: DisplayInfoID
    type: u32
    notes: active ItemEntry member
    evidence: L1
  - name: InventoryType
    type: u32
    notes: active ItemEntry member
    evidence: L1
  - name: SheatheType
    type: u32
    notes: active ItemEntry member
    evidence: L1
# Read from AzerothCore src/server/shared/DataStores/DBCStructure.h (ItemEntry) and DBCfmt.h (Itemfmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md.
# Widths and order were separately read out of the handler at 0x0022d960 by tools/extract/packet_layout.py
# (L3, matches the DBC's 8-column/32-byte layout exactly, so nothing is elided on the wire).

client:
  handler:
    rva: "0x0022d960"
    registration_site_rva: "0x00228dab"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h (ItemEntry) and DBCfmt.h (Itemfmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0932 is named SMSG_PATCH_ITEM"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
