# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x072b
name: SMSG_UNNAMED_072B   # PLACEHOLDER: the client's table answers 'unknown'; a handler exists
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Result of purging every learned talent: one result token. The handler fires CHARACTER_ADVANCEMENT_PURGE_TALENTS_RESULT(success, result).

layout:
  - name: result
    type: cstring
    notes: a CA_PURGE_TALENTS_* token, matched against a table of 10 strings starting at CA_PURGE_TALENTS_OK; index 0 is success
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c) read next to the CHARACTER_ADVANCEMENT_*_RESULT events' Lua arguments (L2, corpus ascension-ui-patch-b-stock).

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x00170950"
    registration_site_rva: "0x001722bc"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "CA_PURGE_TALENTS_OK"
  - "CHARACTER_ADVANCEMENT_PURGE_TALENTS_RESULT"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: CHARACTER_ADVANCEMENT_PURGE_TALENTS_RESULT
    args: [success, result]
    source: "Interface/AddOns/Ascension_CharacterAdvancement/CharacterAdvancement.lua:2837"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: CHARACTER_ADVANCEMENT_PURGE_TALE
    format: "%b%s"
    helper: FUN_100b96e0

provenance:
  - level: L1
    source: "the capture reference (CoinThrow, 2026-09-20, kept outside the tree -- see docs/crosscheck-cointhrow-2026-09-20.md)"
    claim: "0x0728-0x072C are a family of single C-string payloads carrying Character Advancement UI text"
    note: >
      CONFLICT, and ours is the finer reading. The reference describes 0x0728-0x072C as
      one family carrying 'single C-string payloads (CA UI dynamic text)'. That is not
      what this handler does: it matches a leading result token against a fixed string
      table and then reads the fields its event format string demands, and the event's Lua
      arguments in the UI corpus match those fields one for one. The result token is a C
      string, so a capture that only looked at the first bytes would read as they
      describe. They claim no field list, so nothing here is contradicted at field level
      -- but the family characterisation is wrong for this opcode and should not be
      carried forward.
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c) read next to the CHARACTER_ADVANCEMENT_*_RESULT events' Lua arguments (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "the client's table has no name for 0x072b"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
