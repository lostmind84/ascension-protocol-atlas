# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05b9
name: SMSG_PATCH_CHALLENGE_SPELL
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of ChallengeSpells.dbc: ten dwords and no string. The runtime record (0x28 bytes) is a byte-for-byte copy of the wire block -- this is the only table in the challenge patch family with no string column, so nothing is stitched over. Six dwords are named from the offsets the detailedChallenge spell builder at 0x1013c60f reads. The skeleton's five u64 were pairs of u32: the reader loads +0x1c..+0x20 with one 8-byte move, the rest one dword at a time.

layout:
  - name: id
    type: u32
    notes: dword 0; the row key -- range-checked against minId 0x10be03a8 / maxId 0x10be03a4, indexes the row table at 0x10be03b8, then passed to FUN_10133ac0
    evidence: L3
  - name: unknown_1
    type: u32
    notes: dword 1; record +0x04. Gates the notify path (non-zero runs FUN_1013b8b0 / FUN_100996e0 and stamps a +2s refresh deadline). Not named.
    evidence: L3
  - name: unknown_2
    type: u32
    evidence: L3
    notes: dword 2; record +0x08 and, when non-zero, the sole argument to FUN_10135180 -- the family-wide invalidate call. Read as the owning challenge; inference, not a read symbol.
  - name: unknown_3
    type: u32
    notes: dword 3; record +0x0c. Not named.
    evidence: L3
  - name: pveSpellId
    type: u32
    notes: dword 4; record +0x10 = the Lua key PvESpellID (detailedChallenge spell builder at 0x1013c60f, lua-keys.yaml). ChallengeExtendedInfoMixin.lua:148 reads aura.PvESpellID (L2).
    evidence: L3
  - name: pvpSpellId
    type: u32
    notes: dword 5; record +0x14 = PvPSpellID (ChallengeExtendedInfoMixin.lua:149, L2)
    evidence: L3
  - name: unknown_6
    type: u32
    notes: dword 6; record +0x18. The builder skips it. Not named.
    evidence: L3
  - name: isDungeonPlayerSpell
    type: u32
    notes: dword 7; record +0x1c = IsDungeonPlayerSpell (lua-keys.yaml)
    evidence: L3
  - name: isDungeonBossSpell
    type: u32
    notes: dword 8; record +0x20 = IsDungeonBossSpell (lua-keys.yaml)
    evidence: L3
  - name: isDungeonMinionSpell
    type: u32
    notes: dword 9; record +0x24 = IsDungeonMinionSpell (lua-keys.yaml)
    evidence: L3
# Read from the decompiled handler FUN_1013aa60 (build/ghidra/handlers-c/0x05b9.c) read by hand, and client/symbols/ascension-extensions-2026-08-13/lua-keys.yaml, 2026-09-20. The capture reference (~/CoaServer/reference/cointhrow, L1 for us) reports the builder-verified Lua keys PvESpellID, PvPSpellID, IsDungeonPlayerSpell, IsDungeonBossSpell and IsDungeonMinionSpell for this row. All five are string literals in our own binary (lua-keys.yaml) with the same record offsets, and two of them appear in the client's Lua, so they are graded L3 here.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x0013aa60"
    registration_site_rva: "0x0013b72c"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: ChallengeSpells.dbc
    instance: "0x10be0398"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 40
    fields: 10

provenance:
  - level: L3
    source: "the decompiled handler FUN_1013aa60 (build/ghidra/handlers-c/0x05b9.c) read by hand, and client/symbols/ascension-extensions-2026-08-13/lua-keys.yaml, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05b9 is named SMSG_PATCH_CHALLENGE_SPELL"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
