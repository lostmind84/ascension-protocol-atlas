# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x06d4
name: SMSG_PATCH_CHARACTER_CREATION_ARCHETYPE_ROLES
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One CharacterCreationArchetypeRoles.dbc row: an id and four strings, plus columns not followed.

layout:
  - name: id
    type: u32
    notes: range-checked against the CharacterCreationArchetypeRoles.dbc table object (0x10bdf6cc)
    evidence: L3
  - name: strings
    type: bytes_variable
    notes: four NUL-scanned strings copied into the record; the columns between were not followed
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read as far as its visible reads; the rest is noted as not followed.

client:
  handler:
    rva: "0x001df0c0"
    registration_site_rva: "0x001e6abd"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: CharacterCreationArchetypeRoles.dbc
    instance: "0x10bdf6cc"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 284
    fields: 71

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read as far as its visible reads; the rest is noted as not followed"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x06d4 is named SMSG_PATCH_CHARACTER_CREATION_ARCHETYPE_ROLES"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
