# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05b1
name: SMSG_CHALLENGE_CREATOR_ACTIVE_CHALLENGE_UPDATE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  The player's active custom trial: one length-prefixed trial id, assigned into the trial manager at +0xc (empty clears it). The DISAGREEMENT recorded against this opcode is RESOLVED, and both halves of it were run-on artefacts of the same pair of functions. Server side: server_layout.py's chain ran out of SendActiveTrial (one string) into the next function in the file, AppendTrialCompletionEntry, whose nine fields belong to the trial-completion packet -- hence the phantom 'ten fields'. Client side: Ghidra's decompilation of FUN_101226d0 runs past a noreturn __fastfail (swi(0x29)) in the string destructor's bounds check and continues into the next function, FUN_10122780, which handlers.yaml registers for 0x05CB SMSG_CHALLENGE_CREATOR_COMPLETION -- that function, not this one, installs a second SEH frame, parses 0x88-byte completion records through FUN_10125860 and fires TRIAL_COMPLETION_ADDED with %s%u.

# AzerothCore calls 0x05b1 SMSG_COA_TRIAL_ACTIVE. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout:
  - name: trialId
    type: lpstring
    notes: 'u32 length then that many bytes, no NUL (FUN_100d3680). The whole handler is this one read: it fetches the trial manager (FUN_10124310) and assigns the string into the manager at +0xc, guarded by the usual std::string self-assignment test, then frees the temporary and returns. Nothing else is read and no event is fired. The fork''s SendActiveTrial writes exactly one AppendConfigString(trialID), so the two sides agree; an empty string clears the client''s active trial.

      '
    evidence: L3
# Read from the decompiled handler FUN_101226d0 (build/ghidra/handlers-c/0x05b1.c) read by hand; the run-on region past swi(0x29) identified as FUN_10122780 = the 0x05CB handler via client/symbols/ascension-extensions-2026-08-13/handlers.yaml, 2026-09-20.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# packet_layout.py had followed a call into 0x000d3680, not the handler. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x001226d0"
    registration_site_rva: "0x001242d4"
    evidence: L3

# RETRACTED 2026-09-20. The generated blocks below read
#   client_strings: ["TRIAL_COMPLETION_ADDED"]
#   client_fire: [{event: TRIAL_COMPLETION_ADDED, format: "%s%u", helper: FUN_10278c90}]
# Both came from the decompilation of FUN_101226d0 running past a noreturn
# __fastfail (swi(0x29)) in the string destructor's bounds check and into the
# next function, FUN_10122780 -- which handlers.yaml registers for 0x05CB
# SMSG_CHALLENGE_CREATOR_COMPLETION. That function has its own prologue (a
# second SEH frame and a second security cookie), parses 0x88-byte completion
# records through FUN_10125860 and fires TRIAL_COMPLETION_ADDED. This handler
# references no string literal and fires no event.

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-coa-challenges/src/CoA.Challenges.Config.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Lifecycle.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Trials.cpp

provenance:
  - level: L3
    source: "the decompiled handler FUN_101226d0 (build/ghidra/handlers-c/0x05b1.c) read by hand; the run-on region past swi(0x29) identified as FUN_10122780 = the 0x05CB handler via client/symbols/ascension-extensions-2026-08-13/handlers.yaml, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05b1 is named SMSG_CHALLENGE_CREATOR_ACTIVE_CHALLENGE_UPDATE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
