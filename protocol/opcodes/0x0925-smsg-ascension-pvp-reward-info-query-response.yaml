# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0925
name: SMSG_ASCENSION_PVP_REWARD_INFO_QUERY_RESPONSE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  PvP reward info: seven u32 kept for the UI to query.

layout:
  - name: unknown_0
    type: u32
    evidence: L3
  - name: unknown_1
    type: u32
    evidence: L3
  - name: unknown_2
    type: u32
    evidence: L3
  - name: unknown_3
    type: u32
    evidence: L3
  - name: unknown_4
    type: u32
    evidence: L3
  - name: unknown_5
    type: u32
    evidence: L3
  - name: unknown_6
    type: u32
    notes: seven u32 stored into a reward-info struct, then ASCENSION_CUSTOM_PVP_REWARD_INFO_AVAILABLE (Ascension_PVP.lua:1084)
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock).

# Widths and order, read out of the handler at 0x000e04a0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x000e04a0"
    registration_site_rva: "0x000e03c0"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "ASCENSION_CUSTOM_PVP_REWARD_INFO_AVAILABLE"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: ASCENSION_CUSTOM_PVP_REWARD_INFO_AVAILABLE
    args: []
    source: "Interface/FrameXML/Ascension_LFG/Ascension_PVP.lua:1084"

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0925 is named SMSG_ASCENSION_PVP_REWARD_INFO_QUERY_RESPONSE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
