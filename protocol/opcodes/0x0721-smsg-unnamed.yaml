# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0721
name: SMSG_UNNAMED_0721   # PLACEHOLDER: the client's table answers 'unknown'; a handler exists
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One SeasonalAppearances.dbc row: ten u32 in column order, replacing the row with that id or appending it.

layout:
  - name: id
    type: u32
    notes: SeasonalAppearances.dbc column 0; range-checked against the table object (0x10be0014)
    evidence: L3
  - name: column_1
    type: u32
    evidence: L3
  - name: column_2
    type: u32
    evidence: L3
  - name: column_3
    type: u32
    evidence: L3
  - name: column_4
    type: u32
    evidence: L3
  - name: column_5
    type: u32
    evidence: L3
  - name: column_6
    type: u32
    evidence: L3
  - name: column_7
    type: u32
    evidence: L3
  - name: column_8
    type: u32
    evidence: L3
  - name: column_9
    type: u32
    notes: 'field i is stored at slot i of a 0x28-byte record: the DBC''s ten columns in order; the DBC''s column names are not available offline'
    evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c), read by hand; table identity from client/symbols/.../patch-tables.yaml (the instance the handler range-checks).

# Widths and order, read out of the handler at 0x001e2db0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x001e2db0"
    registration_site_rva: "0x001e6bb4"
    evidence: L3

patches_table:   # the client DBC table this packet writes a row into (client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml, L3; sizes from ascension-live-2026-09-17, L2)
  - dbc: SeasonalAppearances.dbc
    instance: "0x10be0014"   # the DLL's table object in .data; rows at +0x20, minId +0x10, maxId +0xc
    record_size: 40
    fields: 10

provenance:
  - level: L1
    source: "the capture reference (CoinThrow, 2026-09-20, kept outside the tree -- see docs/crosscheck-cointhrow-2026-09-20.md)"
    claim: "0x0721-0x0724 are Character Advancement data-record updates, 8-12 dwords keyed by an id, stored into the registry record; handler 0x101E2DB0"
    note: >
      CONFLICT on what the table is, agreement on the shape. Their handler address
      converts to exactly our registered handler RVA for this opcode (subtract ImageBase
      0x10000000), and the same holds for all four of 0x0721-0x0724. That is a useful side
      result: for this block their addresses and ours describe the same code, which is not
      true everywhere -- docs/crosscheck-cointhrow-2026-09-20.md records six of their 232
      addresses that do not reproduce on our build. But we read this handler storing its
      ten u32 into the table object at 0x10be0014, which patch-tables.yaml identifies as
      SeasonalAppearances.dbc -- not a Character Advancement table at all. Their line is a
      prose grouping of four adjacent opcodes and claims no payload ('Payloads TBD'); ours
      is read from the instructions and names the instance. We keep ours. If their
      grouping is right, what would settle it is the table object, not the opcode number.
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand; table identity from client/symbols/.../patch-tables.yaml (the instance the handler range-checks)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "the client's table has no name for 0x0721"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
