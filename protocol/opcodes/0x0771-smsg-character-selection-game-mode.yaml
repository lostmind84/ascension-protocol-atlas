# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0771
name: SMSG_CHARACTER_SELECTION_GAME_MODE
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named SMSG_CHARACTER_SELECTION_GAME_MODE by the client's own opcode table. Layout and behaviour not
  established.

layout:
  - name: listIndex
    type: u32
    evidence: L3
  - name: activeGameModes
    type: u32
    evidence: L3
    notes: "Enum.GameMode bitmask"
  - name: enabledGameModes
    type: u32
    evidence: L3
    notes: "Enum.GameMode bitmask"
  - name: teamId
    type: u32
    evidence: L3
    notes: "classic faction-group id; decides the banner"
  - name: isMercenary
    type: u8
    evidence: L3
  - name: ruleset
    type: u8
    evidence: L3
    notes: "CHARACTER_SELECTION_RULESET_*; high-risk vs normal"
# name from mod-ascension-compat's serializer (L1); width and order from the decompiled handler (L3)
# decompiled: FUN_100d3910 reads exactly 18 bytes -- four u32 and two bytes -- then stores them on the character's selection record; the extractor's three extra fields were a map insert

client:
  handler:
    rva: "0x000d3910"
    registration_site_rva: "0x000d4deb"
    evidence: L3

used_by_server:   # the CoA server module sends or handles this
  server_name: SMSG_ASCENSION_CHARACTER_SELECTION_GAME_MODE
  files:
    - modules/mod-ascension-compat/src/AscensionCharacterSelection.cpp
    - modules/mod-ascension-compat/src/AscensionCharacterSelection.h

provenance:
  - level: L3
    source: "tools/ghidra/ghidra.sh run Decomp.java <handler VA> (see client/server-layouts.md)"
    claim: "field widths and order, from the decompiled handler"
  - level: L1
    source: "mod-ascension-compat's serializer, client/server-layouts.yaml"
    claim: "field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0771 is named SMSG_CHARACTER_SELECTION_GAME_MODE"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
