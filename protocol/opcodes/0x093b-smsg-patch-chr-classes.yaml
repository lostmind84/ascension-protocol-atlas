# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x093b
name: SMSG_PATCH_CHR_CLASSES
direction: server_to_client
status: hypothetical
evidence: L1
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  One row of the stock ChrClasses.dbc (AzerothCore already loads this table; ADR 0005), read by the handler
  at RVA 0x0022ab90 as 7 individuated dwords then 5 lpstrings. ClassID (leading, the universal 'n' convention
  for these tables, and ChrClassesEntry's own first column) is named from DBCStructure.h (L1). The other 6
  numeric reads and 5 strings do not reconcile with ChrClassesEntry: only 4 more numeric columns are active
  (powerType, spellfamily, CinematicSequence, expansion) against 6 observed, and the file has 3 locale-string
  blocks (name, kept; nameFemale and nameNeutralGender, both commented out) against 5 lpstrings on the wire,
  so nothing past ClassID is named here. Read against the real decompiled handler
  (build/ghidra/handlers-c/0x093b.c): confirms the 7-dwords-then-5-strings shape exactly (a clean sequential
  read, no branching), but the mismatch is real, not a coarse-trace artifact -- ChrClassesEntryfmt's prefix
  before the name[16] block is only 4 columns (ClassID, an unused column, powerType, another unused column),
  so at least 3 of these 7 reads land on bytes the file format calls part of the name locale block, read here
  as plain dwords rather than through the string helper. The simplest explanation is that AzerothCore's own
  ChrClassesEntry (written for the server's needs) keeps fewer columns than the client's own internal
  consumer of this table actually reads off the wire -- DBCStructure.h describes the file, not what any given
  client-side reader does with it, and this function is graphic proof that the two can diverge. Not enough to
  name any individual field past ClassID.

layout:
  - name: ClassID
    type: u32
    notes: active ChrClassesEntry member (column 0, 'n')
    evidence: L1
  - name: field_1
    type: u32
    evidence: L3
  - name: field_2
    type: u32
    evidence: L3
  - name: field_3
    type: u32
    evidence: L3
  - name: field_4
    type: u32
    evidence: L3
  - name: field_5
    type: u32
    evidence: L3
  - name: field_6
    type: u32
    evidence: L3
  - name: field_7
    type: lpstring
    evidence: L3
  - name: field_8
    type: lpstring
    evidence: L3
  - name: field_9
    type: lpstring
    evidence: L3
  - name: field_10
    type: lpstring
    evidence: L3
  - name: field_11
    type: lpstring
    evidence: L3
# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# the asm idiom (packet_layout.py) saw the same reads minus the strings. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x0022ab90"
    registration_site_rva: "0x00228e35"
    evidence: L3

provenance:
  - level: L1
    source: "AzerothCore src/server/shared/DataStores/DBCStructure.h (ChrClassesEntry) and DBCfmt.h (ChrClassesEntryfmt), read directly, cross-checked against the client DBC header in client/patch-table-names.md: ChrClassesEntry's column 0 ('n') is ClassID, the leading field of every table in this bucket"
    claim: "the name ClassID for the leading dword"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x093b is named SMSG_PATCH_CHR_CLASSES"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"

replay: null
