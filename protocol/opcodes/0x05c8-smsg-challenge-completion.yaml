# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x05c8
name: SMSG_CHALLENGE_COMPLETION
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  One leaderboard completion entry, inserted into the completion store and followed by CHALLENGE_COMPLETION_ADDED. Ten fields, read by the parser FUN_1013efd0: two u32, a length-prefixed name, two 64-bit times each read as a lo/hi pair, and three length-prefixed tokens. The record ends at +0x78 -- exactly the stride the handler advances by, so the map is complete. The first two dwords are the store key the Lua getter takes as (challengeID, level); the names of the three trailing strings come from the leaderboard model, whose key set forces them but whose order is the fork's.

# AzerothCore calls 0x05c8 SMSG_COA_CHALLENGE_COMPLETION_ADDED. Ascension uses the number for
# something else; which of the two a given realm means is worth checking.

layout:
  - name: challengeId
    type: u32
    notes: 'record +0x00. The handler builds the completion store''s key from record dwords 0 and 1 (FUN_10130150 then FUN_101305b0) and appends the 0x78 entry to the bucket it finds; the Lua getter over that store is C_Challenge.GetChallengeCompletions(challengeID, level, count) (ChallengeExtendedInfoMixin.lua:199, L2), so the PAIR is the (challengeID, level) key -- that much is read. Which of the two is which follows the getter''s argument order and the fork''s writer, and is not itself read out. The same two dwords are the first two %u of CHALLENGE_COMPLETION_ADDED; its third is the new entry''s index, not a packet field.

      '
    evidence: L3
  - name: level
    type: u32
    notes: record +0x04; the other half of the store key (see above)
    evidence: L3
  - name: playerName
    type: lpstring
    notes: 'u32 length then bytes, no NUL (FUN_100d3680), move-assigned into record +0x08; a std::string is 0x18 here, so it spans +0x08..+0x1f. Named: it is the only string before the two time pairs, and the leaderboard model''s only such key is PlayerName (ChallengeExtendedInfoMixin.lua:230-268, L2).'
    evidence: L3
  - name: startTime
    type: u32
    notes: record +0x20. Read with the next dword in one 8-byte cursor bump -- the client's 64-bit lo/hi idiom -- so the two pairs are two 64-bit times. The UI computes CompleteTime - StartTime as a duration and formats CompleteTime with date() (L2); which pair is the start follows the fork's writer and the fact that a completion cannot precede its start.
    evidence: L3
  - name: startTimeHigh
    type: u32
    notes: record +0x24; the high half. The fork sends 0.
    evidence: L3
  - name: completeTime
    type: u32
    notes: record +0x28; the second pair, one 8-byte cursor bump
    evidence: L3
  - name: completeTimeHigh
    type: u32
    notes: record +0x2c. The fork sends 0.
    evidence: L3
  - name: playerRace
    type: lpstring
    notes: record +0x30. Three length-prefixed strings close the record; the parser names none of them and the converter that builds the Lua table is not decompiled here. The leaderboard model's remaining keys are PlayerRace, PlayerGender and PlayerClass (L2) -- the SET is forced, the ORDER within it is the fork's (L1). The UI slices them with :sub(6), :sub(8) and GetLeaderboardClassInfo, i.e. RACE_*, GENDER_* and CLASS_* tokens.
    evidence: L3
  - name: playerGender
    type: lpstring
    notes: record +0x48
    evidence: L3
  - name: playerClass
    type: lpstring
    notes: record +0x60; ends at +0x78, exactly the 0x78 stride the handler advances the completion vector by, so the record map closes.
    evidence: L3
# Read from the parser FUN_1013efd0 decompiled and read by hand field by field, offset by offset, and the handler FUN_10136c40's store-key construction and event arguments; names from ChallengeExtendedInfoMixin.lua:199,230-268 in corpus ascension-ui-patch-b-stock (2026-09-20). The fork's AppendCompletionEntry (mod-coa-challenges, CoA.Challenges.History.cpp) writes challengeId, level, name, startTime + u32(0), completeTime + u32(0), race, gender, class -- ten fields in exactly this order and with exactly these widths, and its stated 0x78 stride is the one the handler uses. Its reconstruction is CONFIRMED against the parser. What the parser does NOT settle, and the fork therefore still supplies at L1, is the order inside two groups: challengeId before level, and race before gender before class.

# Read from Interface/AddOns/Ascension_ChallengesUI/Challenges/ChallengeExtendedInfoMixin.lua:230-268 in corpus ascension-ui-patch-b-stock for the names (L2), and mod-coa-challenges AppendCompletionEntry for the order (L1); the client's handler FUN_10136c40 read by hand to confirm it delegates to FUN_1013efd0, 2026-09-20. The capture reference (~/CoaServer/reference/cointhrow, L1 for us) lists 'Leaderboard entries: Challenge, PlayerName, StartTime, CompleteTime, PlayerRace, PlayerGender, PlayerClass, Upvoted, Downvoted, Score'. The first seven re-derive from the client's UI source here (L2). The last three do NOT belong to this entry: Upvoted, Downvoted and Score are custom-trial fields, set on the trial model from C_TrialCreator.GetTrialInfo (CustomTrialItemMixin.lua:71-77, L2) and carried by SMSG_CHALLENGE_CREATOR_QUERY_CHALLENGES_RESULT 0x05AC, not by a leaderboard entry.

# Read from the decompiled handler (build/ghidra/handlers-c) next to mod-coa-challenges' writer (the fork's reconstruction, L1 for names).

# Widths and order, read out of the handler at 0x0013efd0 by tools/extract/packet_layout.py.
# Names are placeholders and the structure is flat: a count followed by a
# repeated record appears here as the count plus one record's fields.

client:
  handler:
    rva: "0x00136c40"
    registration_site_rva: "0x0013b878"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_ChallengesUI/Challenges/ChallengeItemMixin.lua

client_strings:   # literals the handler references, from its decompilation
  - "%u%u%u"
  - "CHALLENGE_COMPLETION_ADDED"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: CHALLENGE_COMPLETION_ADDED
    format: null
    helper: FUN_100e09f0

used_by_server:   # the CoA server module sends or handles this
  files:
    - modules/mod-coa-challenges/src/CoA.Challenges.History.cpp
    - modules/mod-coa-challenges/src/CoA.Challenges.Trials.cpp

provenance:
  - level: L3
    source: "the parser FUN_1013efd0 decompiled and read by hand field by field, offset by offset, and the handler FUN_10136c40's store-key construction and event arguments; names from ChallengeExtendedInfoMixin.lua:199,230-268 in corpus ascension-ui-patch-b-stock (2026-09-20)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L1
    source: "Interface/AddOns/Ascension_ChallengesUI/Challenges/ChallengeExtendedInfoMixin.lua:230-268 in corpus ascension-ui-patch-b-stock for the names (L2), and mod-coa-challenges AppendCompletionEntry for the order (L1); the client's handler FUN_10136c40 read by hand to confirm it delegates to FUN_1013efd0, 2026-09-20"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c) next to mod-coa-challenges' writer (the fork's reconstruction, L1 for names)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x05c8 is named SMSG_CHALLENGE_COMPLETION"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
