# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0526
name: CMSG_FEL_COMMUTATION_CHANGE_SLOT
direction: client_to_server
status: unknown
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Named CMSG_FEL_COMMUTATION_CHANGE_SLOT by the client's own opcode table. A raw byte sequence
  spelling `mov esi, 0x526` exists in `.text` at one site, but that site is data embedded in
  code, not a real instruction (one of four such coincidences `client/client-senders.md`
  names and excludes); beyond that single false lead the opcode value appears nowhere else in
  this build -- not in `.rdata`/`.data`, not before any `Put(u32)` opcode write, not in the
  shipped Lua. This build has no sender for it; layout is not established because there is
  nothing to read, not because nobody has looked.

layout: []   # unknown -- confirmed absent (one candidate site checked and ruled out as embedded data), not merely unexamined

provenance:
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0526 is named CMSG_FEL_COMMUTATION_CHANGE_SLOT"
  - level: L3
    source: >
      client/client-senders.md ("What the scan still does not reach": the 0x526 byte
      sequence at one .text site is embedded data, not code; the opcode is also listed among
      the 33 with no trace at all, 2026-09-20)
    claim: "the one raw-byte hit for 0x526 in .text is not a real sender; the opcode value has no sender in this build"

replay: null
