# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x09c2
name: SMSG_CLEAR_ALL_SPELL_CHARGES
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13

summary: >
  Clear all spell charges: no payload; the handler fires its event and reads nothing.

layout: []  # empty
# Read from the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock).
#
# The handler is `undefined4 __fastcall handler_0x09c2(undefined4 param_1)`, and unlike its
# siblings on the same registrar (0x0909, 0x090c, 0x0911, all registered through the same
# register_rva 0x002cfeb0) it does not use the packet directly with the documented
# CDataStore cursor idiom (`+0x14` pos / `+4` base); instead it hands its one argument
# straight to a helper, func_0x10320a80(param_1), before ever checking anything -- exactly
# the "hands the store to a helper" trap. Chased that helper (DecompAt 0x10320a80,
# 2026-09-20): it reads param_1+8, +4, +0xc, +0x10, +0x1c in a count/capacity check and
# either merges one bucket (via a further call, func_0x103208d0) or walks and frees a
# linked list of 0x1c-byte nodes and re-inits it -- a hash-table clear/rehash shape, not a
# sequential field read. None of those offsets are the documented +0x14/+4 pair, so this is
# not reading packet *fields* the way the other handlers here do. It does not, however,
# rule out that param_1 is something other than the CDataStore (e.g. a manager instance
# whose own container this clears as a side effect of the event) -- which object param_1
# actually is was not pinned down further. Consistent with that reading: our own fork
# (modules/..., L1, a reconstruction) carries a comment on SMSG_CLEAR_ALL_SPELL_CHARGES at
# azerothcore-wotlk-coa src/server/game/Entities/Player/Player.cpp:17191, "no payload, wipes
# every charge entry" for a client-side charge map keyed by category -- exactly the
# hash-table clear func_0x10320a80 performs. Corroborating, not authoritative (L1); the L3
# finding above already stood on its own.

client:
  handler:
    rva: "0x00320120"
    registration_site_rva: "0x003200a0"
    evidence: L3

client_strings:   # literals the handler references, from its decompilation
  - "SPELL_UPDATE_CHARGES"

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c), read by hand next to the UI's event handlers (L2, corpus ascension-ui-patch-b-stock)"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "func_0x10320a80, decompiled by hand (GHIDRA_WORKSPACE=world-session DecompAt.java 10320a80, 2026-09-20), the helper the handler hands its argument to before doing anything else"
    claim: "that helper does a hash-table-style clear/rehash over its argument's +4/+8/+0xc/+0x10/+0x1c fields, not a CDataStore +0x14/+4 sequential read, so it is not reading packet fields the documented way"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x09c2 is named SMSG_CLEAR_ALL_SPELL_CHARGES"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L1
    source: "~/CoaServer/reference/cointhrow/cointhrow-opcode-dbc-reference-2026-09-20.md, '09C2-09C5: spell charges: clear-all / clear / send / set {u32 spellId; u32 slot; u32 value} (handlers 0x10320120/.../0x10320350, charge map at 0x10BE43E4)' (third-party, not in this repo)"
    claim: "independently names the same handler address (0x10320120 = this fiche's 0x00320120 + image base) for 'clear-all', gives it no payload (the {u32,u32,u32} shape belongs to 09C5 'set', not this opcode), and names a 'charge map at 0x10BE43E4' -- consistent with func_0x10320a80's hash-table clear/rehash, likely the very structure it operates on"

replay: null
