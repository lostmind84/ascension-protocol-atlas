# Generated by tools/codegen/stub_fiches.py. Nothing here is a wire format:
# a name and a handler address say what the client calls this and that it has
# code for it, not what the packet contains. Fill in `layout` from evidence.
id: 0x0654
name: SMSG_PENDING_SKILL_CARD_ADDED
direction: server_to_client
status: hypothetical
evidence: L3
client_builds:
  - ascension-extensions-2026-08-13
client_datasets:
  - ascension-live-2026-09-17

summary: >
  Skill cards pending unlock: a count, then (cardID, rank) pairs; for each the handler fires PENDING_SKILL_CARD_ADDED(pendingIndex, cardID, rank) with format %u%u%u, the last two being the record's reads (proven positions) and the index the loop's.

layout:
  - name: count
    type: u32
    evidence: L3
  - repeat: count
    fields:
    - name: cardId
      type: u32
      evidence: L3
    - name: rank
      type: u32
      evidence: L3
# Read from the decompiled handler (build/ghidra/handlers-c, DumpC.java), its fire format and the UI's event arguments.

# Widths and order, from the handler's DECOMPILED read sequence (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml);
# tools/extract/packet_layout.py found no read idiom here. `lpstring` is FUN_100d3680: u32 length then that many bytes, no NUL.
# A bytes_N field is a block copy of N bytes, `unknown` a computed length. Names are
# placeholders; check the handler before trusting a width.

client:
  handler:
    rva: "0x0031a290"
    registration_site_rva: "0x0031baa4"
    evidence: L3

used_by:   # UI source naming this opcode's event
  - Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardUnlockFrame/SkillCardUnlockFrame.lua
  - Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardsFrame.lua
  - Interface/FrameXML/Util/SkillCardsUtil.lua

client_strings:   # literals the handler references, from its decompilation
  - "%u%u%u"
  - "PENDING_SKILL_CARD_ADDED"

client_events:   # Lua arguments of the events this handler names (corpus ascension-ui-patch-b-stock, L2)
  - event: PENDING_SKILL_CARD_ADDED
    args: [...]
    source: "Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardsFrame.lua:531"
  - event: PENDING_SKILL_CARD_ADDED
    args: [pendingIndex, cardID, rank]
    source: "Interface/AddOns/Ascension_SkillCards/SkillCardFrame/SkillCardUnlockFrame/SkillCardUnlockFrame.lua:729"

client_fire:   # what the handler fires, from its decompilation (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml, L3)
  - event: PENDING_SKILL_CARD_ADDED
    format: "%u%u%u"
    helper: FUN_10278c90
    read_slots: [null, 1, 2]   # indices into the 3 reads this pass saw; the layout has 2, so not field numbers

provenance:
  - level: L3
    source: "the decompiled handler (build/ghidra/handlers-c, DumpC.java), its fire format and the UI's event arguments"
    claim: "the layout: widths, order, repeat structure and field names"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml (decompiled read sequence)"
    claim: "the read widths and their order, as decompiled"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/layouts.yaml (handler read sequence)"
    claim: "the field widths and their order"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/opcodes.yaml (opcode-name switch)"
    claim: "opcode 0x0654 is named SMSG_PENDING_SKILL_CARD_ADDED"
  - level: L3
    source: "client/symbols/ascension-extensions-2026-08-13/handlers.yaml (handler registration scan)"
    claim: "the client registers a handler for it"
  - level: L2
    source: "corpus ascension-ui-patch-b-stock"
    claim: "the UI names this event"

replay: null
