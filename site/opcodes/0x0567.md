# `0x0567` SMSG_PATCH_CREATURE

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0567-smsg-patch-creature.yaml`](../../protocol/opcodes/0x0567-smsg-patch-creature.yaml)

SMSG_PATCH_CREATURE: one packet that writes a row into TWO tables, Creature.dbc and DungeonEncounterExtra.dbc, from a handler shared with the other opcode in this pair (0x0567 and 0x0568 decompile to the identical function body, FUN_101e14f0 at RVA 0x001e14f0; each opcode is registered at its own small entry point that reaches the same code). Neither row is a full-width copy of its DBC's own record: the Creature.dbc side stores only 7 of that table's 23 fields (appended past the table's own maxId, ignoring this packet's own first field as an index), while the DungeonEncounterExtra.dbc side is a genuine full match of that table's 4 fields. No column name is established for either table.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | record 1 (Creature.dbc side): offset 0; not used as the row's id -- the id actually used is Creature.dbc's own maxId+1 (DAT_10be0468, an auto-increment append), not this field |
| `field_1` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further |
| `field_2` | `u32` | L3 | offset 8 |
| `field_3` | `u32` | L3 | offset 0xc |
| `field_4` | `u32` | L3 | offset 0x10 |
| `field_5` | `u32` | L3 | offset 0x14 |
| `field_6` | `lpstring` | L3 | via FUN_100d3680; record 1 is 7 fields (0x1c=28 bytes) allocated by FUN_10ae5d20, far short of Creature.dbc's own 23-field/92-byte row |
| `field_7` | `u32` | L3 | record 2 starts here (DungeonEncounterExtra.dbc side), offset 0 of the second record; used as the min/maxId-bounded array index into the table object at 0x10bdfabc (matches DungeonEncounterExtra.dbc exactly: 4 fields, 16-byte record) |
| `field_8` | `u32` | L3 | offset 4 of record 2, first half of an 8-byte load |
| `field_9` | `u32` | L3 | offset 8 of record 2, second half of the same 8-byte load; read as one 8-byte load in the handler; split into two 4-byte fields here to line up with the table's own field count -- could be a single 64-bit value instead, not resolved further |
| `field_10` | `u32` | L3 | offset 0xc of record 2, last of DungeonEncounterExtra.dbc's 4 fields |

## Client

- handler RVA `0x001e14f0` in `ascension-extensions-2026-08-13`
