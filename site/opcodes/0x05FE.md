# `0x05FE` SMSG_UPDATE_RANDOM_ENCHANT_SLOT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05fe-smsg-update-random-enchant-slot.yaml`](../../protocol/opcodes/0x05fe-smsg-update-random-enchant-slot.yaml)

One random-enchant slot: index and value; MYSTIC_ENCHANT_SLOT_UPDATE(slot + 1) when it changed. The skeleton's four u32 were the event struct's writes.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `slot` | `u32` | L3 | index into the slot vector; ignored when out of range |
| `enchant` | `u32` | L3 | the slot's new value; when it differs from the old one the handler fires MYSTIC_ENCHANT_SLOT_UPDATE with slot + 1 |

## Client

- handler RVA `0x002e94b0` in `ascension-extensions-2026-08-13`

## Events

- fires `MYSTIC_ENCHANT_SLOT_UPDATE` with format `%u`
- the UI receives `MYSTIC_ENCHANT_SLOT_UPDATE` (`client/event-args.yaml`)
- the UI receives `MYSTIC_ENCHANT_SLOT_UPDATE` (`client/event-args.yaml`)
