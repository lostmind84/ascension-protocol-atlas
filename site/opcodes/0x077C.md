# `0x077C` SMSG_AREA_POI_PAYLOAD

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x077c-smsg-area-poi-payload.yaml`](../../protocol/opcodes/0x077c-smsg-area-poi-payload.yaml)

An area POI payload: an id and a JSON string.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L3 | named in the capture reference; our handler reads it and keeps it with the payload |
| `json` | `cstring` | L3 | AREA_POI_PAYLOAD(json), MapPOI.lua:79 -- the string is a JSON document |

## Client

- handler RVA `0x00258cc0` in `ascension-extensions-2026-08-13`

## Events

- fires `AREA_POI_PAYLOAD`
- the UI receives `AREA_POI_PAYLOAD` (`client/event-args.yaml`)
