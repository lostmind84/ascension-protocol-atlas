# `0x06C1` SMSG_PATCH_SPELL_TAGS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06c1-smsg-patch-spell-tags.yaml`](../../protocol/opcodes/0x06c1-smsg-patch-spell-tags.yaml)

Named SMSG_PATCH_SPELL_TAGS by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_12` | L3 | one row of SpellTags.dbc (3 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e40c0` in `ascension-extensions-2026-08-13`

## Events

- fires `SPELL_TAGS_CHANGED`
- the UI receives `SPELL_TAGS_CHANGED` (`client/event-args.yaml`)
