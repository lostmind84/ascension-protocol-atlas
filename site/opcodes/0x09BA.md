# `0x09BA` SMSG_ASCENSIONGM_TRANSACTION_HISTORY

**server to client** · status `unknown` · evidence `L3` · layout `unknown` · client code `none`

Fiche: [`protocol/opcodes/0x09ba-smsg-ascensiongm-transaction-history.yaml`](../../protocol/opcodes/0x09ba-smsg-ascensiongm-transaction-history.yaml)

Named SMSG_ASCENSIONGM_TRANSACTION_HISTORY by the client's own opcode table. Not among the 532 handlers the exhaustive push/push/call registrar scan of `.text` finds (`tools/extract/opcode_handlers.py`, three registrars, `client/symbols/ascension-extensions-2026-08-13/handlers.yaml`); this build registers no handler for it, and the server modules in this fork never send it either (`client/server-usage.yaml` has no entry for it, not even a false-positive literal). Layout is not established because there is nothing to read, not because nobody has looked.

## Layout

*No layout established.*
