# `0x0621` SMSG_WILDCARD_ENTRY_LEARNED

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0621-smsg-wildcard-entry-learned.yaml`](../../protocol/opcodes/0x0621-smsg-wildcard-entry-learned.yaml)

A Wildcard entry learned: the entry whose Wildcard-store record (0x2c bytes, keyed by this id) the handler fills, its new rank, and a NUL-terminated reason; WILDCARD_ENTRY_LEARNED(internalID, newRank, preRollRank) fires with format %u%u%u, the second argument being the rank read here (proven position), the internal id looked up from the entry, the pre-roll rank taken from the store. WILDCARD_RAPID_ROLL_LEARNED adds the reason string as a fourth argument.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `entryId` | `u32` | L3 | the key of the Wildcard store record the handler fills |
| `newRank` | `u32` | L3 |  |
| `reason` | `cstring` | L3 |  |

## Client

- handler RVA `0x00a30260` in `ascension-extensions-2026-08-13`

## Events

- fires `WILDCARD_ENTRY_LEARNED` with format `%u%u%u`
- the UI receives `WILDCARD_ENTRY_LEARNED` (`client/event-args.yaml`)
- the UI receives `WILDCARD_ENTRY_LEARNED` (`client/event-args.yaml`)
- the UI receives `WILDCARD_RAPID_ROLL_LEARNED` (`client/event-args.yaml`)
