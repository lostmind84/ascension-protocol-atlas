# `0x0626` CMSG_BUILD_CREATOR_CREATE_BUILD

**client to server** · status `hypothetical` · evidence `L3` · layout `named` · client code `sender`

Fiche: [`protocol/opcodes/0x0626-cmsg-build-creator-create-build.yaml`](../../protocol/opcodes/0x0626-cmsg-build-creator-create-build.yaml)

Create a build-creator build: the build record, serialized by a helper the reading did not follow. The Lua caller passes self.build.ID; the binding attribution (IsOwnedBuild) is Ghidra's merge of neighbouring functions.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `build` | `bytes_variable` | L3 | the build record serialized by a helper (FUN_100f9c30 over the build struct) between the opcode and Finalize; the sender writes nothing itself. Not followed |

## Client

- sender `0x001050eb`
