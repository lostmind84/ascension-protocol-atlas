# `0x0736` SMSG_SUBMIT_GAME_FEEDBACK_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0736-smsg-submit-game-feedback-result.yaml`](../../protocol/opcodes/0x0736-smsg-submit-game-feedback-result.yaml)

One NUL-terminated string the handler hands to SUBMIT_GAME_FEEDBACK_RESULT with format %s -- the only read and the only argument; named 'result' after the UI's parameter (default name: no Lua parameter declared).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 |  |

## Client

- handler RVA `0x000d4440` in `ascension-extensions-2026-08-13`

## Events

- fires `SUBMIT_GAME_FEEDBACK_RESULT` with format `%s`
- the UI receives `SUBMIT_GAME_FEEDBACK_RESULT` (`client/event-args.yaml`)
