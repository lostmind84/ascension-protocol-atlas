# `0x0775` CMSG_SINGLE_ASSET_QUERY

**client to server** · status `hypothetical` · evidence `L3` · layout `named` · client code `sender`

Fiche: [`protocol/opcodes/0x0775-cmsg-single-asset-query.yaml`](../../protocol/opcodes/0x0775-cmsg-single-asset-query.yaml)

A single asset query: two u32 taken from RequestSingleAssetQuery(a, b). No UI in the stock corpus calls it; the meaning of the two numbers is inferred from 0x0776's shape only.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `unknown_0` | `u32` | L3 | the binding's first Lua number; 0x0776 sends the same leading value before its id list, so this is the query kind and the next the id -- an inference, no UI call in the corpus |
| `unknown_1` | `u32` | L3 | the second Lua number |

## Client

- sender `0x000dcf54`
