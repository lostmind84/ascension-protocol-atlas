# `0x093E` SMSG_PATCH_CURRENCY_CATEGORY

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x093e-smsg-patch-currency-category.yaml`](../../protocol/opcodes/0x093e-smsg-patch-currency-category.yaml)

Named SMSG_PATCH_CURRENCY_CATEGORY by the client's own opcode table. The opcode is not bound in patch-tables.yaml -- the DLL's own custom-table list does not mention this opcode; CurrencyCategory.dbc is the table patch-table-names.md associates with it by name and geometry (grade L1). The handler's own reads fall far short of CurrencyCategory.dbc's own WDBC header (19 fields, 76 bytes), so whatever this patches, it is not a full row of that table. No source names any of the table's columns.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 |  |
| `field_1` | `u32` | L3 |  |
| `field_2` | `lpstring` | L3 |  |

## Client

- handler RVA `0x0022bb00` in `ascension-extensions-2026-08-13`
