# `0x05F3` SMSG_PATCH_CHR_SPECS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05f3-smsg-patch-chr-specs.yaml`](../../protocol/opcodes/0x05f3-smsg-patch-chr-specs.yaml)

One row of ChrSpecs.dbc: 33 dwords (132 bytes), of which 15 are throwaway placeholders overwritten by 15 trailing lpstrings the wire sends right after, in an order this pass confirmed from the handler's own stack layout. All 33 dwords are named from the client's native getter C_ClassInfo.GetSpecInfo (VA 0x100dac60, called from GameTooltip.lua:259-260), which builds a Lua table from exactly this set of offsets -- 18 scalar/boolean columns plus 4 three-element lists (PrimaryStats, PrimaryResources, SecondaryResources, ExampleSpells) this pass numbered itself, since the getter names each list once, not element by element. ChrSpecs.dbc's own header reports 65 fields / 260 bytes; only this 132-byte prefix is ever sent by this SMSG.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L3 | record +0x0. the row key. Found by a linear scan comparing the input classFile/specFile strings against fields 1 and 2 of each row (FUN_100d0770 + a byte-wise string compare), not by direct indexing. |
| `Class` | `bytes_4` | L3 | record +0x4, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. GetSpecInfo (native, VA 0x100dac60; C_ClassInfo.GetSpecInfo(classFile, specFile), called from GameTooltip.lua:259-260 via GameTooltip_GetSpellCastReqIcon)'s row-match key #1 (compared against the input classFile) and its own returned key `Class`. |
| `Spec` | `bytes_4` | L3 | record +0x8, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. Key literal at 0x10b28924 ("Spec", read back from the .rdata bytes). The getter's row-match key #2 (compared against the input specFile). |
| `SpecFilename` | `bytes_4` | L3 | record +0xc, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. GetSpecInfo (native, VA 0x100dac60; C_ClassInfo.GetSpecInfo(classFile, specFile), called from GameTooltip.lua:259-260 via GameTooltip_GetSpellCastReqIcon): `puVar13 = piVar6[3]; push("SpecFilename", puVar13)`. Matches the Lua call site's use: `specInfo.SpecFilename` (GameTooltip.lua:260). |
| `Cloth` | `u32` | L3 | record +0x10. read as a C++ bool (`piVar6[4] != 0`). Key literal "Cloth". |
| `Leather` | `u32` | L3 | record +0x14. Key literal "Leather", bool. |
| `Mail` | `u32` | L3 | record +0x18. Key literal at 0x10b2c7bc ("Mail", read back from the .rdata bytes), bool. |
| `Plate` | `u32` | L3 | record +0x1c. Key literal "Plate", bool. |
| `PrimaryStats_1` | `bytes_4` | L3 | record +0x20, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. part of the 3-element `PrimaryStats` list the getter builds from fields 8-10 and pushes under one key, "PrimaryStats" -- the getter names the whole list, not this element individually; PrimaryStats_1/2/3 here is this pass's own indexing, not a name read from source. |
| `PrimaryStats_2` | `bytes_4` | L3 | record +0x24, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. see PrimaryStats_1. |
| `PrimaryStats_3` | `bytes_4` | L3 | record +0x28, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. see PrimaryStats_1. |
| `MeleeDPS` | `u32` | L3 | record +0x2c. Key literal "MeleeDPS", bool. |
| `RangedDPS` | `u32` | L3 | record +0x30. Key literal "RangedDPS", bool. |
| `CasterDPS` | `u32` | L3 | record +0x34. Key literal "CasterDPS", bool. |
| `Tank` | `u32` | L3 | record +0x38. Key literal at 0x10b2c800 ("Tank", read back from the .rdata bytes), bool. |
| `Healer` | `u32` | L3 | record +0x3c. Key literal "Healer", bool. |
| `Support` | `u32` | L3 | record +0x40. Key literal "Support", bool. |
| `DifficultyRating` | `bytes_4` | L3 | record +0x44, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. Key literal "DifficultyRating". |
| `PrimaryResources_1` | `bytes_4` | L3 | record +0x48, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. part of the 3-element `PrimaryResources` list built from fields 18-20 under one key "PrimaryResources"; the per-element numbering is this pass's own, not read from source. |
| `PrimaryResources_2` | `bytes_4` | L3 | record +0x4c, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. see PrimaryResources_1. |
| `PrimaryResources_3` | `bytes_4` | L3 | record +0x50, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. see PrimaryResources_1. |
| `SecondaryResources_1` | `bytes_4` | L3 | record +0x54, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. part of the 3-element `SecondaryResources` list built from fields 21-23 under one key "SecondaryResources"; the per-element numbering is this pass's own. |
| `SecondaryResources_2` | `bytes_4` | L3 | record +0x58, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. see SecondaryResources_1. |
| `SecondaryResources_3` | `bytes_4` | L3 | record +0x5c, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. see SecondaryResources_1. |
| `ExampleSpells_1` | `u32` | L3 | record +0x60. part of the 3-element `ExampleSpells` list built from fields 24-26 (spell ids, deduplicated by FUN_100c0270) under one key "ExampleSpells"; the per-element numbering is this pass's own. |
| `ExampleSpells_2` | `u32` | L3 | record +0x64. see ExampleSpells_1. |
| `ExampleSpells_3` | `u32` | L3 | record +0x68. see ExampleSpells_1. |
| `PassiveSpell` | `u32` | L3 | record +0x6c. Key literal "PassiveSpell". |
| `PassiveID` | `u32` | L3 | record +0x70. Key literal "PassiveID". |
| `Name` | `bytes_4` | L3 | record +0x74, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. Key literal at 0x10b28718 ("Name", read back from the .rdata bytes). |
| `Description` | `bytes_4` | L3 | record +0x78, SKIPPED: sent on the wire as a throwaway placeholder dword, then overwritten by one of the 15 trailing lpstrings below. Key literal "Description". |
| `SortOrder` | `u32` | L3 | record +0x7c. Key literal "SortOrder", the last field the getter reads by index (piVar6[0x1f]). |
| `LootSpecID` | `u32` | L3 | record +0x80. record +0x80, the last field this opcode writes. Key literal "LootSpecID" -- read from piVar6[0x20], one past SortOrder, even though it is pushed to Lua after PassiveSpell/PassiveID (push order is not index order here). ChrSpecs.dbc's own header reports 65 fields / 260 bytes; only these first 33 (132 bytes) are ever sent by this SMSG. |
| `string_1` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 1st of 15 trailing strings; lands at record +0x4 (Class). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*1) bytes below the read cursor, i.e. record index 1. |
| `string_2` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 2nd of 15 trailing strings; lands at record +0x8 (Spec). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*2) bytes below the read cursor, i.e. record index 2. |
| `string_3` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 3rd of 15 trailing strings; lands at record +0xc (SpecFilename). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*3) bytes below the read cursor, i.e. record index 3. |
| `string_4` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 4th of 15 trailing strings; lands at record +0x20 (PrimaryStats_1). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*8) bytes below the read cursor, i.e. record index 8. |
| `string_5` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 5th of 15 trailing strings; lands at record +0x24 (PrimaryStats_2). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*9) bytes below the read cursor, i.e. record index 9. |
| `string_6` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 6th of 15 trailing strings; lands at record +0x28 (PrimaryStats_3). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*10) bytes below the read cursor, i.e. record index 10. |
| `string_7` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 7th of 15 trailing strings; lands at record +0x44 (DifficultyRating). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*17) bytes below the read cursor, i.e. record index 17. |
| `string_8` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 8th of 15 trailing strings; lands at record +0x48 (PrimaryResources_1). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*18) bytes below the read cursor, i.e. record index 18. |
| `string_9` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 9th of 15 trailing strings; lands at record +0x4c (PrimaryResources_2). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*19) bytes below the read cursor, i.e. record index 19. |
| `string_10` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 10th of 15 trailing strings; lands at record +0x50 (PrimaryResources_3). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*20) bytes below the read cursor, i.e. record index 20. |
| `string_11` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 11th of 15 trailing strings; lands at record +0x54 (SecondaryResources_1). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*21) bytes below the read cursor, i.e. record index 21. |
| `string_12` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 12th of 15 trailing strings; lands at record +0x58 (SecondaryResources_2). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*22) bytes below the read cursor, i.e. record index 22. |
| `string_13` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 13th of 15 trailing strings; lands at record +0x5c (SecondaryResources_3). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*23) bytes below the read cursor, i.e. record index 23. |
| `string_14` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 14th of 15 trailing strings; lands at record +0x74 (Name). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*29) bytes below the read cursor, i.e. record index 29. |
| `string_15` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). The wire's 15th of 15 trailing strings; lands at record +0x78 (Description). Order confirmed by the handler's own stack-frame layout: each string's destination local sits exactly (0x200 - 4*30) bytes below the read cursor, i.e. record index 30. |

## Client

- handler RVA `0x001e0570` in `ascension-extensions-2026-08-13`
