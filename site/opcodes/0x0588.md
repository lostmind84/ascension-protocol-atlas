# `0x0588` SMSG_BATTLEGROUND_COUNTDOWN_UPDATE

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0588-smsg-battleground-countdown-update.yaml`](../../protocol/opcodes/0x0588-smsg-battleground-countdown-update.yaml)

Battleground countdown: one u32 handed to BATTLEGROUND_COUNTDOWN_STARTED(seconds) with format %u (CountdownTimer.lua); a zero fires BATTLEGROUND_STARTED instead.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `seconds` | `u32` | L3 |  |

## Client

- handler RVA `0x000d3800` in `ascension-extensions-2026-08-13`

## Events

- fires `BATTLEGROUND_COUNTDOWN_STARTED` with format `%u`
- fires `BATTLEGROUND_STARTED` with format `%u`
- the UI receives `BATTLEGROUND_COUNTDOWN_STARTED` (`client/event-args.yaml`)
- the UI receives `BATTLEGROUND_STARTED` (`client/event-args.yaml`)
