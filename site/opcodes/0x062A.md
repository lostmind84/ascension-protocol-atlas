# `0x062A` CMSG_BUILD_CREATOR_RATE_BUILD

**client to server** · status `hypothetical` · evidence `L3` · layout `named` · client code `sender`

Fiche: [`protocol/opcodes/0x062a-cmsg-build-creator-rate-build.yaml`](../../protocol/opcodes/0x062a-cmsg-build-creator-rate-build.yaml)

Rate a build-creator build: the id string and a boolean. The skeleton missed the string.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `buildId` | `cstring` | L3 | RateBuild(self.data.ID, up): the id as a NUL-terminated string |
| `rating` | `u8` | L3 | the boolean |

## Client

- sender `0x001056cb`
