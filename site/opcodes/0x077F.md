# `0x077F` SMSG_CHARACTER_ADVANCEMENT_LOADOUT_ACTIVE_PAYLOAD

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x077f-smsg-character-advancement-loadout-active-payload.yaml`](../../protocol/opcodes/0x077f-smsg-character-advancement-loadout-active-payload.yaml)

A talent-loadout payload for the UI: a u32 the handler passes to its fire helper as the second argument, then a NUL-terminated JSON string handed to CHARACTER_ADVANCEMENT_LOADOUT_ACTIVE_PAYLOAD(json) (TalentLoadoutUtil.lua). What the u32 means is not established; the helper FUN_1025a6c0 formats %s%u.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `unknown_0` | `u32` | L3 | the %u of the fire helper's format; meaning not established |
| `json` | `cstring` | L3 |  |

## Client

- handler RVA `0x00258e20` in `ascension-extensions-2026-08-13`

## Events

- fires `CHARACTER_ADVANCEMENT_LOADOUT_AC`
- the UI receives `CHARACTER_ADVANCEMENT_LOADOUT_ACTIVE_PAYLOAD` (`client/event-args.yaml`)
