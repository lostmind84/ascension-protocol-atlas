# `0x0910` SMSG_POI_REMOVE

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0910-smsg-poi-remove.yaml`](../../protocol/opcodes/0x0910-smsg-poi-remove.yaml)

Remove a point of interest the server had placed: one NUL-terminated key handed to ASCENSION_POI_REMOVE with format %s (no Lua parameter declared in the corpus; the key is what the matching add packet named it).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `key` | `cstring` | L3 |  |

## Client

- handler RVA `0x002cf7e0` in `ascension-extensions-2026-08-13`

## Events

- fires `ASCENSION_POI_REMOVE` with format `%s`
