# `0x072A` SMSG_UNNAMED_072A

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x072a-smsg-unnamed.yaml`](../../protocol/opcodes/0x072a-smsg-unnamed.yaml)

Result of purging every learned ability: one result token. The handler fires CHARACTER_ADVANCEMENT_PURGE_ABILITIES_RESULT(success, result).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 | a CA_PURGE_ABILITIES_* token, matched against a table of 10 strings starting at CA_PURGE_ABILITIES_OK; index 0 is success |

## Client

- handler RVA `0x00170750` in `ascension-extensions-2026-08-13`

## Events

- fires `CHARACTER_ADVANCEMENT_PURGE_ABIL` with format `%b%s`
- the UI receives `CHARACTER_ADVANCEMENT_PURGE_ABILITIES_RESULT` (`client/event-args.yaml`)
