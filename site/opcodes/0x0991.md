# `0x0991` SMSG_PATCH_ZONE_INTRO_MUSIC_TABLE

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0991-smsg-patch-zone-intro-music-table.yaml`](../../protocol/opcodes/0x0991-smsg-patch-zone-intro-music-table.yaml)

A record patched into one of Extensions.dll's internal id-keyed containers -- looked up by id (RVA 0x000b2bd0), allocated fresh as a 0x14-byte heap struct when new (RVA 0x000ae5d20), and inserted (RVA 0x000b0b50) -- not one of the 114 CustomDBCMgr `.data` tables client/patch-tables.md documents: nothing in the decompiled handler references a fixed .data table object (no `+0xc`/`+0x10`/`+0x20` min/max/rows idiom anywhere in it, unlike 0x09d1/0x09d2 in this same shard, which do). client/patch-table-names.md's third-party reference names `ZoneIntroMusicTable.dbc` for this opcode (5 fields, 20-byte record, 179 rows) and the handler's fixed portion happens to be the same 20 bytes -- but that binding is `L1 ref` only (unverified by our own extraction: this opcode is absent from client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml's 104-opcode CustomDBCMgr scan), and the byte-count match does not, by itself, prove the store target is that table. No source in this repository names the struct's own fields (see naming_source below).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_20` | L3 | width: L3 (block-copy read, 20 bytes; re-verified this pass against the cached decompile, build/ghidra/handlers-c/0x0991.c, against client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming source (see naming_source below). 20 happens to equal ZoneIntroMusicTable.dbc's record_size (20 bytes, 5 fields, per client/patch-table-names.md) -- but the store target below is not that table, so the match may be coincidental rather than confirming the binding.
 |
| `field_1` | `lpstring` | L3 | width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming source.
 |

## Client

- handler RVA `0x002355d0` in `ascension-extensions-2026-08-13`
