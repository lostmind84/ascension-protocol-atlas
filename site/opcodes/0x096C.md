# `0x096C` SMSG_PATCH_ITEM_COND_EXT_COSTS

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x096c-smsg-patch-item-cond-ext-costs.yaml`](../../protocol/opcodes/0x096c-smsg-patch-item-cond-ext-costs.yaml)

One row of ItemCondExtCosts.dbc (bound_by: L1 ref, patch-table-names.md): the handler's single 16-byte bulk read exactly equals the table's 4-column/16-byte record (client DBC header). AzerothCore never actually loads this table -- ItemCondExtCostsEntryfmt, the ItemCondExtCostsEntry struct, its DBCStorage declaration and its LOAD_DBC call are all commented out in the fork (dead code) -- but the commented-out struct still names 3 of the 4 columns (condExtendedCost, itemExtendedCostEntry, arenaSeason); reported here at L1 with that caveat rather than treated as a live core source.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | DBC column 0. AzerothCore never loads ItemCondExtCosts.dbc: ItemCondExtCostsEntryfmt is commented out in DBCfmt.h ('//char constexpr ItemCondExtCostsEntryfmt[] = "xiii";'), the struct is commented out in DBCStructure.h, sItemCondExtCostsStore's declaration is commented out in DBCStores.cpp, and its LOAD_DBC call is commented out too -- this table is entirely dead code, not an active naming source. Even that dead fmt string marks column 0 'x' (skipped), so it names nothing here regardless. Width L3 (handler-fire.yaml: reads [[uVar5,16]], one 16-byte bulk read == the dead fmt's own 4-column/16-byte shape, for what that is worth). Unnamed. |
| `condExtendedCost` | `u32` | L1 | DBC column 1. Dead code (see field_0's note): DBCStructure.h's commented-out ItemCondExtCostsEntry names this 'condExtendedCost -- ItemTemplate::CondExtendedCost', but the struct, the fmt string and the loader are all disabled -- never compiled, never populated at runtime. Named here only because the ONE RULE treats a name written in the core's file as a real naming source regardless of whether the surrounding code is live; graded L1 same as an active core struct, but flagged as the weakest case in this shard (report it). |
| `itemExtendedCostEntry` | `u32` | L1 | DBC column 2. Dead code (see field_0's note): commented-out struct names this 'itemextendedcostentry -- ItemTemplate::ExtendedCost'. Same dead-code caveat as condExtendedCost. |
| `arenaSeason` | `u32` | L1 | DBC column 3. Dead code (see field_0's note): commented-out struct names this 'arenaseason -- arena season number(1-4)'. Same dead-code caveat as condExtendedCost. |

## Client

- handler RVA `0x0022cef0` in `ascension-extensions-2026-08-13`
