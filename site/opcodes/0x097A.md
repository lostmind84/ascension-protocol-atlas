# `0x097A` SMSG_PATCH_VEHICLE

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x097a-smsg-patch-vehicle.yaml`](../../protocol/opcodes/0x097a-smsg-patch-vehicle.yaml)

One row of Vehicle.dbc: 29 named plain dwords/floats, then four skipped-and-appended strings (m_msslTrgtArcTexture, m_msslTrgtImpactTexture, m_msslTrgtImpactModel[0..1] -- all kept/named by AzerothCore), then five more named fields, then two unnamed trailing dwords ('new in 3.1', no name in DBCStructure.h). 40 dwords (160B) match the table exactly; the fixed block's declared stack size undersells the true span in the decompilation (Ghidra reuses the string temporaries' stack slots), confirmed by matching the four string positions to the format string's four consecutive 's' characters. Table binding is L1 (the third-party reference names it; patch-tables.yaml does not).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `m_ID` | `u32` | L1 | DBC column 0 (n); name 'm_ID' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_flags` | `u32` | L1 | DBC column 1 (i); name 'm_flags' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_turnSpeed` | `f32` | L1 | DBC column 2 (f); name 'm_turnSpeed' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_pitchSpeed` | `f32` | L1 | DBC column 3 (f); name 'm_pitchSpeed' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_pitchMin` | `f32` | L1 | DBC column 4 (f); name 'm_pitchMin' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_pitchMax` | `f32` | L1 | DBC column 5 (f); name 'm_pitchMax' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_seatID_0` | `u32` | L1 | DBC column 6 (i); name 'm_seatID_0' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_seatID_1` | `u32` | L1 | DBC column 7 (i); name 'm_seatID_1' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_seatID_2` | `u32` | L1 | DBC column 8 (i); name 'm_seatID_2' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_seatID_3` | `u32` | L1 | DBC column 9 (i); name 'm_seatID_3' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_seatID_4` | `u32` | L1 | DBC column 10 (i); name 'm_seatID_4' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_seatID_5` | `u32` | L1 | DBC column 11 (i); name 'm_seatID_5' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_seatID_6` | `u32` | L1 | DBC column 12 (i); name 'm_seatID_6' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_seatID_7` | `u32` | L1 | DBC column 13 (i); name 'm_seatID_7' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_mouseLookOffsetPitch` | `f32` | L1 | DBC column 14 (f); name 'm_mouseLookOffsetPitch' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_cameraFadeDistScalarMin` | `f32` | L1 | DBC column 15 (f); name 'm_cameraFadeDistScalarMin' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_cameraFadeDistScalarMax` | `f32` | L1 | DBC column 16 (f); name 'm_cameraFadeDistScalarMax' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_cameraPitchOffset` | `f32` | L1 | DBC column 17 (f); name 'm_cameraPitchOffset' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_facingLimitRight` | `f32` | L1 | DBC column 18 (f); name 'm_facingLimitRight' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_facingLimitLeft` | `f32` | L1 | DBC column 19 (f); name 'm_facingLimitLeft' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtTurnLingering` | `f32` | L1 | DBC column 20 (f); name 'm_msslTrgtTurnLingering' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtPitchLingering` | `f32` | L1 | DBC column 21 (f); name 'm_msslTrgtPitchLingering' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtMouseLingering` | `f32` | L1 | DBC column 22 (f); name 'm_msslTrgtMouseLingering' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtEndOpacity` | `f32` | L1 | DBC column 23 (f); name 'm_msslTrgtEndOpacity' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtArcSpeed` | `f32` | L1 | DBC column 24 (f); name 'm_msslTrgtArcSpeed' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtArcRepeat` | `f32` | L1 | DBC column 25 (f); name 'm_msslTrgtArcRepeat' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtArcWidth` | `f32` | L1 | DBC column 26 (f); name 'm_msslTrgtArcWidth' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtImpactRadius_0` | `f32` | L1 | DBC column 27 (f); name 'm_msslTrgtImpactRadius_0' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtImpactRadius_1` | `f32` | L1 | DBC column 28 (f); name 'm_msslTrgtImpactRadius_1' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtArcTexture` | `lpstring` | L3 | DBC column 29 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. Name 'm_msslTrgtArcTexture' is AzerothCore's own kept DBCStructure.h member (L1); the lpstring's presence, width and position are independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtImpactTexture` | `lpstring` | L3 | DBC column 30 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. Name 'm_msslTrgtImpactTexture' is AzerothCore's own kept DBCStructure.h member (L1); the lpstring's presence, width and position are independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtImpactModel_0` | `lpstring` | L3 | DBC column 31 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. Name 'm_msslTrgtImpactModel_0' is AzerothCore's own kept DBCStructure.h member (L1); the lpstring's presence, width and position are independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtImpactModel_1` | `lpstring` | L3 | DBC column 32 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. Name 'm_msslTrgtImpactModel_1' is AzerothCore's own kept DBCStructure.h member (L1); the lpstring's presence, width and position are independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_cameraYawOffset` | `f32` | L1 | DBC column 33 (f); name 'm_cameraYawOffset' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_uiLocomotionType` | `u32` | L1 | DBC column 34 (i); name 'm_uiLocomotionType' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_msslTrgtImpactTexRadius` | `f32` | L1 | DBC column 35 (f); name 'm_msslTrgtImpactTexRadius' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_uiSeatIndicatorType` | `u32` | L1 | DBC column 36 (i); name 'm_uiSeatIndicatorType' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `m_powerDisplayId` | `u32` | L1 | DBC column 37 (i); name 'm_powerDisplayId' is AzerothCore's own DBCStructure.h struct member for Vehicle.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097a.c). |
| `unknown_38` | `u32` | L3 | DBC column 38 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/097a.c). comment: 'new in 3.1', no further description. |
| `unknown_39` | `u32` | L3 | DBC column 39 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/097a.c). comment: 'new in 3.1', no further description. |

## Client

- handler RVA `0x00234460` in `ascension-extensions-2026-08-13`
