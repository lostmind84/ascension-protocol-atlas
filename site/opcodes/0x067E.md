# `0x067E` SMSG_PATCH_MANASTORM_MESSAGES

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x067e-smsg-patch-manastorm-messages.yaml`](../../protocol/opcodes/0x067e-smsg-patch-manastorm-messages.yaml)

SMSG_PATCH_MANASTORM_MESSAGES: writes a 7-field, 28-byte record into ManastormMessages.dbc's table object, well short of that table's own 39-field, 156-byte row. Three strings are read, each with the client's own NUL-scan idiom (a byte loop hunting the terminator, then FUN_10086e30 to intern it) rather than the length-prefixed FUN_100d3680 helper other opcodes use -- this repository's vocabulary calls that shape `cstring`. No column name is established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0 |
| `field_1` | `u32` | L3 | offset 4, first half of an 8-byte load |
| `field_2` | `u32` | L3 | offset 8, second half of the same load; read as one 8-byte load in the handler; split into two 4-byte fields here to line up with the table's own field count -- could be a single 64-bit value instead, not resolved further |
| `field_3` | `u32` | L3 | offset 0xc |
| `field_4` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (first of three consecutive gaps) |
| `field_5` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (second of three consecutive gaps) |
| `field_6` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (third of three consecutive gaps) |
| `field_7` | `cstring` | L3 | NUL-terminated, scanned byte by byte; not FUN_100d3680 |
| `field_8` | `cstring` | L3 | NUL-terminated, scanned byte by byte; not FUN_100d3680 |
| `field_9` | `cstring` | L3 | NUL-terminated, scanned byte by byte; not FUN_100d3680 |

## Client

- handler RVA `0x002a3b50` in `ascension-extensions-2026-08-13`
