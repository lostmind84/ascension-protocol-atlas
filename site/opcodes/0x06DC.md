# `0x06DC` SMSG_PATCH_COLLECTOR_CACHE_ITEMS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06dc-smsg-patch-collector-cache-items.yaml`](../../protocol/opcodes/0x06dc-smsg-patch-collector-cache-items.yaml)

Named SMSG_PATCH_COLLECTOR_CACHE_ITEMS by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_104` | L3 | one row of CollectorCacheItems.dbc (26 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e0ea0` in `ascension-extensions-2026-08-13`
