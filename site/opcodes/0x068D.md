# `0x068D` SMSG_PATCH_MANASTORM_MODIFIERS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x068d-smsg-patch-manastorm-modifiers.yaml`](../../protocol/opcodes/0x068d-smsg-patch-manastorm-modifiers.yaml)

Named SMSG_PATCH_MANASTORM_MODIFIERS by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_60` | L3 | one row of ManastormModifiers.dbc (15 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x002a3e60` in `ascension-extensions-2026-08-13`
