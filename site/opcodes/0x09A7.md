# `0x09A7` SMSG_PATCH_TAXI_PATH_NODE

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x09a7-smsg-patch-taxi-path-node.yaml`](../../protocol/opcodes/0x09a7-smsg-patch-taxi-path-node.yaml)

One row of TaxiPathNode.dbc: an id (the FT_SORT index column, commented out of DBCStructure.h but conventionally the row's id), then path, index, mapid, x, y, z, actionFlag, delay, arrivalEventID, departureEventID -- all kept and named. No strings. 11 dwords (44B), matching the table exactly.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0, format char 'd' (FT_SORT/FT_IND, the record's own index field per src/common/DataStores/DBCFileLoader.h); DBCStructure.h comments this column out (not a compiled struct member) but the format character itself conventionally marks it as the row's id, so named 'id' at L1; width/position confirmed from the decompiled handler (build/ghidra/handlers-c/09a7.c). |
| `path` | `u32` | L1 | DBC column 1 (i); name 'path' is AzerothCore's own DBCStructure.h struct member for TaxiPathNode.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a7.c). |
| `index` | `u32` | L1 | DBC column 2 (i); name 'index' is AzerothCore's own DBCStructure.h struct member for TaxiPathNode.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a7.c). |
| `mapid` | `u32` | L1 | DBC column 3 (i); name 'mapid' is AzerothCore's own DBCStructure.h struct member for TaxiPathNode.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a7.c). |
| `x` | `f32` | L1 | DBC column 4 (f); name 'x' is AzerothCore's own DBCStructure.h struct member for TaxiPathNode.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a7.c). |
| `y` | `f32` | L1 | DBC column 5 (f); name 'y' is AzerothCore's own DBCStructure.h struct member for TaxiPathNode.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a7.c). |
| `z` | `f32` | L1 | DBC column 6 (f); name 'z' is AzerothCore's own DBCStructure.h struct member for TaxiPathNode.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a7.c). |
| `actionFlag` | `u32` | L1 | DBC column 7 (i); name 'actionFlag' is AzerothCore's own DBCStructure.h struct member for TaxiPathNode.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a7.c). |
| `delay` | `u32` | L1 | DBC column 8 (i); name 'delay' is AzerothCore's own DBCStructure.h struct member for TaxiPathNode.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a7.c). |
| `arrivalEventID` | `u32` | L1 | DBC column 9 (i); name 'arrivalEventID' is AzerothCore's own DBCStructure.h struct member for TaxiPathNode.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a7.c). |
| `departureEventID` | `u32` | L1 | DBC column 10 (i); name 'departureEventID' is AzerothCore's own DBCStructure.h struct member for TaxiPathNode.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a7.c). |

## Client

- handler RVA `0x00233b50` in `ascension-extensions-2026-08-13`
