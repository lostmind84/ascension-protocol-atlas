# `0x053D` CMSG_ASCENSIONGM_TICKET_COMPLETE_REQUEST

**client to server** · status `unknown` · evidence `L3` · layout `unknown` · client code `none`

Fiche: [`protocol/opcodes/0x053d-cmsg-ascensiongm-ticket-complete-request.yaml`](../../protocol/opcodes/0x053d-cmsg-ascensiongm-ticket-complete-request.yaml)

Named CMSG_ASCENSIONGM_TICKET_COMPLETE_REQUEST by the client's own opcode table. No sender exists for this opcode in this build: the value appears nowhere in `.text` as a 32- or 16-bit immediate, nowhere in `.rdata`/`.data`, and nowhere in the shipped Lua (client/client-senders.md's exhaustive scan), and it is absent from every symbol dump (senders/handlers/layouts/handler-fire/handler-strings.yaml). Layout is not established; there is no code to read it from. Unlike the sibling CMSG_ASCENSIONGM_TICKET_LIST_REQUEST (0x053B), which has a live empty-payload sender, this ticket-complete opcode has none.

## Layout

*No layout established.*
