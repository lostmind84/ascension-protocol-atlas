# `0x05BB` SMSG_UPDATE_BROKEN_CHALLENGE_RULE

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05bb-smsg-update-broken-challenge-rule.yaml`](../../protocol/opcodes/0x05bb-smsg-update-broken-challenge-rule.yaml)

The CoA server calls it `SMSG_COA_CHALLENGE_RULE_BROKEN`.

One challenge rule's broken state: the rule key as a length-prefixed string and a byte, handed to CHALLENGE_RULE_BROKEN_UPDATE with format %s%b (the byte's position is the handler's).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `rule` | `lpstring` | L3 |  |
| `broken` | `u8` | L3 |  |

## Client

- handler RVA `0x0013b0f0` in `ascension-extensions-2026-08-13`

## Events

- fires `CHALLENGE_RULE_BROKEN_UPDATE` with format `%s%b`
