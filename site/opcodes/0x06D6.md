# `0x06D6` SMSG_PATCH_CHARACTER_CREATION_ARCHETYPES

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06d6-smsg-patch-character-creation-archetypes.yaml`](../../protocol/opcodes/0x06d6-smsg-patch-character-creation-archetypes.yaml)

One CharacterCreationArchetypes.dbc row with many inline strings. Partially followed.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_variable` | L3 | a CharacterCreationArchetypes.dbc row (157 columns) as the DLL keeps it: two strings, then a loop of 0x30 string slots and further string loops; not followed |

## Client

- handler RVA `0x001df460` in `ascension-extensions-2026-08-13`
