# `0x0976` SMSG_PATCH_CREATURE_DISPLAY_INFO

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0976-smsg-patch-creature-display-info.yaml`](../../protocol/opcodes/0x0976-smsg-patch-creature-display-info.yaml)

One row of CreatureDisplayInfo.dbc: Displayid, ModelId, an unnamed dword (soundID), ExtendedDisplayInfoID, scale, another unnamed dword (creatureModelAlpha), then FOUR skipped-and-appended strings (AzerothCore's own comments call them textureVariation[0..2] and portraitTextureName, but the lines are bare and not compiled, so left unnamed), then six more unnamed dwords. 16 dwords (64B) match the table's 16 columns exactly. The stub's automated extraction only reported one bytes_64 blob; reading the decompiled handler by hand found the four string reads and the exact skip positions. Table binding is L0 (name match only, not in patch-tables.yaml).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `Displayid` | `u32` | L1 | DBC column 0 (n); name 'Displayid' is AzerothCore's own DBCStructure.h struct member for CreatureDisplayInfo.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0976.c). |
| `ModelId` | `u32` | L1 | DBC column 1 (i); name 'ModelId' is AzerothCore's own DBCStructure.h struct member for CreatureDisplayInfo.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0976.c). |
| `unknown_2` | `u32` | L3 | DBC column 2 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/0976.c). DBCStructure.h's bare comment calls this m_soundID. |
| `ExtendedDisplayInfoID` | `u32` | L1 | DBC column 3 (i); name 'ExtendedDisplayInfoID' is AzerothCore's own DBCStructure.h struct member for CreatureDisplayInfo.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0976.c). |
| `scale` | `f32` | L1 | DBC column 4 (f); name 'scale' is AzerothCore's own DBCStructure.h struct member for CreatureDisplayInfo.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0976.c). |
| `unknown_5` | `u32` | L3 | DBC column 5 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/0976.c). DBCStructure.h's bare comment calls this m_creatureModelAlpha. |
| `unknown_6_str` | `lpstring` | L3 | DBC column 6 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/0976.c). DBCStructure.h's comment for this column reads 'm_textureVariation[3] (element 0)' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_7_str` | `lpstring` | L3 | DBC column 7 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/0976.c). DBCStructure.h's comment for this column reads 'm_textureVariation[3] (element 1)' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_8_str` | `lpstring` | L3 | DBC column 8 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/0976.c). DBCStructure.h's comment for this column reads 'm_textureVariation[3] (element 2)' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_9_str` | `lpstring` | L3 | DBC column 9 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/0976.c). DBCStructure.h's comment for this column reads 'm_portraitTextureName' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_10` | `u32` | L3 | DBC column 10 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/0976.c). DBCStructure.h's bare comment calls this m_sizeClass. |
| `unknown_11` | `u32` | L3 | DBC column 11 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/0976.c). DBCStructure.h's bare comment calls this m_bloodID. |
| `unknown_12` | `u32` | L3 | DBC column 12 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/0976.c). DBCStructure.h's bare comment calls this m_NPCSoundID. |
| `unknown_13` | `u32` | L3 | DBC column 13 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/0976.c). DBCStructure.h's bare comment calls this m_particleColorID. |
| `unknown_14` | `u32` | L3 | DBC column 14 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/0976.c). DBCStructure.h's bare comment calls this m_creatureGeosetData. |
| `unknown_15` | `u32` | L3 | DBC column 15 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/0976.c). DBCStructure.h's bare comment calls this m_objectEffectPackageID. |

## Client

- handler RVA `0x0022b0e0` in `ascension-extensions-2026-08-13`
