# `0x097F` SMSG_PATCH_GAME_OBJECT_ART_KIT

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x097f-smsg-patch-game-object-art-kit.yaml`](../../protocol/opcodes/0x097f-smsg-patch-game-object-art-kit.yaml)

One row of GameObjectArtKit.dbc: ID, then seven skipped-and-appended strings, all unnamed (AzerothCore comments them TextureVariation[0..2] and AttachModel[0..3] but the lines are commented out, not compiled). 8 dwords (32B) match the table exactly -- id is a plain read, the other seven positions are never captured as values, only as string-pointer targets. Table binding is L1 (third-party reference only).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `ID` | `u32` | L1 | DBC column 0 (n); name 'ID' is AzerothCore's own DBCStructure.h struct member for GameObjectArtKit.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/097f.c). |
| `unknown_1_str` | `lpstring` | L3 | DBC column 1 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/097f.c). DBCStructure.h's comment for this column reads 'TextureVariation[0]' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_2_str` | `lpstring` | L3 | DBC column 2 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/097f.c). DBCStructure.h's comment for this column reads 'TextureVariation[1]' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_3_str` | `lpstring` | L3 | DBC column 3 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/097f.c). DBCStructure.h's comment for this column reads 'TextureVariation[2]' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_4_str` | `lpstring` | L3 | DBC column 4 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/097f.c). DBCStructure.h's comment for this column reads 'AttachModel[0]' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_5_str` | `lpstring` | L3 | DBC column 5 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/097f.c). DBCStructure.h's comment for this column reads 'AttachModel[1]' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_6_str` | `lpstring` | L3 | DBC column 6 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/097f.c). DBCStructure.h's comment for this column reads 'AttachModel[2]' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_7_str` | `lpstring` | L3 | DBC column 7 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/097f.c). DBCStructure.h's comment for this column reads 'AttachModel[3]' but the line is commented out (not compiled), so that text is not used as the field name. |

## Client

- handler RVA `0x0022c460` in `ascension-extensions-2026-08-13`
