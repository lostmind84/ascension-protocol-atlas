# `0x0969` SMSG_PATCH_ITEM_PURCHASE_GROUP

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0969-smsg-patch-item-purchase-group.yaml`](../../protocol/opcodes/0x0969-smsg-patch-item-purchase-group.yaml)

Named SMSG_PATCH_ITEM_PURCHASE_GROUP by the client's own opcode table. The opcode is not bound in patch-tables.yaml -- the DLL's own custom-table list does not mention this opcode; ItemPurchaseGroup.dbc is the table patch-table-names.md associates with it by name and geometry (grade L1). The handler's own reads fall far short of ItemPurchaseGroup.dbc's own WDBC header (26 fields, 104 bytes), so whatever this patches, it is not a full row of that table. No source names any of the table's columns.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_40` | L3 |  |
| `field_1` | `lpstring` | L3 |  |

## Client

- handler RVA `0x0022dbd0` in `ascension-extensions-2026-08-13`
