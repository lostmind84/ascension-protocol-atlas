# `0x09B3` SMSG_UNUSED1

**server to client** · status `unknown` · evidence `L3` · layout `unknown` · client code `none`

Fiche: [`protocol/opcodes/0x09b3-smsg-unused1.yaml`](../../protocol/opcodes/0x09b3-smsg-unused1.yaml)

Named SMSG_UNUSED1 by the client's own opcode table. Not among the 532 handlers the exhaustive push/push/call registrar scan of `.text` finds (`tools/extract/opcode_handlers.py`, three registrars, `client/symbols/ascension-extensions-2026-08-13/handlers.yaml`); this build registers no handler for it, and the server modules in this fork never send it either (`client/server-usage.yaml` has no entry for it, not even a false-positive literal). Layout is not established because there is nothing to read, not because nobody has looked. The client's own table names it SMSG_UNUSED1: self-described as unused, consistent with there being no registered handler.

## Layout

*No layout established.*
