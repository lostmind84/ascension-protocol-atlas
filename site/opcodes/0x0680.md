# `0x0680` SMSG_PATCH_ZONE_LIGHT

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0680-smsg-patch-zone-light.yaml`](../../protocol/opcodes/0x0680-smsg-patch-zone-light.yaml)

SMSG_PATCH_ZONE_LIGHT: a clean, full-width row write into ZoneLight.dbc -- 7 fields, 28 bytes, matching that table's own header geometry exactly. Unusually for this shard the string is the SECOND field on the wire (right after the id), not the last: the remaining 5 numeric fields are read after it, at cursor positions re-based on the string's own length. No column name is established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0; bounds-checked against the table object's minId/maxId |
| `field_1` | `cstring` | L3 | NUL-terminated, scanned byte by byte, immediately after the id -- not last on the wire |
| `field_2` | `u32` | L3 | first u32 after the string |
| `field_3` | `u32` | L3 | second u32 after the string |
| `field_4` | `u32` | L3 | third u32 after the string |
| `field_5` | `u32` | L3 | fourth u32 after the string |
| `field_6` | `u32` | L3 | fifth u32 after the string |

## Client

- handler RVA `0x0021a4d0` in `ascension-extensions-2026-08-13`
