# `0x06F6` SMSG_PATCH_SPELL_ACTIVATION_OVERLAYS

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x06f6-smsg-patch-spell-activation-overlays.yaml`](../../protocol/opcodes/0x06f6-smsg-patch-spell-activation-overlays.yaml)

SMSG_PATCH_SPELL_ACTIVATION_OVERLAYS: a clean, full-width row write into SpellActivationOverlays.dbc -- 14 fields, 56 bytes, matching that table's own header geometry exactly once the two string columns' wire-slots are counted. This replaces the file's earlier partial layout (`row: bytes_56` + one `unknown` tail field), which undercounted the fields and left the second string unaccounted for. No column name is established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0; bounds-checked against the table object's minId/maxId |
| `field_1` | `u32` | L3 | offset 4 |
| `field_2` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (1st of two non-adjacent gaps) |
| `field_3` | `u32` | L3 | offset 0xc |
| `field_4` | `u32` | L3 | offset 0x10 |
| `field_5` | `u32` | L3 | offset 0x14 |
| `field_6` | `u32` | L3 | offset 0x18 |
| `field_7` | `u32` | L3 | offset 0x1c, first half of an 8-byte load |
| `field_8` | `u32` | L3 | offset 0x20, second half; read as one 8-byte load in the handler; split into two 4-byte fields here to line up with the table's own field count -- could be a single 64-bit value instead, not resolved further |
| `field_9` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (2nd of two non-adjacent gaps) |
| `field_10` | `u32` | L3 | offset 0x28 |
| `field_11` | `u32` | L3 | offset 0x2c |
| `field_12` | `u32` | L3 | offset 0x30 |
| `field_13` | `u32` | L3 | offset 0x34 |
| `field_14` | `cstring` | L3 | 1st of 2, NUL-terminated, scanned byte by byte; fills field_2's slot |
| `field_15` | `cstring` | L3 | 2nd of 2; fills field_9's slot |

## Client

- handler RVA `0x001e3430` in `ascension-extensions-2026-08-13`
