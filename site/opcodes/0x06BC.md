# `0x06BC` SMSG_PURCHASE_CUSTOM_STORE_ITEM_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06bc-smsg-purchase-custom-store-item-result.yaml`](../../protocol/opcodes/0x06bc-smsg-purchase-custom-store-item-result.yaml)

One NUL-terminated string the handler hands to PURCHASE_CUSTOM_STORE_ITEM_RESUL with format %s -- the only read and the only argument; named 'result' after the UI's parameter (default name: no Lua parameter declared).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 |  |

## Client

- handler RVA `0x001a5440` in `ascension-extensions-2026-08-13`

## Events

- fires `PURCHASE_CUSTOM_STORE_ITEM_RESUL` with format `%s`
- the UI receives `PURCHASE_CUSTOM_STORE_ITEM_RESULT` (`client/event-args.yaml`)
