# `0x075C` SMSG_DRAFT_HAND_OF_FATE_SACRIFICE_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x075c-smsg-draft-hand-of-fate-sacrifice-result.yaml`](../../protocol/opcodes/0x075c-smsg-draft-hand-of-fate-sacrifice-result.yaml)

Answer to a draft-mode request: the handler fires DRAFT_RESULT with one string it reads through a callee. Only the string is established; whether the packet carries more is not (the earlier width reading belonged to a callee shared with 0x0753).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 | read in a callee; the only argument of the fire |

## Client

- handler RVA `0x00225970` in `ascension-extensions-2026-08-13`

## Events

- fires `DRAFT_RESULT` with format `%s`
