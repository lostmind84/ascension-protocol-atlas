# `0x0683` SMSG_UNNAMED_0683

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0683-smsg-unnamed.yaml`](../../protocol/opcodes/0x0683-smsg-unnamed.yaml)

Answer to the recovery query for the worldforged-item (the unnamed sixth category: a u32 count, then that many records the handler parses in a callee (not read here) into its per-category list, then RECOVERY_QUERY_RESULT(category) fired with the category string. The record shape is what the UI's RecoveryItemMixin displays (item entry, count, id); it is not established from the binary here.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 |  |
| *repeat × count* | | | |
| &nbsp;&nbsp;`record` | `unknown` | L3 | parsed by a callee the decompiled handler passes the store to; shape not established |

## Client

- handler RVA `0x003052d0` in `ascension-extensions-2026-08-13`

## Events

- fires `RECOVERY_QUERY_RESULT` with format `%s`
- the UI receives `RECOVERY_QUERY_RESULT` (`client/event-args.yaml`)
