# `0x0979` SMSG_PATCH_UNIT_BLOOD_LEVELS

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0979-smsg-patch-unit-blood-levels.yaml`](../../protocol/opcodes/0x0979-smsg-patch-unit-blood-levels.yaml)

Named SMSG_PATCH_UNIT_BLOOD_LEVELS by the client's own opcode table. The opcode is not bound in patch-tables.yaml -- the DLL's own custom-table list does not mention this opcode; UnitBloodLevels.dbc is the table patch-table-names.md associates with it by name and geometry (grade L0). The handler's own reads match UnitBloodLevels.dbc's own WDBC header exactly (4 fields, 16 bytes), so this looks like a full, unmodified row of that table. No source names any of the table's columns.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_16` | L3 |  |

## Client

- handler RVA `0x002343a0` in `ascension-extensions-2026-08-13`
