# `0x09AC` SMSG_PATCH_WORLD_MAP_OVERLAY

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x09ac-smsg-patch-world-map-overlay.yaml`](../../protocol/opcodes/0x09ac-smsg-patch-world-map-overlay.yaml)

One row of WorldMapOverlay.dbc: ID, an unnamed dword (worldMapAreaId), areatableID[0..3] (named), two unnamed dwords ('always 0'), then one skipped-and-appended string (internal_name, unnamed), then eight unnamed trailing dwords ('some ints'). 17 dwords (68B) plus one string, matching the table exactly.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `ID` | `u32` | L1 | DBC column 0 (n); name 'ID' is AzerothCore's own DBCStructure.h struct member for WorldMapOverlay.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09ac.c). |
| `unknown_1` | `u32` | L3 | DBC column 1 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09ac.c). DBCStructure.h's comment calls this worldMapAreaId (index into WorldMapArea.dbc). |
| `areatableID_0` | `u32` | L1 | DBC column 2 (i); name 'areatableID_0' is AzerothCore's own DBCStructure.h struct member for WorldMapOverlay.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09ac.c). |
| `areatableID_1` | `u32` | L1 | DBC column 3 (i); name 'areatableID_1' is AzerothCore's own DBCStructure.h struct member for WorldMapOverlay.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09ac.c). |
| `areatableID_2` | `u32` | L1 | DBC column 4 (i); name 'areatableID_2' is AzerothCore's own DBCStructure.h struct member for WorldMapOverlay.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09ac.c). |
| `areatableID_3` | `u32` | L1 | DBC column 5 (i); name 'areatableID_3' is AzerothCore's own DBCStructure.h struct member for WorldMapOverlay.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09ac.c). |
| `unknown_6` | `u32` | L3 | DBC column 6 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09ac.c). DBCStructure.h's comment: 'always 0, possible part of areatableID[]'. |
| `unknown_7` | `u32` | L3 | DBC column 7 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09ac.c). DBCStructure.h's comment: 'always 0, possible part of areatableID[]'. |
| `unknown_8_str` | `lpstring` | L3 | DBC column 8 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/09ac.c). DBCStructure.h's comment for this column reads 'internal_name' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_9` | `u32` | L3 | DBC column 9 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09ac.c). DBCStructure.h's comment: 'some ints'. |
| `unknown_10` | `u32` | L3 | DBC column 10 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09ac.c). DBCStructure.h's comment: 'some ints'. |
| `unknown_11` | `u32` | L3 | DBC column 11 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09ac.c). DBCStructure.h's comment: 'some ints'. |
| `unknown_12` | `u32` | L3 | DBC column 12 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09ac.c). DBCStructure.h's comment: 'some ints'. |
| `unknown_13` | `u32` | L3 | DBC column 13 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09ac.c). DBCStructure.h's comment: 'some ints'. |
| `unknown_14` | `u32` | L3 | DBC column 14 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09ac.c). DBCStructure.h's comment: 'some ints'. |
| `unknown_15` | `u32` | L3 | DBC column 15 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09ac.c). DBCStructure.h's comment: 'some ints'. |
| `unknown_16` | `u32` | L3 | DBC column 16 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09ac.c). DBCStructure.h's comment: 'some ints'. |

## Client

- handler RVA `0x00234d00` in `ascension-extensions-2026-08-13`
