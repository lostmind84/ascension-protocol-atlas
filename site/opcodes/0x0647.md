# `0x0647` SMSG_PATCH_SKILL_CARD

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0647-smsg-patch-skill-card.yaml`](../../protocol/opcodes/0x0647-smsg-patch-skill-card.yaml)

One row of SkillCard.dbc: 12 dwords (48 bytes), of which the 4 at +0x14..+0x20 arrive as throwaway placeholders and are then overwritten by 4 length-prefixed strings the wire sends right after. All 12 fields are named from the client's own Lua-table builder for this row, FUN_1009bc60 (VA 0x1009bc60, client/symbols/.../lua-keys.yaml's f_1009bc60), which pushes each one by an explicit key literal read at the matching record offset; two of its key literals (Rank at +0x08, Type at +0x14) are .rdata strings the automatic extractor did not capture. lua-keys.yaml's own offset for CollectedRank (16) is a false pairing: FUN_1009bc60 shows CollectedRank is computed from a separate per-player hashmap, not read from the record, so that key is not one of this row's columns.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L3 | record +0x00, the row key -- range-checked against minId 0x10bdf508 / maxId 0x10bdf504, indexes the row table at 0x10bdf518, then passed to FUN_10133ac0. |
| `CardID` | `u32` | L3 | record +0x04. Name and offset from the Lua-table builder FUN_1009bc60 (VA 0x1009bc60): `SetKey(param_1,"CardID"); SetInt(param_1,*(param_2+4))`. |
| `Rank` | `u32` | L3 | record +0x08. Key literal at 0x10b28c38 ("Rank", read back from the .rdata bytes), pushed by FUN_1009bc60 from `*(param_2+8)`. |
| `ItemID` | `u32` | L3 | record +0x0c. FUN_1009bc60: `SetKey(param_1,"ItemID"); SetInt(param_1,*(param_2+0xc))`. |
| `SpellID` | `u32` | L3 | record +0x10. FUN_1009bc60: `SetKey(param_1,"SpellID"); SetInt(param_1,*(param_2+0x10))`; matches lua-keys.yaml's `f_1009bc60` entry `{key: SpellID, offset: 16}`. |
| `Type` | `cstring` | L3 | record +0x14, the wire's 1st of 4 trailing strings, overwriting the placeholder dword read at this offset. Key literal at 0x10b28708 ("Type", read back from the .rdata bytes), pushed by FUN_1009bc60 from the string pointer at `*(param_2+0x14)`. lua-keys.yaml's automatic extractor never found this key (it only paired offset 0x10 with SpellID and, wrongly, with the unrelated runtime value CollectedRank -- see summary). |
| `Expansion` | `cstring` | L3 | record +0x18, the wire's 2nd trailing string. FUN_1009bc60: `SetKey(param_1,"Expansion"); SetString(param_1,*(param_2+0x18))`; matches lua-keys.yaml. |
| `Class` | `cstring` | L3 | record +0x1c, the wire's 3rd trailing string. FUN_1009bc60: `SetKey(param_1,"Class"); SetString(param_1,*(param_2+0x1c))`; matches lua-keys.yaml. |
| `Quality` | `cstring` | L3 | record +0x20, the wire's 4th trailing string -- sent as text (e.g. a quality name), not a numeric quality enum. FUN_1009bc60: `SetKey(param_1,"Quality"); SetString(param_1,*(param_2+0x20))`; matches lua-keys.yaml. |
| `IsWildcard` | `u32` | L3 | record +0x24, read as a C++ bool by the getter (`*(int *)(param_2+0x24) != 0`). FUN_1009bc60: `SetKey(param_1,"IsWildcard")`; matches lua-keys.yaml offset 36. |
| `IsDraftMode` | `u32` | L3 | record +0x28, read as a bool. FUN_1009bc60: `SetKey(param_1,"IsDraftMode")`; matches lua-keys.yaml offset 40. |
| `IsStarterCard` | `u32` | L3 | record +0x2c, read as a bool. FUN_1009bc60: `SetKey(param_1,"IsStarterCard")`; matches lua-keys.yaml offset 44. The record ends here (0x30 = 48 bytes = the 12 fields of SkillCard.dbc); IsCollected, CollectedProgress, CollectedRank, QualityCost and MaxRank, which FUN_1009bc60 also pushes, are computed from player state and a separate hashmap lookup keyed by SpellID, not read from this record. |

## Client

- handler RVA `0x001e2f10` in `ascension-extensions-2026-08-13`
