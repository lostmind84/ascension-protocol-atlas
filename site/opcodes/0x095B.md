# `0x095B` SMSG_PATCH_PARTICLE_COLOR

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x095b-smsg-patch-particle-color.yaml`](../../protocol/opcodes/0x095b-smsg-patch-particle-color.yaml)

Named SMSG_PATCH_PARTICLE_COLOR by the client's own opcode table. The opcode is not bound in patch-tables.yaml -- the DLL's own custom-table list does not mention this opcode; ParticleColor.dbc is the table patch-table-names.md associates with it by name and geometry (grade L1). The handler's own reads match ParticleColor.dbc's own WDBC header exactly (10 fields, 40 bytes), so this looks like a full, unmodified row of that table. No source names any of the table's columns.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_40` | L3 |  |

## Client

- handler RVA `0x0022f670` in `ascension-extensions-2026-08-13`
