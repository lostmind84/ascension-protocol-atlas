# `0x095F` SMSG_PATCH_SPELL_RUNE_COST

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x095f-smsg-patch-spell-rune-cost.yaml`](../../protocol/opcodes/0x095f-smsg-patch-spell-rune-cost.yaml)

One row of SpellRuneCost.dbc (bound_by: L1 ref, patch-table-names.md): the handler's single 20-byte bulk read exactly equals the table's 5-column/20-byte record (client DBC header). All 5 columns are named by AzerothCore's own SpellRuneCostEntry struct (L1, another project's reading of the same file).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0 (index field, fmt 'n'); core: SpellRuneCostEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar2,20]], one 20-byte bulk read == the full 5-column/20-byte SpellRuneCost.dbc record); name L1 (core DBCStructure.h, another project's reading of the same file). |
| `runeCost_blood` | `u32` | L1 | DBC column 1; core: SpellRuneCostEntry::RuneCost[0], '0=blood' per core's comment. Width L3; name L1 (core DBCStructure.h). |
| `runeCost_frost` | `u32` | L1 | DBC column 2; core: RuneCost[1], '1=frost'. Width L3; name L1 (core DBCStructure.h). |
| `runeCost_unholy` | `u32` | L1 | DBC column 3; core: RuneCost[2], '2=unholy'. Width L3; name L1 (core DBCStructure.h). |
| `runePowerGain` | `u32` | L1 | DBC column 4; core: SpellRuneCostEntry::runePowerGain. Width L3; name L1 (core DBCStructure.h). |

## Client

- handler RVA `0x00232d60` in `ascension-extensions-2026-08-13`
