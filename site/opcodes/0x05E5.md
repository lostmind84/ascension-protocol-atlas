# `0x05E5` SMSG_RECOVER_DELETED_ITEM_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05e5-smsg-recover-deleted-item-result.yaml`](../../protocol/opcodes/0x05e5-smsg-recover-deleted-item-result.yaml)

A recovery result (DELETED_ITEM): one result string; the event's category is a local constant.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 | one string read from the packet; the category RECOVERY_RESULT(category, result) carries is a RECOVERY_SERVICE_CATEGORY_* literal the handler builds locally, not a second field |

## Client

- handler RVA `0x00303300` in `ascension-extensions-2026-08-13`

## Events

- fires `RECOVERY_RESULT`
- the UI receives `RECOVERY_RESULT` (`client/event-args.yaml`)
