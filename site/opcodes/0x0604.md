# `0x0604` SMSG_REFORGE_RANDOM_ENCHANT_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0604-smsg-reforge-random-enchant-result.yaml`](../../protocol/opcodes/0x0604-smsg-reforge-random-enchant-result.yaml)

Answer to a random-enchant reforge: a NUL-terminated result string, then a u32; the handler fires MYSTIC_ENCHANT_REFORGE_RESULT with format %s%u (EnchantCollection.lua receives the result). The u32's meaning is not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 |  |
| `unknown_1` | `u32` | L3 | the %u of the fire; meaning not established |

## Client

- handler RVA `0x002e9040` in `ascension-extensions-2026-08-13`

## Events

- fires `MYSTIC_ENCHANT_REFORGE_RESULT` with format `%s%u`
- the UI receives `MYSTIC_ENCHANT_REFORGE_RESULT` (`client/event-args.yaml`)
- the UI receives `MYSTIC_ENCHANT_REFORGE_RESULT` (`client/event-args.yaml`)
