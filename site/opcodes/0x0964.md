# `0x0964` SMSG_PATCH_ITEM_VISUALS

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0964-smsg-patch-item-visuals.yaml`](../../protocol/opcodes/0x0964-smsg-patch-item-visuals.yaml)

Named SMSG_PATCH_ITEM_VISUALS by the client's own opcode table. The opcode is not bound in patch- tables.yaml -- the DLL's own custom-table list does not mention this opcode; ItemVisuals.dbc is the table patch-table-names.md associates with it by name and geometry (grade L1). The handler's own reads match ItemVisuals.dbc's own WDBC header exactly (6 fields, 24 bytes), so this looks like a full, unmodified row of that table. No source names any of the table's columns.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_24` | L3 |  |

## Client

- handler RVA `0x0022e1f0` in `ascension-extensions-2026-08-13`
