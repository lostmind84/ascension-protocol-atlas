# `0x0698` SMSG_APPLY_APPEARANCES_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0698-smsg-apply-appearances-result.yaml`](../../protocol/opcodes/0x0698-smsg-apply-appearances-result.yaml)

Named SMSG_APPLY_APPEARANCES_RESULT by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 | the client reads a NUL-terminated string; the server writes its `result` variable, whose type was not confirmed here |

## Client

- handler RVA `0x000c4a50` in `ascension-extensions-2026-08-13`

## Events

- fires `APPLY_PENDING_APPEARANCE_RESULT` with format `%b%s`
- the UI receives `APPLY_PENDING_APPEARANCE_RESULT` (`client/event-args.yaml`)
- the UI receives `APPLY_PENDING_APPEARANCE_RESULT` (`client/event-args.yaml`)

## Server

- `modules/mod-ascension-compat/src/AscensionCompat.cpp`
