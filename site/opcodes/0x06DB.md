# `0x06DB` SMSG_PATCH_COLLECTOR_CACHE_RARITY_RATES

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06db-smsg-patch-collector-cache-rarity-rates.yaml`](../../protocol/opcodes/0x06db-smsg-patch-collector-cache-rarity-rates.yaml)

Named SMSG_PATCH_COLLECTOR_CACHE_RARITY_RATES by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_16` | L3 | one row of CollectorCacheRarityRates.dbc (4 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e0f40` in `ascension-extensions-2026-08-13`
