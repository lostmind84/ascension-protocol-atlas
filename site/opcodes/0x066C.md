# `0x066C` SMSG_PATCH_SEALED_CARD_COSTS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x066c-smsg-patch-sealed-card-costs.yaml`](../../protocol/opcodes/0x066c-smsg-patch-sealed-card-costs.yaml)

Named SMSG_PATCH_SEALED_CARD_COSTS by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_100` | L3 | one row of SealedCardCosts.dbc (25 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x0031a1f0` in `ascension-extensions-2026-08-13`
