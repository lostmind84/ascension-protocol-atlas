# `0x056A` SMSG_PATCH_ITEM_DISPLAY_INFO_COLLECTIONS

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x056a-smsg-patch-item-display-info-collections.yaml`](../../protocol/opcodes/0x056a-smsg-patch-item-display-info-collections.yaml)

SMSG_PATCH_ITEM_DISPLAY_INFO_COLLECTIONS: patch_tables.yaml does not bind this opcode to any table (it is absent from its handlers: map, even though the decompiled C this binding is built from was on disk). Hand-tracing the handler shows it DOES follow the standard CustomDBCMgr idiom -- minId/maxId-bounded lookup into a pointer array at +0x20 of a table object -- but at instance address 0x10bdff3c, which our own name-getter/instance-pairing scan never names: it falls in a one-slot gap between SpellAlternativePowerType.dbc (0x10bdff18) and LFGActivities.dbc (0x10bdff60), exactly one 0x24-byte span short. The stored record is 8 fields / 32 bytes, which matches ItemDisplayInfoCollections.dbc's own header (8 fields, 32-byte record) exactly -- real corroboration of the third party's claim that this is that table, but not proof: our own extraction still does not name the address this handler actually touches, so the table identity stays L1 and the column names stay unestablished either way.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0; the id bounds-checked against minId/maxId of the table object at 0x10bdff3c |
| `field_1` | `u32` | L3 | offset 4, first half of an 8-byte load |
| `field_2` | `u32` | L3 | offset 8, second half of the same load; read as one 8-byte load in the handler; split into two 4-byte fields here to line up with the table's own field count -- could be a single 64-bit value instead, not resolved further |
| `field_3` | `u32` | L3 | offset 0xc |
| `field_4` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (first of two consecutive gaps) |
| `field_5` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (second of two consecutive gaps) |
| `field_6` | `u32` | L3 | offset 0x18 |
| `field_7` | `u32` | L3 | offset 0x1c |
| `field_8` | `lpstring` | L3 | via FUN_100d3680 |
| `field_9` | `lpstring` | L3 | via FUN_100d3680 |

## Client

- handler RVA `0x001e1b50` in `ascension-extensions-2026-08-13`
