# `0x06ED` SMSG_PATCH_APPEARANCE_DETAILS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06ed-smsg-patch-appearance-details.yaml`](../../protocol/opcodes/0x06ed-smsg-patch-appearance-details.yaml)

One row of AppearanceDetails.dbc: id, a dword, a cstring (over a skipped placeholder dword), then a fourth dword -- only 20 of the table's 84 bytes (5 of its 21 fields), the row allocation size the handler uses. Despite sharing decompiled code with 0x0691 (Ghidra put both opcodes' handlers in one function at 0x101dca00), this opcode's own entry point (registration RVA 0x001e6b4b, reached via the label at 0x101dcd30) only ever touches AppearanceDetails.dbc's row array (0x10bdfa4c matches its CustomDBCMgr object exactly) -- AppearanceCategories.dbc, which patch-tables.yaml also lists for this opcode, is written by 0x0691's own entry point, not this one. No column names were found this pass: patch-table-names.md's cited 'ui getter FrameXML/UIParent.lua:1787' is C_Appearance.GetAppearanceDisplayInfo, which this pass confirmed (by its minId/maxId/rows addresses) reads Appearances.dbc's row array, not AppearanceDetails.dbc's -- it names no field here. A second, name-shaped candidate, C_Appearance.GetAppearanceDetails (VA 0x100c7f60), was also checked and reads an unrelated 20-byte-stride cache at a different base address, not this table either.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L3 | record +0x00, the row key -- range-checked against minId 0x10bdfa3c / maxId 0x10bdfa38, indexes the row table at 0x10bdfa4c, then passed to FUN_10133ac0. 0x10bdfa4c is AppearanceDetails.dbc's own rows pointer (instance 0x10bdfa2c + 0x20), confirming this handler writes AppearanceDetails.dbc and not AppearanceCategories.dbc. |
| `unknown_1` | `u32` | L3 | record +0x04. No naming source located this pass (see summary). Not named. |
| `unknown_2` | `bytes_4` | L3 | record +0x08, SKIPPED by the reader: sent on the wire as a throwaway placeholder dword, then overwritten by the trailing cstring below. |
| `unknown_3` | `u32` | L3 | record +0x0c. No naming source located this pass. Not named. |
| `unknown_4` | `u32` | L3 | record +0x10, the last field this opcode writes. AppearanceDetails.dbc's own header reports 21 fields / 84 bytes; only these first 5 (20 bytes) are ever touched by this SMSG -- the allocation size passed to the row constructor is literally 0x14 (20), not the table's full 84-byte record. |
| `string_1` | `cstring` | L3 | record +0x08, over the skipped dword. The row's only string column, sent as a plain inline cstring (not length-prefixed). No naming source located this pass. |

## Client

- handler RVA `0x001dcd30` in `ascension-extensions-2026-08-13`
