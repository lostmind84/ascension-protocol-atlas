# `0x0954` SMSG_PATCH_SPELL_DIFFICULTY

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0954-smsg-patch-spell-difficulty.yaml`](../../protocol/opcodes/0x0954-smsg-patch-spell-difficulty.yaml)

One row of SpellDifficulty.dbc (bound_by: L1 ref, patch-table-names.md): the handler's single 20-byte bulk read exactly equals the table's 5-column/20-byte record (client DBC header). All 5 columns are named by AzerothCore's own SpellDifficultyEntry struct (L1, another project's reading of the same file).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0 (index field, fmt 'n'); core: SpellDifficultyEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar2,20]], one 20-byte bulk read == the full 5-column/20-byte SpellDifficulty.dbc record); name L1 (core DBCStructure.h, another project's reading of the same file). |
| `spellId_10n` | `i32` | L1 | DBC column 1; core: SpellDifficultyEntry::SpellID[0] (int32), '10N' instance mode per core's comment. Width L3; name L1 (core DBCStructure.h). |
| `spellId_25n` | `i32` | L1 | DBC column 2; core: SpellDifficultyEntry::SpellID[1] (int32), '25N'. Width L3; name L1 (core DBCStructure.h). |
| `spellId_10h` | `i32` | L1 | DBC column 3; core: SpellDifficultyEntry::SpellID[2] (int32), '10H'. Width L3; name L1 (core DBCStructure.h). |
| `spellId_25h` | `i32` | L1 | DBC column 4; core: SpellDifficultyEntry::SpellID[3] (int32), '25H'. Width L3; name L1 (core DBCStructure.h). |

## Client

- handler RVA `0x00231b00` in `ascension-extensions-2026-08-13`
