# `0x06BE` SMSG_PATCH_SUPER_TRACK

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06be-smsg-patch-super-track.yaml`](../../protocol/opcodes/0x06be-smsg-patch-super-track.yaml)

Named SMSG_PATCH_SUPER_TRACK by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_32` | L3 | one row of SuperTrack.dbc (8 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e4200` in `ascension-extensions-2026-08-13`
