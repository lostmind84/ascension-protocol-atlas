# `0x0677` SMSG_PATCH_CHALLENGE_LEVELS

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0677-smsg-patch-challenge-levels.yaml`](../../protocol/opcodes/0x0677-smsg-patch-challenge-levels.yaml)

SMSG_PATCH_CHALLENGE_LEVELS: a clean, full-width row write into ChallengeLevels.dbc -- 5 numeric fields, 20 bytes, no strings, matching that table's own header geometry exactly (client/patch-table-names.md). A Lua getter of a similar name exists (`C_Challenge.GetChallengeLevels(id)` in Ascension_ChallengesUI/Challenges/ChallengeItemMixin.lua:654, returning maxLevel/highestCompletedLevel/maxPossibleLevel at runtime) but it is a native C_Challenge call over a player's own progress, not a getter over this static scaling table -- checked and read here as an unrelated name collision, not a naming source. No column name is established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0x0 |
| `field_1` | `u32` | L3 | offset 0x4 |
| `field_2` | `u32` | L3 | offset 0x8 |
| `field_3` | `u32` | L3 | offset 0xc |
| `field_4` | `u32` | L3 | offset 0x10 |

## Client

- handler RVA `0x00139100` in `ascension-extensions-2026-08-13`
