# `0x0937` SMSG_PATCH_ACHIEVEMENT_CRITERIA

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0937-smsg-patch-achievement-criteria.yaml`](../../protocol/opcodes/0x0937-smsg-patch-achievement-criteria.yaml)

One row of the stock Achievement_Criteria.dbc (AzerothCore already loads this table; ADR 0005). Reading the actual decompiled handler (build/ghidra/handlers-c/0x0937.c, RVA 0x00229b40) by hand resolves the earlier 'unknown repeat' guess: 9 leading dwords match AchievementCriteriaEntry's ID/referredAchievement/requiredType, its 2-dword requiredType-dependent union, and its 4-dword additionalRequirements[MAX_CRITERIA_REQUIREMENTS=2] array exactly, then a length-prefixed blob (memcpy, not the lpstring helper, then a transform of unconfirmed purpose) whose repeat count is provably exactly 1 by the same stack-address arithmetic as 0x0935, then 5 trailing dwords match name's own flag dword plus flags/timedType/timerStartEvent/timeLimit exactly. 9 + 17 (one collapsed locale block, name[16]+flag) + 5 = 31, the file's full column count, leaving only the fully-uncommented showOrder (column 30) unaccounted -- consistent with this bucket's pattern of not sending columns the core does not comment at all. A further 'string' the earlier reading of this same function guessed at is NOT included here, for the same reason given on 0x0935: it is always empty, a decompilation artifact of stack-slot reuse. 15 of 20 fields are named from DBCStructure.h (L1); the type-dependent union (2 fields) keeps the 'x' convention's unnamed field_N.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `ID` | `u32` | L1 | active AchievementCriteriaEntry member (column 0) |
| `referredAchievement` | `u32` | L1 | active AchievementCriteriaEntry member (column 1) |
| `requiredType` | `u32` | L1 | active AchievementCriteriaEntry member (column 2); selects which arm of the criteria union the next 2 columns belong to |
| `field_3` | `u32` | L1 | type-dependent union member of AchievementCriteriaEntry (column 3, e.g. creatureID/bgMapID/level/skillID depending on requiredType); the core gives it no single stable name, so none is asserted |
| `field_4` | `u32` | L1 | type-dependent union member of AchievementCriteriaEntry (column 4); see field_3 |
| `additionalRequirement_type[0]` | `u32` | L1 | active AchievementCriteriaEntry member (additionalRequirements[MAX_CRITERIA_REQUIREMENTS], MAX_CRITERIA_REQUIREMENTS=2; columns 5-8) |
| `additionalRequirement_value[0]` | `u32` | L1 | active AchievementCriteriaEntry member |
| `additionalRequirement_type[1]` | `u32` | L1 | active AchievementCriteriaEntry member |
| `additionalRequirement_value[1]` | `u32` | L1 | active AchievementCriteriaEntry member |
| *repeat × 1* | | | |
| &nbsp;&nbsp;`field_9` | `bytes_variable` | L3 | a u32 length then that many raw bytes (memcpy, NOT the FUN_100d3680 lpstring idiom used elsewhere in this bucket), the result passed through FUN_10086e30 (transform of unconfirmed purpose) and freed, not stored verbatim. The repeat count is confirmed at exactly 1 by stack layout, the same reasoning as 0x0935 (see that fiche's note). By column arithmetic (9 prefix + 1x17 collapsed here + 5 more read after it = 31, the file's full column count) this occupies the file's disabled name[16]+flag locale block (columns 9-25) -- an inference from arithmetic, not a direct read, so no name is asserted. |
| `field_10` | `u32` | L1 | core format char 'x' (column 25, name's own flag dword, per the same locale-block convention as other tables); no formal name |
| `flags` | `u32` | L1 | active AchievementCriteriaEntry member (column 26) |
| `timedType` | `u32` | L1 | active AchievementCriteriaEntry member (column 27) |
| `timerStartEvent` | `u32` | L1 | active AchievementCriteriaEntry member (column 28) |
| `timeLimit` | `u32` | L1 | active AchievementCriteriaEntry member (column 29, the last column this handler reads; the file's 31st column, showOrder, is commented out in the core and is not read here either) |

## Client

- handler RVA `0x00229b40` in `ascension-extensions-2026-08-13`
