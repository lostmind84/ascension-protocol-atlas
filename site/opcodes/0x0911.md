# `0x0911` SMSG_POI_CLEAR

**server to client** · status `hypothetical` · evidence `L3` · layout `empty` · client code `handler`

Fiche: [`protocol/opcodes/0x0911-smsg-poi-clear.yaml`](../../protocol/opcodes/0x0911-smsg-poi-clear.yaml)

POI clear: no payload. The handler takes no packet parameter at all (FUN_102cf6a0(void)); it clears the POI map and formats an event.

## Layout

*No layout established.*

## Client

- handler RVA `0x002cf6a0` in `ascension-extensions-2026-08-13`
