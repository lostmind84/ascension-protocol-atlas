# `0x058F` SMSG_ITEM_QUERY_RESPONSE_BULK

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x058f-smsg-item-query-response-bulk.yaml`](../../protocol/opcodes/0x058f-smsg-item-query-response-bulk.yaml)

Bulk item query response: a count, then that many stock responses fed one by one to an exe routine.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 |  |
| *repeat × count* | | | |
| &nbsp;&nbsp;`response` | `bytes_variable` | L3 | each entry goes to the exe routine at 0x00635190 -- the stock item query response, by the opcode name (ADR 0005) |

## Client

- handler RVA `0x000d3dc0` in `ascension-extensions-2026-08-13`
