# `0x0748` SMSG_PLAYER_POLL_SUBMIT_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0748-smsg-player-poll-submit-result.yaml`](../../protocol/opcodes/0x0748-smsg-player-poll-submit-result.yaml)

Answer to a poll submission: a NUL-terminated result string handed to PLAYER_POLL_ANSWER_RESULT(result) (format %s%u, the u32 being the poll id the manager already holds; its log line reads result={} pollID={} opcode={}). The handler then reads more when bytes remain (the poll's own state), not decoded here.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 |  |

## Client

- handler RVA `0x002d8030` in `ascension-extensions-2026-08-13`

## Events

- fires `PLAYER_POLL_ANSWER_RESULT` with format `%s%u`
- the UI receives `PLAYER_POLL_ANSWER_RESULT` (`client/event-args.yaml`)
