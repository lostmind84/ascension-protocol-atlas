# `0x066F` SMSG_TOKEN_LIST

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x066f-smsg-token-list.yaml`](../../protocol/opcodes/0x066f-smsg-token-list.yaml)

The token list: a count, then per token its type name and fields this reading did not follow.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 |  |
| *repeat × count* | | | |
| &nbsp;&nbsp;`tokenType` | `cstring` | L3 | matched against the 62 TOKEN_TYPE_* names (table at 0x10b58850) to an index |
| &nbsp;&nbsp;`rest` | `bytes_variable` | L3 | the record continues (a 0x10-byte token record is filled); the reads after the string were not followed -- 0x0670 carries two u32 after the same string |

## Client

- handler RVA `0x0033c8a0` in `ascension-extensions-2026-08-13`
