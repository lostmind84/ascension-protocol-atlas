# `0x06FA` SMSG_PATCH_OBJECT_SPAWN_VISIBILITY_SETTINGS

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x06fa-smsg-patch-object-spawn-visibility-settings.yaml`](../../protocol/opcodes/0x06fa-smsg-patch-object-spawn-visibility-settings.yaml)

SMSG_PATCH_OBJECT_SPAWN_VISIBILITY_SETTINGS: a clean, full-width row write into ObjectSpawnVisibilitySettings.dbc -- 7 fields, 28 bytes, matching that table's own header geometry exactly once the string column's wire-slot is counted. This replaces the file's earlier partial layout (`row: bytes_28` + one `unknown` tail field). No column name is established. The table's own snapshot row count is 0: the manifest lists no rows for it, so there is currently nothing to see this packet apply to.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0; bounds-checked against the table object's minId/maxId |
| `field_1` | `u32` | L3 | offset 4 |
| `field_2` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further |
| `field_3` | `u32` | L3 | offset 0xc |
| `field_4` | `u32` | L3 | offset 0x10 |
| `field_5` | `u32` | L3 | offset 0x14 |
| `field_6` | `u32` | L3 | offset 0x18 |
| `field_7` | `cstring` | L3 | NUL-terminated, scanned byte by byte; fills field_2's slot |

## Client

- handler RVA `0x001e2680` in `ascension-extensions-2026-08-13`
