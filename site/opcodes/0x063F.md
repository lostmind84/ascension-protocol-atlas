# `0x063F` SMSG_ASCENSIONGM_TICKET_HISTORY_LIST

**server to client** · status `unknown` · evidence `L3` · layout `unknown` · client code `none`

Fiche: [`protocol/opcodes/0x063f-smsg-ascensiongm-ticket-history-list.yaml`](../../protocol/opcodes/0x063f-smsg-ascensiongm-ticket-history-list.yaml)

Named SMSG_ASCENSIONGM_TICKET_HISTORY_LIST by the client's own opcode table. No handler is registered for this opcode anywhere in this build's handler-registration scan (532 opcodes registered in total, this one not among them), and it is likewise absent from the read-sequence, handler-fire and handler-strings dumps. Layout is not established; there is no code to read it from. Its request, CMSG_ASCENSIONGM_TICKET_HISTORY_REQUEST (0x063E), likewise has no sender in this build (see that fiche). The server module does not send this opcode either (absent from client/server-layouts.md's 29 verified sends).

## Layout

*No layout established.*
