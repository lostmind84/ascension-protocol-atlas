# `0x0920` SMSG_PGF_GROUP_DELISTING_RESPONSE

**server to client** · status `unknown` · evidence `L3` · layout `unknown` · client code `none`

Fiche: [`protocol/opcodes/0x0920-smsg-pgf-group-delisting-response.yaml`](../../protocol/opcodes/0x0920-smsg-pgf-group-delisting-response.yaml)

Named SMSG_PGF_GROUP_DELISTING_RESPONSE by the client's own opcode table. Extensions.dll's handler-registration table -- an exhaustive scan of .text for the push-handler/push-opcode/call-register idiom that recovers all 532 handlers this build registers -- has no entry for 0x0920, and none of the PGF (player group finder) request/response cluster (0x0533-0x053A, 0x091A-0x0924) has one either. This build has no code that reads the packet: layout is not established and cannot be inferred from the name.

## Layout

*No layout established.*
