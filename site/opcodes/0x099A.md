# `0x099A` SMSG_PATCH_AREA_GROUP

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x099a-smsg-patch-area-group.yaml`](../../protocol/opcodes/0x099a-smsg-patch-area-group.yaml)

One row of AreaGroup.dbc: AreaGroupId, AreaId[0..5], nextGroup -- all eight columns kept and named by AzerothCore, no strings, no skips. A single straight-line read, 8 dwords (32B), matching the table exactly. Table binding is L1 (third-party reference only).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `AreaGroupId` | `u32` | L1 | DBC column 0 (n); name 'AreaGroupId' is AzerothCore's own DBCStructure.h struct member for AreaGroup.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/099a.c). |
| `AreaId_0` | `u32` | L1 | DBC column 1 (i); name 'AreaId_0' is AzerothCore's own DBCStructure.h struct member for AreaGroup.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/099a.c). |
| `AreaId_1` | `u32` | L1 | DBC column 2 (i); name 'AreaId_1' is AzerothCore's own DBCStructure.h struct member for AreaGroup.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/099a.c). |
| `AreaId_2` | `u32` | L1 | DBC column 3 (i); name 'AreaId_2' is AzerothCore's own DBCStructure.h struct member for AreaGroup.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/099a.c). |
| `AreaId_3` | `u32` | L1 | DBC column 4 (i); name 'AreaId_3' is AzerothCore's own DBCStructure.h struct member for AreaGroup.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/099a.c). |
| `AreaId_4` | `u32` | L1 | DBC column 5 (i); name 'AreaId_4' is AzerothCore's own DBCStructure.h struct member for AreaGroup.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/099a.c). |
| `AreaId_5` | `u32` | L1 | DBC column 6 (i); name 'AreaId_5' is AzerothCore's own DBCStructure.h struct member for AreaGroup.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/099a.c). |
| `nextGroup` | `u32` | L1 | DBC column 7 (i); name 'nextGroup' is AzerothCore's own DBCStructure.h struct member for AreaGroup.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/099a.c). |

## Client

- handler RVA `0x0022a270` in `ascension-extensions-2026-08-13`
