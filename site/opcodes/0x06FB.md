# `0x06FB` SMSG_PATCH_GAMEOBJECT_DISPLAY_INFO_ADDON

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x06fb-smsg-patch-gameobject-display-info-addon.yaml`](../../protocol/opcodes/0x06fb-smsg-patch-gameobject-display-info-addon.yaml)

Named SMSG_PATCH_GAMEOBJECT_DISPLAY_INFO_ADDON by the client's own opcode table. patch-tables.yaml binds this handler to GameObjectDisplayInfoAddon.dbc's own CustomDBCMgr object; the handler reads exactly the table's 2 fields (an id, then a NUL-terminated string standing in for the disk row's string-offset column) and stores them by id. No source names either column.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 |  |
| `field_1` | `cstring` | L3 |  |

## Client

- handler RVA `0x0020c190` in `ascension-extensions-2026-08-13`
