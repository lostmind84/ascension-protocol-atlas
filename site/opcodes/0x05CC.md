# `0x05CC` SMSG_UPDATE_SPECIALIZATION_FLAGS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05cc-smsg-update-specialization-flags.yaml`](../../protocol/opcodes/0x05cc-smsg-update-specialization-flags.yaml)

Specialization flags changed: two u32 the handler passes, in order, to SPECIALIZATION_FLAGS_UPDATED with format %u%u (the mapping is the handler's), and the second alone to ASCENSION_CUSTOM_GAME_MODE_CHANGED(modes). The names come from the events' names, not from a declared Lua parameter list.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `specializationId` | `u32` | L3 | first %u of SPECIALIZATION_FLAGS_UPDATED; named after the event |
| `flags` | `u32` | L3 | second %u, and the `modes` of ASCENSION_CUSTOM_GAME_MODE_CHANGED |

## Client

- handler RVA `0x0031eca0` in `ascension-extensions-2026-08-13`

## Events

- fires `SPECIALIZATION_FLAGS_UPDATED` with format `%u%u`
- fires `ASCENSION_CUSTOM_GAME_MODE_CHANG` with format `%u`
- the UI receives `ASCENSION_CUSTOM_GAME_MODE_CHANGED` (`client/event-args.yaml`)
- the UI receives `ASCENSION_CUSTOM_GAME_MODE_CHANGED` (`client/event-args.yaml`)
