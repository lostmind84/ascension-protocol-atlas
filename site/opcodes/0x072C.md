# `0x072C` SMSG_UNNAMED_072C

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x072c-smsg-unnamed.yaml`](../../protocol/opcodes/0x072c-smsg-unnamed.yaml)

Result of a batched entries update: two result tokens, the entry and the rank. The handler fires CHARACTER_ADVANCEMENT_UPDATE_ENTRIES_RESULT(success, result, traversalResult, entryID, rank). The skeleton missed the two leading strings.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 | a CA_UPDATE_ENTRIES_* token, matched against a table of 9 strings starting at CA_UPDATE_ENTRIES_OK; index 0 is success |
| `traversalResult` | `cstring` | L3 | the second string the event carries; name from the Lua handler |
| `entryId` | `u32` | L3 |  |
| `rank` | `u32` | L3 |  |

## Client

- handler RVA `0x00170f30` in `ascension-extensions-2026-08-13`

## Events

- fires `CHARACTER_ADVANCEMENT_UPDATE_ENT` with format `%b%s%s%u%u`
- the UI receives `CHARACTER_ADVANCEMENT_UPDATE_ENTRIES_RESULT` (`client/event-args.yaml`)
- the UI receives `CHARACTER_ADVANCEMENT_UPDATE_ENTRIES_RESULT` (`client/event-args.yaml`)
