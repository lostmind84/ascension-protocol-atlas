# `0x0565` SMSG_CREATE_BUG_REPORT_ERROR

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0565-smsg-create-bug-report-error.yaml`](../../protocol/opcodes/0x0565-smsg-create-bug-report-error.yaml)

One length-prefixed string the handler hands to CREATE_BUG_REPORT_ERROR with format %s -- the only read and the only argument; named 'result' after the UI's parameter (default name: no Lua parameter declared).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `lpstring` | L3 |  |

## Client

- handler RVA `0x000e7340` in `ascension-extensions-2026-08-13`

## Events

- fires `CREATE_BUG_REPORT_ERROR` with format `%s`
