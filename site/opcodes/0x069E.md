# `0x069E` CMSG_SAVE_APPEARANCE_OUTFIT

**client to server** · status `hypothetical` · evidence `L3` · layout `named` · client code `sender`

Fiche: [`protocol/opcodes/0x069e-cmsg-save-appearance-outfit.yaml`](../../protocol/opcodes/0x069e-cmsg-save-appearance-outfit.yaml)

Save an appearance outfit: its name, then the ids of the pending outfit. The skeleton's two u32 were the count and the first id; the string comes before them.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `name` | `cstring` | L3 | SaveOutfit(name); a NEW_OUTFIT literal is special-cased before the write |
| `count` | `u32` | L3 | the size of the pending outfit's id vector |
| *repeat × count* | | | |
| &nbsp;&nbsp;`id` | `u32` | L3 | one entry of that vector -- the appearance ids the outfit holds, by the surrounding code, not read to the end |

## Client

- sender `0x000cb1c7`
