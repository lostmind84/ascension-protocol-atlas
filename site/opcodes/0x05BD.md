# `0x05BD` SMSG_PATCH_CHALLENGE_RULE_TYPE

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05bd-smsg-patch-challenge-rule-type.yaml`](../../protocol/opcodes/0x05bd-smsg-patch-challenge-rule-type.yaml)

One row of ChallengeRuleTypes.dbc in the compact wire form: the row id, three skipped string-offset dwords, then three length-prefixed strings written over them. The runtime record is 0x10 bytes while the client's ChallengeRuleTypes.dbc has 36 fields.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L3 | dword 0, the only dword the reader loads; the row key -- range-checked against minId 0x10be0384 / maxId 0x10be0380, indexes the row table at 0x10be0394, then passed to FUN_10133ac0 |
| `stringOffsets` | `bytes_12` | L3 | dwords 1..3, SKIPPED: the cursor jumps from +0x04 to the end of the 0x10 block. The row's three string-offset columns; the client writes the three string pointers that follow over record +0x04 .. +0x0c. |
| `string_1` | `lpstring` | L3 | u32 length then that many bytes, no NUL; copied and stored at record +0x04 |
| `string_2` | `lpstring` | L3 | record +0x08 |
| `string_3` | `lpstring` | L3 | record +0x0c. The UI resolves a rule type through C_Challenge.GetRuleLocalization (ChallengeUtil.lua, L2); which string feeds which return value is NOT established here. |

## Client

- handler RVA `0x0013a830` in `ascension-extensions-2026-08-13`
