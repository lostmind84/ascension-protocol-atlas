# `0x05F8` SMSG_PATCH_LOADING_SCREENS

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x05f8-smsg-patch-loading-screens.yaml`](../../protocol/opcodes/0x05f8-smsg-patch-loading-screens.yaml)

SMSG_PATCH_LOADING_SCREENS: despite the name, this handler does not touch any of the 114 CustomDBCMgr table objects -- no minId/maxId/rows-array reference anywhere in the decompiled C. It looks its key up in a hash-map-style container (FUN_100b1bd0) and inserts through a matching helper (FUN_100ab220), the pattern patch-tables.md calls out for the other 141 patch handlers that store into a DLL-internal container rather than a client DBC row. The opcode's own name happens to match LoadingScreens.dbc, a real 4-field/16-byte table in the snapshot, but nothing here ties the two together -- that binding (patch-table-names.md's 'L0 name' bucket) is exactly this: an inferred label from the opcode's own text, not a confirmed table write. No `patches_table` is claimed.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0; the key passed to the FUN_100b1bd0 hash-map lookup |
| `field_1` | `bytes_8` | L3 | cursor advances 8 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further |
| `field_2` | `u32` | L3 | offset 0xc |
| `field_3` | `lpstring` | L3 | via FUN_100d3680 |
| `field_4` | `lpstring` | L3 | via FUN_100d3680 |

## Client

- handler RVA `0x00228b40` in `ascension-extensions-2026-08-13`
