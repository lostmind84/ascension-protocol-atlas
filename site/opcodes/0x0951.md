# `0x0951` SMSG_PATCH_SPELL_CAST_TIMES

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0951-smsg-patch-spell-cast-times.yaml`](../../protocol/opcodes/0x0951-smsg-patch-spell-cast-times.yaml)

One row of SpellCastTimes.dbc (bound_by: L1 ref, patch-table-names.md): the handler's single 16-byte bulk read exactly equals the table's 4-column/16-byte record (client DBC header). 2 of 4 columns are named by AzerothCore's own SpellCastTimesEntry struct (L1, another project's reading of the same file); the other 2 are columns the core's format string marks 'x' (skipped) and never names, despite dead comments guessing at them.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0 (index field, fmt 'n'); core: SpellCastTimesEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar5,16]], one 16-byte bulk read == the full 4-column/16-byte SpellCastTimes.dbc record); name L1 (core DBCStructure.h, another project's reading of the same file). |
| `castTime` | `i32` | L1 | DBC column 1; core: SpellCastTimesEntry::CastTime (int32). Width L3; name L1 (core DBCStructure.h). |
| `field_2` | `u32` | L3 | DBC column 2; SpellCastTimefmt marks it 'x' (skipped). Core's own dead comment guesses 'float CastTimePerLevel, unsure / per skill?' but does not store it -- unnamed per the ONE RULE (a comment on a skipped column is not a name the core keeps). Width L3 only. |
| `field_3` | `u32` | L3 | DBC column 3; SpellCastTimefmt marks it 'x' (skipped). Core's dead comment guesses 'int32 MinCastTime, unsure' but does not store it -- unnamed. Width L3 only. |

## Client

- handler RVA `0x00231730` in `ascension-extensions-2026-08-13`
