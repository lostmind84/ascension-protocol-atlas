# `0x073F` SMSG_LOOT_ROLL_ADDON

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x073f-smsg-loot-roll-addon.yaml`](../../protocol/opcodes/0x073f-smsg-loot-roll-addon.yaml)

Addon-side loot roll: one u32, filed at the roll index the client currently holds.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `value` | `u32` | L3 | the only read: stored into the loot-roll array at the index the DLL global 0x10bcb88c holds. Everything before it in the handler resizes that array |

## Client

- handler RVA `0x0027fa50` in `ascension-extensions-2026-08-13`
