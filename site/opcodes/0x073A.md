# `0x073A` CMSG_SET_SPECIALIZATION_EQUIPMENT_SET_ID

**client to server** · status `hypothetical` · evidence `L3` · layout `named` · client code `sender`

Fiche: [`protocol/opcodes/0x073a-cmsg-set-specialization-equipment-set-id.yaml`](../../protocol/opcodes/0x073a-cmsg-set-specialization-equipment-set-id.yaml)

Bind an equipment set to a specialization: the binding SetEquipmentSetIDForSpecialization(specID, setName) writes u32, u8, u64; the u64 is the equipment set's id the binding resolves from the name, the byte is not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `specializationId` | `u32` | L3 |  |
| `unknown_1` | `u8` | L3 |  |
| `equipmentSetId` | `u64` | L3 | resolved by the binding from the set name it is given |

## Client

- sender `0x0031f2b7`
