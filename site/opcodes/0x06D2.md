# `0x06D2` SMSG_PATCH_SPELL_STAT_SUGGESTIONS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06d2-smsg-patch-spell-stat-suggestions.yaml`](../../protocol/opcodes/0x06d2-smsg-patch-spell-stat-suggestions.yaml)

Named SMSG_PATCH_SPELL_STAT_SUGGESTIONS by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_16` | L3 | one row of SpellStatSuggestions.dbc (4 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e3b20` in `ascension-extensions-2026-08-13`
