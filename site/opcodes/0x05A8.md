# `0x05A8` SMSG_CHALLENGE_CREATOR_SAVE_CHALLENGE_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05a8-smsg-challenge-creator-save-challenge-result.yaml`](../../protocol/opcodes/0x05a8-smsg-challenge-creator-save-challenge-result.yaml)

The CoA server calls it `SMSG_COA_TRIAL_SAVE_RESULT`.

Answer to the trial-creator save request: two length-prefixed strings the handler reads with FUN_100d3680 and hands to TRIAL_SAVE_RESULT(trialID, response), format %s%s. Names from the server's writer and the UI's arguments. The server then writes a count and a list of u32 the handler does not read in its own body; not established further.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `trialId` | `lpstring` | L3 |  |
| `response` | `lpstring` | L3 |  |

## Client

- handler RVA `0x001240c0` in `ascension-extensions-2026-08-13`

## Events

- fires `TRIAL_SAVE_RESULT` with format `%s%s`
- the UI receives `TRIAL_SAVE_RESULT` (`client/event-args.yaml`)

## Server

- `modules/mod-coa-challenges/src/CoA.Challenges.History.cpp`
