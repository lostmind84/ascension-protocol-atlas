# `0x06F0` SMSG_LIST_WILDCARD_QUICK_ROLLING_CONFIGS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06f0-smsg-list-wildcard-quick-rolling-configs.yaml`](../../protocol/opcodes/0x06f0-smsg-list-wildcard-quick-rolling-configs.yaml)

Wildcard quick-rolling configs: a count, then 9-byte records (eight bytes and one byte) stored as they come. Nothing in the handler reads the fields back.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 |  |
| *repeat × count* | | | |
| &nbsp;&nbsp;`config` | `bytes_8` | L3 | eight bytes copied whole into a 9-byte record; whether two u32 or one u64 is not visible in the copy |
| &nbsp;&nbsp;`flag` | `u8` | L3 | the ninth byte of the record |

## Client

- handler RVA `0x00a34860` in `ascension-extensions-2026-08-13`
