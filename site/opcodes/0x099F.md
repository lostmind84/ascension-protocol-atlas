# `0x099F` SMSG_PATCH_QUEST_INFO

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x099f-smsg-patch-quest-info.yaml`](../../protocol/opcodes/0x099f-smsg-patch-quest-info.yaml)

A record patched into one of Extensions.dll's internal id-keyed containers -- looked up by id (RVA 0x000b1e70), allocated fresh as a 0x8-byte heap struct when new (RVA 0x000ae5d20), and inserted (RVA 0x000abff0) -- not one of the 114 CustomDBCMgr `.data` tables client/patch-tables.md documents: nothing in the decompiled handler references a fixed .data table object (no `+0xc`/`+0x10`/`+0x20` min/max/rows idiom anywhere in it, unlike 0x09d1/0x09d2 in this same shard, which do). client/patch-table-names.md's third-party reference names `QuestInfo.dbc` for this opcode (18 fields, 72-byte record) but the handler's fixed portion is 8 bytes, not 72 -- the widths do not match, on top of the binding already being unverified (`L1 ref`, absent from our own patch-tables.yaml scan). No source in this repository names the struct's own fields (see naming_source below).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u64` | L3 | width: L3 (block-copy read, 8 bytes; re-verified this pass against the cached decompile, build/ghidra/handlers-c/0x099f.c, against client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming source (see naming_source below). 8 bytes does NOT match QuestInfo.dbc's claimed record_size (72 bytes, 18 fields, per client/patch-table-names.md) at all, which further undermines that already-unverified (L1) binding.
 |
| `field_1` | `lpstring` | L3 | width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming source.
 |

## Client

- handler RVA `0x0022f890` in `ascension-extensions-2026-08-13`
