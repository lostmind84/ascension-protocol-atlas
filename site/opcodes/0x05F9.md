# `0x05F9` SMSG_UPDATE_KNOWN_RANDOM_ENCHANTS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05f9-smsg-update-known-random-enchants.yaml`](../../protocol/opcodes/0x05f9-smsg-update-known-random-enchants.yaml)

The player's known mystic (random) enchants, sent whole: a count then that many ids. The sibling ADD_KNOWN 0x05FA hands the same kind of id to MYSTIC_ENCHANT_LEARNED(spellID), which is the name used here; the handler stores them and fires no event of its own.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 |  |
| *repeat × count* | | | |
| &nbsp;&nbsp;`spellId` | `u32` | L3 | named after the sibling event's argument (L2); the client treats enchants as spell ids |

## Client

- handler RVA `0x002e93f0` in `ascension-extensions-2026-08-13`
