# `0x0639` SMSG_BUILD_DRAFT_ROLE_PROMPT

**server to client** · status `hypothetical` · evidence `L3` · layout `empty` · client code `handler`

Fiche: [`protocol/opcodes/0x0639-smsg-build-draft-role-prompt.yaml`](../../protocol/opcodes/0x0639-smsg-build-draft-role-prompt.yaml)

Build-draft role prompt: no payload; the handler fires BUILD_DRAFT_ROLE_PROMPT and reads nothing off the wire. After firing it also writes a state byte at offset +0x30 of the container FUN_10111410() returns (build/ghidra/handlers-c/0x0639.c:46-48) -- an internal flag, not a packet field; 0x0661's handler has no equivalent write.

## Layout

*No layout established.*

## Client

- handler RVA `0x00110340` in `ascension-extensions-2026-08-13`

## Events

- fires `BUILD_DRAFT_ROLE_PROMPT`
- the UI receives `BUILD_DRAFT_ROLE_PROMPT` (`client/event-args.yaml`)
