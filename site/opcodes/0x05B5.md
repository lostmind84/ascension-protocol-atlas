# `0x05B5` SMSG_PATCH_CHALLENGE_GROUP

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05b5-smsg-patch-challenge-group.yaml`](../../protocol/opcodes/0x05b5-smsg-patch-challenge-group.yaml)

One row of ChallengeGroups.dbc: three dwords and no string; the runtime record (0x0c bytes) is a byte-for-byte copy of the wire block. This is the shortest packet in the challenge patch family. Dword 1 is passed to the group-refresh routine FUN_10135130.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L3 | dword 0; the row key -- range-checked against minId 0x10be0240 / maxId 0x10be023c, indexes the row table at 0x10be0250, then passed to FUN_10133ac0 |
| `unknown_1` | `u32` | L3 | dword 1; record +0x04, and passed unconditionally to FUN_10135130 after the insert -- the same routine SMSG_PATCH_CHALLENGE 0x0591 calls with a challenge id and SMSG_PATCH_CHALLENGE_GROUP_REWARD does not. A key the group layer re-reads; the name is inference. |
| `unknown_2` | `u32` | L3 | dword 2; record +0x08. Not named. |

## Client

- handler RVA `0x00138e50` in `ascension-extensions-2026-08-13`
