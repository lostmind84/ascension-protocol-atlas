# `0x0687` SMSG_TALKING_HEAD_FRAME

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0687-smsg-talking-head-frame.yaml`](../../protocol/opcodes/0x0687-smsg-talking-head-frame.yaml)

A talking-head display: two texts, three sizes, two textures, an animation id and a loop flag. The skeleton missed the four strings.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `header` | `cstring` | L3 |  |
| `body` | `cstring` | L3 |  |
| `headerSize` | `u32` | L3 |  |
| `bodySize` | `u32` | L3 |  |
| `duration` | `u32` | L3 |  |
| `portrait` | `cstring` | L3 |  |
| `art` | `cstring` | L3 |  |
| `animId` | `u32` | L3 |  |
| `loopAnim` | `u8` | L3 | names from TALKING_HEAD_FRAME_DISPLAY(header, body, headerSize, bodySize, duration, portrait, art, animID, loopAnim), TalkingHeadFrame.lua:188; the handler reads exactly that sequence (%s%s%u%u%u%s%s%u%b) |

## Client

- handler RVA `0x000d45a0` in `ascension-extensions-2026-08-13`

## Events

- the UI receives `TALKING_HEAD_FRAME_DISPLAY` (`client/event-args.yaml`)
- the UI receives `TALKING_HEAD_FRAME_DISPLAY` (`client/event-args.yaml`)
