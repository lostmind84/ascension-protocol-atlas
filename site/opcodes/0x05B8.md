# `0x05B8` SMSG_PATCH_CHALLENGE_RULE

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05b8-smsg-patch-challenge-rule.yaml`](../../protocol/opcodes/0x05b8-smsg-patch-challenge-rule.yaml)

One row of ChallengeRules.dbc: five dwords then the rule-type token as a length-prefixed string. The runtime record is dword-equal to the wire block (0x14 bytes) with the string pointer written over dword 4, which the reader skips. Only the row key and the string are named; no Lua builder reads this record field by field, the model exposes rules as a plain id array.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L3 | dword 0; the row key -- range-checked against minId 0x10be0360 / maxId 0x10be035c, indexes the row table at 0x10be0370, then passed to FUN_10133ac0 |
| `unknown_1` | `u32` | L3 | dword 1; record +0x04. Gates the notify path. Not named. |
| `unknown_2` | `u32` | L3 | dword 2; record +0x08 and, when non-zero, the sole argument to FUN_10135180 -- the family-wide invalidate call. Inference. |
| `unknown_3` | `u32` | L3 | dword 3; record +0x0c. Not named. |
| `unknown_4` | `bytes_4` | L3 | dword 4 is SKIPPED by the reader: the cursor jumps from +0x0c to the end of the 0x14 block. It is the row's string-offset column; the client writes the string pointer over record +0x10. |
| `ruleType` | `lpstring` | L3 | u32 length then that many bytes, no NUL (FUN_100d3680). Stored at record +0x10 over the skipped dword 4. A CHALLENGE_RULES_TYPE_* token -- the same vocabulary SMSG_LIST_BROKEN_CHALLENGE_RULES 0x05BA carries and C_Challenge.GetRuleLocalization resolves (ChallengeUtil.lua, L2). |

## Client

- handler RVA `0x0013a670` in `ascension-extensions-2026-08-13`
