# `0x0953` SMSG_PATCH_SPELL_RANGE

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0953-smsg-patch-spell-range.yaml`](../../protocol/opcodes/0x0953-smsg-patch-spell-range.yaml)

Candidate: one row of SpellRange.dbc (bound_by: L1 ref, patch-table-names.md). The handler reads a 32-byte block then two strings (handler-fire.yaml), short of the table's full 40-column/160-byte record. The block's first 6 dwords (id, RangeMin x2, RangeMax x2, Flags) are a contiguous, unambiguous run at the very start of the DBC record and are named; the block's last 2 dwords have no confirmed identity (candidates: the two locale-mask flags) and stay unnamed. The two trailing strings (DisplayName, DisplayNameShort) are count- and order-matched to the table's two locale blocks.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0 (index field, fmt 'n'); core: SpellRangeEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar3,32],[local_44,lpstring],[local_2c,lpstring]], one 32-byte bulk read then two strings, decomposed here into 8 dwords since SpellRangeEntry's first 6 columns are a contiguous, unambiguous run at the very start of the record); name L1 (core DBCStructure.h, another project's reading of the same file). |
| `rangeMinHostile` | `f32` | L1 | DBC column 1; core: SpellRangeEntry::RangeMin[0], 'Hostile' per core's comment. Contiguous with id, no ordering ambiguity. Width L3; name L1 (core DBCStructure.h). |
| `rangeMinFriendly` | `f32` | L1 | DBC column 2; core: RangeMin[1], 'Friendly'. Width L3; name L1 (core DBCStructure.h). |
| `rangeMaxHostile` | `f32` | L1 | DBC column 3; core: RangeMax[0], 'Hostile'. Width L3; name L1 (core DBCStructure.h). |
| `rangeMaxFriendly` | `f32` | L1 | DBC column 4; core: RangeMax[1], 'Friendly'. Width L3; name L1 (core DBCStructure.h). |
| `flags` | `u32` | L1 | DBC column 5; core: SpellRangeEntry::Flags. Width L3; name L1 (core DBCStructure.h). |
| `field_6` | `u32` | L3 | 7th of the 8 dwords in the fixed block. The named columns (id..flags) account for only 6 of the 8 observed dwords; the other 2 have no confirmed identity. Candidates, not placed: DisplayName_lang_mask (DBC col 22) and DisplayNameShort_lang_mask (col 39), both of which SpellRangefmt marks 'x' and neither of which core names beyond a commented mention. Width L3 only. |
| `field_7` | `u32` | L3 | 8th of the 8 dwords. Same caveat as field_6. Width L3 only. |
| `displayName` | `lpstring` | L1 | DBC columns 6-21; core: SpellRangeEntry has a commented-out DisplayName[16] locale block ('char const* DisplayName[16]'), matching this first of two observed lpstrings if string order is preserved. Width L3 (handler-fire.yaml); name L1 (dead-comment name, not a stored core member). |
| `displayNameShort` | `lpstring` | L1 | DBC columns 23-38; core: commented-out DisplayNameShort[16] locale block, the second string in DBC order. Width L3 (handler-fire.yaml); name L1 (dead-comment name, not a stored core member). |

## Client

- handler RVA `0x00232ba0` in `ascension-extensions-2026-08-13`
