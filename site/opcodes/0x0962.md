# `0x0962` SMSG_PATCH_CAMERA_SHAKES

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0962-smsg-patch-camera-shakes.yaml`](../../protocol/opcodes/0x0962-smsg-patch-camera-shakes.yaml)

Named SMSG_PATCH_CAMERA_SHAKES by the client's own opcode table. The opcode is not bound in patch- tables.yaml -- the DLL's own custom-table list does not mention this opcode; CameraShakes.dbc is the table patch-table-names.md associates with it by name and geometry (grade L1). The handler's own reads match CameraShakes.dbc's own WDBC header exactly (8 fields, 32 bytes), so this looks like a full, unmodified row of that table. No source names any of the table's columns.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_32` | L3 |  |

## Client

- handler RVA `0x0022a9a0` in `ascension-extensions-2026-08-13`
