# `0x0725` SMSG_UNNAMED_0725

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0725-smsg-unnamed.yaml`](../../protocol/opcodes/0x0725-smsg-unnamed.yaml)

Unnamed in the client's own opcode table, but the client registers a handler for 0x0725 -- there is code behind it. Direction inferred from that registration. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `slot` | `u32` | L5 | Zero-based. The handler fires ASCENSION_CA_SPECIALIZATION_ACTIVE_ID_CHANGED with slot + 1: forged 7 -> event 8, forged 2 -> event 3 (2026-09-19). #4027 sends 0 -- 'slot 0 of 1'. What the shim-overridden GetActiveChrSpec answers was not read. The value goes to the setter FUN_101499e0, which is also where SMSG 0x06E2's third u32 lands for an inspected character; the capture reference calls this field active_slot and reports a live value of 0, which agrees.
 |
| `slotCount` | `u32` | L3 | #4027 sends 1; forged 9 with no observable effect on the event. The value goes to the twin setter FUN_101499f0, and SMSG 0x06E2's fourth u32 -- the bound of its per-slot loop -- is the same quantity for an inspected character, which is the clearest statement of what it counts: how many specialization slots exist. The capture reference calls it slot_count and reports a live value of 1.
 |

## Client

- handler RVA `0x00171d90` in `ascension-extensions-2026-08-13`

## Events

- fires `ASCENSION_CA_SPECIALIZATION_ACTI` with format `%u`
- the UI receives `ASCENSION_CA_SPECIALIZATION_ACTIVE_ID_CHANGED` (`client/event-args.yaml`)
- the UI receives `ASCENSION_CA_SPECIALIZATION_ACTIVE_ID_CHANGED` (`client/event-args.yaml`)

## Server

- `modules/mod-ascension-compat/src/AscensionCompat.cpp`

## Replay

`tools/replay/ca_vertical.py`
