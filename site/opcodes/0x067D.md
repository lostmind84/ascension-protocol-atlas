# `0x067D` SMSG_PATCH_MANASTORM

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x067d-smsg-patch-manastorm.yaml`](../../protocol/opcodes/0x067d-smsg-patch-manastorm.yaml)

Named SMSG_PATCH_MANASTORM by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_36` | L3 | one row of Manastorm.dbc (9 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x002a3f40` in `ascension-extensions-2026-08-13`
