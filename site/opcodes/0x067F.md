# `0x067F` SMSG_MANASTORM_CHAOTIC_LINK_UPDATE

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x067f-smsg-manastorm-chaotic-link-update.yaml`](../../protocol/opcodes/0x067f-smsg-manastorm-chaotic-link-update.yaml)

Named SMSG_MANASTORM_CHAOTIC_LINK_UPDATE by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `stacks` | `u32` | L3 | uint32(run.encounter->guards.size()) |

## Client

- handler RVA `0x002a28d0` in `ascension-extensions-2026-08-13`

## Events

- fires `MANASTORM_CHAOTIC_LINK_UPDATED` with format `%u%u`
- the UI receives `MANASTORM_CHAOTIC_LINK_UPDATED` (`client/event-args.yaml`)

## Server

- `modules/mod-ascension-compat/src/AscensionManastormProtocol.h`
