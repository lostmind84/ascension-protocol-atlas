# `0x0606` SMSG_COLLECTION_REFORGE_RANDOM_ENCHANT_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0606-smsg-collection-reforge-random-enchant-result.yaml`](../../protocol/opcodes/0x0606-smsg-collection-reforge-random-enchant-result.yaml)

One NUL-terminated string the handler hands to MYSTIC_ENCHANT_APPLY_RESULT with format %s -- the only read and the only argument; named 'result' after the UI's parameter (default name: no Lua parameter declared).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 |  |

## Client

- handler RVA `0x002e7d70` in `ascension-extensions-2026-08-13`

## Events

- fires `MYSTIC_ENCHANT_APPLY_RESULT` with format `%s`
- fires `MYSTIC_ENCHANT_COLLECTION_REFORG` with format `%s`
