# `0x0743` SMSG_INSTANCED_AREAS_LIST

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0743-smsg-instanced-areas-list.yaml`](../../protocol/opcodes/0x0743-smsg-instanced-areas-list.yaml)

The instanced areas list: a u32, a count and records with a string; ZONE_INSTANCE_LIST afterwards. Partially followed.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `unknown_0` | `u32` | L3 |  |
| `count` | `u32` | L3 | bounds a loop of records that include a string; ZONE_INSTANCE_LIST afterwards |
| *repeat × count* | | | |
| &nbsp;&nbsp;`area` | `bytes_variable` | L3 | the record (u32 fields and a NUL-terminated string in the skeleton) was not followed |

## Client

- handler RVA `0x00a4f2b0` in `ascension-extensions-2026-08-13`

## Events

- fires `ZONE_INSTANCE_LIST`
