# `0x066B` SMSG_PURCHASE_SEALED_CARD_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x066b-smsg-purchase-sealed-card-result.yaml`](../../protocol/opcodes/0x066b-smsg-purchase-sealed-card-result.yaml)

One NUL-terminated string the handler hands to PURCHASE_SEALED_CARD_RESULT with format %s -- the only read and the only argument; named 'result' after the UI's parameter (default name: no Lua parameter declared).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 |  |

## Client

- handler RVA `0x0031ad50` in `ascension-extensions-2026-08-13`

## Events

- fires `PURCHASE_SEALED_CARD_RESULT` with format `%s`
- the UI receives `PURCHASE_SEALED_CARD_RESULT` (`client/event-args.yaml`)
