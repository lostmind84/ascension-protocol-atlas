# `0x0600` SMSG_UPDATE_ACTIVE_RANDOM_ENCHANT_PRESET

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0600-smsg-update-active-random-enchant-preset.yaml`](../../protocol/opcodes/0x0600-smsg-update-active-random-enchant-preset.yaml)

The active mystic-enchant preset changed: one u32 the handler stores as the active preset (the opcode's own name; no event fired).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `presetId` | `u32` | L3 | named after the opcode; the handler stores it, no getter was followed |

## Client

- handler RVA `0x002f2d10` in `ascension-extensions-2026-08-13`
