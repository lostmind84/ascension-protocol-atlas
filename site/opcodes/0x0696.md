# `0x0696` SMSG_PATCH_UI_CAMERAS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0696-smsg-patch-ui-cameras.yaml`](../../protocol/opcodes/0x0696-smsg-patch-ui-cameras.yaml)

Named SMSG_PATCH_UI_CAMERAS by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_40` | L3 | one row of UICameras.dbc (10 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e5520` in `ascension-extensions-2026-08-13`
