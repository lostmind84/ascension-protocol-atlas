# `0x092D` SMSG_PATCH_SPELL_VISUAL_KIT

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x092d-smsg-patch-spell-visual-kit.yaml`](../../protocol/opcodes/0x092d-smsg-patch-spell-visual-kit.yaml)

Named SMSG_PATCH_SPELL_VISUAL_KIT by the client's own opcode table. The opcode is not bound in patch-tables.yaml -- the DLL's own custom-table list does not mention this opcode; SpellVisualKit.dbc is the table patch-table-names.md associates with it by name and geometry (grade L1). The handler's registered entry point lies inside a decompiled function shared with another opcode in this shard, so whether it reads a full SpellVisualKit.dbc row is not established. No source names any of the table's columns.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_12` | L3 |  |
| `field_1` | `lpstring` | L3 |  |

## Client

- handler RVA `0x002333b0` in `ascension-extensions-2026-08-13`
