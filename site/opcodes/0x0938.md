# `0x0938` SMSG_PATCH_ACHIEVEMENT_CATEGORY

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0938-smsg-patch-achievement-category.yaml`](../../protocol/opcodes/0x0938-smsg-patch-achievement-category.yaml)

One row of the stock Achievement_Category.dbc (AzerothCore already loads this table; ADR 0005). Reading the actual decompiled handler (build/ghidra/handlers-c/0x0938.c, RVA 0x00229850) by hand: ID and parentCategory (AchievementCategoryEntry's only 2 active members) are the leading dwords, then a length-prefixed blob (memcpy, not the lpstring helper, then a transform of unconfirmed purpose) whose repeat count is provably exactly 1 -- the loop's exit test is a stack-address comparison exactly 24 bytes (one element) past its start, computed here from the declared locals -- then 1 trailing dword. 2 + 17 (one collapsed locale block, the disabled name[16]+flag) + 1 = 20, the file's full column count, with nothing left over. ID and parentCategory are named from DBCStructure.h (L1); the trailing dword is AchievementCategoryEntry's commented-out sortOrder (column 19), identified but kept as an unnamed field_N per the 'x' convention.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `ID` | `u32` | L1 | active AchievementCategoryEntry member (column 0) |
| `parentCategory` | `u32` | L1 | active AchievementCategoryEntry member (column 1, -1 for a main category) |
| *repeat × 1* | | | |
| &nbsp;&nbsp;`field_2` | `bytes_variable` | L3 | a u32 length then that many raw bytes (memcpy, NOT the FUN_100d3680 lpstring idiom used elsewhere in this bucket), the result passed through FUN_10086e30 (transform of unconfirmed purpose) and freed, not stored verbatim. The repeat count is confirmed at exactly 1 by stack layout: the loop writes a 24-byte element starting at the vector object's own address, and its exit test compares against a fixed local variable exactly 24 bytes further up the frame -- one iteration exactly spans that gap (the same reasoning as 0x0935 and 0x0937). By column arithmetic (2 prefix + 1x17 collapsed here + 1 more read after it = 20, the file's full column count) this occupies the file's disabled name[16]+flag locale block (columns 2-18) -- an inference from arithmetic, not a direct read, so no name is asserted. |
| `field_3` | `u32` | L1 | core format char 'x' (column 19, sortOrder, per DBCStructure.h's comment "//uint32 sortOrder; // 19"); fully commented out, no formal name asserted |

## Client

- handler RVA `0x00229850` in `ascension-extensions-2026-08-13`
