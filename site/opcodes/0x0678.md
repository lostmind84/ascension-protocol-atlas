# `0x0678` SMSG_PATCH_ZONE_STORY

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0678-smsg-patch-zone-story.yaml`](../../protocol/opcodes/0x0678-smsg-patch-zone-story.yaml)

One row of ZoneStory.dbc: id, a race/class filter dword, storyAchievementID, a map/continent filter dword, then atlasName as a length-free cstring. storyAchievementID and atlasName are named from the native getter C_QuestLog.GetZoneStoryInfo (VA 0x1021af00), which reads exactly these two offsets and is called at WorldMapFrame.lua:1863; the other two dwords are used only as row-selection filters inside that same getter, so their column names are not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L3 | record +0x00, the row key -- range-checked against minId 0x10bdf91c / maxId 0x10bdf918, indexes the row table at 0x10bdf92c, then passed to FUN_10133ac0. |
| `unknown_1` | `u32` | L3 | record +0x04. GetZoneStoryInfo (native, VA 0x1021af00; C_QuestLog.GetZoneStoryInfo, called from Interface/FrameXML/WorldMapFrame.lua:1863) uses this as a race/class-type filter value when scanning rows (compares it against 0, 2 or 3 depending on the player's class) -- a filter, not a value the getter returns, so its column name is not established. |
| `storyAchievementID` | `u32` | L3 | record +0x08. GetZoneStoryInfo's first return value, read from this offset and pushed unconditionally (VA 0x1021af00: `(*DAT_10bc90ac)(param_1,*(undefined4 *)(iVar2 + 8))`). Lua: `local storyAchievementID, atlasName = C_QuestLog.GetZoneStoryInfo(mapID)` (WorldMapFrame.lua:1863). |
| `unknown_3` | `u32` | L3 | record +0x0c. GetZoneStoryInfo compares this against a computed continent/area id (`*(int *)(iVar2 + 0xc) == iVar3`) to pick the row for the given mapID -- a filter key, not a returned value, so its column name is not established. |
| `atlasName` | `cstring` | L3 | record +0x10, the row's only string column. GetZoneStoryInfo's second return value (VA 0x1021af00: `*(undefined1 **)(iVar2 + 0x10)`, pushed with the default-empty-string idiom). Lua destructures it as `atlasName` (WorldMapFrame.lua:1863). |

## Client

- handler RVA `0x0021a9b0` in `ascension-extensions-2026-08-13`
