# `0x094F` SMSG_PATCH_SPELL_CATEGORY

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x094f-smsg-patch-spell-category.yaml`](../../protocol/opcodes/0x094f-smsg-patch-spell-category.yaml)

One row of SpellCategory.dbc (bound_by: L1 ref, patch-table-names.md): the handler's two dwords exactly equal the table's 2-column/8-byte record (client DBC header). Both columns are named by AzerothCore's own SpellCategoryEntry struct (L1, another project's reading of the same file).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0 (index field, fmt 'n'); core: SpellCategoryEntry::Id. Width L3 (handler-fire.yaml: reads [[uVar2,4],[uVar3,4]], two dwords == the full 2-column/8-byte SpellCategory.dbc record); name L1 (core DBCStructure.h, another project's reading of the same file). |
| `flags` | `u32` | L1 | DBC column 1; core: SpellCategoryEntry::Flags. Width L3; name L1 (core DBCStructure.h). |

## Client

- handler RVA `0x002317d0` in `ascension-extensions-2026-08-13`
