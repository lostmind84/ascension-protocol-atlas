# `0x0673` SMSG_AURA_UPDATE_ADDON

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0673-smsg-aura-update-addon.yaml`](../../protocol/opcodes/0x0673-smsg-aura-update-addon.yaml)

Named SMSG_AURA_UPDATE_ADDON by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `targetGuid` | `u64` | L3 | full ObjectGuid, NOT the packed form; the client reads it as two u32, low then high |

## Client

- handler RVA `0x00a121e0` in `ascension-extensions-2026-08-13`

## Server

- `modules/mod-ascension-compat/src/AscensionAuraAmounts.cpp`
