# `0x0695` SMSG_PATCH_UI_CAMERA_APPEARANCE_WEAPONS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0695-smsg-patch-ui-camera-appearance-weapons.yaml`](../../protocol/opcodes/0x0695-smsg-patch-ui-camera-appearance-weapons.yaml)

Named SMSG_PATCH_UI_CAMERA_APPEARANCE_WEAPONS by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_20` | L3 | one row of UICameraAppearanceWeapons.dbc (5 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e5490` in `ascension-extensions-2026-08-13`
