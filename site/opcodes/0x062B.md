# `0x062B` SMSG_BUILD_CREATOR_RATE_BUILD_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x062b-smsg-build-creator-rate-build-result.yaml`](../../protocol/opcodes/0x062b-smsg-build-creator-rate-build-result.yaml)

Result of rating a build: a result string, then the build id and rating. Partially followed.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 |  |
| `rest` | `bytes_variable` | L3 | BUILD_CREATOR_RATE_RESULT(result, buildID, rating) is fired with %s%s%u; the build id string and rating after the result were not followed |

## Client

- handler RVA `0x000fedc0` in `ascension-extensions-2026-08-13`

## Events

- fires `BUILD_CREATOR_RATE_RESULT` with format `%s%s%u`
- the UI receives `BUILD_CREATOR_RATE_RESULT` (`client/event-args.yaml`)
