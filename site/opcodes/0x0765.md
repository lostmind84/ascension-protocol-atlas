# `0x0765` SMSG_UNNAMED_0765

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0765-smsg-unnamed.yaml`](../../protocol/opcodes/0x0765-smsg-unnamed.yaml)

A unit-scoped movement packet: a packed guid, the stock MovementInfo block and six u32. The skeleton's seven fields were the movement block's floats.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `guid` | `packed_guid` | L3 | resolved with ClntObjMgrObjectPtr(guid, 0x18) unless it equals the u64 global at exe 0x00ca1238 (the local player, by use) |
| `movementInfo` | `bytes_variable` | L3 | the stock 3.3.5a MovementInfo, unpacked by the exe's own reader (0x004f4d40): flags u32, flags2 u16, time u32, position 4 f32, then the transport, pitch, fall and spline-elevation blocks the flags select. Not re-documented here (ADR 0005) |
| `extra` | `bytes_24` | L3 | six u32 read one after another by FUN_102ba040 into a zeroed 24-byte struct; the struct's consumer was not read, so the six stay unnamed |

## Client

- handler RVA `0x002b9140` in `ascension-extensions-2026-08-13`
