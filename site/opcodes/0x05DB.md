# `0x05DB` SMSG_RECOVERY_QUERY_DELETED_CHARACTER_RESPONSE

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05db-smsg-recovery-query-deleted-character-response.yaml`](../../protocol/opcodes/0x05db-smsg-recovery-query-deleted-character-response.yaml)

Recovery: the deleted characters found by a query. A count and per character a record this reading did not follow; RECOVERY_QUERY_RESULT(category) afterwards. The skeleton's second u32 was the record's first field.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 |  |
| *repeat × count* | | | |
| &nbsp;&nbsp;`character` | `bytes_variable` | L3 | a deleted-character record read over some 150 lines of the handler (strings and numbers), not followed; the handler then fires RECOVERY_QUERY_RESULT with the RECOVERY_SERVICE_CATEGORY_DELETE token |

## Client

- handler RVA `0x003042f0` in `ascension-extensions-2026-08-13`

## Events

- fires `RECOVERY_QUERY_RESULT` with format `%s`
- fires `RECOVERY_QUERY_RESULT` with format `%s`
- the UI receives `RECOVERY_QUERY_RESULT` (`client/event-args.yaml`)
