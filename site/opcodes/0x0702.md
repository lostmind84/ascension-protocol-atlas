# `0x0702` SMSG_PLAYER_GM_TICKET_UPDATE

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0702-smsg-player-gm-ticket-update.yaml`](../../protocol/opcodes/0x0702-smsg-player-gm-ticket-update.yaml)

A player ticket update: one ticket record (0x0709 lists its fields), diffed against the held ticket.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `ticket` | `bytes_variable` | L3 | one ticket record, the same FUN_10248730 shape 0x0709 lists field by field: TicketID, Title, CreationDate, optional LastMessageDate, Creator, Status, the AI-suggestion and Messages containers, six flag bytes, optional CloseDate and CreatorLocale. The handler diffs it against the held ticket and fires PLAYER_TICKET_UPDATE or PLAYER_TICKET_NEW_MESSAGE(messageID).
 |

## Client

- handler RVA `0x00253ec0` in `ascension-extensions-2026-08-13`

## Events

- fires `PLAYER_TICKET_UPDATE` with format `%u`
- the UI receives `PLAYER_TICKET_NEW_MESSAGE` (`client/event-args.yaml`)
- the UI receives `PLAYER_TICKET_NEW_MESSAGE` (`client/event-args.yaml`)
- the UI receives `PLAYER_TICKET_UPDATE` (`client/event-args.yaml`)
