# `0x0950` SMSG_PATCH_SPELL_DURATION

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0950-smsg-patch-spell-duration.yaml`](../../protocol/opcodes/0x0950-smsg-patch-spell-duration.yaml)

One row of SpellDuration.dbc (bound_by: L1 ref, patch-table-names.md): the handler's single 16-byte bulk read exactly equals the table's 4-column/16-byte record (client DBC header), so this is a full, position-by-position decomposition. All 4 columns are named by AzerothCore's own SpellDurationEntry struct (L1, another project's reading of the same file).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0 (index field, fmt 'n'); core: SpellDurationEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar5,16]], one 16-byte bulk read == the full 4-column/16-byte SpellDuration.dbc record); name L1 (core DBCStructure.h, another project's reading of the same file). |
| `duration_0` | `i32` | L1 | DBC column 1; core: SpellDurationEntry::Duration[0] (int32). Width L3; name L1 (core DBCStructure.h). |
| `duration_1` | `i32` | L1 | DBC column 2; core: SpellDurationEntry::Duration[1] (int32). Width L3; name L1 (core DBCStructure.h). |
| `duration_2` | `i32` | L1 | DBC column 3; core: SpellDurationEntry::Duration[2] (int32). Width L3; name L1 (core DBCStructure.h). |

## Client

- handler RVA `0x00231b90` in `ascension-extensions-2026-08-13`
