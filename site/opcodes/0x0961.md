# `0x0961` SMSG_PATCH_SPELL_FOCUS_OBJECT

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0961-smsg-patch-spell-focus-object.yaml`](../../protocol/opcodes/0x0961-smsg-patch-spell-focus-object.yaml)

Candidate: one row of SpellFocusObject.dbc (bound_by: L1 ref, patch-table-names.md). The handler reads two dwords then one string (handler-fire.yaml). id (column 0) is named -- it is the only column AzerothCore's SpellFocusObjectEntry struct stores at all -- and the trailing lpstring is count-matched to the table's one locale block (Name[16]); the second dword has no naming source and stays unnamed.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0 (index field, fmt 'n'); core: SpellFocusObjectEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar5,8],[local_2c,lpstring]], two dwords then one string); name L1 (core DBCStructure.h, another project's reading of the same file). ID is the ONLY column AzerothCore's SpellFocusObjectEntry struct names at all (the whole rest of the table, columns 1-17, is one commented-out Name[16] locale block + a flags column) -- 8 fixed bytes is short of even that minimal shape (4 bytes), so the second dword has no candidate at all. |
| `field_1` | `u32` | L3 | Second dword of the handler's read sequence. No naming source: AzerothCore's SpellFocusObjectEntry struct stores nothing besides ID (see id's note). Width L3 only. |
| `name` | `lpstring` | L1 | The focus object's display name: SpellFocusObject.dbc's only other content is Name[16] (DBC columns 1-16, a 16-locale block AzerothCore's fmt marks 'x' and never stores), and the handler reads exactly one trailing lpstring -- count matches, per the map's locale-block trap (client/patch-table-names.md): the wire carries only the primary locale. Width L3 (handler-fire.yaml); name L1 (candidate, from the DBC's own commented locale block, not a stored core member). |

## Client

- handler RVA `0x00231cd0` in `ascension-extensions-2026-08-13`
