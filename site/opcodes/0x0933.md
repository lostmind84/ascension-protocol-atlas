# `0x0933` SMSG_PATCH_SKILL_LINE

**server to client** · status `unknown` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0933-smsg-patch-skill-line.yaml`](../../protocol/opcodes/0x0933-smsg-patch-skill-line.yaml)

Named SMSG_PATCH_SKILL_LINE by the client's own opcode table. One row of the stock SkillLine.dbc (AzerothCore already loads this table; ADR 0005), sent as an opaque 12-byte blob (RVA 0x00230370, no per-field split available) followed by 4 lpstrings. SkillLineEntry has 4 active numeric members (id, categoryId, spellIcon, canLink) but only 12 bytes (3 dwords) are read before the strings, and the file has only 3 locale-string blocks (name, kept; description and alternateVerb, both commented out) against 4 lpstrings observed on the wire -- neither count reconciles. This is the concrete counter-example that rules out "prefix = every numeric column, then one lpstring per locale block, in file order" as a safe general assumption for this bucket (see 0x092a's note): whatever this handler actually does, it is not that. No naming source was found that is safe to assert here. DBCStructure.h (SkillLineEntry) and DBCfmt.h (SkillLinefmt) were checked; nothing in the layout is renamed. Read against the real decompiled handler (build/ghidra/handlers-c/0x0933.c): the 12-byte prefix does resolve cleanly (id, then categoryId read but its value discarded by this handler -- not stored, though it is still consumed off the wire -- then skillCostID, matching the file's first 3 columns exactly), but the rest of the function is far more complex than "4 lpstrings": Ghidra shows what looks like an early return after the first string for one code path, and, reached from what reads as an exception-handler continuation, a second block that reads 5 more dwords and 3 more lpstrings before building the final 8-word record. Whether that second block is a genuine part of every read of this packet, or an artifact of how Ghidra represented this function's exception handling, is not established by this pass -- the summary above is left as "no naming source found" for the strings pending a dedicated re-read of this specific function, which needs more care than this sweep's budget.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_12` | L3 |  |
| `field_1` | `lpstring` | L3 |  |
| `field_2` | `lpstring` | L3 |  |
| `field_3` | `lpstring` | L3 |  |
| `field_4` | `lpstring` | L3 |  |

## Client

- handler RVA `0x00230370` in `ascension-extensions-2026-08-13`
