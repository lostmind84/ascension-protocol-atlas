# `0x06D8` SMSG_PATCH_CHARACTER_CREATION_SHAPESHIFT_DETAILS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06d8-smsg-patch-character-creation-shapeshift-details.yaml`](../../protocol/opcodes/0x06d8-smsg-patch-character-creation-shapeshift-details.yaml)

Named SMSG_PATCH_CHARACTER_CREATION_SHAPESHIFT_DETAILS by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_84` | L3 | one row of CharacterCreationShapeshiftDetails.dbc (21 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e0300` in `ascension-extensions-2026-08-13`
