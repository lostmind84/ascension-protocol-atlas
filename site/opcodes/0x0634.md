# `0x0634` SMSG_BUILD_CREATOR_OWNED_BUILDS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0634-smsg-build-creator-owned-builds.yaml`](../../protocol/opcodes/0x0634-smsg-build-creator-owned-builds.yaml)

The player's owned builds: a u32 the handler reads first, then a list parsed in a callee (not read here) -- a count by the shape of the sibling packets.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 | by the shape of the sibling packets; the list that follows is parsed in a callee |

## Client

- handler RVA `0x000fec40` in `ascension-extensions-2026-08-13`
