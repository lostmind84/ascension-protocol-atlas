# `0x05FD` SMSG_UPDATE_RANDOM_ENCHANT_SLOTS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05fd-smsg-update-random-enchant-slots.yaml`](../../protocol/opcodes/0x05fd-smsg-update-random-enchant-slots.yaml)

The whole random-enchant slot table: a count and one u32 per slot. The skeleton's second u32 was the first slot.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 | resizes the service's slot vector to this many entries |
| *repeat × count* | | | |
| &nbsp;&nbsp;`enchant` | `u32` | L3 | the value of slot i (vector at service +0xc); a parallel vector at +0x18 is zeroed. 0x05FE writes one such slot |

## Client

- handler RVA `0x002e9590` in `ascension-extensions-2026-08-13`
