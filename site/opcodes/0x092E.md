# `0x092E` SMSG_PATCH_SPELL_VISUAL_KIT_MODEL_ATTACH

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x092e-smsg-patch-spell-visual-kit-model-attach.yaml`](../../protocol/opcodes/0x092e-smsg-patch-spell-visual-kit-model-attach.yaml)

Named SMSG_PATCH_SPELL_VISUAL_KIT_MODEL_ATTACH by the client's own opcode table. The opcode is not bound in patch-tables.yaml -- the DLL's own custom-table list does not mention this opcode; SpellVisualKitModelAttach.dbc is the table patch-table-names.md associates with it by name and geometry (grade L1). The handler's own reads match SpellVisualKitModelAttach.dbc's own WDBC header exactly (10 fields, 40 bytes), so this looks like a full, unmodified row of that table. No source names any of the table's columns.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_40` | L3 |  |

## Client

- handler RVA `0x00233480` in `ascension-extensions-2026-08-13`
