# `0x074F` CMSG_DRAFT_HAND_OF_FATE_START

**client to server** · status `unknown` · evidence `L3` · layout `unknown` · client code `none`

Fiche: [`protocol/opcodes/0x074f-cmsg-draft-hand-of-fate-start.yaml`](../../protocol/opcodes/0x074f-cmsg-draft-hand-of-fate-start.yaml)

Named CMSG_DRAFT_HAND_OF_FATE_START by the client's own opcode table, the first of the Hand of Fate run (0x074F START, 0x0750 PICK, 0x0751 SACRIFICE, 0x0752 CANCEL). Unlike its three neighbours, which all have a confirmed empty-or-u32 sender, no sender for 0x074F exists anywhere in this build: the opcode's value never appears as a written literal in either Extensions.dll or Ascension.exe. Layout and behaviour are not established because there is nothing to read a layout from -- the same negative shape as CMSG_DRAFT_ROLL (0x074B, see its fiche).

## Layout

*No layout established.*
