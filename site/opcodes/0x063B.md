# `0x063B` SMSG_BUILD_DRAFT_SELECT_ROLE_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x063b-smsg-build-draft-select-role-result.yaml`](../../protocol/opcodes/0x063b-smsg-build-draft-select-role-result.yaml)

One NUL-terminated string the handler hands to BUILD_DRAFT_SELECT_ROLE_RESULT with format %s -- the only read and the only argument; named 'result' after the UI's parameter (default name: no Lua parameter declared).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 |  |

## Client

- handler RVA `0x001103d0` in `ascension-extensions-2026-08-13`

## Events

- fires `BUILD_DRAFT_SELECT_ROLE_RESULT` with format `%s`
