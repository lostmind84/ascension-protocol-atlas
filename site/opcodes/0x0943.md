# `0x0943` SMSG_PATCH_HOLIDAYS

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0943-smsg-patch-holidays.yaml`](../../protocol/opcodes/0x0943-smsg-patch-holidays.yaml)

One row of the stock Holidays.dbc (AzerothCore already loads this table; ADR 0005), read by the handler at RVA 0x0022cd10 as an 8-byte read, a dword, a variable-width read (handler-fire.yaml marks its width as undetermined, likely a loop over one of HolidaysEntry's arrays), 4 more dwords, then a trailing lpstring. Only TextureFilename (the sole locale-string-free char* column in the file) is named, from DBCStructure.h (L1); the numeric portion is array-heavy and does not reconcile cleanly with HolidaysEntry's column list, so nothing else is named here.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_8` | L3 | a single 8-byte read in the decompiled trace (local_74, handler-fire.yaml); not independently confirmed to be two separate 4-byte columns |
| `field_1` | `u32` | L3 |  |
| `field_2` | `bytes_variable` | L3 | the decompiled trace marks this read's width as variable/undetermined (local_44, handler-fire.yaml); likely a loop over one of HolidaysEntry's arrays (Duration[MAX_HOLIDAY_DURATIONS], Date[MAX_HOLIDAY_DATES] or CalendarFlags[MAX_HOLIDAY_FLAGS]), not pinned further |
| `field_3` | `u32` | L3 |  |
| `field_4` | `u32` | L3 |  |
| `field_5` | `u32` | L3 |  |
| `field_6` | `u32` | L3 |  |
| `TextureFilename` | `lpstring` | L1 | the file's only string-type column (col 51, active HolidaysEntry member, a single non-array char*) and the only candidate, so this identification is safe even though field_0..field_6 above are not individually reconciled against HolidaysEntry's column layout. |

## Client

- handler RVA `0x0022cd10` in `ascension-extensions-2026-08-13`
