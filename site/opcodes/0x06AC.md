# `0x06AC` SMSG_PATCH_TUTORIAL_KEYWORDS

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x06ac-smsg-patch-tutorial-keywords.yaml`](../../protocol/opcodes/0x06ac-smsg-patch-tutorial-keywords.yaml)

SMSG_PATCH_TUTORIAL_KEYWORDS: writes a 17-field, 68-byte record (2 numeric fields, 7 strings, then 8 more numeric fields) into TutorialKeywords.dbc's table object -- the widest reduced record in this shard by string count, still well short of that table's own 64-field, 260-byte row. No column name is established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0; bounds-checked against the table object's minId/maxId |
| `field_1` | `u32` | L3 | offset 4 |
| `field_2` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (1st of 7 consecutive gaps) |
| `field_3` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (2nd of 7) |
| `field_4` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (3rd of 7) |
| `field_5` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (4th of 7) |
| `field_6` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (5th of 7) |
| `field_7` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (6th of 7) |
| `field_8` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (7th of 7) |
| `field_9` | `u32` | L3 | offset 0x24 |
| `field_10` | `u32` | L3 | offset 0x28 |
| `field_11` | `u32` | L3 | offset 0x2c |
| `field_12` | `u32` | L3 | offset 0x30 |
| `field_13` | `u32` | L3 | offset 0x34 |
| `field_14` | `u32` | L3 | offset 0x38 |
| `field_15` | `u32` | L3 | offset 0x3c |
| `field_16` | `u32` | L3 | offset 0x40 |
| `field_17` | `cstring` | L3 | 1st of 7, NUL-terminated, scanned byte by byte; not FUN_100d3680 |
| `field_18` | `cstring` | L3 | 2nd of 7 |
| `field_19` | `cstring` | L3 | 3rd of 7 |
| `field_20` | `cstring` | L3 | 4th of 7 |
| `field_21` | `cstring` | L3 | 5th of 7 |
| `field_22` | `cstring` | L3 | 6th of 7 |
| `field_23` | `cstring` | L3 | 7th of 7 |

## Client

- handler RVA `0x001e44c0` in `ascension-extensions-2026-08-13`
