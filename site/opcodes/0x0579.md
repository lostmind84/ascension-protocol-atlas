# `0x0579` SMSG_PATCH_LFG_ACTIVITIES

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0579-smsg-patch-lfg-activities.yaml`](../../protocol/opcodes/0x0579-smsg-patch-lfg-activities.yaml)

SMSG_PATCH_LFG_ACTIVITIES: writes a 7-field, 28-byte record into LFGActivities.dbc's table object, well short of that table's own 23-field, 92-byte row. No column name is established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0; bounds-checked against the table object's minId/maxId |
| `field_1` | `u32` | L3 | offset 4, first half of an 8-byte load |
| `field_2` | `u32` | L3 | offset 8, second half; read as one 8-byte load in the handler; split into two 4-byte fields here to line up with the table's own field count -- could be a single 64-bit value instead, not resolved further |
| `field_3` | `u32` | L3 | offset 0xc |
| `field_4` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further |
| `field_5` | `u32` | L3 | offset 0x14 |
| `field_6` | `u32` | L3 | offset 0x18 |
| `field_7` | `lpstring` | L3 | via FUN_100d3680 |

## Client

- handler RVA `0x001e1df0` in `ascension-extensions-2026-08-13`
