# `0x0985` SMSG_PATCH_SOUND_WATER_TYPE

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0985-smsg-patch-sound-water-type.yaml`](../../protocol/opcodes/0x0985-smsg-patch-sound-water-type.yaml)

Named SMSG_PATCH_SOUND_WATER_TYPE by the client's own opcode table. The opcode is not bound in patch-tables.yaml -- the DLL's own custom-table list does not mention this opcode; SoundWaterType.dbc is the table patch-table-names.md associates with it by name and geometry (grade L1). The handler's own reads match SoundWaterType.dbc's own WDBC header exactly (4 fields, 16 bytes), so this looks like a full, unmodified row of that table. No source names any of the table's columns.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_16` | L3 |  |

## Client

- handler RVA `0x00231690` in `ascension-extensions-2026-08-13`
