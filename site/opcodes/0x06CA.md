# `0x06CA` SMSG_MENTOR_SPECIALIZATIONS_LIST

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06ca-smsg-mentor-specializations-list.yaml`](../../protocol/opcodes/0x06ca-smsg-mentor-specializations-list.yaml)

The mentor specializations list: a leading byte, a count and one u32 per entry. The byte comes first in the handler, against the capture reference's order.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `unknown_0` | `u8` | L3 | read first and stored at the mentor service +0x19c; the capture reference puts the count first, but the handler's own order is the byte then the count |
| `count` | `u32` | L3 |  |
| *repeat × count* | | | |
| &nbsp;&nbsp;`specialization` | `u32` | L3 | one u32 per item, appended to a container |

## Client

- handler RVA `0x00a17a70` in `ascension-extensions-2026-08-13`
