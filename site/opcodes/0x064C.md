# `0x064C` SMSG_WILDCARD_REROLL_UNLOCKED_STARTING_ABILITIES_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x064c-smsg-wildcard-reroll-unlocked-starting-abilities-result.yaml`](../../protocol/opcodes/0x064c-smsg-wildcard-reroll-unlocked-starting-abilities-result.yaml)

One NUL-terminated string the handler hands to WILDCARD_REROLL_UNLOCKED_STARTIN with format %s -- the only read and the only argument; named 'result' after the UI's parameter (default name: no Lua parameter declared).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 |  |

## Client

- handler RVA `0x00a308f0` in `ascension-extensions-2026-08-13`

## Events

- fires `WILDCARD_REROLL_UNLOCKED_STARTIN` with format `%s`
- the UI receives `WILDCARD_REROLL_UNLOCKED_STARTING_ABILITIES_RESULT` (`client/event-args.yaml`)
