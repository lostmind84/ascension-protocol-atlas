# `0x05FA` SMSG_ADD_KNOWN_RANDOM_ENCHANT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05fa-smsg-add-known-random-enchant.yaml`](../../protocol/opcodes/0x05fa-smsg-add-known-random-enchant.yaml)

One mystic enchant learned: its id, handed to MYSTIC_ENCHANT_LEARNED(spellID) with format %u, then one byte the handler stores (not established).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `spellId` | `u32` | L3 |  |
| `unknown_1` | `u8` | L3 |  |

## Client

- handler RVA `0x002e79f0` in `ascension-extensions-2026-08-13`

## Events

- fires `MYSTIC_ENCHANT_LEARNED` with format `%u`
- the UI receives `MYSTIC_ENCHANT_LEARNED` (`client/event-args.yaml`)
- the UI receives `MYSTIC_ENCHANT_LEARNED` (`client/event-args.yaml`)
