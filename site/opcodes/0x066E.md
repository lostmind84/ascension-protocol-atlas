# `0x066E` SMSG_PATCH_TOKEN_TYPES

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x066e-smsg-patch-token-types.yaml`](../../protocol/opcodes/0x066e-smsg-patch-token-types.yaml)

One row of TokenTypes.dbc: 9 dwords (36 bytes), of which the 6 at +0x04..+0x18 are throwaway placeholders overwritten by 6 trailing cstrings the wire sends right after. 8 of the 9 fields are named from the client's own native getter C_Token.GetTokenInfo (VA 0x1033d040, called from TokenObjectMixin:Init in Interface/FrameXML/Util/TokenUtil.lua), whose 7 positional Lua return values (name, description, tooltip, icon, toastStyle, showToast, quality) this pass matched one-for-one to the offsets the getter itself reads. The 6th trailing string (+0x04) is read but never touched by GetTokenInfo, so it is the only field of the 9 left unnamed. TokenTypes.dbc's own header reports 57 fields / 228 bytes; only these first 9 (36 bytes) are ever sent by this SMSG.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L3 | record +0x00, the row key -- range-checked against minId 0x10bdf670 / maxId 0x10bdf66c, indexes the row table at 0x10bdf680, then passed to FUN_10133ac0. |
| `unknown_1` | `bytes_4` | L3 | record +0x04, SKIPPED: consumed from the wire as part of the 36-byte contiguous block (the read cursor only advances once, to +0x24) but never loaded into a named local; overwritten by the wire's 1st trailing cstring below. Not one of GetTokenInfo's five string returns (see string_1 below), so its column name is not established. |
| `name` | `bytes_4` | L3 | record +0x08, SKIPPED the same way; overwritten by the wire's 2nd trailing cstring below (GetTokenInfo's 1st return value names this slot -- see that field). |
| `description` | `bytes_4` | L3 | record +0x0c, SKIPPED; overwritten by the wire's 3rd trailing cstring (GetTokenInfo's 2nd return value). |
| `tooltip` | `bytes_4` | L3 | record +0x10, SKIPPED; overwritten by the wire's 4th trailing cstring (GetTokenInfo's 3rd return value). |
| `icon` | `bytes_4` | L3 | record +0x14, SKIPPED; overwritten by the wire's 5th trailing cstring (GetTokenInfo's 4th return value). |
| `toastStyle` | `bytes_4` | L3 | record +0x18, SKIPPED; overwritten by the wire's 6th trailing cstring (GetTokenInfo's 5th return value). |
| `showToast` | `u32` | L3 | record +0x1c, read as a C++ bool by the getter (`*(int *)(iVar1 + 0x1c) != 0`). GetTokenInfo's (native, VA 0x1033d040; C_Token.GetTokenInfo, called from TokenObjectMixin:Init, Interface/FrameXML/Util/TokenUtil.lua) 6th return value, matching the Lua local `showToast` in `self.name, self.description, self.tooltip, self.icon, self.toastStyle, self.showToast, self.quality = C_Token.GetTokenInfo(self.tokenType)`. |
| `quality` | `u32` | L3 | record +0x20, the last field this opcode writes. GetTokenInfo's 7th and last return value, `*(undefined4 *)(iVar1 + 0x20)`, matching the Lua local `quality`. TokenTypes.dbc's own header reports 57 fields / 228 bytes; only these first 9 (36 bytes) are ever touched by this SMSG. |
| `string_1` | `cstring` | L3 | record +0x04, over the first skipped dword (see unknown_1). The wire's 1st trailing cstring. GetTokenInfo's five string returns start one slot later, at +0x08 (see the `name` field's note), so this one is read but discarded by the getter -- its column name is not established. |
| `string_2` | `cstring` | L3 | record +0x08, over the `name` slot. The wire's 2nd trailing cstring; GetTokenInfo's 1st return value, `*(iVar1 + 8)` -> Lua local `name`. |
| `string_3` | `cstring` | L3 | record +0x0c, over the `description` slot. The wire's 3rd trailing cstring; GetTokenInfo's 2nd return value, `*(iVar1 + 0xc)` -> Lua local `description`. |
| `string_4` | `cstring` | L3 | record +0x10, over the `tooltip` slot. The wire's 4th trailing cstring; GetTokenInfo's 3rd return value, `*(iVar1 + 0x10)` -> Lua local `tooltip`. |
| `string_5` | `cstring` | L3 | record +0x14, over the `icon` slot. The wire's 5th trailing cstring; GetTokenInfo's 4th return value, `*(iVar1 + 0x14)` -> Lua local `icon`. |
| `string_6` | `cstring` | L3 | record +0x18, over the `toastStyle` slot. The wire's 6th and last trailing cstring; GetTokenInfo's 5th return value, `*(iVar1 + 0x18)` -> Lua local `toastStyle`. |

## Client

- handler RVA `0x0033c2c0` in `ascension-extensions-2026-08-13`
