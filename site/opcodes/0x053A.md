# `0x053A` CMSG_PGF_GROUP_APPLICANT_REQUEST

**client to server** · status `unknown` · evidence `L3` · layout `unknown` · client code `none`

Fiche: [`protocol/opcodes/0x053a-cmsg-pgf-group-applicant-request.yaml`](../../protocol/opcodes/0x053a-cmsg-pgf-group-applicant-request.yaml)

Named CMSG_PGF_GROUP_APPLICANT_REQUEST by the client's own opcode table. No sender exists for this opcode in this build: the value appears nowhere in `.text` as a 32- or 16-bit immediate, nowhere in `.rdata`/`.data`, and nowhere in the shipped Lua (client/client-senders.md's exhaustive scan), and it is absent from every symbol dump (senders/handlers/layouts/ handler-fire/handler-strings.yaml). Layout is not established; there is no code to read it from. client/client-senders.md notes, unverified (L0), that this may be the older generation of a feature the live CMSG_GROUP_FINDER_* opcodes (0x057E/0x057F) replaced.

## Layout

*No layout established.*
