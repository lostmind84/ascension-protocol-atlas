# `0x056D` SMSG_PATCH_MYTHIC_KEYSTONE

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x056d-smsg-patch-mythic-keystone.yaml`](../../protocol/opcodes/0x056d-smsg-patch-mythic-keystone.yaml)

SMSG_PATCH_MYTHIC_KEYSTONE: a clean, full-width row write into MythicKeystones.dbc -- 3 numeric fields, 12 bytes, no strings, matching that table's own header geometry exactly (client/patch-table-names.md). Stored through a lookup-or-append helper pair (FUN_101d0500/FUN_101da590) rather than the direct minId/maxId array-bounds idiom most of this shard's tables use, but the geometry still matches exactly: all 3 fields are overwritten whether or not a prior row for this id exists. A same-named texture (`Interface\MythicPlus\MythicKeystone`) appears in AtlasInfo.lua; checked and it is an art-asset path, not a data getter -- a false lead, not a naming source. No column name is established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0x0 |
| `field_1` | `u32` | L3 | offset 0x4 |
| `field_2` | `u32` | L3 | offset 0x8 |

## Client

- handler RVA `0x001e24d0` in `ascension-extensions-2026-08-13`
