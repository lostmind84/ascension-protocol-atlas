# `0x099C` SMSG_PATCH_LFGDUNGEON_EXPANSION

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x099c-smsg-patch-lfgdungeon-expansion.yaml`](../../protocol/opcodes/0x099c-smsg-patch-lfgdungeon-expansion.yaml)

A record patched into one of Extensions.dll's internal id-keyed containers -- looked up by id (RVA 0x000b1a50), allocated fresh as a 0x20-byte heap struct when new (RVA 0x000ae5d20), and inserted (RVA 0x000aa9a0) -- not one of the 114 CustomDBCMgr `.data` tables client/patch-tables.md documents: nothing in the decompiled handler references a fixed .data table object (no `+0xc`/`+0x10`/`+0x20` min/max/rows idiom anywhere in it, unlike 0x09d1/0x09d2 in this same shard, which do). The third-party reference in client/patch-table-names.md names `LFGDungeonExpansion.dbc` for this opcode, but no file of that name exists in either client-DBC snapshot this atlas holds ('Disagreements found' #4 there) -- there is no table to compare against in the first place. No source in this repository names the struct's own fields (see naming_source below).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_32` | L3 | width: L3 (block-copy read, 32 bytes; re-verified this pass against the cached decompile, build/ghidra/handlers-c/0x099c.c, against client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming source (see naming_source below). The third-party reference names LFGDungeonExpansion.dbc for this opcode, but no file of that name exists in either client-DBC snapshot this atlas holds (client/patch-table-names.md, 'Disagreements found' #4): there is no table geometry to compare this record against at all.
 |

## Client

- handler RVA `0x0022e3a0` in `ascension-extensions-2026-08-13`
