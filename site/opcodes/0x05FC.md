# `0x05FC` SMSG_UPDATE_RANDOM_ENCHANT_DATA

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05fc-smsg-update-random-enchant-data.yaml`](../../protocol/opcodes/0x05fc-smsg-update-random-enchant-data.yaml)

Three u32 read unconditionally into the mystic-enchant service at +0x28, +0x2c and +0x30. The handler has no loop, so the capture reference's count/id/value reading is a naming, not a structure: its single packet carries count = 1, where the two readings are the same twelve bytes.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `unknown_0` | `u32` | L3 | read with unknown_1 in one two-u32 move (pos += 8), then stored at the mystic-enchant service +0x28. The capture reference reads this as a record count and observed 1 over a 12-byte payload (L1). The handler reads a fixed three u32 and contains no loop and no comparison against this value, so at count = 1 the two readings are the same twelve bytes and their capture cannot separate them; were the server ever to send count > 1, this client would consume only the first pair and ignore the remaining (count - 1) * 8 bytes. |
| `unknown_1` | `u32` | L3 | stored at +0x2c; the capture reference calls it id and observed 0 (L1) |
| `unknown_2` | `u32` | L3 | read separately (pos += 4) and stored at +0x30; the capture reference calls it value and observed 1 (L1) |

## Client

- handler RVA `0x002e9460` in `ascension-extensions-2026-08-13`
