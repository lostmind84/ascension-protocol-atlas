# `0x05F4` SMSG_PATCH_SPELL_CUSTOM_ATTR

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05f4-smsg-patch-spell-custom-attr.yaml`](../../protocol/opcodes/0x05f4-smsg-patch-spell-custom-attr.yaml)

Named SMSG_PATCH_SPELL_CUSTOM_ATTR by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_44` | L3 | one row of SpellCustomAttr.dbc (11 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e39a0` in `ascension-extensions-2026-08-13`
