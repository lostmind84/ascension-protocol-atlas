# `0x05E8` CMSG_RECOVER_MYTHIC_RECYCLING

**client to server** · status `hypothetical` · evidence `L3` · layout `named` · client code `sender`

Fiche: [`protocol/opcodes/0x05e8-cmsg-recover-mythic-recycling.yaml`](../../protocol/opcodes/0x05e8-cmsg-recover-mythic-recycling.yaml)

Sent by the Lua binding C_RecoveryService.RecoverCategoryItemAtIndex(category, index, id) (FUN_10306a10) when the category string maps to table index 5, RECOVERY_SERVICE_CATEGORY_MYTHIC_RECYCLING. Two fields: a u32 position and the id string, NUL-terminated. The position is the index of the entry in the client's cached list for the category (the binding turns the Lua index into it through a lookup in that list and refuses the call when the entry is not found).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `position` | `u32` | L3 |  |
| `id` | `cstring` | L3 |  |

## Client

- sender `FUN_10306a10`
