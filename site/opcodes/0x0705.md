# `0x0705` CMSG_CLOSE_GM_TICKET

**client to server** · status `hypothetical` · evidence `L3` · layout `named` · client code `sender`

Fiche: [`protocol/opcodes/0x0705-cmsg-close-gm-ticket.yaml`](../../protocol/opcodes/0x0705-cmsg-close-gm-ticket.yaml)

Close a GM ticket: the payload is written by shared ticket code this reading did not follow.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `body` | `bytes_variable` | L3 | the opcode is written by CloseTicket's own code; the payload goes through the shared ticket helpers (FUN_10087390 copies, then a writer not followed) |

## Client

- sender `0x0024dd43`
