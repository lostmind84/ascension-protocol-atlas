# `0x0670` SMSG_TOKEN_UPDATE

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0670-smsg-token-update.yaml`](../../protocol/opcodes/0x0670-smsg-token-update.yaml)

A token balance update: the token type name and two u32, then TOKEN_UPDATED. The skeleton's four u16 were the two u32.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `tokenType` | `cstring` | L3 | matched to the TOKEN_TYPE_* table; the event's first argument |
| `unknown_1` | `u32` | L3 | with the next u32, TOKEN_UPDATED(tokenType, oldAmount, newAmount) -- which is old and which is new was not verified |
| `unknown_2` | `u32` | L3 |  |

## Client

- handler RVA `0x0033cb60` in `ascension-extensions-2026-08-13`

## Events

- fires `TOKEN_UPDATED` with format `%s%u%u`
- the UI receives `TOKEN_UPDATED` (`client/event-args.yaml`)
- the UI receives `TOKEN_UPDATED` (`client/event-args.yaml`)
