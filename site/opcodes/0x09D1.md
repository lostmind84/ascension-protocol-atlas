# `0x09D1` SMSG_PATCH_CHR_CLASSES_ROLES

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x09d1-smsg-patch-chr-classes-roles.yaml`](../../protocol/opcodes/0x09d1-smsg-patch-chr-classes-roles.yaml)

One row of ChrClassesRoles.dbc, written straight into the client's own copy of that table (patch-tables.yaml's CustomDBCMgr scan binds this opcode to it, and the decompile confirms the idiom: the row goes into the table object's rows array at (id - minId), bounds-checked against minId/maxId, exactly as client/patch-tables.md describes). The 11 fields are read off the wire in a flat sequential order (offsets 0x00..0x28) but the client re-orders 5 of them when it stores the row (see field_3/field_4/field_6/field_8/field_9 below) -- the table's own column order is therefore not the wire order. No source in this repository names any of the 11 columns: checked handler-strings.yaml, lua-keys.yaml and the UI corpus (see naming_source below).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | wire offset 0x00; stored at row slot 0 (the bounds-check id). width/order: L3. name: none -- no naming source. |
| `field_1` | `u32` | L3 | wire offset 0x04; stored at row slot 1, unmoved. width/order: L3. name: none -- no naming source. |
| `field_2` | `u32` | L3 | wire offset 0x08; stored at row slot 2, unmoved. width/order: L3. name: none -- no naming source. |
| `field_3` | `u32` | L3 | wire offset 0x0c; stored at row slot 5, not slot 3 -- the client re-orders this field when it writes the row (build/ghidra/handlers-c/0x09d1.c, re-read this pass: `puVar14[5] = uVar6`, where uVar6 is this field). width/order: L3. name: none -- no naming source.
 |
| `field_4` | `u32` | L3 | wire offset 0x10; stored at row slot 8, not slot 4 (`puVar14[8] = uVar7`). width/order: L3. name: none -- no naming source.
 |
| `field_5` | `u32` | L3 | wire offset 0x14; stored at row slot 3, not slot 5 (`puVar14[3] = uVar8`). width/order: L3. name: none -- no naming source.
 |
| `field_6` | `u32` | L3 | wire offset 0x18; stored at row slot 6, unmoved (`puVar14[6] = uVar9`). width/order: L3. name: none -- no naming source.
 |
| `field_7` | `u32` | L3 | wire offset 0x1c; stored at row slot 9, not slot 7 (`puVar14[9] = uVar10`). width/order: L3. name: none -- no naming source.
 |
| `field_8` | `u32` | L3 | wire offset 0x20; stored at row slot 4, not slot 8 (`puVar14[4] = uVar11`). width/order: L3. name: none -- no naming source.
 |
| `field_9` | `u32` | L3 | wire offset 0x24; stored at row slot 7, not slot 9 (`puVar14[7] = uVar12`). width/order: L3. name: none -- no naming source.
 |
| `field_10` | `u32` | L3 | wire offset 0x28; stored at row slot 10, unmoved. width/order: L3. name: none -- no naming source. |

## Client

- handler RVA `0x001e0400` in `ascension-extensions-2026-08-13`
