# `0x074A` CMSG_DRAFT_STOP

**client to server** · status `hypothetical` · evidence `L3` · layout `empty` · client code `sender`

Fiche: [`protocol/opcodes/0x074a-cmsg-draft-stop.yaml`](../../protocol/opcodes/0x074a-cmsg-draft-stop.yaml)

Sent by the Lua binding ToggleDraftMode(enabled) (FUN_100dd6e0) with enabled = false: the opcode is computed as 0x749 + (enabled == false), so one binding sends either CMSG_DRAFT_START or CMSG_DRAFT_STOP. Empty packet.

## Layout

*No layout established.*

## Client

- sender `FUN_100dd6e0`
