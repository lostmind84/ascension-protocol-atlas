# `0x0526` CMSG_FEL_COMMUTATION_CHANGE_SLOT

**client to server** · status `unknown` · evidence `L3` · layout `unknown` · client code `none`

Fiche: [`protocol/opcodes/0x0526-cmsg-fel-commutation-change-slot.yaml`](../../protocol/opcodes/0x0526-cmsg-fel-commutation-change-slot.yaml)

Named CMSG_FEL_COMMUTATION_CHANGE_SLOT by the client's own opcode table. A raw byte sequence spelling `mov esi, 0x526` exists in `.text` at one site, but that site is data embedded in code, not a real instruction (one of four such coincidences `client/client-senders.md` names and excludes); beyond that single false lead the opcode value appears nowhere else in this build -- not in `.rdata`/`.data`, not before any `Put(u32)` opcode write, not in the shipped Lua. This build has no sender for it; layout is not established because there is nothing to read, not because nobody has looked.

## Layout

*No layout established.*
