# `0x0686` SMSG_PATCH_SPELL_ADDON

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0686-smsg-patch-spell-addon.yaml`](../../protocol/opcodes/0x0686-smsg-patch-spell-addon.yaml)

Named SMSG_PATCH_SPELL_ADDON by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_92` | L3 | one row of SpellAddon.dbc (23 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e36b0` in `ascension-extensions-2026-08-13`
