# `0x052A` CMSG_DISCORD_USER_ID_UPDATE

**client to server** · status `hypothetical` · evidence `L3` · layout `named` · client code `sender`

Fiche: [`protocol/opcodes/0x052a-cmsg-discord-user-id-update.yaml`](../../protocol/opcodes/0x052a-cmsg-discord-user-id-update.yaml)

Discord integration: the user's id as a u64 and a NUL-terminated string (by the opcode's name; the sender is not a Lua binding).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `userId` | `u64` | L3 | by the opcode's name |
| `string_1` | `cstring` | L3 |  |

## Client

- sender `0x00221b1a`
