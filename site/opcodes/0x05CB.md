# `0x05CB` SMSG_CHALLENGE_CREATOR_COMPLETION

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05cb-smsg-challenge-creator-completion.yaml`](../../protocol/opcodes/0x05cb-smsg-challenge-creator-completion.yaml)

The CoA server calls it `SMSG_COA_TRIAL_COMPLETION_ADDED`.

One completion entry added to a custom trial: the entry (0x88 bytes per the fork's comment, its first string being the trial id, which is what the handler reads inline), then TRIAL_COMPLETION_ADDED fired with format %s%u. Not read further.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `trialId` | `lpstring` | L3 | the entry's first string; the rest of the entry is parsed in a callee |

## Client

- handler RVA `0x00122780` in `ascension-extensions-2026-08-13`

## Events

- fires `TRIAL_COMPLETION_ADDED` with format `%s%u`

## Server

- `modules/mod-coa-challenges/src/CoA.Challenges.Lifecycle.cpp`
- `modules/mod-coa-challenges/src/CoA.Challenges.Trials.cpp`
