# `0x056E` SMSG_PATCH_MYTHIC_PLUS_SCALING

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x056e-smsg-patch-mythic-plus-scaling.yaml`](../../protocol/opcodes/0x056e-smsg-patch-mythic-plus-scaling.yaml)

Named SMSG_PATCH_MYTHIC_PLUS_SCALING by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_32` | L3 | one row of MythicPlusScaling.dbc (8 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e25c0` in `ascension-extensions-2026-08-13`
