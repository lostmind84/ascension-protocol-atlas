# `0x0746` SMSG_PLAYER_POLL_LIST

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0746-smsg-player-poll-list.yaml`](../../protocol/opcodes/0x0746-smsg-player-poll-list.yaml)

The player poll list: a count and that many 176-byte poll records. The record's own fields were not decoded.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 | read by the poll manager's deserializer (FUN_102d62e0), which logs 'Received {} polls' |
| *repeat × count* | | | |
| &nbsp;&nbsp;`poll` | `bytes_176` | L3 | one 0xb0-byte poll record, built by FUN_102d6f80 and FUN_102d6de0; its fields were not decoded. The handler then fires PLAYER_POLL_NEW_POLL for the first unanswered poll |

## Client

- handler RVA `0x002d7d90` in `ascension-extensions-2026-08-13`

## Events

- fires `PLAYER_POLL_NEW_POLL` with format `%u`
