# `0x0708` SMSG_SEND_GM_TICKET_MESSAGE_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0708-smsg-send-gm-ticket-message-result.yaml`](../../protocol/opcodes/0x0708-smsg-send-gm-ticket-message-result.yaml)

Result of sending a ticket message: the result string and the message index. The handler fires SEND_GM_TICKET_MESSAGE_RESULT or SEND_PLAYER_TICKET_MESSAGE_RESULT (its %s%s%u format carries the event name first). The skeleton had only the u8.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 |  |
| `index` | `u8` | L3 | the message index the UI passes back: SEND_GM_TICKET_MESSAGE_RESULT(result, index) / SEND_PLAYER_TICKET_MESSAGE_RESULT(result, index), TicketMessageMixin.lua:192-202 |

## Client

- handler RVA `0x00254df0` in `ascension-extensions-2026-08-13`

## Events

- fires `SEND_GM_TICKET_MESSAGE_RESULT` with format `%s%s%u`
- fires `SEND_PLAYER_TICKET_MESSAGE_RESUL` with format `%s%u`
- the UI receives `SEND_GM_TICKET_MESSAGE_RESULT` (`client/event-args.yaml`)
- the UI receives `SEND_PLAYER_TICKET_MESSAGE_RESULT` (`client/event-args.yaml`)
