# `0x096A` SMSG_PATCH_ITEM_LIMIT_CATEGORY

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x096a-smsg-patch-item-limit-category.yaml`](../../protocol/opcodes/0x096a-smsg-patch-item-limit-category.yaml)

Candidate: one row of ItemLimitCategory.dbc (bound_by: L1 ref, patch-table-names.md). The handler reads a 16-byte/4-dword block then one string (handler-fire.yaml), short of the table's full 20-column/80-byte record. id, maxCount and mode (the table's last two columns) are named, the latter two on an ascending-order-preservation assumption; one dword has no candidate and stays unnamed. The trailing lpstring is count-matched to the table's one locale block (name[16]).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0 (index field, fmt 'n'); core: ItemLimitCategoryEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar4,16],[local_2c,lpstring]], one 16-byte/4-dword bulk read then one string); name L1 (core DBCStructure.h, another project's reading of the same file). |
| `field_1` | `u32` | L3 | Second of the 4 dwords. Only 2 of the remaining 3 have a candidate core name (maxCount, mode below); this one is unaccounted. Candidate, not placed: the DBC's 'name flags' column (17), which ItemLimitCategoryEntryfmt marks 'x'. Width L3 only. |
| `maxCount` | `u32` | L1 | DBC column 18; core: ItemLimitCategoryEntry::maxCount, 'max allowed equipped as item or in gem slot'. Placed as the third of four dwords on the assumption that the client preserves ascending DBC order within the kept-scalar group -- maxCount and mode are the table's last two columns, contiguous. Width L3; name L1 (core DBCStructure.h). |
| `mode` | `u32` | L1 | DBC column 19; core: ItemLimitCategoryEntry::mode, '0 = have, 1 = equip'. Same order-preservation inference as maxCount. Width L3; name L1 (core DBCStructure.h). |
| `name` | `lpstring` | L1 | DBC columns 1-16; core: ItemLimitCategoryEntry has a commented-out Name[16] locale block ('m_name_lang'), the table's only string-bearing span, matching the one observed lpstring per the map's locale-block trap. Width L3 (handler-fire.yaml); name L1 (dead-comment name, not a stored core member). |

## Client

- handler RVA `0x0022daa0` in `ascension-extensions-2026-08-13`
