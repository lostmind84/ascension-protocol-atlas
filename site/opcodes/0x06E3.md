# `0x06E3` SMSG_PATCH_CHARACTER_CREATION_ARCHETYPE_DETAILS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06e3-smsg-patch-character-creation-archetype-details.yaml`](../../protocol/opcodes/0x06e3-smsg-patch-character-creation-archetype-details.yaml)

Named SMSG_PATCH_CHARACTER_CREATION_ARCHETYPE_DETAILS by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_112` | L3 | one row of CharacterCreationArchetypeDetails.dbc (28 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001df010` in `ascension-extensions-2026-08-13`
