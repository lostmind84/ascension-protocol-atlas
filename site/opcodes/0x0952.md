# `0x0952` SMSG_PATCH_SPELL_RADIUS

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0952-smsg-patch-spell-radius.yaml`](../../protocol/opcodes/0x0952-smsg-patch-spell-radius.yaml)

One row of SpellRadius.dbc (bound_by: L1 ref, patch-table-names.md): the handler's single 16-byte bulk read exactly equals the table's 4-column/16-byte record (client DBC header). All 4 columns are named by AzerothCore's own SpellRadiusEntry struct (L1, another project's reading of the same file).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0 (index field, fmt 'n'); core: SpellRadiusEntry::ID. Width L3 (handler-fire.yaml: reads [[uVar5,16]], one 16-byte bulk read == the full 4-column/16-byte SpellRadius.dbc record); name L1 (core DBCStructure.h, another project's reading of the same file). |
| `radiusMin` | `f32` | L1 | DBC column 1; core: SpellRadiusEntry::RadiusMin (float). Width L3; name L1 (core DBCStructure.h). |
| `radiusPerLevel` | `f32` | L1 | DBC column 2; core: SpellRadiusEntry::RadiusPerLevel (float). Width L3; name L1 (core DBCStructure.h). |
| `radiusMax` | `f32` | L1 | DBC column 3; core: SpellRadiusEntry::RadiusMax (float). Width L3; name L1 (core DBCStructure.h). |

## Client

- handler RVA `0x00232b00` in `ascension-extensions-2026-08-13`
