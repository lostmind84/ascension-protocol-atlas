# `0x0627` SMSG_BUILD_CREATOR_CREATE_BUILD_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0627-smsg-build-creator-create-build-result.yaml`](../../protocol/opcodes/0x0627-smsg-build-creator-create-build-result.yaml)

Answer to a build-creator create request: a NUL-terminated result string; the handler fires BUILD_CREATOR_CREATE_RESULT(result, buildID) with format %s%s, and the build id it passes is not among the reads this pass sees (a second NUL-terminated string or client state; not established).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 |  |

## Client

- handler RVA `0x000fe1d0` in `ascension-extensions-2026-08-13`

## Events

- fires `BUILD_CREATOR_CREATE_RESULT` with format `%s%s`
- the UI receives `BUILD_CREATOR_CREATE_RESULT` (`client/event-args.yaml`)
