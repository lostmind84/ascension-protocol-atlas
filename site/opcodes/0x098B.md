# `0x098B` SMSG_PATCH_SOUND_ENTRIES

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x098b-smsg-patch-sound-entries.yaml`](../../protocol/opcodes/0x098b-smsg-patch-sound-entries.yaml)

One row of SoundEntries.dbc: Id (named), an unnamed dword (Type), then 11 skipped-and-appended strings (InternalName, FileName[0..9] -- all unnamed, AzerothCore comments them but does not compile them), ten unnamed dwords (Freq[0..9]), one more skipped-and-appended string (Path, unnamed), then six unnamed trailing dwords. 30 dwords (120B) and 12 strings match the table exactly; AzerothCore's own format string marks every column but the id 'x' (unused), so none of the string positions come from the core -- they come from the decompiled handler's own read/skip pattern, matched 1:1 against DBCStructure.h's bare positional comments. Table binding is L1 (third-party reference only).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `Id` | `u32` | L1 | DBC column 0 (n); name 'Id' is AzerothCore's own DBCStructure.h struct member for SoundEntries.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/098b.c). |
| `unknown_1` | `u32` | L3 | DBC column 1 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_soundType. |
| `unknown_2_str` | `lpstring` | L3 | DBC column 2 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's comment for this column reads 'InternalName (m_name)' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_3_str` | `lpstring` | L3 | DBC column 3 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's comment for this column reads 'FileName[0] (m_File[10])' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_4_str` | `lpstring` | L3 | DBC column 4 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's comment for this column reads 'FileName[1] (m_File[10])' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_5_str` | `lpstring` | L3 | DBC column 5 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's comment for this column reads 'FileName[2] (m_File[10])' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_6_str` | `lpstring` | L3 | DBC column 6 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's comment for this column reads 'FileName[3] (m_File[10])' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_7_str` | `lpstring` | L3 | DBC column 7 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's comment for this column reads 'FileName[4] (m_File[10])' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_8_str` | `lpstring` | L3 | DBC column 8 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's comment for this column reads 'FileName[5] (m_File[10])' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_9_str` | `lpstring` | L3 | DBC column 9 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's comment for this column reads 'FileName[6] (m_File[10])' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_10_str` | `lpstring` | L3 | DBC column 10 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's comment for this column reads 'FileName[7] (m_File[10])' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_11_str` | `lpstring` | L3 | DBC column 11 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's comment for this column reads 'FileName[8] (m_File[10])' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_12_str` | `lpstring` | L3 | DBC column 12 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's comment for this column reads 'FileName[9] (m_File[10])' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_13` | `u32` | L3 | DBC column 13 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_Freq[10] (unused). |
| `unknown_14` | `u32` | L3 | DBC column 14 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_Freq[10] (unused). |
| `unknown_15` | `u32` | L3 | DBC column 15 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_Freq[10] (unused). |
| `unknown_16` | `u32` | L3 | DBC column 16 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_Freq[10] (unused). |
| `unknown_17` | `u32` | L3 | DBC column 17 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_Freq[10] (unused). |
| `unknown_18` | `u32` | L3 | DBC column 18 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_Freq[10] (unused). |
| `unknown_19` | `u32` | L3 | DBC column 19 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_Freq[10] (unused). |
| `unknown_20` | `u32` | L3 | DBC column 20 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_Freq[10] (unused). |
| `unknown_21` | `u32` | L3 | DBC column 21 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_Freq[10] (unused). |
| `unknown_22` | `u32` | L3 | DBC column 22 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_Freq[10] (unused). |
| `unknown_23_str` | `lpstring` | L3 | DBC column 23 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's comment for this column reads 'Path (m_DirectoryBase)' but the line is commented out (not compiled), so that text is not used as the field name. |
| `unknown_24` | `u32` | L3 | DBC column 24 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_volumeFloat. |
| `unknown_25` | `u32` | L3 | DBC column 25 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_flags. |
| `unknown_26` | `u32` | L3 | DBC column 26 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_minDistance. |
| `unknown_27` | `u32` | L3 | DBC column 27 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_distanceCutoff. |
| `unknown_28` | `u32` | L3 | DBC column 28 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). DBCStructure.h's bare comment calls this m_EAXDef. |
| `unknown_29` | `u32` | L3 | DBC column 29 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/098b.c). comment: 'new in 3.1', no further description. |

## Client

- handler RVA `0x00230ac0` in `ascension-extensions-2026-08-13`
