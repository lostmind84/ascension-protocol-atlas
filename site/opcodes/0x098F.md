# `0x098F` SMSG_PATCH_UISOUND_LOOKUPS

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x098f-smsg-patch-uisound-lookups.yaml`](../../protocol/opcodes/0x098f-smsg-patch-uisound-lookups.yaml)

A record patched into one of Extensions.dll's internal id-keyed containers -- looked up by id (RVA 0x000b28d0), allocated fresh as a 0x28-byte heap struct when new (RVA 0x000ae5d20), and inserted (RVA 0x000afa50) -- not one of the 114 CustomDBCMgr `.data` tables client/patch-tables.md documents: nothing in the decompiled handler references a fixed .data table object (no `+0xc`/`+0x10`/`+0x20` min/max/rows idiom anywhere in it, unlike 0x09d1/0x09d2 in this same shard, which do). client/patch-table-names.md's third-party reference names `UISoundLookups.dbc` for this opcode (3 fields, 12-byte record, 130 rows) and the handler's fixed portion happens to be the same 12 bytes -- but that binding is `L1 ref` only (unverified by our own extraction: this opcode is absent from client/symbols/ascension-extensions-2026-08-13/patch-tables.yaml's 104-opcode CustomDBCMgr scan), and the byte-count match does not, by itself, prove the store target is that table. No source in this repository names the struct's own fields (see naming_source below). The raw decompile (build/ghidra/handlers-c/0x098f.c) is hard to follow past the compiler's stack-guard check: it reads as two id-keyed stores back to back, apparently a cold-path duplication artifact around `__security_check_cookie`. The wire read sequence below is the tool-extracted one (client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml), which is a single coherent trace and is what this fiche trusts; neither visible store touches a CustomDBCMgr table object.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_12` | L3 | width: L3 (block-copy read, 12 bytes; re-verified this pass against the cached decompile, build/ghidra/handlers-c/0x098f.c, against client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming source (see naming_source below). 12 happens to equal UISoundLookups.dbc's record_size (12 bytes, 3 fields, per client/patch-table-names.md) -- but the store target below is not that table, so the match may be coincidental rather than confirming the binding.
 |
| `field_1` | `lpstring` | L3 | width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming source.
 |
| `field_2` | `lpstring` | L3 | width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming source.
 |
| `field_3` | `lpstring` | L3 | width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming source.
 |
| `field_4` | `lpstring` | L3 | width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming source.
 |
| `field_5` | `lpstring` | L3 | width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming source.
 |
| `field_6` | `lpstring` | L3 | width: L3 (u32 length then that many bytes, no NUL -- FUN_100d3680, per client/symbols/ascension-extensions-2026-08-13/handler-fire.yaml). name: none -- no naming source.
 |

## Client

- handler RVA `0x00233ef0` in `ascension-extensions-2026-08-13`
