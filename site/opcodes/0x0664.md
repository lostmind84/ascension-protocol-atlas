# `0x0664` SMSG_PATCH_CHALLENGE_MODIFIER_TYPE

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0664-smsg-patch-challenge-modifier-type.yaml`](../../protocol/opcodes/0x0664-smsg-patch-challenge-modifier-type.yaml)

One row of ChallengeModifierTypes.dbc in the compact wire form: the row id, four skipped string-offset dwords, then four NUL-terminated strings written over them. The runtime record is 0x14 bytes while the client's ChallengeModifierTypes.dbc has 37 fields. Like 0x0663 the strings here are NUL-terminated, and the first three are kept as pointers into the received packet rather than copied.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L3 | dword 0, the only dword the reader loads; the row key -- range-checked against minId 0x10be02d0 / maxId 0x10be02cc, indexes the row table at 0x10be02e0, then passed to FUN_10133ac0 |
| `stringOffsets` | `bytes_16` | L3 | dwords 1..4, SKIPPED: the cursor jumps from +0x04 to the end of the 0x14 block. The row's four string-offset columns; the client writes the four string pointers that follow over record +0x04 .. +0x10. |
| `string_1` | `cstring` | L3 | NUL-TERMINATED, not length-prefixed -- like SMSG_PATCH_CHALLENGE_MODIFIER 0x0663 and unlike the rest of the family. The first three are stored as pointers INTO the packet buffer (record +0x04, +0x08, +0x0c); only the fourth is copied out. |
| `string_2` | `cstring` | L3 | record +0x08, a pointer into the packet buffer |
| `string_3` | `cstring` | L3 | record +0x0c, a pointer into the packet buffer |
| `string_4` | `cstring` | L3 | record +0x10; this one is strcpy_s'd into its own allocation. The UI resolves a modifier type through C_Challenge.GetModifierLocalization (L2); which string feeds which return value is NOT established here. |

## Client

- handler RVA `0x00139440` in `ascension-extensions-2026-08-13`
