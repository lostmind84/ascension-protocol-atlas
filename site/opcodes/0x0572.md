# `0x0572` SMSG_PATCH_TIMED_DUNGEON

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0572-smsg-patch-timed-dungeon.yaml`](../../protocol/opcodes/0x0572-smsg-patch-timed-dungeon.yaml)

Named SMSG_PATCH_TIMED_DUNGEON by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_24` | L3 | one row of TimedDungeons.dbc (6 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e42c0` in `ascension-extensions-2026-08-13`
