# `0x0672` SMSG_PLAYER_DATA

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0672-smsg-player-data.yaml`](../../protocol/opcodes/0x0672-smsg-player-data.yaml)

Player data: the client reads one byte and keeps it. The capture shows a four-byte payload, so the packet may carry more than the client consumes.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `unknown_0` | `u8` | L3 | The handler reads exactly one byte and stores it in the DLL global at 0x10d3d74a, then returns. The capture reference reports a four-byte payload holding a zero count; both hold if the server sends four bytes and the client consumes one, so the wire size is not settled here -- the client's reading is.
 |

## Client

- handler RVA `0x00a4f4d0` in `ascension-extensions-2026-08-13`
