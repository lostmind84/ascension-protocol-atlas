# `0x056C` SMSG_PATCH_MYTHIC_AFFIXES

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x056c-smsg-patch-mythic-affixes.yaml`](../../protocol/opcodes/0x056c-smsg-patch-mythic-affixes.yaml)

Named SMSG_PATCH_MYTHIC_AFFIXES by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_64` | L3 | one row of MythicAffixes.dbc (16 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e23e0` in `ascension-extensions-2026-08-13`
