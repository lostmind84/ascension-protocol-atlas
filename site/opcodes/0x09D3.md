# `0x09D3` SMSG_PATCH_OCCLUSION_VOLUME_POINTS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x09d3-smsg-patch-occlusion-volume-points.yaml`](../../protocol/opcodes/0x09d3-smsg-patch-occlusion-volume-points.yaml)

Named SMSG_PATCH_OCCLUSION_VOLUME_POINTS by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_20` | L3 | one row of OcclusionVolumePoints.dbc (5 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x001e2990` in `ascension-extensions-2026-08-13`

## Server

- `modules/mod-ascension-compat/src/AscensionCompat.cpp`
