# `0x0589` SMSG_CALLBOARD_VIEW

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0589-smsg-callboard-view.yaml`](../../protocol/opcodes/0x0589-smsg-callboard-view.yaml)

The callboard view: a count and that many 100-byte entries. The entry's own fields were not decoded.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 | bounded by the reader at 42 949 672 entries; it allocates count records of 100 bytes |
| *repeat × count* | | | |
| &nbsp;&nbsp;`entry` | `bytes_100` | L3 | one callboard entry; the reader (FUN_10116c10) fills it through per-field sub-readers whose order was not followed. CALLBOARD_VIEW_UPDATED afterwards |

## Client

- handler RVA `0x001194e0` in `ascension-extensions-2026-08-13`

## Events

- the UI receives `CALLBOARD_VIEW_UPDATED` (`client/event-args.yaml`)
