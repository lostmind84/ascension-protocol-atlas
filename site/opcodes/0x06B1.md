# `0x06B1` SMSG_TUTORIAL_REWARDED

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06b1-smsg-tutorial-rewarded.yaml`](../../protocol/opcodes/0x06b1-smsg-tutorial-rewarded.yaml)

A tutorial was rewarded: one u32 handed to TUTORIAL_ENTRY_UPDATE(%u); the tutorial id by the opcode's name and the sibling request 0x06A8 (no Lua parameter declared).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `tutorialId` | `u32` | L3 | by the opcode's name |

## Client

- handler RVA `0x00a18aa0` in `ascension-extensions-2026-08-13`

## Events

- fires `TUTORIAL_ENTRY_UPDATE` with format `%u`
