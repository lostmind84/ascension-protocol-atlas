# `0x061E` SMSG_WILDCARD_UNLEARN_ABILITY_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x061e-smsg-wildcard-unlearn-ability-result.yaml`](../../protocol/opcodes/0x061e-smsg-wildcard-unlearn-ability-result.yaml)

Result of a wildcard unlearn: one result string.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 | one NUL-terminated string, the %s of WILDCARD_UNLEARN_ABILITY_RESULT (WildCard.lua:281) |

## Client

- handler RVA `0x00a30dd0` in `ascension-extensions-2026-08-13`

## Events

- fires `WILDCARD_UNLEARN_ABILITY_RESULT` with format `%s`
- the UI receives `WILDCARD_UNLEARN_ABILITY_RESULT` (`client/event-args.yaml`)
