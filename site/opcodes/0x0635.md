# `0x0635` SMSG_BUILD_CREATOR_UPVOTED_BUILDS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0635-smsg-build-creator-upvoted-builds.yaml`](../../protocol/opcodes/0x0635-smsg-build-creator-upvoted-builds.yaml)

The player's upvoted builds: a u32 the handler reads first, then a list parsed in a callee (not read here) -- a count by the shape of the sibling packets.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 | by the shape of the sibling packets; the list that follows is parsed in a callee |

## Client

- handler RVA `0x00100000` in `ascension-extensions-2026-08-13`
