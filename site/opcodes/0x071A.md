# `0x071A` SMSG_REOPEN_GM_TICKET_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x071a-smsg-reopen-gm-ticket-result.yaml`](../../protocol/opcodes/0x071a-smsg-reopen-gm-ticket-result.yaml)

One NUL-terminated string the handler hands to REOPEN_PLAYER_TICKET_RESULT with format %s -- the only read and the only argument; named 'result' after the UI's parameter (default name: no Lua parameter declared).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 |  |

## Client

- handler RVA `0x00254c90` in `ascension-extensions-2026-08-13`

## Events

- fires `REOPEN_PLAYER_TICKET_RESULT` with format `%s`
