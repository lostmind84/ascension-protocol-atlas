# `0x063A` CMSG_BUILD_DRAFT_SELECT_ROLE

**client to server** · status `hypothetical` · evidence `L3` · layout `named` · client code `sender`

Fiche: [`protocol/opcodes/0x063a-cmsg-build-draft-select-role.yaml`](../../protocol/opcodes/0x063a-cmsg-build-draft-select-role.yaml)

Select a build-draft role: one string.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `role` | `cstring` | L3 | SelectRole(buildRole): the string written with its NUL through PutData(len + 1) |

## Client

- sender `0x001119f8`
