# `0x0724` SMSG_UNNAMED_0724

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0724-smsg-unnamed.yaml`](../../protocol/opcodes/0x0724-smsg-unnamed.yaml)

One CharacterAdvancementTabTypes.dbc row, reduced to the id and two strings: the DLL keeps a compact record, not the 19-column DBC row. The first string is the tab name the CoA server module reads from column 1.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L3 | CharacterAdvancementTabTypes.dbc column 0; range-checked against the table |
| `name` | `cstring` | L3 | column 1 per the server module (the tab token it upper-cases); copied into the row |
| `string_1` | `cstring` | L3 | a second string the handler copies; which column is not read here |

## Client

- handler RVA `0x001dea90` in `ascension-extensions-2026-08-13`
