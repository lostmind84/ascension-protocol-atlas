# `0x06C5` SMSG_PATCH_MENTOR_SPECIALIZATIONS

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x06c5-smsg-patch-mentor-specializations.yaml`](../../protocol/opcodes/0x06c5-smsg-patch-mentor-specializations.yaml)

SMSG_PATCH_MENTOR_SPECIALIZATIONS: writes a 3-field, 12-byte record (id, two strings) into MentorSpecializations.dbc's table object, short of that table's own 19-field, 76-byte row. Unlike the rest of this shard, two of its three fields have a naming LEAD, not a naming source: PathBecomeMentorPanelMixin.lua calls `C_Tutorial.GetMentorSpecializationInfo(specID)` and unpacks the result as `name, atlas` -- a native getter whose client-visible name matches this table exactly, returning two strings in that order, matching this record's own two strings. The getter's own names are L2 (read from client Lua); pairing them onto these two specific wire fields is this pass's own inference, graded L0 per client/patch-table-names.md's own convention for a "ui getter" lead ("L2 for the names, L0 again for 'these are the row's columns'"). Because that L0 pairing -- not the L2 name -- is the weaker claim, the fields keep their field_N placeholders rather than being renamed; the lead is recorded in the field notes and provenance instead.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0; bounds-checked against the table object's minId/maxId; almost certainly the specID C_Tutorial's getters take, not otherwise named |
| `field_1` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further |
| `field_2` | `cstring` | L3 | NUL-terminated, scanned byte by byte, not FUN_100d3680. A naming LEAD, not a naming source: PathBecomeMentorPanelMixin.lua:31 calls `name, atlas = C_Tutorial.GetMentorSpecializationInfo(specID)`, a native getter whose client-visible name matches this table, returning two strings in that order. The getter's own return-value names are L2 (client Lua source); pairing them onto this specific wire position over field_3 is our own inference and stays L0 per client/patch-table-names.md's own convention for a 'ui getter' lead (L2 for the names, L0 for 'these are the row's columns') -- not renamed to `name` here because that L0 pairing, not the L2 fact, is this field's weakest link |
| `field_3` | `cstring` | L3 | NUL-terminated, scanned byte by byte, not FUN_100d3680. Same getter and same caveat as field_2; if the pairing holds, this is the getter's second return value (`atlas`) |

## Client

- handler RVA `0x001e21d0` in `ascension-extensions-2026-08-13`
