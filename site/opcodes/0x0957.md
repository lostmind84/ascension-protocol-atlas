# `0x0957` SMSG_PATCH_SPELL_CHAIN_EFFECTS

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0957-smsg-patch-spell-chain-effects.yaml`](../../protocol/opcodes/0x0957-smsg-patch-spell-chain-effects.yaml)

Named SMSG_PATCH_SPELL_CHAIN_EFFECTS by the client's own opcode table. The opcode is not bound in patch-tables.yaml -- the DLL's own custom-table list does not mention this opcode; SpellChainEffects.dbc is the table patch-table-names.md associates with it by name and geometry (grade L1). The handler's own reads fall far short of SpellChainEffects.dbc's own WDBC header (48 fields, 177 bytes), so whatever this patches, it is not a full row of that table. No source names any of the table's columns.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `bytes_180` | L3 |  |
| `field_1` | `lpstring` | L3 |  |
| `field_2` | `lpstring` | L3 |  |
| `field_3` | `lpstring` | L3 |  |

## Client

- handler RVA `0x00231840` in `ascension-extensions-2026-08-13`
