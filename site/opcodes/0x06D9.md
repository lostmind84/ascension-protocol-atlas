# `0x06D9` SMSG_PATCH_COLLECTOR_CACHE_TYPES

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x06d9-smsg-patch-collector-cache-types.yaml`](../../protocol/opcodes/0x06d9-smsg-patch-collector-cache-types.yaml)

SMSG_PATCH_COLLECTOR_CACHE_TYPES: writes a 6-field, 24-byte record (id, two numeric fields, three strings) into CollectorCacheTypes.dbc's table object, well short of that table's own 38-field, 152-byte row. No column name is established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0; bounds-checked against the table object's minId/maxId |
| `field_1` | `u32` | L3 | offset 4 |
| `field_2` | `u32` | L3 | offset 8 |
| `field_3` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (1st of three consecutive gaps) |
| `field_4` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (2nd of three) |
| `field_5` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (3rd of three) |
| `field_6` | `cstring` | L3 | 1st of 3, NUL-terminated, scanned byte by byte; not FUN_100d3680 |
| `field_7` | `cstring` | L3 | 2nd of 3 |
| `field_8` | `cstring` | L3 | 3rd of 3 |

## Client

- handler RVA `0x001e1150` in `ascension-extensions-2026-08-13`
