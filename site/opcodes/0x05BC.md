# `0x05BC` SMSG_PATCH_CHALLENGE_REQUIREMENT_TYPE

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05bc-smsg-patch-challenge-requirement-type.yaml`](../../protocol/opcodes/0x05bc-smsg-patch-challenge-requirement-type.yaml)

One row of ChallengeRequirementTypes.dbc, in the compact form the wire uses: the row id, eight skipped string-offset dwords, then eight length-prefixed strings written over them. The runtime record is 0x24 bytes -- an id and eight char pointers -- while the client's own ChallengeRequirementTypes.dbc has 41 fields, so the wire row is a projection of the file, not a copy of it.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L3 | dword 0, the only dword the reader loads; the row key -- range-checked against minId 0x10be0318 / maxId 0x10be0314, indexes the row table at 0x10be0328, then passed to FUN_10133ac0 |
| `stringOffsets` | `bytes_32` | L3 | dwords 1..8, SKIPPED: the cursor jumps from +0x04 to the end of the 0x24 block without loading them. They are the row's eight string-offset columns; the client writes the eight string pointers that follow over record +0x04 .. +0x20. |
| `string_1` | `lpstring` | L3 | u32 length then that many bytes, no NUL; copied and stored at record +0x04 |
| `string_2` | `lpstring` | L3 | record +0x08 |
| `string_3` | `lpstring` | L3 | record +0x0c |
| `string_4` | `lpstring` | L3 | record +0x10 |
| `string_5` | `lpstring` | L3 | record +0x14 |
| `string_6` | `lpstring` | L3 | record +0x18 |
| `string_7` | `lpstring` | L3 | record +0x1c |
| `string_8` | `lpstring` | L3 | record +0x20. The UI reads a requirement type through C_Challenge.GetRequirementLocalization, which returns (name, description, atlas, altAtlas, formatter1, formatter2, formatter3) -- seven values for eight strings (ChallengeUtil.lua:5, L2). Which string is which is NOT established: that getter is not decompiled here. |

## Client

- handler RVA `0x00139eb0` in `ascension-extensions-2026-08-13`
