# `0x065C` SMSG_UPDATE_QUEST_ICON

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x065c-smsg-update-quest-icon.yaml`](../../protocol/opcodes/0x065c-smsg-update-quest-icon.yaml)

Update the quest icon: two u32, then a type and a state that select the .m2 model of the quest-giver icon.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `unknown_0` | `u32` | L3 |  |
| `unknown_1` | `u32` | L3 | the first two u32 are read and kept; the visible switch does not use them |
| `type` | `u32` | L3 | outer switch case (1..) |
| `state` | `u32` | L3 | inner switch case; the pair selects the quest-giver icon model (talktome_new.m2, talktomequestion_new_ltblue.m2, ...) stored in a DLL pointer |

## Client

- handler RVA `0x00a500d0` in `ascension-extensions-2026-08-13`
