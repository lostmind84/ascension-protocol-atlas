# `0x09A6` SMSG_PATCH_TAXI_PATH

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x09a6-smsg-patch-taxi-path.yaml`](../../protocol/opcodes/0x09a6-smsg-patch-taxi-path.yaml)

One row of TaxiPath.dbc: ID, from, to, price -- all four columns kept and named by AzerothCore, no strings. 4 dwords (16B), matching the table exactly.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `ID` | `u32` | L1 | DBC column 0 (n); name 'ID' is AzerothCore's own DBCStructure.h struct member for TaxiPath.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a6.c). |
| `from` | `u32` | L1 | DBC column 1 (i); name 'from' is AzerothCore's own DBCStructure.h struct member for TaxiPath.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a6.c). |
| `to` | `u32` | L1 | DBC column 2 (i); name 'to' is AzerothCore's own DBCStructure.h struct member for TaxiPath.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a6.c). |
| `price` | `u32` | L1 | DBC column 3 (i); name 'price' is AzerothCore's own DBCStructure.h struct member for TaxiPath.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a6.c). |

## Client

- handler RVA `0x00233ab0` in `ascension-extensions-2026-08-13`
