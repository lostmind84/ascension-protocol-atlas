# `0x0660` SMSG_MANASTORM_ACTIVE_DATA

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0660-smsg-manastorm-active-data.yaml`](../../protocol/opcodes/0x0660-smsg-manastorm-active-data.yaml)

Named SMSG_MANASTORM_ACTIVE_DATA by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `depth` | `u32` | L3 | 0 when no run is active |
| `scene` | `u32` | L3 | stage, or endgameStage when mode >= 4 |
| `type` | `cstring` | L3 | Types.at(mode): the mode's name |
| `caches` | `u32` | L3 | server sends 0 |
| `chance` | `f32` | L3 | CacheChance(...) / 100.0f |
| `item` | `u32` | L3 | CacheForLevel(...) |

## Client

- handler RVA `0x002a1e90` in `ascension-extensions-2026-08-13`

## Events

- fires `ACTIVE_MANASTORM_UPDATED` with format `%u%u`
- the UI receives `ACTIVE_MANASTORM_UPDATED` (`client/event-args.yaml`)
- the UI receives `ACTIVE_MANASTORM_UPDATED` (`client/event-args.yaml`)
- the UI receives `MANASTORM_CACHE_INFO_UPDATED` (`client/event-args.yaml`)

## Server

- `modules/mod-ascension-compat/src/AscensionManastormProtocol.h`
