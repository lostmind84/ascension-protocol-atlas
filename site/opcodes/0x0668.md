# `0x0668` SMSG_SET_CVAR

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0668-smsg-set-cvar.yaml`](../../protocol/opcodes/0x0668-smsg-set-cvar.yaml)

Set a CVar: its name, its value and a flag byte.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `name` | `cstring` | L3 | the CVar name |
| `value` | `cstring` | L3 | its value as text |
| `flag` | `u8` | L3 | read by the setter (FUN_10a4f5c0) after the two strings; which CVar flag it carries was not read |

## Client

- handler RVA `0x00a4f5c0` in `ascension-extensions-2026-08-13`
