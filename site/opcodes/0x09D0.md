# `0x09D0` SMSG_CHARACTER_CUSTOMIZATION_UNLOCKS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x09d0-smsg-character-customization-unlocks.yaml`](../../protocol/opcodes/0x09d0-smsg-character-customization-unlocks.yaml)

Character customization unlocks: a u32 the client never reads. The handler clears its unlock container and ignores the payload.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `unknown_0` | `u32` | L3 | Four bytes on the wire, always zero in the capture, and the handler does not read them: it clears the client's customization-unlock container and returns. So the packet has a payload the client ignores.
 |

## Client

- handler RVA `0x0032e300` in `ascension-extensions-2026-08-13`
