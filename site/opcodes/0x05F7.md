# `0x05F7` SMSG_REALM_MERGE_SET_TARGET_REALM_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05f7-smsg-realm-merge-set-target-realm-result.yaml`](../../protocol/opcodes/0x05f7-smsg-realm-merge-set-target-realm-result.yaml)

One length-prefixed string the handler hands to REALM_MERGE_SET_TARGET_RESULT with format %s -- the only read and the only argument; named 'result' after the UI's parameter (default name: no Lua parameter declared).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `lpstring` | L3 |  |

## Client

- handler RVA `0x002fd410` in `ascension-extensions-2026-08-13`

## Events

- fires `REALM_MERGE_SET_TARGET_RESULT` with format `%s`
