# `0x06A4` SMSG_PATCH_TUTORIAL_CATEGORIES

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x06a4-smsg-patch-tutorial-categories.yaml`](../../protocol/opcodes/0x06a4-smsg-patch-tutorial-categories.yaml)

SMSG_PATCH_TUTORIAL_CATEGORIES: writes a 2-field, 8-byte record (id, one string) into TutorialCategories.dbc's table object -- just an id and a label, far short of that table's own 18-field, 72-byte row. No column name is established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0; bounds-checked against the table object's minId/maxId |
| `field_1` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further |
| `field_2` | `cstring` | L3 | NUL-terminated, scanned byte by byte; not FUN_100d3680 |

## Client

- handler RVA `0x001e4360` in `ascension-extensions-2026-08-13`
