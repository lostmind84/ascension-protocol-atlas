# `0x0649` SMSG_SKILL_CARD_COLLECTED

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0649-smsg-skill-card-collected.yaml`](../../protocol/opcodes/0x0649-smsg-skill-card-collected.yaml)

Skill cards collected: a count, then 9-byte records keyed by two u32; each fires SKILL_CARD_COLLECTED or SKILL_CARD_AUTO_REVEALED(cardID, rank) from the looked-up card.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 |  |
| *repeat × count* | | | |
| &nbsp;&nbsp;`unknown_0` | `u32` | L3 | with the next u32 forms the FNV-1a key of the card looked up in the service's map; the event's cardID and rank come from that record |
| &nbsp;&nbsp;`unknown_1` | `u32` | L3 |  |
| &nbsp;&nbsp;`flag` | `u8` | L3 | chooses between SKILL_CARD_COLLECTED and SKILL_CARD_AUTO_REVEALED by the branch structure; not verified |

## Client

- handler RVA `0x0031b050` in `ascension-extensions-2026-08-13`

## Events

- fires `SKILL_CARD_AUTO_REVEALED` with format `%u%u`
- fires `SKILL_CARD_COLLECTED` with format `%u%u`
- the UI receives `SKILL_CARD_AUTO_REVEALED` (`client/event-args.yaml`)
- the UI receives `SKILL_CARD_AUTO_REVEALED` (`client/event-args.yaml`)
- the UI receives `SKILL_CARD_AUTO_REVEALED` (`client/event-args.yaml`)
