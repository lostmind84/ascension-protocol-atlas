# `0x0931` SMSG_PATCH_SPELL_MISSILE_MOTION

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0931-smsg-patch-spell-missile-motion.yaml`](../../protocol/opcodes/0x0931-smsg-patch-spell-missile-motion.yaml)

Named SMSG_PATCH_SPELL_MISSILE_MOTION by the client's own opcode table. The opcode is not bound in patch-tables.yaml -- the DLL's own custom-table list does not mention this opcode; SpellMissileMotion.dbc is the table patch-table-names.md associates with it by name and geometry (grade L1). The handler's own reads match SpellMissileMotion.dbc's own WDBC header exactly (5 fields, 20 bytes), so this looks like a full, unmodified row of that table. No source names any of the table's columns.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 |  |
| `field_1` | `lpstring` | L3 |  |
| `field_2` | `lpstring` | L3 |  |
| `field_3` | `u32` | L3 |  |
| `field_4` | `u32` | L3 |  |

## Client

- handler RVA `0x00232920` in `ascension-extensions-2026-08-13`
