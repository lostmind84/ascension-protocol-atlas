# `0x06A9` SMSG_PATCH_CHARACTER_ADVANCEMENT_CATEGORIES

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x06a9-smsg-patch-character-advancement-categories.yaml`](../../protocol/opcodes/0x06a9-smsg-patch-character-advancement-categories.yaml)

SMSG_PATCH_CHARACTER_ADVANCEMENT_CATEGORIES: one packet that writes a row into TWO tables, CharacterAdvancementCategories.dbc and CharacterAdvancementClassTypes.dbc. Both rows are reduced: 7 of 39 fields for the first table, 7 of 23 for the second. The second record is read behind the same apparent stack-cookie branch (`swi(0x29)`) as the shared 0x0567/0x0568 handler -- see that fiche's note; read here as real, always-executed code for the same reasons, not confirmed at the assembly level. No column name is established for either table.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | record 1 (CharacterAdvancementCategories.dbc side), offset 0; bounds-checked against that table object's minId/maxId |
| `field_1` | `u32` | L3 | offset 4, first half of an 8-byte load |
| `field_2` | `u32` | L3 | offset 8, second half; read as one 8-byte load in the handler; split into two 4-byte fields here to line up with the table's own field count -- could be a single 64-bit value instead, not resolved further |
| `field_3` | `u32` | L3 | offset 0xc |
| `field_4` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (1st of three consecutive gaps) |
| `field_5` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (2nd of three) |
| `field_6` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further (3rd of three) |
| `field_7` | `cstring` | L3 | 1st of 3, NUL-terminated, scanned byte by byte; not FUN_100d3680 |
| `field_8` | `cstring` | L3 | 2nd of 3 |
| `field_9` | `cstring` | L3 | 3rd of 3; end of record 1 (7 fields, matches the struct FUN_10ae5d20(0x1c) allocates) |
| `field_10` | `u32` | L3 | record 2 (CharacterAdvancementClassTypes.dbc side) starts here, offset 0 of record 2; bounds-checked against that table object's minId/maxId at 0x10bdfa98 |
| `field_11` | `cstring` | L3 | immediately after the id, NUL-terminated, scanned byte by byte |
| `field_12` | `u32` | L3 | first u32 after the string |
| `field_13` | `u32` | L3 | second u32 after the string |
| `field_14` | `u32` | L3 | third u32 after the string |
| `field_15` | `u32` | L3 | fourth u32 after the string |
| `field_16` | `cstring` | L3 | 2nd string of record 2, NUL-terminated, scanned byte by byte; end of record 2 (7 fields) |

## Client

- handler RVA `0x001dd0b0` in `ascension-extensions-2026-08-13`
