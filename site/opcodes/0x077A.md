# `0x077A` CMSG_CHARACTER_ADVANCEMENT_LOADOUT_SET_SORT_ORDER_REQUEST

**client to server** · status `hypothetical` · evidence `L3` · layout `named` · client code `sender`

Fiche: [`protocol/opcodes/0x077a-cmsg-character-advancement-loadout-set-sort-order-request.yaml`](../../protocol/opcodes/0x077a-cmsg-character-advancement-loadout-set-sort-order-request.yaml)

Set a talent loadout's sort order: the binding SetLoadoutSortOrder(uuid, sortOrder) is called with two arguments (TalentLoadoutUtil.lua) but the sender writes one u32; which argument it is, and where the uuid goes, is not established from the binding's body.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `unknown_0` | `u32` | L3 | SetLoadoutSortOrder(uuid, sortOrder) writes one u32; not established which |

## Client

- sender `0x0017ed8e`
