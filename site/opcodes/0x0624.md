# `0x0624` SMSG_UPDATE_SKILL_CARDS

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0624-smsg-update-skill-cards.yaml`](../../protocol/opcodes/0x0624-smsg-update-skill-cards.yaml)

Skill cards, whole update: nested lists ending in 9-byte card records. Partially followed.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 |  |
| *repeat × count* | | | |
| &nbsp;&nbsp;`group` | `bytes_variable` | L3 | three nesting levels: per entry a count read outside the visible idiom, then per group a u32 count and that many 9-byte records (eight bytes and one byte), stored into the skill-card service. Only the outer count and the innermost records were followed |

## Client

- handler RVA `0x002f4ab0` in `ascension-extensions-2026-08-13`
