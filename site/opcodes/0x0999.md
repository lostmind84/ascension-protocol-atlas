# `0x0999` SMSG_PATCH_AREA_POI

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0999-smsg-patch-area-poi.yaml`](../../protocol/opcodes/0x0999-smsg-patch-area-poi.yaml)

One row of AreaPOI.dbc, collapsed: id, icon[0..10], x, y, z, mapId, an unnamed dword (val1), zoneId, then TWO strings (the table's two separate 16-slot locale blocks, `name` and `name2` -- both commented out in DBCStructure.h so left unnamed, each collapsed to its primary locale with its own flags dword dropped entirely), then worldState and an unnamed trailing dword (val2). 22 wire dwords (88B) plus two strings, against the table's own 54 columns/216 bytes -- confirmed field-by-field from the decompiled handler's read/skip offsets.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0 (n); name 'id' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `icon_0` | `u32` | L1 | DBC column 1 (i); name 'icon_0' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `icon_1` | `u32` | L1 | DBC column 2 (i); name 'icon_1' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `icon_2` | `u32` | L1 | DBC column 3 (i); name 'icon_2' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `icon_3` | `u32` | L1 | DBC column 4 (i); name 'icon_3' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `icon_4` | `u32` | L1 | DBC column 5 (i); name 'icon_4' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `icon_5` | `u32` | L1 | DBC column 6 (i); name 'icon_5' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `icon_6` | `u32` | L1 | DBC column 7 (i); name 'icon_6' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `icon_7` | `u32` | L1 | DBC column 8 (i); name 'icon_7' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `icon_8` | `u32` | L1 | DBC column 9 (i); name 'icon_8' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `icon_9` | `u32` | L1 | DBC column 10 (i); name 'icon_9' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `icon_10` | `u32` | L1 | DBC column 11 (i); name 'icon_10' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `x` | `f32` | L1 | DBC column 12 (f); name 'x' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `y` | `f32` | L1 | DBC column 13 (f); name 'y' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `z` | `f32` | L1 | DBC column 14 (f); name 'z' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `mapId` | `u32` | L1 | DBC column 15 (i); name 'mapId' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `unknown_16` | `u32` | L3 | DBC column 16 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/0999.c). DBCStructure.h's comment calls this val1. |
| `zoneId` | `u32` | L1 | DBC column 17 (i); name 'zoneId' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `unknown_name_locale` | `lpstring` | L3 | DBC columns 18-33 (16 string-offset columns, 's') plus a flags dword at column 34; DBCStructure.h comments out `name[16]` (not a compiled struct member), so unnamed. On the wire the whole 17-column locale block collapses to ONE length-prefixed string at this position; the other 15 locale slots and the flags dword are not present on the wire at all -- confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `unknown_name2_locale` | `lpstring` | L3 | DBC columns 35-50 (16 string-offset columns, 's') plus a flags dword at column 51; DBCStructure.h comments out `name2[16]` (not a compiled struct member), so unnamed. On the wire the whole 17-column locale block collapses to ONE length-prefixed string at this position; the other 15 locale slots and the flags dword are not present on the wire at all -- confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `worldState` | `u32` | L1 | DBC column 52 (i); name 'worldState' is AzerothCore's own DBCStructure.h struct member for AreaPOI.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0999.c). |
| `unknown_53` | `u32` | L3 | DBC column 53 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/0999.c). DBCStructure.h's comment calls this val2. |

## Client

- handler RVA `0x0022a330` in `ascension-extensions-2026-08-13`
