# `0x093D` SMSG_PATCH_CREATURE_TYPE

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x093d-smsg-patch-creature-type.yaml`](../../protocol/opcodes/0x093d-smsg-patch-creature-type.yaml)

One row of the stock CreatureType.dbc (AzerothCore already loads this table; ADR 0005), read by the handler at RVA 0x0022b9e0 as 2 dwords then 1 lpstring. This is the rare case in this shard where the wire read reconciles exactly with the file: CreatureType.dbc has 19 columns (client/patch-table-names.md) -- ID (active), the 17-column Name[16]+flag locale block (fully commented out, format char 'x' not 's'), and no_expirience (commented out, "no exp? critters, non-combat pets, gas cloud."). 2 numeric + 1 string is exactly the file's 2 non-string columns plus its 1 (disabled) locale block, with nothing left over. ID is named from DBCStructure.h's only active member (L1). no_expirience stays an unnamed field per the 'x' rule even though the core's own comment names it (by elimination it is field_1, the only other real column in the file). The lpstring is named Name: even though it is core 'x'-marked (disabled), it is the only string-type candidate in the whole record, so the identification carries no order ambiguity (L1).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `ID` | `u32` | L1 | active CreatureTypeEntry member (column 0) |
| `field_1` | `u32` | L1 | core format char 'x': skipped, no formal name. By elimination (the file's only other real column) this is CreatureTypeEntry's commented-out no_expirience (column 18): "no exp? critters, non-combat pets, gas cloud." Not asserted as the field's name per the 'x' convention (client/patch-table-names.md bucket 3). |
| `Name` | `lpstring` | L1 | the file's only string-type column (Name[16], columns 1-16, plus its flag at 17; core 'x'-marked/ disabled, CreatureTypeEntry comments it "//char const* Name[16]"). The only candidate in the record, so this identification carries no order ambiguity. |

## Client

- handler RVA `0x0022b9e0` in `ascension-extensions-2026-08-13`
