# `0x09AA` SMSG_PATCH_WORLD_MAP_AREA

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x09aa-smsg-patch-world-map-area.yaml`](../../protocol/opcodes/0x09aa-smsg-patch-world-map-area.yaml)

One row of WorldMapArea.dbc: an unnamed dword at column 0 (DBCStructure.h comments it ID, but the format character is 'x', not the sort/index type -- area_id at column 2 is the table's real index), map_id, area_id, then one skipped-and-appended string (internal_name, unnamed -- commented out), y1, y2, x1, x2, virtual_map_id, and two unnamed trailing dwords (dungeonMap_id, parentMapID). 11 dwords (44B) plus one string, matching the table exactly.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `unknown_0` | `u32` | L3 | DBC column 0 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09aa.c). DBCStructure.h's comment calls this ID; format char is 'x' (unused), not the sort/index field -- area_id at column 2 is the real index here. |
| `map_id` | `u32` | L1 | DBC column 1 (i); name 'map_id' is AzerothCore's own DBCStructure.h struct member for WorldMapArea.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09aa.c). |
| `area_id` | `u32` | L1 | DBC column 2 (n); name 'area_id' is AzerothCore's own DBCStructure.h struct member for WorldMapArea.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09aa.c). |
| `unknown_3_str` | `lpstring` | L3 | DBC column 3 (string offset on disk, 's'); the wire sends the VALUE inline as a length-prefixed string (lpstring = FUN_100d3680: u32 length then that many bytes, no NUL), not the 4-byte offset the .dbc file itself stores. DBCStructure.h does not keep a named member for this column, so unnamed; the lpstring's presence, order and position are confirmed from the decompiled handler (build/ghidra/handlers-c/09aa.c). DBCStructure.h's comment for this column reads 'internal_name' but the line is commented out (not compiled), so that text is not used as the field name. |
| `y1` | `f32` | L1 | DBC column 4 (f); name 'y1' is AzerothCore's own DBCStructure.h struct member for WorldMapArea.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09aa.c). |
| `y2` | `f32` | L1 | DBC column 5 (f); name 'y2' is AzerothCore's own DBCStructure.h struct member for WorldMapArea.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09aa.c). |
| `x1` | `f32` | L1 | DBC column 6 (f); name 'x1' is AzerothCore's own DBCStructure.h struct member for WorldMapArea.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09aa.c). |
| `x2` | `f32` | L1 | DBC column 7 (f); name 'x2' is AzerothCore's own DBCStructure.h struct member for WorldMapArea.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09aa.c). |
| `virtual_map_id` | `u32` | L1 | DBC column 8 (i); name 'virtual_map_id' is AzerothCore's own DBCStructure.h struct member for WorldMapArea.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09aa.c). |
| `unknown_9` | `u32` | L3 | DBC column 9 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09aa.c). DBCStructure.h's comment calls this dungeonMap_id. |
| `unknown_10` | `u32` | L3 | DBC column 10 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09aa.c). DBCStructure.h's comment calls this parentMapID. |

## Client

- handler RVA `0x00234ab0` in `ascension-extensions-2026-08-13`
