# `0x0654` SMSG_PENDING_SKILL_CARD_ADDED

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0654-smsg-pending-skill-card-added.yaml`](../../protocol/opcodes/0x0654-smsg-pending-skill-card-added.yaml)

Skill cards pending unlock: a count, then (cardID, rank) pairs; for each the handler fires PENDING_SKILL_CARD_ADDED(pendingIndex, cardID, rank) with format %u%u%u, the last two being the record's reads (proven positions) and the index the loop's.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 |  |
| *repeat × count* | | | |
| &nbsp;&nbsp;`cardId` | `u32` | L3 |  |
| &nbsp;&nbsp;`rank` | `u32` | L3 |  |

## Client

- handler RVA `0x0031a290` in `ascension-extensions-2026-08-13`

## Events

- fires `PENDING_SKILL_CARD_ADDED` with format `%u%u%u`
- the UI receives `PENDING_SKILL_CARD_ADDED` (`client/event-args.yaml`)
- the UI receives `PENDING_SKILL_CARD_ADDED` (`client/event-args.yaml`)
