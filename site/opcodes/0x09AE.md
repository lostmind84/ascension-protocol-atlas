# `0x09AE` SMSG_PATCH_TRANSPORT_ANIMATION

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x09ae-smsg-patch-transport-animation.yaml`](../../protocol/opcodes/0x09ae-smsg-patch-transport-animation.yaml)

One row of TransportAnimation.dbc: an id (FT_SORT index column, conventional name), TransportEntry, TimeSeg, X, Y, Z -- all named -- then one unnamed trailing dword (MovementId, commented out). No strings. 7 dwords (28B), matching the table exactly.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0, format char 'd' (FT_SORT/FT_IND, the record's own index field per src/common/DataStores/DBCFileLoader.h); DBCStructure.h comments this column out (not a compiled struct member) but the format character itself conventionally marks it as the row's id, so named 'id' at L1; width/position confirmed from the decompiled handler (build/ghidra/handlers-c/09ae.c). |
| `TransportEntry` | `u32` | L1 | DBC column 1 (i); name 'TransportEntry' is AzerothCore's own DBCStructure.h struct member for TransportAnimation.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09ae.c). |
| `TimeSeg` | `u32` | L1 | DBC column 2 (i); name 'TimeSeg' is AzerothCore's own DBCStructure.h struct member for TransportAnimation.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09ae.c). |
| `X` | `f32` | L1 | DBC column 3 (f); name 'X' is AzerothCore's own DBCStructure.h struct member for TransportAnimation.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09ae.c). |
| `Y` | `f32` | L1 | DBC column 4 (f); name 'Y' is AzerothCore's own DBCStructure.h struct member for TransportAnimation.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09ae.c). |
| `Z` | `f32` | L1 | DBC column 5 (f); name 'Z' is AzerothCore's own DBCStructure.h struct member for TransportAnimation.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09ae.c). |
| `unknown_6` | `u32` | L3 | DBC column 6 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/09ae.c). DBCStructure.h's comment names it MovementId but the line is commented out. |

## Client

- handler RVA `0x00233cc0` in `ascension-extensions-2026-08-13`
