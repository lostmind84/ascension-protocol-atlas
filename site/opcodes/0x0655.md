# `0x0655` SMSG_PENDING_SKILL_CARD_REMOVED

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0655-smsg-pending-skill-card-removed.yaml`](../../protocol/opcodes/0x0655-smsg-pending-skill-card-removed.yaml)

Pending skill cards removed: a count and one name per removed card; the event's numbers come from the matched pending record, not the packet. Whether the record carries more than the name was not followed.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 |  |
| *repeat × count* | | | |
| &nbsp;&nbsp;`name` | `cstring` | L3 | matched against the pending entries' names; the match fires PENDING_SKILL_CARD_REMOVED(%u%u%u) with values from the matched record (pendingIndex, cardID, ...) |

## Client

- handler RVA `0x0031a850` in `ascension-extensions-2026-08-13`

## Events

- fires `PENDING_SKILL_CARD_REMOVED` with format `%u%u%u`
- the UI receives `PENDING_SKILL_CARD_REMOVED` (`client/event-args.yaml`)
- the UI receives `PENDING_SKILL_CARD_REMOVED` (`client/event-args.yaml`)
