# `0x0675` SMSG_GROUP_CONNECTION_UPDATE

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0675-smsg-group-connection-update.yaml`](../../protocol/opcodes/0x0675-smsg-group-connection-update.yaml)

A group member's connection state: a u64 guid as two u32 and a byte. The skeleton's extra fields and string were the unit token built locally.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `guidLow` | `u32` | L3 |  |
| `guidHigh` | `u32` | L3 | compared with the active player's guid (exe 0x004d3790 by its use); the player's own guid fires GROUP_JOINED/GROUP_LEFT, another's UNIT_CONNECTION(unitToken, connected) |
| `connected` | `u8` | L3 | the %b of UNIT_CONNECTION |

## Client

- handler RVA `0x00a4f0c0` in `ascension-extensions-2026-08-13`

## Events

- fires `GROUP_LEFT`
- fires `GROUP_JOINED`
- fires `UNIT_CONNECTION` with format `%s%b`
