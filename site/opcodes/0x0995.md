# `0x0995` SMSG_PATCH_BATTLEMASTER_LIST

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0995-smsg-patch-battlemaster-list.yaml`](../../protocol/opcodes/0x0995-smsg-patch-battlemaster-list.yaml)

One row of BattlemasterList.dbc, in the collapsed form the wire actually sends: id, mapid[0..7], type, an unnamed dword (canJoinAsGroup), then ONE string (the kept, named `name[16]` locale block, collapsed to its primary locale -- the other 15 locale slots and the nameFlags dword are not on the wire at all, confirmed from the decompiled handler), then maxGroupSize, HolidayWorldStateId, and two unnamed trailing dwords (MinLevel, SomeLevel). 16 wire dwords (64B) plus one string -- half the table's own 32-column/128-byte record, because the whole 17-column locale block plus its flag collapse to one wire slot and the flag itself is dropped. Table binding is L1 (third-party reference only).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0 (n); name 'id' is AzerothCore's own DBCStructure.h struct member for BattlemasterList.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0995.c). |
| `mapid_0` | `u32` | L1 | DBC column 1 (i); name 'mapid_0' is AzerothCore's own DBCStructure.h struct member for BattlemasterList.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0995.c). |
| `mapid_1` | `u32` | L1 | DBC column 2 (i); name 'mapid_1' is AzerothCore's own DBCStructure.h struct member for BattlemasterList.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0995.c). |
| `mapid_2` | `u32` | L1 | DBC column 3 (i); name 'mapid_2' is AzerothCore's own DBCStructure.h struct member for BattlemasterList.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0995.c). |
| `mapid_3` | `u32` | L1 | DBC column 4 (i); name 'mapid_3' is AzerothCore's own DBCStructure.h struct member for BattlemasterList.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0995.c). |
| `mapid_4` | `u32` | L1 | DBC column 5 (i); name 'mapid_4' is AzerothCore's own DBCStructure.h struct member for BattlemasterList.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0995.c). |
| `mapid_5` | `u32` | L1 | DBC column 6 (i); name 'mapid_5' is AzerothCore's own DBCStructure.h struct member for BattlemasterList.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0995.c). |
| `mapid_6` | `u32` | L1 | DBC column 7 (i); name 'mapid_6' is AzerothCore's own DBCStructure.h struct member for BattlemasterList.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0995.c). |
| `mapid_7` | `u32` | L1 | DBC column 8 (i); name 'mapid_7' is AzerothCore's own DBCStructure.h struct member for BattlemasterList.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0995.c). |
| `type` | `u32` | L1 | DBC column 9 (i); name 'type' is AzerothCore's own DBCStructure.h struct member for BattlemasterList.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0995.c). |
| `unknown_10` | `u32` | L3 | DBC column 10 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/0995.c). DBCStructure.h's comment calls this canJoinAsGroup (0 or 1). |
| `name` | `lpstring` | L3 | DBC columns 11-26 (16 string-offset columns, 's') plus a flags dword at column 27 form AzerothCore's kept struct member `name[16]` (L1). On the wire the whole 17-column locale block collapses to ONE length-prefixed string (the primary locale's text) at this position; the other 15 locale slots and the flags dword (column 27) are not present on the wire at all -- confirmed from the decompiled handler (build/ghidra/handlers-c/0995.c): the read cursor skips straight from the end of the fixed block to the lpstring with no placeholder dwords for this block. |
| `maxGroupSize` | `u32` | L1 | DBC column 28 (i); name 'maxGroupSize' is AzerothCore's own DBCStructure.h struct member for BattlemasterList.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0995.c). |
| `HolidayWorldStateId` | `u32` | L1 | DBC column 29 (i); name 'HolidayWorldStateId' is AzerothCore's own DBCStructure.h struct member for BattlemasterList.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/0995.c). |
| `unknown_30` | `u32` | L3 | DBC column 30 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/0995.c). DBCStructure.h's comment calls this MinLevel. |
| `unknown_31` | `u32` | L3 | DBC column 31 (x); DBCfmt.h/DBCStructure.h in azerothcore-wotlk-coa does not keep this column as a named struct member, so no name is asserted here (unnamed placeholder); width/position from our own read of the decompiled handler (build/ghidra/handlers-c/0995.c). DBCStructure.h's comment calls this SomeLevel, 'may be max level'. |

## Client

- handler RVA `0x0022a830` in `ascension-extensions-2026-08-13`
