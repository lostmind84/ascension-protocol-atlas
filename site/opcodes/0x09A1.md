# `0x09A1` SMSG_PATCH_QUEST_XP

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x09a1-smsg-patch-quest-xp.yaml`](../../protocol/opcodes/0x09a1-smsg-patch-quest-xp.yaml)

One row of QuestXP.dbc: id, Exp[0..9] -- all eleven columns kept and named by AzerothCore, no strings. 11 dwords (44B), matching the table exactly.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0 (n); name 'id' is AzerothCore's own DBCStructure.h struct member for QuestXP.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a1.c). |
| `Exp_0` | `u32` | L1 | DBC column 1 (i); name 'Exp_0' is AzerothCore's own DBCStructure.h struct member for QuestXP.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a1.c). |
| `Exp_1` | `u32` | L1 | DBC column 2 (i); name 'Exp_1' is AzerothCore's own DBCStructure.h struct member for QuestXP.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a1.c). |
| `Exp_2` | `u32` | L1 | DBC column 3 (i); name 'Exp_2' is AzerothCore's own DBCStructure.h struct member for QuestXP.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a1.c). |
| `Exp_3` | `u32` | L1 | DBC column 4 (i); name 'Exp_3' is AzerothCore's own DBCStructure.h struct member for QuestXP.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a1.c). |
| `Exp_4` | `u32` | L1 | DBC column 5 (i); name 'Exp_4' is AzerothCore's own DBCStructure.h struct member for QuestXP.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a1.c). |
| `Exp_5` | `u32` | L1 | DBC column 6 (i); name 'Exp_5' is AzerothCore's own DBCStructure.h struct member for QuestXP.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a1.c). |
| `Exp_6` | `u32` | L1 | DBC column 7 (i); name 'Exp_6' is AzerothCore's own DBCStructure.h struct member for QuestXP.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a1.c). |
| `Exp_7` | `u32` | L1 | DBC column 8 (i); name 'Exp_7' is AzerothCore's own DBCStructure.h struct member for QuestXP.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a1.c). |
| `Exp_8` | `u32` | L1 | DBC column 9 (i); name 'Exp_8' is AzerothCore's own DBCStructure.h struct member for QuestXP.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a1.c). |
| `Exp_9` | `u32` | L1 | DBC column 10 (i); name 'Exp_9' is AzerothCore's own DBCStructure.h struct member for QuestXP.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a1.c). |

## Client

- handler RVA `0x0022fab0` in `ascension-extensions-2026-08-13`
