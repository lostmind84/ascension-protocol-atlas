# `0x056B` SMSG_PATCH_MYSTIC_ENCHANT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x056b-smsg-patch-mystic-enchant.yaml`](../../protocol/opcodes/0x056b-smsg-patch-mystic-enchant.yaml)

Named SMSG_PATCH_MYSTIC_ENCHANT by the client's own opcode table. Layout and behaviour not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_124` | L3 | one row of MysticEnchant.dbc (31 fields of 4 bytes), as the client's table stores it; the strings that follow are the row's string columns, inline |

## Client

- handler RVA `0x002e8990` in `ascension-extensions-2026-08-13`

## Events

- fires `MYSTIC_ENCHANT_PATCHED`
