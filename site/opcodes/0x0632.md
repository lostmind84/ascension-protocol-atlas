# `0x0632` SMSG_BUILD_CREATOR_ACTIVE_BUILD

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0632-smsg-build-creator-active-build.yaml`](../../protocol/opcodes/0x0632-smsg-build-creator-active-build.yaml)

The active build changed: one u32 the handler passes to BUILD_CREATOR_ACTIVE_BUILD_UPDATED with format %u (the build id, by the opcode's name and the argument's use; the event's Lua parameters are not declared in the corpus).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `buildId` | `u32` | L3 | named after the opcode and the fire argument |

## Client

- handler RVA `0x000fdfd0` in `ascension-extensions-2026-08-13`

## Events

- fires `BUILD_CREATOR_ACTIVE_BUILD_UPDAT` with format `%u`
