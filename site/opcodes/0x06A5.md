# `0x06A5` SMSG_PATCH_TUTORIAL

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06a5-smsg-patch-tutorial.yaml`](../../protocol/opcodes/0x06a5-smsg-patch-tutorial.yaml)

One Tutorial.dbc row with its strings sent inline. Partially followed.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `row` | `bytes_variable` | L3 | a Tutorial.dbc row (92 columns) as the DLL keeps it: after a first value, five NUL-scanned strings copied into the record; the numeric columns before and between them were not followed |

## Client

- handler RVA `0x001e4ea0` in `ascension-extensions-2026-08-13`
