# `0x0706` SMSG_CLOSE_GM_TICKET_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0706-smsg-close-gm-ticket-result.yaml`](../../protocol/opcodes/0x0706-smsg-close-gm-ticket-result.yaml)

Result of closing a ticket: one result string. The skeleton's two strings were the same read on the handler's two branches (GM view vs player view), which fire CLOSE_GM_TICKET_RESULT or CLOSE_PLAYER_TICKET_RESULT.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 | one string; the handler reads it on both of its branches -- the GM branch fires CLOSE_GM_TICKET_RESULT(result), the player branch CLOSE_PLAYER_TICKET_RESULT(result), chosen by a flag of the ticket service object (+8), not by the packet |

## Client

- handler RVA `0x00253800` in `ascension-extensions-2026-08-13`

## Events

- fires `CLOSE_GM_TICKET_RESULT` with format `%s`
- fires `CLOSE_PLAYER_TICKET_RESULT` with format `%s`
- the UI receives `CLOSE_PLAYER_TICKET_RESULT` (`client/event-args.yaml`)
- the UI receives `CLOSE_PLAYER_TICKET_RESULT` (`client/event-args.yaml`)
