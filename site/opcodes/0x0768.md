# `0x0768` SMSG_HONORABLE_COMBAT_ZONES

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0768-smsg-honorable-combat-zones.yaml`](../../protocol/opcodes/0x0768-smsg-honorable-combat-zones.yaml)

The honorable combat zones: a count and records with a string. Partially followed.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 |  |
| *repeat × count* | | | |
| &nbsp;&nbsp;`zone` | `bytes_variable` | L3 | a record with a u32 and a NUL-terminated string per the skeleton; not followed. HONORABLE_COMBAT_ZONES_PAYLOAD afterwards |

## Client

- handler RVA `0x000d3b30` in `ascension-extensions-2026-08-13`

## Events

- fires `HONORABLE_COMBAT_ZONES_PAYLOAD`

## Server

- `modules/mod-ascension-compat/src/AscensionCharacterSelection.cpp`
