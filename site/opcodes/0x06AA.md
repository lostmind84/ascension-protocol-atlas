# `0x06AA` SMSG_HAND_OF_FATE_REWARDS_LIST

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x06aa-smsg-hand-of-fate-rewards-list.yaml`](../../protocol/opcodes/0x06aa-smsg-hand-of-fate-rewards-list.yaml)

The Hand of Fate rewards list, and the only compressed packet found so far: a u32 uncompressed size then a deflate stream the client inflates with zlib before parsing. The inflated content was not decoded.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `uncompressedSize` | `u32` | L3 | the size the payload inflates to; the reader allocates that much |
| `deflateStream` | `bytes_variable` | L3 | the rest of the packet, inflated by the exe's zlib uncompress (0x00778180) before anything is parsed. The inflated bytes are the rewards list proper, whose records were not read here |

## Client

- handler RVA `0x00226ff0` in `ascension-extensions-2026-08-13`
