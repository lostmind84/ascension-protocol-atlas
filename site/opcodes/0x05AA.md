# `0x05AA` SMSG_CHALLENGE_CREATOR_DELETE_CHALLENGE_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05aa-smsg-challenge-creator-delete-challenge-result.yaml`](../../protocol/opcodes/0x05aa-smsg-challenge-creator-delete-challenge-result.yaml)

The CoA server calls it `SMSG_COA_TRIAL_DELETE_RESULT`.

Answer to the trial-creator delete request: two length-prefixed strings the handler reads with FUN_100d3680 and hands to TRIAL_DELETE_RESULT(trialID, response), format %s%s. Names from the server's writer and the UI's arguments. The server then writes a count and a list of u32 the handler does not read in its own body; not established further.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `trialId` | `lpstring` | L3 |  |
| `response` | `lpstring` | L3 |  |

## Client

- handler RVA `0x00122c00` in `ascension-extensions-2026-08-13`

## Events

- fires `TRIAL_DELETE_RESULT` with format `%s%s`

## Server

- `modules/mod-coa-challenges/src/CoA.Challenges.History.cpp`
