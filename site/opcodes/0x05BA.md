# `0x05BA` SMSG_LIST_BROKEN_CHALLENGE_RULES

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05ba-smsg-list-broken-challenge-rules.yaml`](../../protocol/opcodes/0x05ba-smsg-list-broken-challenge-rules.yaml)

The rules currently broken in the player's challenge, sent whole: a count then one length-prefixed rule-type token per entry; the handler rebuilds its list.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `count` | `u32` | L3 |  |
| *repeat × count* | | | |
| &nbsp;&nbsp;`rule` | `lpstring` | L3 | u32 length then that many bytes, no NUL. A CHALLENGE_RULES_TYPE_* token, the same vocabulary SMSG_PATCH_CHALLENGE_RULE 0x05B8 carries in its string column and C_Challenge.GetRuleLocalization / HasBrokenChallengeRule resolve (ChallengeUtil.lua and Ascension_ChallengesUI, L2). The capture reference reports a live login packet of 2,107 bytes carrying count = 47 and lists those 47 tokens; that is L1 for us and is not reproduced here. Our own evidence gives the shape, not the vocabulary's size.
 |

## Client

- handler RVA `0x00138170` in `ascension-extensions-2026-08-13`
