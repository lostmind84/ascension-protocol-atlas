# `0x05B3` SMSG_PATCH_CHALLENGE_FEATURED

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x05b3-smsg-patch-challenge-featured.yaml`](../../protocol/opcodes/0x05b3-smsg-patch-challenge-featured.yaml)

One row of ChallengeFeatured.dbc: five dwords and no string. The runtime record (0x14 bytes) is a byte-for-byte copy of the wire block. The skeleton's three u32 stopped short -- the reader loads four dwords in one block plus a fifth. Dwords 2, 3 and 4 are each fed to the per-challenge notify path, so the row points at up to three challenges.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L3 | dword 0; the row key -- range-checked against minId 0x10be021c / maxId 0x10be0218, indexes the row table at 0x10be022c, then passed to FUN_10133ac0 |
| `unknown_1` | `u32` | L3 | dword 1; record +0x04. Not named. |
| `unknown_2` | `u32` | L3 | dword 2; record +0x08. After the insert the handler walks dwords 2, 3 and 4 in a three-iteration loop and, for each non-zero value, runs the notify path (FUN_1013b8b0 then FUN_100996e0 with that value) and stamps a +2s refresh deadline -- the same shape the single-challenge tables use with their dword 1. Read as three challenge ids; inference from the loop, not a read symbol. |
| `unknown_3` | `u32` | L3 | dword 3; record +0x0c, second iteration of that loop |
| `unknown_4` | `u32` | L3 | dword 4; record +0x10, third iteration |

## Client

- handler RVA `0x00138d00` in `ascension-extensions-2026-08-13`
