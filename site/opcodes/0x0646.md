# `0x0646` SMSG_WILDCARD_RESET_ABILITIES_RESULT

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0646-smsg-wildcard-reset-abilities-result.yaml`](../../protocol/opcodes/0x0646-smsg-wildcard-reset-abilities-result.yaml)

One NUL-terminated string the handler hands to WILDCARD_RESET_ABILITIES_RESULT with format %s -- the only read and the only argument; named 'result' after the UI's parameter (default name: no Lua parameter declared).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `result` | `cstring` | L3 |  |

## Client

- handler RVA `0x00a30a50` in `ascension-extensions-2026-08-13`

## Events

- fires `WILDCARD_RESET_ABILITIES_RESULT` with format `%s`
