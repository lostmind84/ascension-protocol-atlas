# `0x0676` SMSG_PATCH_GLOBAL_STRINGS

**server to client** · status `hypothetical` · evidence `L3` · layout `widths` · client code `handler`

Fiche: [`protocol/opcodes/0x0676-smsg-patch-global-strings.yaml`](../../protocol/opcodes/0x0676-smsg-patch-global-strings.yaml)

SMSG_PATCH_GLOBAL_STRINGS: writes a 4-field, 16-byte record (id, one u32, two strings) into GlobalStrings.dbc's table object -- far short of that table's own 20-field, 80-byte row. Strings are read with FUN_1008baf0, a different helper from the FUN_100d3680 used by most of this shard's opcodes. After storing the row the handler conditionally calls a hook through the pointer table (DAT_10bc90a0) when a flag bit in the second field is set, passing the two strings through -- some sort of client-side announcement, not traced further. No column name is established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L3 | offset 0; the id bounds-checked against the table object's minId/maxId |
| `field_1` | `u32` | L3 | offset 4; bit 0 (after an optional >>1 shift gated on *DAT_10bcb458) selects whether the post-store hook fires |
| `field_2` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further |
| `field_3` | `bytes_4` | L3 | cursor advances 4 bytes here without loading them into a variable (read from the decompiled handler, not from packet_layout.py's mechanical scan); on the tables in this shard that carry inline strings, a gap in this position matches where the trailing string ends up in the stored record, so it is read here as that string's on-the-wire slot, not as padding -- not proven further |
| `field_4` | `lpstring` | L3 | via FUN_1008baf0, not FUN_100d3680 |
| `field_5` | `lpstring` | L3 | via FUN_1008baf0, not FUN_100d3680; passed to the post-store hook (DAT_10bc90a0) together with field_4 when field_1's bit is set |

## Client

- handler RVA `0x0025ad00` in `ascension-extensions-2026-08-13`
