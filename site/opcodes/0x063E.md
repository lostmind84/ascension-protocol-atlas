# `0x063E` CMSG_ASCENSIONGM_TICKET_HISTORY_REQUEST

**client to server** · status `unknown` · evidence `L3` · layout `unknown` · client code `none`

Fiche: [`protocol/opcodes/0x063e-cmsg-ascensiongm-ticket-history-request.yaml`](../../protocol/opcodes/0x063e-cmsg-ascensiongm-ticket-history-request.yaml)

Named CMSG_ASCENSIONGM_TICKET_HISTORY_REQUEST by the client's own opcode table. No sender exists for this opcode in this build: the value appears nowhere in `.text` as a 32- or 16-bit immediate, nowhere in `.rdata`/`.data`, and nowhere in the shipped Lua (client/client-senders.md's exhaustive scan), and it is absent from every symbol dump (senders/handlers/layouts/handler-fire/handler-strings.yaml). Layout is not established; there is no code to read it from. Its response, SMSG_ASCENSIONGM_TICKET_HISTORY_LIST (0x063F), likewise has no handler in this build (see that fiche).

## Layout

*No layout established.*
