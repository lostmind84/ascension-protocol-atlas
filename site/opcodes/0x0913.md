# `0x0913` SMSG_ASCENSIONGM_TICKET_EDITED

**server to client** · status `unknown` · evidence `L3` · layout `unknown` · client code `none`

Fiche: [`protocol/opcodes/0x0913-smsg-ascensiongm-ticket-edited.yaml`](../../protocol/opcodes/0x0913-smsg-ascensiongm-ticket-edited.yaml)

Named SMSG_ASCENSIONGM_TICKET_EDITED by the client's own opcode table. Extensions.dll's handler-registration table -- an exhaustive scan of .text for the push-handler/push-opcode/call-register idiom that recovers all 532 handlers this build registers -- has no entry for 0x0913, and none of the other seven opcodes in the same AscensionGM ticket SMSG cluster (0x0912-0x0919) has one either. This build has no code that reads the packet: layout is not established and cannot be inferred from the name.

## Layout

*No layout established.*
