# `0x09B0` SMSG_PATCH_TRANSPORT_ROTATION

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x09b0-smsg-patch-transport-rotation.yaml`](../../protocol/opcodes/0x09b0-smsg-patch-transport-rotation.yaml)

One row of TransportRotation.dbc: an id (FT_SORT index column, conventional name), TransportEntry, TimeSeg, X, Y, Z, W -- all seven columns named. No strings. 7 dwords (28B), matching the table exactly.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `id` | `u32` | L1 | DBC column 0, format char 'd' (FT_SORT/FT_IND, the record's own index field per src/common/DataStores/DBCFileLoader.h); DBCStructure.h comments this column out (not a compiled struct member) but the format character itself conventionally marks it as the row's id, so named 'id' at L1; width/position confirmed from the decompiled handler (build/ghidra/handlers-c/09b0.c). |
| `TransportEntry` | `u32` | L1 | DBC column 1 (i); name 'TransportEntry' is AzerothCore's own DBCStructure.h struct member for TransportRotation.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09b0.c). |
| `TimeSeg` | `u32` | L1 | DBC column 2 (i); name 'TimeSeg' is AzerothCore's own DBCStructure.h struct member for TransportRotation.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09b0.c). |
| `X` | `f32` | L1 | DBC column 3 (f); name 'X' is AzerothCore's own DBCStructure.h struct member for TransportRotation.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09b0.c). |
| `Y` | `f32` | L1 | DBC column 4 (f); name 'Y' is AzerothCore's own DBCStructure.h struct member for TransportRotation.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09b0.c). |
| `Z` | `f32` | L1 | DBC column 5 (f); name 'Z' is AzerothCore's own DBCStructure.h struct member for TransportRotation.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09b0.c). |
| `W` | `f32` | L1 | DBC column 6 (f); name 'W' is AzerothCore's own DBCStructure.h struct member for TransportRotation.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09b0.c). |

## Client

- handler RVA `0x00233e40` in `ascension-extensions-2026-08-13`
