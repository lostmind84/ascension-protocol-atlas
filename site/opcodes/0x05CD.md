# `0x05CD` CMSG_TRANSMOG_COMPONENT_APPLY

**client to server** · status `hypothetical` · evidence `L3` · layout `empty` · client code `none`

Fiche: [`protocol/opcodes/0x05cd-cmsg-transmog-component-apply.yaml`](../../protocol/opcodes/0x05cd-cmsg-transmog-component-apply.yaml)

Named CMSG_TRANSMOG_COMPONENT_APPLY by the client's own opcode table, with its own small SMSG pair immediately after it in the table (0x05ce SMSG_TRANSMOG_COMPONENT_UPDATE, 0x05d1 SMSG_TRANSMOG_COMPONENT_SUCCESS -- see those fiches; 0x05cf/0x05d0 sit unnamed between them). Nothing in this build constructs a packet with this opcode: no `push 0x5cd` / `mov r32, 0x5cd` occurs anywhere in Extensions.dll's `.text`, byte-exhaustively. The capture reference groups it with the rest of the legacy suite as superseded but does not name a specific modern-cluster opcode that replaces the "component" concept the way it does for outfits or the visibility toggle; what a "transmog component" was (a sub-slot of an appearance? an ingredient item?) is not established by anything read in this pass.

## Layout

*No layout established.*
