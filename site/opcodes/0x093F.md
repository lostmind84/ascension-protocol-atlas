# `0x093F` SMSG_PATCH_CURRENCY_TYPES

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x093f-smsg-patch-currency-types.yaml`](../../protocol/opcodes/0x093f-smsg-patch-currency-types.yaml)

One row of the stock CurrencyTypes.dbc (AzerothCore already loads this table; ADR 0005), sent as the single 16-byte blob the handler at RVA 0x0022bc20 reads (L3). Nothing was elided (4 cols x 4 bytes = 16 bytes matches the client header exactly). ItemId ('n', struct comment: used as the real index) and BitIndex are CurrencyTypesEntry's only active members and are named; the file's own ID (col 0) and Category (col 2) are core 'x' columns with no name (L1 throughout).

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `field_0` | `u32` | L1 | core format char 'x'/'X' at this column: skipped, no name in DBCStructure.h |
| `ItemId` | `u32` | L1 | active CurrencyTypesEntry member ('n'/FT_IND); struct comment: used as real index |
| `field_2` | `u32` | L1 | core format char 'x'/'X' at this column: skipped, no name in DBCStructure.h |
| `BitIndex` | `u32` | L1 | active CurrencyTypesEntry member |

## Client

- handler RVA `0x0022bc20` in `ascension-extensions-2026-08-13`
