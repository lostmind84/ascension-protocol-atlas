# `0x09A5` SMSG_PATCH_TAXI_NODES

**server to client** · status `hypothetical` · evidence `L1` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x09a5-smsg-patch-taxi-nodes.yaml`](../../protocol/opcodes/0x09a5-smsg-patch-taxi-nodes.yaml)

One row of TaxiNodes.dbc, collapsed: ID, map_id, x, y, z, then ONE string (the kept, named `name[16]` locale block, collapsed to its primary locale), then MountCreatureID[0..1] -- the string-flags dword (DBC column 21) is dropped entirely, not on the wire. 8 wire dwords (32B) plus one string, against the table's own 24 columns/96 bytes.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `ID` | `u32` | L1 | DBC column 0 (n); name 'ID' is AzerothCore's own DBCStructure.h struct member for TaxiNodes.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a5.c). |
| `map_id` | `u32` | L1 | DBC column 1 (i); name 'map_id' is AzerothCore's own DBCStructure.h struct member for TaxiNodes.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a5.c). |
| `x` | `f32` | L1 | DBC column 2 (f); name 'x' is AzerothCore's own DBCStructure.h struct member for TaxiNodes.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a5.c). |
| `y` | `f32` | L1 | DBC column 3 (f); name 'y' is AzerothCore's own DBCStructure.h struct member for TaxiNodes.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a5.c). |
| `z` | `f32` | L1 | DBC column 4 (f); name 'z' is AzerothCore's own DBCStructure.h struct member for TaxiNodes.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a5.c). |
| `name` | `lpstring` | L3 | DBC columns 5-20 (16 string-offset columns, 's') plus a flags dword at column 21 form AzerothCore's kept struct member `name[16]` (L1). On the wire the whole 17-column locale block collapses to ONE length-prefixed string (the primary locale's text) at this position; the other 15 locale slots and the flags dword (column 21) are not present on the wire at all -- confirmed from the decompiled handler (build/ghidra/handlers-c/09a5.c): the read cursor skips straight from the end of the fixed block to the lpstring with no placeholder dwords for this block. |
| `MountCreatureID_0` | `u32` | L1 | DBC column 23 (i); name 'MountCreatureID_0' is AzerothCore's own DBCStructure.h struct member for TaxiNodes.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a5.c). |
| `MountCreatureID_1` | `u32` | L1 | DBC column 24 (i); name 'MountCreatureID_1' is AzerothCore's own DBCStructure.h struct member for TaxiNodes.dbc (L1, another project's reading of the same client DBC file, not corroborated by client Lua); width/position independently confirmed from the decompiled handler (build/ghidra/handlers-c/09a5.c). |

## Client

- handler RVA `0x00233960` in `ascension-extensions-2026-08-13`
