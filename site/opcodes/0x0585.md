# `0x0585` SMSG_SUGGESTED_GROUP_INVITE

**server to client** · status `hypothetical` · evidence `L3` · layout `named` · client code `handler`

Fiche: [`protocol/opcodes/0x0585-smsg-suggested-group-invite.yaml`](../../protocol/opcodes/0x0585-smsg-suggested-group-invite.yaml)

A suggested group invite: two length-prefixed strings the handler reads with FUN_100d3680 and passes, in order, to SUGGESTED_GROUP_INVITE with format %s%s. The UI's handler for the event names neither; the first is presumably the inviter and the second the activity, which is not established.

## Layout

| field | type | evidence | notes |
| --- | --- | --- | --- |
| `string_0` | `lpstring` | L3 |  |
| `string_1` | `lpstring` | L3 |  |

## Client

- handler RVA `0x002721b0` in `ascension-extensions-2026-08-13`

## Events

- fires `SUGGESTED_GROUP_INVITE` with format `%s%s`
