# `Ascension.exe`: stock → CoA client patch rev4

`python3 tools/extract/peinfo.py --compare Ascension.exe.ORIGINAL Ascension.exe` on
2026-09-19. Same sections, same RVAs, plus one new section; 74 differing bytes in 7 runs.
The bytes are `L3`; what they do is read from the stock decompilation where the row says
so, and `not read` otherwise.

| VA | Stock | Patched | Reading |
| --- | --- | --- | --- |
| `0x00465dde` (.text) | `8b cf ff d0 85` | `e9 5d 62 99 00` | `jmp 0x00dfc040` into `.local`: a trampoline out of the realm-info/auth path (the function references `__AUREALM_INFO__`); not read further |
| `0x004d9fe0` (.text) | `e8 ab 69 1d 00` | `33 c0 c3 90 90` | the function is stubbed to `return 0`: stock, it fetched the `ClientServices` singleton and called its vtable `+200`; not read further |
| `0x008cc803` (.text) | `call 0x0047b6b0` | `call 0x00dfc020` | inside a packet reader (`Get(u32)`, `Get(u32)` follow): a `CDataStore` call redirected into `.local`; not read further |
| `0x008cc860` (.text) | `call 0x009a8880`-ish (`5c c0 0d`) | `call 0x00dfc030`-ish (`cc f7 52`) | same reader, second redirected call; not read further |
| `0x00a147a8` (.rdata) | `0x0095f990` | `0x00dfc0a0` | a function pointer in a table swapped for one in `.local` |
| `0x00a309bc` (.rdata) | `us.logon.worldofwarcraft.com:3724` | `127.0.0.1` | the built-in realmlist |
| `0x00dfb7da` (.rsrc) | `requireAdministrator` | `asInvoker` | the manifest's requested execution level |
| `.local` section | absent | `rva 0x9fc000, 0x460 bytes` | the trampolines the four code patches jump into |

Together with the `Extensions.dll` delta (`coa-extensions-rev4-2026-09-14.delta.md`), this
is the whole CoA client patch. The wire path (`client/wire-path.md`) is untouched: the
DLL's own detours and the opcode-bound patch are stock Ascension behaviour, present in
`Ascension.exe.ORIGINAL`.
